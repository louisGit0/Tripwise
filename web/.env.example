# URL du backend NestJS (sans slash final)
NEXT_PUBLIC_API_URL=http://localhost:3000/api/v1

# Token Mapbox public (visible côté client — restreindre aux domaines dans account.mapbox.com)
NEXT_PUBLIC_MAPBOX_TOKEN=
