---
gsd_state_version: 1.0
milestone: v1.1
milestone_name: post-launch refinements + iOS release
status: executing
last_updated: "2026-06-03T14:13:04.026Z"
progress:
  total_phases: 4
  completed_phases: 2
  total_plans: 10
  completed_plans: 9
  percent: 50
---

# Project State — verygoodtrip

## Project Reference

- **Core value:** Give an accurate, trustworthy total trip cost (energy + tolls) for a specific vehicle, instantly.
- **Milestone:** Precise tolls + editorial premium redesign + multi-source vehicle catalog (web + mobile)
- **Mode:** Vertical MVP
- **Current focus:** Phase 8 — onboarding

## Current Position

Phase: 8 (onboarding) — EXECUTING
Plan: 3 of 3 (08-01 web + 08-02 mobile onboarding complete)

- **Phase:** 1 of 5 — Precise Tolls End-to-End (Web) — ✅ complete (estimate-primary)
- **Phase 01.1:** Route-aware free toll estimator — ✅ complete (1/1 plan)
- **Status:** Ready to execute
- **Progress:** [█████████░] 90%

## Roadmap Snapshot

| Phase | Goal | Requirements | UI |
|-------|------|--------------|----|
| 1. Precise Tolls End-to-End (Web) | Trustworthy real/estimate tolls, broken out + persisted on web | TOLL-01..06 | - |
| 2. Editorial Dark DS + Trip Result | Documented editorial-dark tokens + data-viz, proven on trip result | DES-01..04, WEB-02 | yes |
| 3. Web Redesign Rollout | Editorial language across all remaining web screens, CWV preserved | WEB-01, WEB-03, WEB-04 | yes |
| 4. Multi-Source Vehicle Catalog + Scaled Showroom | Multi-source real-consumption catalog (thousands), normalized/deduped, server-side searched showroom | CAT-01..06 | yes |
| 5. Mobile Tolls + Editorial Redesign | Tolls + editorial language + server-side catalog browse on Expo via shared tokens | MOB-01..03 | yes |

## Performance Metrics

- Phases complete: 1/5
- Requirements mapped: 23/23
- Plans executed: 13 (08-01 — web onboarding tour: per-user localStorage seen-flag util + self-contained editorial-dark 7-step FR OnboardingTour (auto-show on unset /auth/me flag, replay via window event, reduced-motion friendly, no new dep/backend) + AppLayout mount + Settings "Revoir le tutoriel"; ONB-01 web, ~9min, 2 tasks, 4 files; 08-02 — mobile onboarding tour: per-user expo-secure-store seen-flag util (hasSeenOnboarding/markOnboardingSeen, key verygoodtrip.onboarding.seen.<userId>, never-throw) + ONBOARDING_OPEN_EVENT + self-contained RN Modal editorial-dark 7-step FR/EN OnboardingTour (auto-show on unset /auth/me flag, DeviceEventEmitter replay, reduced-motion → animationType 'none', dismissible, no serif/blue) + (tabs)/_layout.tsx Fragment mount sibling to <Tabs> (additive MD-2) + Settings "Aide" SectionCard secondary Button emitting the replay event; symmetric onboarding namespace fr/en + settings.help; mobile tsc --noEmit GREEN, grep gates pass; ONB-01 mobile, ~10min, 2 tasks, 6 files; 01-01 — backend toll engine, ~25min, 3 tasks, 5 files; 01-02 — toll-estimate persistence, ~15min, 3 tasks, 5 files; 01-03 — web toll display, ~12min, 3 tasks, 4 files; 01-04 — verification checkpoint, re-scoped D-07; 01.1-01 — route-aware free toll estimator, ~7min, 3 tasks, 6 files; 02-01 — editorial-dark token foundation, ~10min, 3 tasks, 3 files; 02-02 — motion hooks, ~4min, 2 tasks, 2 files; 02-03 — DataBar/Skeleton data-viz primitives, ~9min, 2 tasks, 2 files; 02-04 — trip result page editorial-dark redesign, ~13min, 2 tasks, 2 files; 03-01 — atom + AppLayout shell normalization (2-weight 400/700, canonical accent focus ring, accent-color fix), ~9min, 2 tasks, 14 files; 03-02 — Cluster A landing + auth editorial-dark restyle, ~8min, 2 tasks, 3 files; 03-05 — Cluster D trips history + detail + favorites editorial-dark restyle (Variant A DataBar on detail, standardized chips, Skeleton loading, canonical focus, toll badge preserved), ~11min, 3 tasks, 3 files; 03-06 — Cluster E fuel-prices + settings editorial-dark restyle (lucide source toggle, weight-constant theme picker, danger logout, Skeleton loading, canonical focus; price/theme/logout flows preserved MD-2), ~8min, 2 tasks, 2 files; 04-01 — multi-source catalog schema foundation (source provenance varchar + canonical UNIQUE(brand,model,fuel_type) entity @Index + dedupe-before-UNIQUE pg_trgm/btree migration; CAT-02/03/06), ~12min, 2 tasks, 2 files; 04-02 — multi-source ingestion spine: CatalogSourceAdapter contract + conversions (TDD RED→GREEN, 235.214583/MPG + combE/1.609344) + EPA→FuelType map + AdemeAdapter/EpaAdapter + committed ;-delimited EPA snapshot (26749 rows, 1.86 MB) via build-epa-snapshot.ts; CAT-01/02/04/05, ~8min, 3 tasks, 8 files; 04-03 — server-side catalog search API: CatalogQueryDto +brand/+fuelCategory, categoryToFuelTypes helper, findCatalog LOWER(brand||' '||model) LIKE (index-aligned) +brand/+fuelCategory filters, findCatalogBrands [{brand,count}] facet, catalog/brands route before catalog/:id, +7 e2e (search/brand/fuelCategory/pagination/400/brands); CAT-06, ~10min, 2 tasks, 5 files; 04-05 — server-side showroom search: garage/add switched from client load-all to debounced (useDebounce 300) GET /vehicles/catalog, two-effect cancellable pagination (page 1 replaces / page>1 appends), chip→one fuelCategory/fuelType param, brand grouping over accumulated items + 'Charger plus', optional source on web VehicleModel; Phase 3 visuals preserved; CAT-06 web slice, ~6min, 2 tasks, 2 files; 05-01 — shared editorial-dark token module: shared/src/tokens.ts (zero-import canonical dark+light hex mirroring globals.css + fontFamily/fontWeight/fontSize/spacing/radius) + barrel re-export, mobile/metro.config.js (Expo SDK 54 monorepo watchFolders+nodeModulesPaths), mobile/constants/theme.ts rewritten to one ThemeColors shape consuming tokens (blue Primary ramp deleted, legacy keys preserved as superset) → mobile tsc 7→2 (deferred MapboxMap/Button to 05-02); MOB-01/MOB-03, ~11min, 3 tasks, 4 files; 05-02 — mobile editorial fonts + atom normalization + clean tsc: _layout.tsx useFonts (Space Grotesk + JetBrains Mono 400/700, splash gate, no serif) + font deps committed; Button/Card/Input/Wordmark/AutocompleteInput/MapboxMap repointed to editorial-dark tokens (intact APIs); last 2 baseline errors fixed (Button StyleProp<ViewStyle>, MapboxMap MV children cast) → mobile tsc 2→0 GREEN; TripResult.tollCost/tollIsEstimate + VehicleModel.source + CatalogPage type; FR/EN toll+catalog i18n pre-seeded; MOB-01/MOB-03, ~6min, 3 tasks, 12 files; 05-03 — RN data-viz/motion primitives + editorial atoms: useReducedMotion (AccessibilityInfo.isReduceMotionEnabled + reduceMotionChanged subscription/.remove() cleanup), AnimatedCounter (Animated.timing 0→value easeOutCubic ~700ms, FR comma 2dp JetBrains Mono, INSTANT under reduced motion, listener+anim torn down), DataBar (discriminated Variant A segmented Énergie/Péage hide-when-0 D-04 + Variant B single muted-0.45, editorial token fills, opacity reveal gated off under reduced motion), Pill/SectionCard/Eyebrow atoms (web-parity naming, withAlpha tints, no hardcoded blue) + additive theme.ts FontSize export; mobile tsc GREEN (0→0); PD5-3, ~5min, 3 tasks, 7 files; 05-04 — editorial-dark mobile auth surface: login + register repoint legacy aliases to editorial tokens (bg/ink/mutedText/hairline/accent), Eyebrow subtitle, divider hairline + 'ou' mutedText, switch links accent display 700, unused 800 title style removed; tab bar accent active / mutedText inactive / surface bg / hairline border; auth Stack contentStyle bg; all auth/OAuth/zod/navigation preserved (MD-2); mobile tsc GREEN; MOB-01, ~7min, 2 tasks, 4 files; 05-05 — editorial-dark dashboard + mobile tolls: dashboard.tsx restyled (bg/ink tokens, scheme dark, display heading + Eyebrow labels, accent/#0e0c0a vs surface2/hairline chips+charging buttons, StatItem→ThemeColors mono, neutral EV disclaimer, 400/700) + in-file ResultCard mirrors web trip-result (hero AnimatedCounter total=energy+toll on surface3, Variant-A DataBar energyFill by fuelType, Énergie/Péage legend, réel/≈ estimé Pill from tollIsEstimate, hide-when-toll-0 D-04, tap→Alert tooltip); calc/charging/vehicle/share/save-favorite preserved MD-2; mobile tsc GREEN; MOB-01/MOB-02, ~9min, 2 tasks, 1 file; 05-06 — editorial-dark garage + server-side catalog browse: vehicles.tsx garage list + add/edit modals restyled (bg/ink/SectionCard/Eyebrow, FuelPill accent-EV/neutral, emoji ✏️/🗑 → token text controls delete=fuelGas, 400/700, no blue) AND the add-vehicle catalog rewired from the BROKEN r.data.data ({data,total}) to the frozen Phase-4 r.data.items (GET /vehicles/catalog → {items,...}): page state + reset effect [debouncedSearch] + cancellable loader [debouncedSearch,page] (page 1 replaces / >1 appends, no client load-all, limit 30), brand-grouped SectionList (Eyebrow headers), 'Charger plus' Button when page<totalPages, resultsCount/noResults; selected/handleSave POST /vehicles/me preserved MD-2; mobile tsc GREEN; MOB-01 + CAT-06 mobile slice, ~8min, 2 tasks, 1 file; 05-07 — editorial-dark favorites + settings: favorites.tsx (bg, Eyebrow surtitle + display 700 heading, rows → SectionCard with ink name/ink2 lines/mutedText arrow, use-trip → accent Button sm, 🗑 emoji → fuelGas token text label, empty mutedText) + settings.tsx (SectionCard title=Eyebrow sections, weight-constant accent/#0e0c0a vs surface2/hairline toggles, destructive logout Button, mutedText mono version); 400/700, no blue; use-trip deep-link/delete + theme/language/logout preserved MD-2; mobile tsc GREEN; MOB-01, ~6min, 2 tasks, 2 files; 06-01 — backend POL-03 PATCH /users/me (JWT-guarded display_name edit): UpdateProfileDto (@Transform trim + @IsNotEmpty + @MaxLength(40), single field → global whitelist blocks other keys) + UsersService.updateProfile (update + findOneByOrFail) + controller @Patch('me') @UseGuards(JwtAuthGuard) @CurrentUser returning /auth/me profile shape (no passwordHash); IDOR-safe, no migration; TDD RED→GREEN, full e2e 157/157, tsc + nest build clean; POL-03 backend, ~15min, 2 tasks, 4 files; 06-02 — web POL-01/02/03: toll réel/≈ estimé badge removed from trips/result + trips/[id] (amount + énergie/péage DataBar kept, tollIsEstimate still flows, Pill/Tooltip imports + badge plumbing removed); result Partager CTAButton → handleShare toll-inclusive FR summary via navigator.share (AbortError swallow) + navigator.clipboard fallback + toast; Settings pseudo editable Input(maxLength 40)+Enregistrer → apiClient.patch<UserProfile>('/users/me',{displayName}) → setUserProfile + reseed + toast (email read-only, theme/logout preserved MD-2); tsc clean, next build 18/18 routes; POL-01/02/03 web, ~12min, 3 tasks, 3 files; 06-03 — mobile POL-01/02/03: toll réel/≈ estimé Pill badge removed from dashboard.tsx ResultCard (Péage amount + DataBar kept, tollIsEstimate still flows, Pill+Alert imports dropped); handleShare enriched → route + toll-inclusive FR total + énergie/péage breakdown + distance/durée, shareText fr+en rewritten as multi-line {{route}}/{{cost}}/{{breakdown}}/{{distance}}/{{duration}} template; Settings new Pseudo SectionCard Input(maxLength 40)+Enregistrer seeded from GET /auth/me → client.patch('/users/me',{displayName}) + success toast, settings.pseudo/pseudoSave/pseudoSaved i18n fr+en (theme/language/logout/version unchanged MD-2); mobile tsc --noEmit GREEN; POL-01/02/03 mobile, closes Phase 6, ~10min, 3 tasks, 4 files; 07-01 — backend SHOW-02 image foundation: VehicleImageService.resolveImageUrl(make,model) mirroring TollService (ConfigService CARIMAGES_API_KEY server-side, Map cache w/ expiresAt 30-day hits+misses, AbortSignal.timeout 8s, try/catch never-throw → https-only URL|null via defensive extractor) + JWT-guarded GET /vehicles/catalog/image?make=&model= (before catalog/:id) → {imageUrl}; CatalogImageQueryDto (@IsString/@IsNotEmpty/@MaxLength 80); .env.example CARIMAGES block (free 5k/mo, server-side only, graceful-without-key); TDD RED→GREEN 16/16 unit + 3 e2e (no-key→{imageUrl:null}/no-token→401/missing-param→400); tsc + nest build clean, vehicles e2e 34/34, full suite 160/160; SHOW-02 backend, ~13min, 2 tasks, 7 files; 07-02 — web SHOW-01/02/03: new VehicleImage client atom (cancellable GET /vehicles/catalog/image make/model, key-safe 07-01; states Skeleton/img(lazy+alt+onError→placeholder)/stylized brand placeholder with per-brand brandHue tint in a fixed aspect-[16/10] frame = no CLS, never-throw) + brandHue exported + CatalogImageResult type; garage/add ModelCard reworked into photo-led editorial-dark card (VehicleImage + model + FuelBadge + consumption), config modal preview augmented with VehicleImage, search promoted to primary affordance (h-12 + clearer label/icon/clear) + quieter fuel chips + taller loading skeleton; Phase-4 server search + page1-replaces/page>1-appends pagination + fuelParams + jumpToBrand + handleAdd POST /vehicles/me preserved verbatim (MD-2, no client load-all / no .filter over catalog); tsc clean, next build 18/18 routes 0 ESLint; SHOW-01/02/03 web, ~9min, 2 tasks, 4 files; 07-03 — mobile SHOW-01/02/03: new RN VehicleImage atom (cancellable client.get /vehicles/catalog/image make/model, key-safe 07-01; states surface2-frame+accent-spinner / RN <Image> resizeMode=cover onError→placeholder / stylized per-brand placeholder mirroring web BrandAvatar brandHue+initials via hsl()/hsla() over surface2 + centered avatar, never-throw, no blue) + CatalogImageResult type; vehicles.tsx catalog renderItem reworked text-row→designed editorial-dark card (92×58 VehicleImage thumbnail + model display-700 + FuelPill + mono consumption on surface/hairline/Radius.card), VehicleImage added to selected-model confirm step (16/10), search promoted (selectModel Eyebrow) + fuel chips (Tous/Essence/Diesel/EV/GPL→one fuelCategory param, reset page 1, no client filter); Phase-4 cancellable [debouncedSearch,fuelFilter,page] loader (page1 replaces/page>1 appends, r.data.items) + brand-grouped SectionList + 'Charger plus' + handleSave POST /vehicles/me + EditVehicleModal PATCH preserved verbatim (MD-2, no client load-all / .filter×0); fuel chip i18n fr+en; mobile tsc --noEmit GREEN, grep gates pass; SHOW-01/02/03 mobile, ~12min, 2 tasks, 5 files)

## Accumulated Context

### Roadmap Evolution

- Phase 01.1 inserted after Phase 1: Route-aware free toll estimator (D-08 — free alternative to paid TollGuru) (URGENT)

### Key Decisions

- **D-34 (2026-06-03, plan 08-02) — mobile ONB-01: client-side per-user onboarding tour (auto-show once + replay from Settings, zero backend), mirroring web 08-01 via expo-secure-store + DeviceEventEmitter.** A new `mobile/src/lib/onboarding.ts` exports `hasSeenOnboarding(userId)` / `markOnboardingSeen(userId)` (SecureStore key **exactly** `verygoodtrip.onboarding.seen.${userId}`, stores `'1'`, both try/catch never-throw — ONB-3 per-user/per-device, no backend) + `ONBOARDING_OPEN_EVENT = 'verygoodtrip:onboarding:open'`. A self-contained `mobile/src/components/OnboardingTour.tsx` (commit `a8870a1`) calls `client.get<{id}>('/auth/me')` on mount (errors swallowed → never auto-show), keys the flag by `user.id`, and **auto-opens only when `hasSeenOnboarding(id)` resolves false** (ONB-5); a `DeviceEventEmitter.addListener(ONBOARDING_OPEN_EVENT,…)` re-opens it **regardless of the flag** (ONB-4 replay), removed via `sub.remove()` on unmount (a `mountedRef` guards all setState). RN `<Modal animationType={reduced?'none':'fade'} transparent visible onRequestClose=dismiss>` (Android back dismisses, not blocks): translucent `c.bg`+`d9` backdrop + centered editorial panel (`c.surface`/`c.hairline`/`Radius.card`); each step = an `Eyebrow` "Étape N / 7" counter (i18next `onboarding.step` n/total), a `Fonts.display` 700 `c.ink` title, a `Fonts.displayRegular` 400 `c.ink2` body, a row of **7 progress dots** (active `c.accent`+widened / inactive `c.surface2`), and controls — **Passer** (ghost `TouchableOpacity`), **Précédent** (secondary, hidden step 0), **Suivant** (0–5), **Terminer** (last); skip/Terminer/`onRequestClose` all `markOnboardingSeen(userId)` then close (no auto-reshow). **7 i18next steps FR+EN symmetric** (`onboarding.*`): welcome value-prop / calcul / garage&showroom / **péages = "estimés et déjà inclus dans le coût total"** — NO real/estimate badge wording (removed Phase 6) / trajets&historique / favoris / paramètres, plus nav labels (skip/next/prev/finish/replay) + the `step` counter. **Mount + replay (commit `d3c0bb4`):** `(tabs)/_layout.tsx` wraps the return in a Fragment with a single `<OnboardingTour />` sibling to `<Tabs>` (authenticated mount — the `(tabs)` group is token-gated by AuthContext; additive MD-2 — no tab/tabBar/`TabIcon` change); `(tabs)/settings.tsx` gains an "Aide" `SectionCard` with a `secondary` `Button` `t('onboarding.replay')` ("Revoir le tutoriel") whose `onPress` calls `DeviceEventEmitter.emit(ONBOARDING_OPEN_EVENT)` — theme/language/pseudo/logout untouched (MD-2). `settings.help` key added fr/en. Editorial display font only (D-11), no new dependency (RN `Modal` + `DeviceEventEmitter` + `expo-secure-store` already present), no `console.log`. No deviations (Rules 1–4 not triggered). Threat register held (T-08-02-01/02/03 accept — cosmetic boolean keyed by the user's own id; T-08-02-SC no installs). Verify: mobile `tsc --noEmit` clean (per task) + grep gates (key present, `onboarding` namespace fr+en, no `serif`/`Instrument` in `OnboardingTour.tsx`, `OnboardingTour`×1 in `_layout.tsx`, `DeviceEventEmitter` + `ONBOARDING_OPEN_EVENT` in `settings.tsx`). With web 08-01 this completes ONB-01 web+mobile. Not yet pushed — orchestrator pushes after phase verification.

- **D-33 (2026-06-03, plan 08-01) — web ONB-01: client-side per-user onboarding tour (auto-show once + replay from Settings, zero backend).** A new `web/src/lib/onboarding.ts` exports `hasSeenOnboarding(userId)` / `markOnboardingSeen(userId)` (localStorage key **exactly** `verygoodtrip.onboarding.seen.${userId}`, SSR-safe via `typeof window` guards, never-throw try/catch — ONB-3 per-user/per-device, no backend/migration) + `ONBOARDING_OPEN_EVENT = 'verygoodtrip:onboarding:open'`. A self-contained `'use client'` `web/src/components/OnboardingTour.tsx` (commit `04416d2`) fetches `apiClient.get<UserProfile>('/auth/me')` on mount (errors swallowed → never auto-show), keys the seen flag by `user.id`, and **auto-opens only when `hasSeenOnboarding(id) === false`** (ONB-5); it also registers a `window` `ONBOARDING_OPEN_EVENT` listener that re-opens the tour **regardless of the flag** (ONB-4 replay), removed on unmount. Full-screen `z-[120]` fixed overlay: carbon backdrop (`bg-carbon-bg/85`, click-to-dismiss) + centered editorial-dark panel (`bg-carbon-surface`/`border-carbon-hairline`/`rounded-card`); each step renders an `Eyebrow` "Étape N / 7", a lucide icon in a token chip, an `h2 font-display` title, a `text-carbon-ink2` body, a row of 7 progress dots (active `bg-carbon-accent` + widened, inactive `bg-carbon-surface2`), and footer controls — **Passer** (ghost), **Précédent** (hidden on step 0), **Suivant** (steps 0–5), **Terminer** (last). Skip/Terminer/close(X)/backdrop/Escape all call `markOnboardingSeen(userId)` then close (no auto-reshow). **7 hardcoded-FR steps** (welcome value-prop / calcul / garage&showroom / **péages = "estimé, inclus dans le total"** — NO real/estimate badge wording, removed Phase 6 / trajets&historique / favoris / paramètres). Transitions are **transform/opacity only**, suppressed when `useReducedMotion()` is true; no serif (D-11), no new dependency, no `console.log`. **Mount + replay (commit `51ea9bd`):** AppLayout renders a single `<OnboardingTour />` alongside `<TripModal />` (shows across `/app/*`, additive MD-2 — no nav/sidebar/drawer/topbar change); Settings gains an "Aide" `SectionCard` with a ghost `CTAButton` "Revoir le tutoriel" (lucide `PlayCircle`) whose `onClick` dispatches `window.dispatchEvent(new Event(ONBOARDING_OPEN_EVENT))` — theme/pseudo/logout flows untouched (MD-2). No deviations (Rules 1–4 not triggered). Threat register held (T-08-01-01/02/03 accept — cosmetic boolean keyed by the user's own id; T-08-01-SC no installs). Verify: web `tsc --noEmit` clean + `next build` **18/18 routes** (`/app/settings` 5.89 kB) + grep gates (key present, no serif, `OnboardingTour`×1 in AppLayout, `Revoir le tutoriel` + `dispatchEvent` in Settings). Mobile onboarding (08-02) mirrors the same flow via expo-secure-store. Not yet pushed — orchestrator pushes after phase verification.

- **D-31 (2026-06-02, plan 07-03) — mobile SHOW-01/02/03: RN `VehicleImage` atom + photo-led showroom cards + simpler search (consumes the 07-01 endpoint; Phase-4 data layer preserved MD-2).** A new `mobile/src/components/ui/VehicleImage.tsx` (`{ brand, model, fuelType, style }`) runs a **cancellable** effect keyed `[brand, model]` calling `client.get<CatalogImageResult>('/vehicles/catalog/image', { params: { make: brand, model } })` — the CarImages key never reaches the app (07-01 / T-07-03-01). Three states: **loading** → a `surface2` frame + a centered accent `ActivityIndicator`; **image** → an RN `<Image source={{ uri }} resizeMode="cover" onError→placeholder />` used ONLY as a URI source (no code execution; T-07-03-02); **placeholder** (null result OR any failure OR a dead URL) → a stylized per-brand panel **mirroring the web BrandAvatar** (the SAME `brandHue` hash + `initials` algorithm) rendered via derived `hsl()`/`hsla()` hues over `surface2` with a centered brand-hued avatar — designed, never a flat gray/broken box, **no blue literals**. It **never throws** (catch → placeholder); no `console.log`. New `CatalogImageResult { imageUrl: string | null }` type added (the GET generic); `VehicleModel` unchanged. **vehicles.tsx (`bf798f1`):** the catalog `renderItem` reworked from a text row into a **designed editorial-dark card** — a leading `<VehicleImage>` 92×58 thumbnail + the info block (model `Fonts.display` 700, `FuelPill`, `Fonts.mono` consumption) on `surface`/`hairline`/`Radius.card`; a `<VehicleImage>` (`width 100%`, `aspectRatio 16/10`) added to the **selected-model confirm step** so the user confirms the right car; **SHOW-03** — the search promoted to the clear primary affordance (a `selectModel` `Eyebrow` above the `Input`) + **fuel chips** (Tous/Essence/Diesel/EV/GPL) mapping to ONE server `fuelCategory` param (`gas`/`diesel`/`ev`/`gpl`, `all`→none) that **resets to page 1** via the `[debouncedSearch, fuelFilter]` effect — **NO client-side filtering**. **PRESERVED verbatim (MD-2):** the cancellable `[debouncedSearch, fuelFilter, page]` loader (page 1 REPLACES / page>1 APPENDS, `r.data.items`, no client load-all), the brand-grouped `SectionList`, the 'Charger plus' pagination, `handleSave` (`POST /vehicles/me`), `EditVehicleModal` `PATCH`, the garage list + delete. Fuel chip i18n added fr+en. No serif; no blue literals (only the sanctioned `#0e0c0a` accent ink mirroring the `Button` atom). **TDD gate gap:** mobile has no test runner (CLAUDE.md) — verified via `npx tsc --noEmit` **GREEN** (per task) + grep gates (`VehicleImage`×3, `vehicles/catalog` GET + `r.data.items` present, `POST /vehicles/me` + `PATCH` unchanged, `.filter(` over items ×0, blue literals ×0). No deviations (Rules 1–4 not triggered). Threat register held (T-07-03-01 key server-side; T-07-03-02 `<Image source uri>`-only + `onError`; T-07-03-03 07-01 cache + pagination; T-07-03-04 JWT auto-injected + 401 interceptor; T-07-03-SC no installs). Completes the mobile showroom slice (SHOW-01/02/03). Not yet pushed — orchestrator pushes after phase verification.

- **D-30 (2026-06-02, plan 07-02) — web SHOW-01/02/03: photo-led showroom cards + `VehicleImage` atom + simpler search (consumes the 07-01 endpoint; data layer preserved MD-2).** A new client atom `web/src/components/ui/VehicleImage.tsx` (`{ brand, model, fuelType, className }`) runs a **cancellable** effect (keyed `[brand, model]`) calling `apiClient.get<CatalogImageResult>('/vehicles/catalog/image', { params: { make: brand, model } })` — the CarImages key never reaches the browser (07-01 / T-07-02-01). Three states: **loading** → `Skeleton` filling the frame; **image** → a native `<img>` (`loading="lazy"`, `decoding="async"`, `alt={brand model}`, `object-contain`, `onError`→placeholder) rendered ONLY as `<img src>` (image context — no `innerHTML`/`dangerouslySetInnerHTML`; T-07-02-02); **placeholder** (null result OR any failure OR dead URL) → a stylized brand panel tinted with the stable **per-brand `brandHue`** (exported from `BrandAvatar`) via a `radial-gradient` over `bg-carbon-surface2` with a large centered `BrandAvatar` — reads as designed, never a flat gray/broken box. A **fixed `aspect-[16/10]` frame** reserves the space up-front so loading the photo causes **no CLS** (CWV). The atom **never throws** (catch → placeholder); no `console.log`. New `CatalogImageResult { imageUrl: string | null }` type added (the GET generic); `VehicleModel` unchanged. `fuelType` kept in the prop interface for caller consistency (typed, not destructured — no unused-var). **garage/add (`ae43615`):** `ModelCard` reworked into a photo-led editorial-dark card — a `<VehicleImage>` photo region on top, then the info block (model name `font-display` bold truncate, `FuelBadge`, consumption + optional capacity `font-mono` muted) on `bg-carbon-surface`/`border-carbon-hairline`/`rounded-card` with the existing hover (`hover:border-carbon-accent`) + `FOCUS_RING` + `Plus` add affordance + per-card `aria-label`; the config **Modal preview** augmented with a `<VehicleImage>` above the BrandAvatar header so the user confirms the right car; **SHOW-03** — the search is now the clear **primary** affordance (`h-12`, larger icon, clearer placeholder/aria "Rechercher votre voiture — marque ou modèle", clear button) while fuel chips read quieter; the loading skeleton card height bumped (208px) to match the taller photo cards (no jump). **PRESERVED verbatim (MD-2):** the reset effect `[debounced, fuelFilter]→setPage(1)`, the cancellable loader effect (page 1 REPLACES / page>1 APPENDS), `fuelParams` (chip → ONE server param), `jumpToBrand`, `openConfig`, and `handleAdd` (`POST /vehicles/me` body + redirect to `/app/garage/:id`) — **no client load-all, no `.filter(` over the catalog** (grep 0). `<img>` carries a scoped `eslint-disable-next-line @next/next/no-img-element` (CarImages CDN host not configured for next/image; `<img>` is the intended XSS-safe renderer). No serif; animation on transform/opacity only. **TDD gate gap:** web has no test runner (CLAUDE.md) — verified via `tsc --noEmit` (clean) + `next build` **18/18 routes, 0 ESLint** (`/app/garage/add` 4.16 kB) + grep gates (`VehicleImage`×3, `vehicles/catalog` GET present, `POST /vehicles/me` present, `.filter(`×0). No deviations (Rules 1–4 not triggered). Threat register held (T-07-02-01 key server-side; T-07-02-02 `<img src>`-only + `onError`; T-07-02-03 07-01 cache + lazy + pagination; T-07-02-SC no installs). Unblocks mobile 07-03 (same endpoint). Not yet pushed — orchestrator pushes after phase verification.

- **D-29 (2026-06-02, plan 07-01) — backend SHOW-02 image foundation: server-side CarImages resolve + JWT endpoint, mirroring TollService (key-safe, cached, never-throw → graceful placeholder).** A new `VehicleImageService` (`backend/src/vehicles/vehicle-image.service.ts`) mirrors the TollGuru integration shape verbatim: `resolveImageUrl(make, model)` reads `CARIMAGES_API_KEY` via `ConfigService` (SERVER-SIDE ONLY), trims/normalizes make+model and short-circuits to `null` **without fetching** on blank input or no key; otherwise it calls a hard-coded `CARIMAGES_BASE_URL` with make/model passed through `encodeURIComponent` (anti-SSRF — no user URL is ever fetched), the key in the `x-api-key` header, and `AbortSignal.timeout(8000)`. A defensive `extractImageUrl` reader pulls the FIRST plausible `https://` URL across several JSON shapes (`url`/`image`/`imageUrl`/`image_url` at root + `results[]`/`data[]` first element), dropping non-https/non-string values. **Every failure path → `null` + one `Logger.warn`, never throws:** no key, 401/429/5xx, timeout/network, parse error, unrecognised shape, missing model. A 30-day in-memory `Map` cache stores **hits AND misses** (a missing model never re-hits the API), keeping usage under the ~5k/mo free tier; identical resolves issue exactly one outbound call. **Endpoint (`eba290f`):** `@Get('catalog/image')` `@UseGuards(JwtAuthGuard)` declared **BEFORE `catalog/:id`** (NOTE comment, mirroring `catalog/brands`) validated by `CatalogImageQueryDto` (`make`/`model` `@IsString`+`@IsNotEmpty`+`@MaxLength(80)` — the DoS/whitelist bound) returning `{ imageUrl: string | null }` only — the key never appears in any payload (unit test asserts no key substring in the return value nor the outbound URL). `VehicleImageService` added to `VehiclesModule` providers (ConfigModule `@Global` — no extra import). `.env.example` gained a `CARIMAGES_API_KEY=` block (free tier, server-side only, graceful-without-key — the feature SHIPS without it, photos light up once set in prod, like tolls). **TDD:** RED `158ed5b` (16-case spec, ConfigService stub + `jest.spyOn(globalThis,'fetch')` + okResponse/errResponse, mirroring toll.service.spec.ts) → GREEN `1decb60` (service, 16/16). e2e (`eba290f`): no-key → `200 {imageUrl:null}`, no-token → `401`, missing param → `400` (CARIMAGES_API_KEY unset in harness). `tsc --noEmit` + `nest build` clean; vehicles e2e **34/34** (was 31, +3); full suite **160/160** (was 157, +3). No deviations (Rules 1–4 not triggered). Threat register held (T-07-01-01..05; T-07-01-SC N/A — no installs). **Integration note:** exact CarImages endpoint/auth(header vs query)/response shape flagged for live-docs confirmation when `CARIMAGES_API_KEY` is first set in prod — built defensively so a mismatch degrades to placeholder, not breakage. Unblocks web 07-02 + mobile 07-03. Not yet pushed — orchestrator pushes after phase verification.

- **D-28 (2026-06-02, plan 06-03) — mobile POL-01/02/03: toll badge removed + enriched share + editable pseudo (editorial-dark, visual-layer-only MD-2; closes Phase 6).** **POL-01 (`c5a68a1`):** the redundant toll **réel/≈ estimé** `Pill` badge (a `TouchableOpacity`>`Pill` whose `onPress` opened an `Alert.alert` tooltip) was deleted from `mobile/app/(tabs)/dashboard.tsx`'s `ResultCard` along with its single `{tollBadge}` legend usage and the now-unused local `tollIsEstimate`; the `Pill` import and the `Alert` import (used only by the badge) were removed, `TouchableOpacity` kept (vehicle chips + charging-mode buttons still use it). The **Péage legend amount line** (`{t('dashboard.tollLabel')} · {formatEur(tollCost)}`) + the énergie/péage `DataBar` + the `hasToll` hide-when-0 guard stay; `result.tollIsEstimate` keeps flowing in the data (types unchanged). **POL-02 (`8363534`):** `handleShare` now composes `route` (`origin?.label → destination?.label`, `'—'` fallback), a **toll-INCLUSIVE** `cost = formatEur(energyCost + tollCost)` (FR comma — matches the hero), a `breakdown` (`Énergie {…}` + ` · Péage {…}` only when `tollCost > 0`), `distance` (`{km} km`) and `duration` (`formatted`), passing all five to `Share.share`; `dashboard.shareText` in both `fr.ts` + `en.ts` was rewritten as a multi-line `{{route}}`/`{{cost}}`/`{{breakdown}}`/`{{distance}}`/`{{duration}}` template ending `Calculé avec verygoodtrip` / `Calculated with verygoodtrip` (symmetric). **POL-03 (`26635af`):** a new Pseudo `SectionCard` (above the Account section) holds an `Input` (`maxLength 40`, `autoCapitalize="words"`) bound to a `displayName` state + an **Enregistrer** `Button` (`size="sm"`, disabled while empty, `loading`); an unmount-guarded effect seeds it from `client.get('/auth/me')`, and `handleSaveName` trims+guards, calls `client.patch('/users/me', { displayName })`, reseeds from the typed response (`r.data as ProfileResponse`) + `Toast.show({type:'success', text1: t('settings.pseudoSaved')})` (error → `common.error`); new `settings.pseudo`/`pseudoSave`/`pseudoSaved` i18n in fr+en. Theme/language/logout/`version` sections unchanged (MD-2). **Polish (`598458c`):** the `client.patch` call was written without the axios generic (`client.patch('/users/me', …)` + typed `r.data as ProfileResponse` cast) to match the literal key-link pattern, and the stale `ResultCard` JSDoc was updated to describe the badge removal. `npx tsc --noEmit` **GREEN** after every task. No deviations (Rules 1–4 not triggered). Threat register held (T-06-03-01 server-side authz/validation via 06-01 + RN `<Text>` no-sink; T-06-03-03 JWT auto-injected + 401 interceptor). Completes the POL mobile slice; with web 06-02 + backend 06-01 this **closes Phase 6**. Not yet pushed — orchestrator pushes after phase verification.

- **D-27 (2026-06-02, plan 06-02) — web POL-01/02/03: toll badge removed + result share + editable pseudo (editorial-dark, visual-layer-only MD-2).** **POL-01 (`c7b0f37`):** the redundant toll **réel/≈ estimé** `Pill`+`Tooltip` badge was deleted from both `trips/result/page.tsx` (the `tollBadge` const, the `badge: tollBadge` PÉAGES tile entry, the `{tollBadge}` legend ref) and `trips/[id]/page.tsx` (the `Tooltip`>`Pill` block in the Péage legend) — the **toll amount + the énergie/péage `DataBar` stay**, and `tollIsEstimate` keeps flowing in the data (`handleSave` payload + `trip.tollIsEstimate` untouched). The now-unused `Pill`/`Tooltip` imports, the local `tollIsEstimate` var, the `ReactNode` import, and the whole `badge?` plumbing (type field, destructure, render) were removed so `next build` ESLint stays clean (grep `Pill`/`Tooltip`/`réel`/`≈ estimé` in `trips/**` → 0). **POL-02 (`fd7ae32`):** a **Partager** `CTAButton` (ghost/lg/lucide `Share2`) was added to the result CTA block; `handleShare` (declared in the render body, closing over the computed `totalCost`/`energyCost`/`tollCost`/`hasToll`/`fmtEur`) builds a **toll-INCLUSIVE** FR summary (route `from → to` pre-comma labels, `Coût total : {fmtEur}`, `Énergie {…}` + conditional ` · Péage {…}`, `{km} km · {durée}`, `Calculé avec verygoodtrip`) and does feature-checked `navigator.share({title,text})` inside a try/catch that **swallows AbortError** (user cancel = no-op), falling back on any other failure / missing API to `navigator.clipboard.writeText` + `showToast('success','Résumé copié…')`. **POL-03 (`bff1790`):** the Settings Account section's read-only `displayName` became an editable `Input` (label "Pseudo", `maxLength 40`) seeded from `/auth/me` + an **Enregistrer** accent `CTAButton` (disabled while empty); `handleSaveName` trims+guards, calls `apiClient.patch<UserProfile>('/users/me', { displayName })` (the 06-01 endpoint), then `setUserProfile(data)` + reseeds the input + `showToast('success','Pseudo mis à jour')` (error → error toast) — immediate reflect; email read-only, hydration skeleton + theme picker + logout preserved (MD-2). `tsc --noEmit` clean (run per task); `next build` **18/18 routes** green, 0 ESLint. No deviations (Rules 1–4 not triggered). Threat register held (T-06-02-01 server-side authz/validation via 06-01 + escaped React text; T-06-02-03 feature-check + clipboard fallback + AbortError swallow). Completes the POL web slice; mobile (06-03) mirrors the same three. Not yet pushed — orchestrator pushes after phase verification.

- **D-26 (2026-06-02, plan 06-01) — backend POL-03: JWT-guarded `PATCH /users/me` edits `display_name` only.** The empty `users.controller.ts` was wired to a single `@Patch('me')` route guarded by `@UseGuards(JwtAuthGuard)`; the target id comes **only** from `@CurrentUser().id` (no `:id`/`userId` param) → IDOR-safe (T-06-01-01). `UpdateProfileDto` carries one field — `@Transform(({value}) => typeof value === 'string' ? value.trim() : value)` then `@IsString` + `@IsNotEmpty` + `@MaxLength(40)` — so the global `ValidationPipe` (`whitelist` + `forbidNonWhitelisted`) makes `displayName` the ONLY writable key (sending `{displayName, email}` → 400, email untouched; T-06-01-02) and blank/whitespace-only/>40 → 400 (T-06-01-03). `UsersService.updateProfile(userId, displayName)` = `userRepo.update(userId, {displayName})` + `findOneByOrFail` (mirrors `linkOAuthProvider`). The handler returns the SAME projection as `GET /auth/me` (`{id, email, displayName, locale, provider, createdAt}` — no `passwordHash`). **No migration** (the `display_name` column already existed). TDD: RED `086ed94` (7 e2e in `backend/test/users.e2e-spec.ts` — update / trim / 400-empty / 400-whitespace / 400-too-long / 401-no-token / whitelist-reject-with-email-unchanged, mirroring the auth SQLite in-memory harness) → GREEN `191b252`; full e2e **157/157**, `tsc --noEmit` + `nest build` clean. No deviations (Rules 1–4 not triggered). Unblocks the web 06-02 + mobile 06-03 Settings pseudo-edit UI (Wave 2). Not yet pushed — orchestrator pushes after phase verification.

- **D-25 (2026-06-02) — Milestone v1.1 from user feedback (web+mobile, then iOS release):** After the v1.0 on-device review the user requested: (POL-01) remove the toll "≈ estimé" badge (keep amount); (POL-02) trip share; (POL-03) edit pseudo via `display_name` (column EXISTS — just need `PATCH /users/me` + Settings UI, no migration); (SHOW-01) restyled showroom cards; (SHOW-02) vehicle photos **only if a free image source is viable** (spike-gated, else dropped); (SHOW-03) simplify showroom nav/search ("too complicated"); (ONB-01) post-signup onboarding tutorial replayable from Settings. **Every change ships web AND mobile.** Then (REL-01) publish to the **iOS App Store** (Google Play later). Phases 6 (Polish & Account) → 7 (Showroom v2, starts with the image spike) → 8 (Onboarding) → 9 (iOS release, account/credential/Apple-review-gated). v1.0 marked complete.

- **D-24 (2026-06-02, plan 05-07) — editorial-dark favorites + settings (MOB-01, mobile rollout complete):** The last two mobile screens were restyled editorial-dark, visual-layer-only (MD-2), completing the editorial-dark rollout across all mobile screens. **Favorites (Task 1, `620f5fd`):** page bg → editorial `bg`; an `Eyebrow` surtitle (`nav.favorites`) over a `Fonts.display` 700 `c.ink` heading; favorite rows moved from the legacy `Card` into the editorial `SectionCard` — name `ink` display 700, origin/destination lines `ink2` (display-regular 400), the `↓` arrow `mutedText`; the "Utiliser" use-trip control is now the accent `Button` atom (`size="sm"`); the **🗑 emoji delete control was replaced** with a token-styled text label (`common.delete`) colored `fuelGas` (mirrors the D-23 vehicles pattern — Expo-Go-safe, no vector-icons); empty state `mutedText`. `load`, `handleUseTrip` (deep-link `/(tabs)/dashboard?...`), and `handleDelete` (`DELETE /favorites/:id`) preserved verbatim. **Settings (Task 2, `9475d10`):** page bg → editorial `bg`, `useColorScheme()` default `'light'`→`'dark'`; Eyebrow surtitle (`nav.settings`) + display 700 heading; the three sections (theme/language/account) use `SectionCard` with the `title` prop (renders the `Eyebrow`), replacing the legacy 600-weight section labels; **option toggles** (light/dark/system, fr/en): active = `accent` bg + `accent` border + `#0e0c0a` ink label, inactive = `surface2` + `hairline` + `ink` — the label weight is **constant** (always display 700, no jump on active); logout uses the destructive `Button` (`fuelGas`); the version line → `mutedText` mono caption (`Fonts.monoRegular`/`FontSize.caption`). `applyTheme`/`Appearance.setColorScheme`, `changeLanguage`/`i18n.changeLanguage`, `handleLogout`/`signOut`, and the `version` read preserved verbatim. All weights 400/700; no blue literals / `c.primary` / 500-600 weights in either file (grep 0). No deviations (Rules 1–4 not triggered). Threat register held (T-05-07-01 signOut, T-05-07-02 authed delete — both unchanged). Mobile `tsc --noEmit` **GREEN** (per-file + full run, EXIT 0). Not yet pushed — orchestrator pushes after phase verification.
- **D-23 (2026-06-02, plan 05-06) — editorial-dark garage + server-side mobile catalog browse (MOB-01 + CAT-06 mobile slice):** `mobile/app/(tabs)/vehicles.tsx` was restyled editorial-dark AND its add-vehicle catalog rewired to the frozen Phase-4 server-side contract — the mobile half of CAT-06 (the showroom now scales to thousands with NO client load-all). **Restyle (Task 1, `4635ece`):** page bg → editorial `bg`; the page header is a `Fonts.display` 700 `c.ink` heading under an `Eyebrow` surtitle (`nav.vehicles`); vehicle rows moved from the legacy `Card` into the editorial `SectionCard` — name in `ink` display 700, a `FuelPill` (accent tint for ELECTRIC, neutral otherwise) + a `Fonts.mono` consumption sub line in `mutedText`; the **emoji icon buttons (✏️/🗑) were replaced** with token-styled text controls (`common.edit` in `ink2`, `common.delete` in `fuelGas` = destructive); both modal containers use `bg` + `hairline` headers with `ink` display-700 titles + `accent` close/back labels; catalog rows carry a `FuelPill` + mono consumption. All weights normalized 400/700; no blue literals. **Server-side catalog (Task 2, `7ebaebc`):** the catalog fetch switched from the BROKEN `r.data.data` (old `{ data, total }` shape) to `r.data.items` via `client.get<CatalogPage>('/vehicles/catalog', { params: { search, page, limit: 30 } })`; added `page`/`total`/`totalPages`/`isLoadingMore` state, a reset effect `[debouncedSearch] → setPage(1)`, and a **cancellable** loader effect `[debouncedSearch, page]` (page 1 REPLACES / page>1 APPENDS; the `cancelled` cleanup flag discards an in-flight page>1 response after a search reset so it never wrongly appends); a `useMemo` folds the accumulated `items` into brand sections rendered by a `SectionList` (Eyebrow brand headers + model rows); a **'Charger plus'** secondary `Button` (`vehicles.loadMore`) shows when `page < totalPages`, guarded by `isLoadingMore`; a results-count header (`vehicles.resultsCount {{loaded}}/{{total}}`) + a `vehicles.noResults` empty state. **NO client load-all / no parallel multi-page prefetch.** The `selected`/`handleSave` `POST /vehicles/me` add flow + `loadVehicles`/`handleDelete`/edit `PATCH` + navigation preserved verbatim (MD-2). No serif (D-11). No deviations (Rules 1–4 not triggered). Mobile `tsc --noEmit` **GREEN** (per-file + full run, EXIT 0); `data.items`×1, `data.data`×0. Not yet pushed — orchestrator pushes after phase verification.
- **D-22 (2026-06-02, plan 05-05) — editorial-dark mobile dashboard + tolls (MOB-01/MOB-02):** `mobile/app/(tabs)/dashboard.tsx` was restyled editorial-dark AND made toll-aware — the milestone mobile toll slice, consistent with the web trip-result. **Shell + form (Task 1, `db912a9`):** page bg → editorial `bg`, `useColorScheme()` default `'light'`→`'dark'`; header = `Fonts.display` 700 `c.ink` heading under an `Eyebrow` surtitle; in-form section labels (vehicle / charging mode) now use the `Eyebrow` atom (replacing the 500-weight label); inputs + result + favorite modal wrapped in `SectionCard` (legacy `Card` dropped); **vehicle chips + charging-mode buttons** active = `accent` bg + `#0e0c0a` ink, inactive = `surface2` + `hairline` + `ink` (old `c.primary`/`c.muted` blue removed); `StatItem` retyped `typeof Colors.light`→`ThemeColors` with `Fonts.mono` numerics; EV disclaimer → neutral boxed `surface2`/`ink2` (NOT amber, D-10); all weights 400/700. **Toll-aware result (Task 2, `5dcdfb4`):** a new in-file `ResultCard` mirrors the web trip-result — `total = (cost?.totalCost ?? 0) + (result.tollCost ?? 0)`, `hasToll = tollCost!=null && >0`, `tollIsEstimate = result.tollIsEstimate ?? false`; a **hero** on the `surface3` plate with an `Eyebrow` `costEstimated` + `<AnimatedCounter value={total}>` (mono, `FontSize.hero`, instant under reduced motion) + a `€` suffix; a **Variant-A `DataBar`** (`energyFill` by `fuelType`: ELECTRIC→`ev`, DIESEL→`fuelDie`, GPL→`fuelGpl`, else `fuelGas`) + a legend with an Énergie row (dot `energyFill`) and — only when `hasToll` — a Péage row (dot `c.toll`) + the toll badge; the **entire toll line/segment is HIDDEN when `tollCost` 0/null** (D-04). The **toll badge** = `<Pill color={tollIsEstimate?'warning':'success'} size="sm">{tollIsEstimate?tollEstimate:tollReal}</Pill>` wrapped in a `TouchableOpacity`→`Alert.alert(tollLabel, tollEstimateTooltip)` (RN tooltip substitute). The cost figure moved from a third `StatItem` into the hero; the metrics row keeps distance + duration only. **All calc / charging-mode / vehicle-picker / share / save-favorite flows + navigation preserved verbatim (MD-2).** Deviation: dropped the unused `TextInput` import (Rule 3 trivial). Mobile `tsc --noEmit` **GREEN (0→0)**; dashboard references `tollIsEstimate`×4 / `DataBar`×4 / `AnimatedCounter`×3. **MOB-02 marked complete.** Not yet pushed — orchestrator pushes after phase verification.
- **D-21 (2026-06-02, plan 05-04) — editorial-dark mobile auth surface (MOB-01):** The two auth screens + the two layouts now wear the editorial-dark direction, visual-layer-only (MD-2). **`login.tsx` / `register.tsx`** — every legacy color alias the screens read (`background`/`text`/`textSecondary`/`border`/`mutedFg`/`primary`) was repointed to the canonical editorial tokens (`bg`/`ink`/`mutedText`/`hairline`/`accent`); `useColorScheme()` default flipped `'light'`→`'dark'` (editorial-dark first paint). The header subtitle is now the **`Eyebrow`** atom (display 700, uppercase, wide tracking, muted) wrapping `auth.login`/`auth.register`; `Wordmark` ink = `c.ink`. The divider line → `hairline`, the "ou" label → `mutedText`; the login↔register switch links → `accent`, both rendered in `Fonts.display` weight 700 (2-weight system, **no serif** D-11). The unused `title` style (`fontWeight:'800'`) was removed; no 500/600/800 weights remain. The normalized `Input`/`Button` atoms (05-02) carry the rest; on login the Google OAuth keeps the quiet `secondary` Button. **All flow preserved verbatim:** `onSubmit` POST `/auth/login`+`/auth/register`, `signIn`, `handleGoogleLogin` + `WebBrowser.openAuthSessionAsync('verygoodtrip://auth/callback')` token parse, the register zod `.refine` confirm-password match + password error mapping, `secureTextEntry`, and the `Link` navigation. **`(tabs)/_layout.tsx`** — `tabBarActiveTintColor`=`accent`, `tabBarInactiveTintColor`=`mutedText`, `tabBarStyle.backgroundColor`=`surface`, `borderTopColor`=`hairline`; the 4 `Tabs.Screen` entries + titles + the `null`-returning `TabIcon` stub (label-only tabs, Expo-Go-safe — swap to `@expo/vector-icons` after an EAS build) + nav structure unchanged. **`(auth)/_layout.tsx`** — kept `headerShown:false`; added `contentStyle.backgroundColor=bg` so the auth Stack paints editorial before a screen mounts. No deviations (Rules 1–4 not triggered). Threat register held: `secureTextEntry` on password/confirm (T-05-04-02), OAuth redirect untouched (T-05-04-01). Mobile `tsc --noEmit` **GREEN** (per-file + full run). Commits `80d76c8` (screens) / `d4dd5ee` (layouts). Not yet pushed — orchestrator pushes after phase verification.
- **D-20 (2026-06-02, plan 05-03) — RN data-viz/motion primitives + editorial atoms (PD5-3):** Six NEW token-driven files mirror the locked web contract in React Native. **`useReducedMotion`** (`mobile/src/hooks/`) — `useState(false)` default (web first-paint parity), effect calls `AccessibilityInfo.isReduceMotionEnabled().then(setReduced)` (+`.catch`→false) and subscribes `AccessibilityInfo.addEventListener('reduceMotionChanged', setReduced)`; cleanup via `subscription.remove()` (RN 0.65+, no `removeEventListener`) + `isMounted` guard. **`AnimatedCounter`** (`{ value; style?; durationMs? }`) — `useRef(Animated.Value(0))`; effect keyed `[value,durationMs,reduced,av]`: reduced/`durationMs<=0` → `stopAnimation()`+`setValue(value)`+`setDisplay(value)` INSTANT, else reset 0 → `av.addListener(({value:v})=>setDisplay(v))` + `Animated.timing({toValue:value,duration:durationMs??700,easing:Easing.out(Easing.cubic),useNativeDriver:false}).start()`; renders `display.toFixed(2).replace('.',',')` in `Fonts.mono` 700 `c.ink`; cleanup stops anim + `removeListener` (no leaked frames — **mitigates T-05-03-01**). `useNativeDriver:false` is required so the JS listener can read+format the tweened value (only the numeric updates — compositor-safe). **`DataBar`** — discriminated `DataBarProps`: Variant A `{energyValue,tollValue,total,energyFill,height?}` = `surface2` track + flush row of energy `Animated.View` (`pct(energyValue,total)%`,`energyFill`) + toll `Animated.View` (`pct(tollValue,total)%`,`c.toll`) **hidden when `tollValue<=0`** (D-04); Variant B `{value,max,fill,muted?,height?}` = one absolute fill, muted→0.45 via `Animated.multiply`. `pct()` clamps 0–100 + guards zero/NaN; `widthPct()` casts the template percent to `DimensionValue`; mount **opacity** reveal (native driver 600ms) gated OFF under reduced motion; heights sm=6/md=10. **Decision:** chose an **opacity fade reveal over the web's `scaleX`+`transformOrigin`** — unambiguously typed + compositor-friendly in RN 0.81, widths final on first paint (no width animation), and the contract only mandates the reduced-motion gate + hide-when-0 + muted. **`Eyebrow`** (display 700, `FontSize.caption`, `letterSpacing 1.6`, uppercase, `mutedText`); **`Pill`** (`success`/`warning`/`neutral`/`accent`, sm/md — tinted via 8-digit-hex `withAlpha(.,0.16)` over matching text: success=`success`, warning=`fuelDie`, accent=`accent`, neutral=`surface2`/`ink2`; `Radius.full`; for the réel/≈ estimé toll badge); **`SectionCard`** (`surface`+`hairline`+`Radius.card`, optional `Eyebrow` title row). **Deviation [Rule 3]:** added one additive `export const FontSize = tokens.fontSize;` to `mobile/constants/theme.ts` so atoms read the editorial caption/body scale via the aliased `@/constants/theme` (the `@verygoodtrip/shared` alias doesn't resolve at Metro runtime — 05-01); no existing export changed. **TDD gate gap:** Task 1 was `tdd=true` but mobile has NO test runner (CLAUDE.md) and every task's only `<verify>` is `tsc --noEmit` — no RED/GREEN test commits produced; behavior mirrors the web `useCountUp`/`useReducedMotion` verbatim, verified via tsc + trace. **MOB-02 NOT marked complete** — this plan builds the primitives only; the mobile toll *display* ships in a Wave-3 screen plan. Mobile `tsc --noEmit` **0→0 GREEN**. Commits `e82a43d` (motion) / `2ac75e3` (DataBar) / `87d00d5` (atoms+FontSize).
- **D-19 (2026-06-02, plan 05-02) — mobile editorial fonts + atom normalization + clean tsc baseline (MOB-01/MOB-03):** `mobile/app/_layout.tsx` now loads the editorial faces before first paint: `SplashScreen.preventAutoHideAsync()` at module scope, `useFonts` (from `expo-font`) for `SpaceGrotesk_400Regular/700Bold` + `JetBrainsMono_400Regular/700Bold` (keys match `tokens.fontFamily`), `if (!loaded) return null` holds the native splash, effect `SplashScreen.hideAsync()` once loaded; AuthProvider/StatusBar/Stack/Toast/i18n import preserved verbatim (MD-2). Font deps `@expo-google-fonts/space-grotesk` + `jetbrains-mono` (^0.4.1) committed (were pre-installed in the working tree per resume note — no `expo install` re-run). The shared RN atoms were repointed to editorial tokens (intact prop APIs, MD-2): **Button** primary=`accent` bg + dark-ink `#0e0c0a` label, secondary=`surface2`+`hairline`+`ink`, ghost=transparent+`accent`, destructive=`fuelGas`+white, label `Fonts.display` 700; **Card** `surface`/`hairline`/radius `card`; **Input** `surface`/`hairline`/focus `accent`/error `fuelGas`/text `ink`/placeholder `mutedText`, label display 700; **Wordmark** defaults from `Colors[scheme]` ink/accent (no hardcoded hex) + display font; **AutocompleteInput** input `surface`/`hairline`/`ink`/`mutedText`, dropdown `surface2`, spinner `accent` (geocode logic verbatim); **MapboxMap** placeholder `surface2`/`hairline`/`mutedText`, route line `#2563eb`→`#4d8bff`. **The last 2 deferred tsc errors are FIXED:** Button `false`-ViewStyle via typing the style array as `StyleProp<ViewStyle>` (permits the conditional falsy entry); MapboxMap overload via adding `children: React.ReactNode` to the `MV` cast type. Mobile `npx tsc --noEmit` → **2→0 errors (FULLY GREEN)** — PD5-2 baseline clean. Types: `TripResult` +optional `tollCost`/`tollIsEstimate`, `VehicleModel` +optional `source`, new `CatalogPage { items,total,page,limit,totalPages }` (frozen Phase-4 contract). i18n (FR+EN symmetric): `dashboard.{tollLabel,tollReal(réel/actual),tollEstimate(≈ estimé/≈ estimate),tollEstimateTooltip,energyLabel,costEstimated,breakdown}` + `vehicles.{loadMore,noResults,resultsCount,searchHint}` — pre-seeded so Wave-3 screens never touch i18n files (no cross-plan overlap). No deviations (Rules 1–4 not triggered). Commits `cb0a445` (fonts) / `faae17b` (atoms) / `0618ed1` (types+i18n).
- **D-18 (2026-06-02, plan 05-01) — shared editorial-dark token module + single mobile ThemeColors (PD5-1/MOB-03):** Created `shared/src/tokens.ts` — a **framework-free** (ZERO imports) module exporting `tokens` (`colors.dark`/`colors.light` ×15 keys, `fontFamily` Space Grotesk + JetBrains Mono loaded-font names NO serif D-11, `fontWeight` 400/700, `fontSize` 56/26/14/11, `spacing` 8-pt, `radius` incl. `card:12`) whose hex VALUES mirror `web/src/app/globals.css` exactly (dark `bg #0e0c0a`…`toll #b8a98c`; light incl. `fuelGpl #7c3aed`/`toll #9b8a6b`). Re-exported via the shared barrel. **Web NOT refactored** (PD5-1: globals.css stays the locked source for web; tokens.ts is a documented mirror mobile consumes). `mobile/metro.config.js` added (Expo SDK 54 monorepo pattern: `getDefaultConfig` + `watchFolders=[repo root]` so Metro bundles the out-of-project `shared/`, + `resolver.nodeModulesPaths=[project,root]`; **hierarchical lookup left ENABLED** — per-project lockfiles, no hoist). `mobile/constants/theme.ts` rewritten: imports `tokens` via a **relative** path (`../../shared/src/tokens` — resolves at Metro runtime; the `@verygoodtrip/shared` alias would NOT without module-resolver), builds `Colors: Record<ColorScheme, ThemeColors>` where **light+dark share one `ThemeColors` interface** (this collapses the `Colors[scheme]` union → **resolves 5 of 7 pre-existing tsc errors**: dashboard ×3, Button ×2). The blue `Primary` ramp (`#3b82f6`) is **deleted**. `ThemeColors` is a **superset**: editorial keys + every legacy key (`text/card/border/muted/etc.`) remapped to editorial tokens, so un-restyled screens keep compiling. **`muted` collision** resolved: legacy `muted` = `surface2` (fill); editorial muted TEXT exposed as `mutedText`. `success` green (`#4ade80`/`#16a34a`) kept as a deliberate non-editorial accessible color. `Fonts`/`Spacing`/`Radius` sourced from tokens; legacy `FontSizes` (xs..2xl) preserved. Mobile `tsc --noEmit` **7→2** (remaining: `MapboxMap.tsx(99)` overload + `Button.tsx(42)` `false`-ViewStyle — deferred to 05-02, not color-union). **Deviation:** `mobile/tsconfig.json` NOT modified (predicted) — the `@verygoodtrip/shared` alias already existed and nothing excludes `../shared`, so the relative import type-resolves with no change. Commits `0462387` (tokens) / `4134396` (metro) / `3f7250f` (theme).
- **D-17 (2026-06-02, plan 04-05) — server-side showroom search (CAT-06 web slice):** `web/src/app/app/garage/add/page.tsx` switched from client load-all (parallel `MAX_PAGES` fetch up to 3000 rows + client substring/type `useMemo` filter) to **debounced** (`useDebounce(search, 300)`) **server-side** `GET /vehicles/catalog`. **Two-effect pagination:** a reset effect `[debounced, fuelFilter] → setPage(1)`; a **cancellable** loader effect `[debounced, fuelFilter, page]` → `apiClient.get('/vehicles/catalog', { params: { search, page, limit: 60, ...fuelParams } })`, **page 1 REPLACES / page>1 APPENDS**; the loader cleanup (`cancelled=true`) discards an in-flight page>1 fetch when the query resets to page 1, so a filter change while paged-in never wrongly appends. **Chip → ONE server param** (`fuelParams`): `ev/diesel/gpl`→`fuelCategory`, `essence`→`fuelCategory=gas` (04-03 expands to SP95/SP95_E10/SP98/E85), `e85`→`fuelType=E85`, `all`→none — never the old multi-type client filter. Brand-grouping `useMemo` now folds over the accumulated `items`; header shows loaded count + `sur {total}` + brand count. A **'Charger plus'** `CTAButton` (`page < totalPages`, `isLoadingMore`-guarded, mirrors trips/history) replaces unbounded client memory (T-04-05-03 mitigated). Web `VehicleModel` type gains optional `source?: 'ademe' | 'epa' | string`. **All Phase 3 editorial-dark visuals (toolbar, chips, BrandAvatar, FuelBadge, brand sections, ModelCard, Modal, FOCUS_RING, Skeleton) + the add-vehicle `POST /vehicles/me` submit flow preserved verbatim** (MD-2, data-layer-only). Deviation (Rule 3): dropped the now-unused `FuelType` import + `FUEL_FILTERS.types` field after removing the client filter (`no-unused-vars` build error). Commit `43ddd23` (feat); web `tsc --noEmit` + `next build` clean, **18/18 routes**, `/app/garage/add` 6.78 kB. Consumes the frozen `{ items, total, page, limit, totalPages }` contract Phase 5 mobile reuses.
- **D-16 (2026-06-02, plan 04-04) — multi-source merge orchestrator + idempotent ON CONFLICT (DEF-04-01-01 resolved):** `VehicleSyncService` is now a precedence-ordered orchestrator over `CatalogSourceAdapter[]`. Exported `mergeCanonical(adapters)` sorts by `precedence` ascending then runs `load()`+`normalize()` into a `Map` keyed by `` `${brand}|${model}|${fuelType}`.toUpperCase() `` with **first-writer-wins** (`if (!map.has(key)) map.set(key, v)`) → **ADEME (precedence 0) wins on overlap (PD4-1), EPA (10) gap-fills**. `doSync` builds `[new AdemeAdapter(), new EpaAdapter()]`, merges, then **splits by source** and upserts in batches of 100 via `createQueryBuilder().insert()`: ADEME `.orUpdate(['consumption','battery_capacity_kwh','tank_capacity_liters','source'],['brand','model','fuel_type'])`, EPA `.orIgnore()` (DO NOTHING — never overwrites ADEME; belt-and-suspenders since the in-memory merge already dropped overlaps). Conflict target = the 04-01 canonical `UNIQUE(brand,model,fuel_type)` → **idempotent re-runs (CAT-05)**; **resolves DEF-04-01-01** (legacy `repo.save()` UNIQUE collision gone). A 3rd source drops into the array with no merge/schema/upsert change (proven by the spec's precedence-20 stub). Public method kept named `syncFromAdeme` (now multi-source) so the 04-03 `POST /vehicles/sync` route is untouched. `SyncResult {created,skipped,total}` shape preserved (`created`=net new via before/after count). **STARTUP_THRESHOLD=500 retained** — prod (~266 ADEME < 500) runs the one-time multi-source population on next deploy, then boots skip; manual sync refreshes. Deviation (Rule 1): **auto-bootstrap sync skipped under `NODE_ENV=test`** — the new ADEME `orUpdate` would overwrite seeded e2e fixtures (Tesla Model 3 14.9→14.4) via the live ADEME fetch; gate also removes a hidden e2e network dependency. TDD `126d97d` (RED, 6 failing) → `03968a4` (GREEN merge, 7/7) → `7f5b511` (ON CONFLICT upsert). tsc + nest build clean; **full e2e 150/150 green**; merge spec 7/7; vehicles e2e 31/31 unchanged.
- **D-15 (2026-06-02, plan 04-03) — server-side catalog search API (CAT-06 contract frozen):** `GET /vehicles/catalog` now searches `LOWER(vm.brand || ' ' || vm.model) LIKE :s` — the **exact expression the 04-01 `pg_trgm` GIN index covers** (index-backed in Postgres, seq-scan-safe under the SQLite e2e harness); deliberately **`LOWER(...) LIKE`, NOT `ILIKE`**, so the e2e harness still runs. `CatalogQueryDto` gains `brand` (`@IsString`) + `fuelCategory` (`@IsIn(['gas','diesel','ev','gpl'])`); `@Max(100)` limit retained (DoS bound). A shared private `applyCatalogFilters(qb, query)` builds the WHERE for **both** the listing and the new facet so counts never drift; `fuelType` (single) takes precedence over `fuelCategory` (expanded via new `categoryToFuelTypes` reverse helper in `common/fuel-type-categories.ts`). New `findCatalogBrands` → `[{ brand, count: Number(count) }]` grouped/ordered by brand ASC; the `GET catalog/brands` route is declared **before `catalog/:id`** so Nest doesn't capture `'brands'` as an id. The `{ items, total, page, limit, totalPages }` shape is **frozen — Phase 5 mobile reuses it**. All user input bound as `:params` (`:s`/`:brand`/`:...fts`) — no SQL interpolation (T-04-03-01/02/03 mitigated). Deviations (both Rule 3 blocking): `import type FuelCategory` (TS1272 under isolatedModules+emitDecoratorMetadata); aligned the vehicles e2e `ValidationPipe` to production (`enableImplicitConversion`) so int query params coerce — required for the pagination test. Commits `d184eaf` (feat) + `d5379b9` (test); tsc + nest build clean; vehicles e2e **31/31**, full suite **150/150** green (+7). `VehicleSyncService.doSync` UNIQUE log persists (DEF-04-01-01, scoped 04-04).
- **D-14 (2026-06-02, plan 04-02) — multi-source ingestion spine (adapters + EPA snapshot):** A `CatalogSourceAdapter` contract (`load` + `normalize` + `source` + `precedence`) + `CanonicalVehicle` shape is the CAT-05 extensibility spine — a 3rd source implements the interface and picks a precedence, no merge/schema change. Two EPA conversions are unit-proven (TDD RED `9f7e1d0` → GREEN `2a893ab`): `mpgToLper100km = 235.214583/MPG`, `combEToKwhPer100km = combE/1.609344` (1 decimal, `null` on ≤0). `epaFuelToType` returns `{ fuelType, metric }` so EVs read `combE` and ICE reads `comb08` — combE never runs through the MPG formula (Pitfall 3); gasoline→SP95, Diesel→DIESEL, Electricity/EV→ELECTRIC, **PHEV/Hybrid→primary combustion fuel (one row, mirrors ADEME)**, **FFV/E85→SP95** (plan action, not E85), CNG/Hydrogen/Fuel Cell→`null` (skip). `AdemeAdapter` (source=`ademe`, precedence=0) extracted from the sync (output identical in shape; exports shared name helpers); `EpaAdapter` (source=`epa`, precedence=10) streams the committed `;`-delimited snapshot via `readline split(';')` (NO csv-parse dep), applies `EPA_BRAND_ALLOWLIST` (Mercedes-Benz→Mercedes), conservative trim-strip, and skips on missing make/fuel/metric. `build-epa-snapshot.ts` (one-time dev tool) downloaded fueleconomy.gov and emitted `epa-vehicles.snapshot.csv` (26749 allow-listed rows, 1.86 MB, provenance header). **CanonicalVehicle carries NO `year`** (locked contract; canonical key excludes year). **`nest-cli.json` now copies `data/**/*.csv` to dist** (Rule 2 — runtime snapshot resolution). Deviations: fetch 406 fixed via UA+`Accept:*/*` (Rule 3). `VehicleSyncService.doSync` deliberately untouched (merge refactor + DEF-04-01-01 remain 04-04). Commits `9f7e1d0`/`2a893ab`/`36e3c5e`/`fc5631c`; tsc + nest build clean, 19/19 conversions.spec green.
- **D-13 (2026-06-02, plan 04-01) — multi-source catalog schema foundation:** `vehicle_models` gains `source` varchar(16) default `'ademe'` (provenance, CAT-02) — a **plain varchar, NOT a PG enum**, so a 3rd source drops in without `ALTER TYPE` (CAT-05). The canonical `UNIQUE(brand, model, fuel_type)` is declared in **both** the entity (`@Index('UQ_vehicle_models_canonical', …, { unique: true })`, so SQLite `synchronize` gives e2e `ON CONFLICT` support) **and** the hand-written Postgres migration `1748100000000` (`IF NOT EXISTS`). **year is excluded from the key** (metadata; cross-source merge collapses model-years). The migration **dedupes (DELETE keep MIN(id) per group) BEFORE the unique index** so `migrationsRun:true` cannot crash the Render deploy (Pitfall 5); it also adds a `pg_trgm` GIN on `lower(brand || ' ' || model)` (expression matches the planned 04-03 ILIKE search) + `btree(brand)` for CAT-06. `down()` drops the 3 indexes + the column, leaves pg_trgm. **`VehicleSyncService.doSync` was NOT migrated to `ON CONFLICT`** — that refactor is scoped to 04-02/04-04 (logged as **DEF-04-01-01** in `04-…/deferred-items.md`); its background bootstrap sync now logs a *caught* UNIQUE collision under the new constraint, but the vehicles e2e is green (24/24) and prod skips the sync (already > STARTUP_THRESHOLD). Commits `1523a57` (entity) + `d8b9f81` (migration).
- Precise tolls via TollGuru (live) — only practical origin→destination toll API; partial branch already in `backend/src/trips/trips.service.ts`.
- Editorial premium dark direction — evolves existing Carbon identity (Linear/Vercel vibe).
- Preserve architecture, redesign visual layer only — lower risk, keeps working flows intact.
- Measured micro-interactions over heavy animation — best impact/effort, safest for performance.
- Apply redesign to web AND mobile for cross-platform consistency.
- Multi-source catalog (ADEME + EPA, extensible) placed AFTER web redesign rollout so the showroom is scaled in its final editorial design, not reworked; placed BEFORE mobile so mobile inherits both the redesign and the server-side catalog search.
- Consumption must stay real (source-attributed, no fabricated defaults) to protect the Core Value of accurate cost.
- **D-07 (2026-06-01) — estimate-primary re-scope:** TollGuru is paid and a key cannot be obtained. The heuristic French-toll **estimate is adopted as the primary production mode**. The precise TollGuru code path stays built and dormant; it activates automatically if a `TOLLGURU_API_KEY` is ever configured. TOLL-01 (precise live) and TOLL-03 (class-1 live) are **deferred** to a future gap-closure plan.
- **D-09 (2026-06-01) — route-aware estimator shipped (Phase 01.1):** The flat speed-fraction heuristic is replaced by a route-aware estimate — `classifyTolledKm(steps)` sums Mapbox `steps[].ref` autoroute km minus a `FREE_AUTOROUTES` set, × `NATIONAL_AVG_RATE_PER_KM = 0.09`. National average only (no per-operator overrides — `ref` yields the A-number, not the operator; YAGNI). Added via an **additive optional `steps?` 4th arg** on `computeTollCost` (cache + never-throw + dormant TollGuru branch untouched, LD-2). Mapbox `getDirections` now requests `steps=true`; `TripsService` threads `directions.steps`. Speed heuristic retained as the no-steps fallback. Always `isEstimate: true`; no UI/schema change (LD-6/LD-7). Paris→Lyon → €40.50 (in €30–45 band).
- **D-11 (2026-06-02) — serif display reverted (Phase 2 checkpoint):** The editorial serif display face (Instrument Serif) was implemented (02-01/02-04), reviewed live, and **dropped** — the user preferred the previous font. Titles/headings revert to **Space Grotesk bold (700)**; no serif family remains. The `--text-display` size token + the rest of Phase 2 (refined palette, 4-size/2-weight scale, DataBar, Skeleton, useCountUp/useReducedMotion, animated counter, motion) are kept. Editorial character comes from palette + scale + data-viz + motion, NOT a serif font. **Phase 3 must not reintroduce a serif display.** Commit `c585592`.
- **D-10 (2026-06-02) — disclaimer washes neutralized (plan 02-04):** The distance-mode + EV disclaimer notes on the result page were converted from `bg-amber-500/10`/`text-amber-400` to neutral `bg-carbon-surface2`/`text-carbon-ink2`/hairline. Reason: the editorial color contract reserves the energy palette (incl. amber `--c-fuel-die`) for data-viz + FuelBadge only, and the plan's verification grep forbids `bg-amber` in the file. Soft informational notes therefore use surface tokens, not a warning color. Applies to any future screen carrying inline disclaimer notes.
- **D-08 (2026-06-01) — free route-aware toll estimator (chosen alternative to TollGuru):** Research confirmed no permanently-free precise toll API for FR without a card (TollGuru = 14-day trial then $80+/mo; Google Routes = card + paid SKU beyond cap; FR open data has toll GATES but no tariff grid). Decision: **improve the estimate** to be route-aware — detect tolled autoroute km on the route and apply a per-network average €/km — fully free, no card, behind the same provider-agnostic `TollService.computeTollCost(coordinates,...)` interface. Stays labelled "≈ estimé". Likely implementation: Mapbox Directions `steps=true` road `ref` (A-roads) minus a free-autoroute exclusion list × per-operator €/km (Vinci/APRR/Sanef ~0.08–0.11 €/km); Overpass `toll=yes` is the fallback technique. Precise providers (Google Routes / open-data tariff engine) remain a later option behind the same interface.

- **D-12 (2026-06-02, plan 03-05) — trip detail toll badge single source of truth:** The trip-detail page (`/app/trips/[id]`) was restyled to MIRROR the result page — the total renders as a mono `text-hero` figure and the toll breaks out via a Variant A `DataBar` (énergie/péage) shown only when `tollsCost > 0` (D-04). The réel/≈ estimé `Pill`+`Tooltip` (Phase 1) was **moved verbatim** (same copy/logic) into the péage legend and the separate "Péages" row was removed, so the toll badge has one canonical location matching the result page. No data/flow change (MD-2). Detail hero is static (no `useCountUp`) since it shows a saved historical value, not a live calculation.

### Todos / Watchpoints

- TollGuru must degrade gracefully to heuristic when key absent or over quota (free tier ~500 req/month).
- TollGuru key is server-side only — never in any client response or bundle.
- Web token must be cached server-side to avoid burning quota on repeated routes.
- ✅ `TollService` extracted from `TripsService` (plan 01-01): precise TollGuru polyline call, defensive parse, 30-day SHA-1 cache, silent heuristic fallback; wired via `Promise.all`. `computeTollCost`/`estimateFrenchTolls` removed from `TripsService`.
- ⚠️ Precise TollGuru branch is unit-tested with a mocked `fetch` only — endpoint/response-shape/precision (RESEARCH A1–A6) remain **unverified against the real API** and are now **deferred indefinitely** (no paid key, D-07). Re-open as a gap-closure plan the day a key is obtained: set key → Paris→Lyon → expect `tollIsEstimate=false` ≈ €35–40 + cache-hit + green "réel" badge.
- ✅ `Trip.tollIsEstimate` persisted (plan 01-02): `toll_is_estimate` boolean column + hand-written migration `1748000000000` (applied dev, `[X] 5`); `SaveTripDto.tollIsEstimate` (optional `@IsBoolean`); `saveTrip` writes `?? false`; crud e2e proves round-trip via `GET /trips/:id` and stats-once. Plan 01-03 renders the badge on the detail page.
- ℹ️ Global `ValidationPipe` uses `enableImplicitConversion: true` — coerces strings to booleans before `@IsBoolean` runs, so a malformed boolean is silently truthy-coerced (not 400). Project-wide; relevant if any future negative DTO test targets a boolean field.
- Existing single-source pipeline: `backend/src/vehicles/vehicle-sync.service.ts` + import script `backend/src/scripts/import-ademe.ts` (~266 deduped ADEME entries) — Phase 4 generalizes this into a multi-source, idempotent ingestion with provenance + cross-source merge.
- Showroom `web/src/app/app/garage/add/page.tsx` currently loads ALL models client-side — will not scale; Phase 4 moves it to server-side search + pagination and exposes an API the mobile phase reuses.
- CAT-06 spans web + mobile server-side search; it is owned by Phase 4 (builds the API + web showroom). Phase 5 consumes that API for the mobile showroom (no client load-all).
- Mobile has ~7 pre-existing TS errors unrelated to this work; do not let Phase 5 inherit blame for them.
- Mobile design tokens must work in RN StyleSheet (no Tailwind); web uses `bg-carbon-*` Tailwind utilities.
- `master` auto-deploys (Render + Vercel) — commit + push after each verified update.
- ✅ Phase 2 (editorial dark DS + result redesign): tokens refined + Instrument Serif (400/700) + 4 type sizes (02-01); useReducedMotion/useCountUp (02-02); DataBar + Skeleton (02-03); result page redesigned — serif title, animated mono hero counter, breakdown + comparison DataBars, staggered reveal, skeleton, stepper a11y (02-04). Code review: CR-01 (invalid `<p>` in `<h2>`) + WR-01 (sr-only total) + WR-02 (FR hero comma) + WR-03 (eyebrow type) + WR-05 (total≤0 bar guard) FIXED (commit 7ec80d2). Build 18/18 green.
- ⏸️ Phase 2 deferred review items (Phase 3 / acceptable): WR-04 (hero counter re-counts 0→target on each passenger change — per-spec; tween-from-previous if it reads glitchy), WR-06 (reveal keyframe rests at opacity:0 — make resting state visible for robustness), IN-04/IN-05 (CTAButton font-medium/600 + per-instance useReducedMotion listeners → normalize when Phase 3 standardizes atoms).
- ⏳ Phase 2 visual checkpoint (02-05) pending USER visual verification on the deployed site (count-up, hide-when-toll-0, prefers-reduced-motion, light/dark parity, serif title). Phase not marked fully complete until confirmed.
- ✅ Phase 1.1 (D-08): route-aware FREE toll estimate shipped — `classifyTolledKm` sums Mapbox `steps[].ref` autoroute km minus `FREE_AUTOROUTES` × `NATIONAL_AVG_RATE_PER_KM` (0.09); Mapbox `getDirections` now requests `steps=true` and surfaces `RouteStep[]`; threaded into `computeTollCost` as an additive optional 4th arg (cache/never-throw/TollGuru branch untouched). `isEstimate` always true. Paris→Lyon ≈ €40.50.
- ℹ️ Phase 1.1 code review — 2 accepted/deferred warnings: WR-01 `getDirections` requests `steps=true` unconditionally so `calculateMulti` fetches a heavier payload it discards (negligible; revisit if Mapbox quota matters); WR-02 e2e mocks `TollService` (stubs use `steps:[]`) so the real classifier isn't driven through HTTP — mitigated by exhaustive unit coverage (toll 26/26 incl. anchor + ref variants) + the e2e 4th-arg wiring assertion. Add a real-classifier e2e if integration confidence ever needs raising.

### Blockers

- None.

## Session Continuity

- **Last action:** Phase 8 plan 08-01 complete — **web onboarding tour** (ONB-01 web slice). 2 atomic commits. `04416d2` (feat) — `web/src/lib/onboarding.ts` (per-user localStorage seen-flag `hasSeenOnboarding`/`markOnboardingSeen`, SSR-safe + never-throw, key `verygoodtrip.onboarding.seen.${userId}`; `ONBOARDING_OPEN_EVENT`) + `web/src/components/OnboardingTour.tsx` (self-contained `'use client'` full-screen `z-[120]` editorial-dark 7-step FR carousel: `/auth/me` → auto-open only on unset flag (ONB-5), `window` event re-opens regardless of flag (ONB-4 replay), Eyebrow step counter + lucide icon + display title + ink2 body + 7 progress dots + Passer/Précédent/Suivant/Terminer, Skip/Terminer/close/backdrop/Escape mark-seen+close, transform/opacity-only suppressed under `useReducedMotion`, no serif/no console.log/no new dep; péages step = "estimé, inclus dans le total" with NO real/estimate badge wording). `51ea9bd` (feat) — AppLayout mounts a single `<OnboardingTour />` across `/app/*` (additive MD-2) + Settings "Aide" SectionCard with a ghost "Revoir le tutoriel" CTAButton dispatching `ONBOARDING_OPEN_EVENT` (theme/pseudo/logout preserved). No deviations. Verify: web `tsc --noEmit` clean + `next build` **18/18 routes** + grep gates pass. Mobile onboarding (08-02) next. Not yet pushed — orchestrator pushes after phase verification.
- **Phase 5 prior:** plan 05-03 complete — **RN data-viz/motion primitives + editorial atoms** (Wave 2; PD5-3, contributes MOB-01/MOB-02). 3 atomic commits. `e82a43d` (feat) — `mobile/src/hooks/useReducedMotion.ts` (`AccessibilityInfo.isReduceMotionEnabled` + `reduceMotionChanged` subscription, `false` until async resolves, `.remove()` + `isMounted` cleanup) + `mobile/src/components/ui/AnimatedCounter.tsx` (`Animated.timing` 0→value easeOutCubic ~700ms via JS-driven `Animated.Value`+`addListener`, FR comma 2dp in JetBrains Mono 700, **INSTANT under reduced motion**, anim stopped + listener removed on cleanup → no leaked frames, T-05-03-01 mitigated; `useNativeDriver:false` so the listener can format the value). `2ac75e3` (feat) — `mobile/src/components/ui/DataBar.tsx` discriminated union: Variant A segmented Énergie/Péage (`energyValue`/`tollValue`/`total`/`energyFill`; toll segment **hidden when `tollValue<=0`**, D-04) + Variant B single comparison (`value`/`max`/`fill`/`muted`→0.45); `pct()` clamp+zero/NaN guard, `widthPct()`→`DimensionValue`, editorial token fills (`energyFill` prop, `c.toll`); mount **opacity** reveal (native driver) gated OFF under `useReducedMotion` (chose opacity over web scaleX — typed+compositor-friendly, stable layout); heights sm=6/md=10; exports `DataBarProps`. `87d00d5` (feat) — `Eyebrow` (uppercase caption, display 700, `FontSize.caption`, tracking 1.6, `mutedText`) / `Pill` (success/warning/neutral/accent tinted via `withAlpha` 0.16, sm/md, `Radius.full` — réel/≈ estimé toll badge) / `SectionCard` (surface+hairline+card radius, optional Eyebrow title row) + additive `export const FontSize = tokens.fontSize` on `mobile/constants/theme.ts` (atoms read editorial scale via aliased `@/constants/theme`; Rule 3). Token-driven, named exports, no hardcoded blue. **MOB-02 not marked complete** (primitives only; toll display ships Wave-3). **TDD gate gap documented** (no mobile test runner; tsc-only verify). Verify: mobile `tsc --noEmit` **0→0 GREEN** (per-file + full run); `useReducedMotion` adds/removes `reduceMotionChanged`; `AnimatedCounter` uses `Animated`+gates on `useReducedMotion`; `DataBar` `tollValue`×5 + `c.toll`. Not yet pushed — orchestrator pushes after phase verification.
- **Phase 5 prior:** plan 05-02 complete — **mobile editorial fonts + shared atom normalization + clean tsc baseline + toll/catalog types & i18n** (Wave 2; MOB-01/MOB-03). 3 atomic commits. `cb0a445` (feat) — `mobile/app/_layout.tsx`: `SplashScreen.preventAutoHideAsync()` module-scope + `useFonts` (expo-font) loads `SpaceGrotesk_400Regular/700Bold` + `JetBrainsMono_400Regular/700Bold` (keys match `tokens.fontFamily`), `if(!loaded) return null` splash gate, effect `hideAsync()`; AuthProvider/StatusBar/Stack/Toast/i18n preserved (MD-2); `@expo-google-fonts/space-grotesk`+`jetbrains-mono` ^0.4.1 committed (pre-installed per resume note, no `expo install` re-run). `faae17b` (feat) — Button (accent/surface2/fuelGas variants, dark-ink `#0e0c0a` CTA label, display 700, `StyleProp<ViewStyle>` array fixing `false`-ViewStyle, `ThemeColors` params) / Card (surface/hairline/radius card) / Input (surface/hairline/accent/ink/mutedText, display 700 label) / Wordmark (Colors[scheme] ink/accent defaults, display font, no hardcoded hex) / AutocompleteInput (surface/hairline/ink/mutedText/surface2/accent, geocode logic verbatim) / MapboxMap (children on MV cast fixing overload, route line `#2563eb`→`#4d8bff`, placeholder repointed) → **mobile tsc 2→0 FULLY GREEN** (PD5-2 clean). `0618ed1` (feat) — `types/api.ts` TripResult +optional tollCost/tollIsEstimate, VehicleModel +optional source, new CatalogPage (frozen Phase-4 shape); fr.ts/en.ts symmetric `dashboard.{tollLabel,tollReal,tollEstimate,tollEstimateTooltip,energyLabel,costEstimated,breakdown}` + `vehicles.{loadMore,noResults,resultsCount,searchHint}` pre-seeded for Wave-3 (no cross-plan i18n overlap). No deviations. Verify: mobile `tsc --noEmit` 0 errors; no `#3b82f6`/`#2563eb`/`#F0ECE4`/`#4D8BFF` in components; route line `#4d8bff`; `tollIsEstimate` in api.ts; `tollEstimate(+Tooltip)` in both fr/en. Not yet pushed — orchestrator pushes after phase verification.
- **Phase 5 prior:** plan 05-01 complete — **shared editorial-dark token module + mobile theme foundation** (Wave 1 BLOCKING; MOB-01/MOB-03). 3 atomic commits. `0462387` (feat) — `shared/src/tokens.ts` framework-free (ZERO imports) `tokens` const: `colors.dark`/`colors.light` ×15 keys mirroring `web/src/app/globals.css` hex exactly (dark `bg #0e0c0a`…`toll #b8a98c`; light incl. `fuelGpl #7c3aed`/`toll #9b8a6b`) + `fontFamily` (Space Grotesk + JetBrains Mono loaded-font names, NO serif D-11) + `fontWeight` 400/700 + `fontSize` 56/26/14/11 + `spacing` 8-pt + `radius` (incl. `card:12`); barrel-re-exported via `shared/src/index.ts`. `4134396` (feat) — `mobile/metro.config.js` Expo SDK 54 monorepo: `getDefaultConfig(__dirname)` + `watchFolders=[repo root]` (bundles out-of-project `shared/`) + `resolver.nodeModulesPaths=[project,root]`; hierarchical lookup left ENABLED (per-project lockfiles). `3f7250f` (feat) — `mobile/constants/theme.ts` rewritten to import `tokens` via relative `../../shared/src/tokens`, single `ThemeColors` interface shared by `Colors.light`/`Colors.dark` (`Record<ColorScheme,ThemeColors>`) → **resolves 5 of 7 tsc errors** (dashboard ×3, Button ×2 union); blue `Primary` ramp DELETED; superset keeps all legacy keys remapped to editorial tokens (`muted` collision → legacy fill=`surface2`, editorial muted text=`mutedText`; `success` green kept); `Fonts`/`Spacing`/`Radius` from tokens, legacy `FontSizes` preserved. **Deviation:** `mobile/tsconfig.json` NOT modified (alias already present, nothing excludes `../shared`). Verify: `shared` tsc exit 0; mobile `tsc --noEmit` **7→2** (remaining MapboxMap overload + Button `false`-ViewStyle deferred to 05-02); metro.config.js loads via node require with watchFolders+nodeModulesPaths; no `#3b82f6`/`Primary` in theme.ts. Not yet pushed — orchestrator pushes after phase verification.
- **Phase 4 prior:** plan 04-05 complete — **server-side showroom search + pagination** (Wave 3; CAT-06 web slice). 1 atomic commit. `43ddd23` (feat) — `web/src/types/api.ts` `VehicleModel` gains optional `source?: 'ademe' | 'epa' | string`; `web/src/app/app/garage/add/page.tsx` switched from client load-all (parallel `MAX_PAGES` up to 3000 rows + client substring/type `useMemo` filter — both REMOVED) to **debounced** `useDebounce(search, 300)` server-side `GET /vehicles/catalog`. Two-effect pagination: reset `[debounced, fuelFilter]→setPage(1)` + cancellable loader `[debounced, fuelFilter, page]→apiClient.get('/vehicles/catalog', { params: { search, page, limit: 60, ...fuelParams } })` (page 1 REPLACES / page>1 APPENDS; cleanup `cancelled=true` discards a lingering page>1 fetch on query reset → never wrongly appends). Chip→ONE server param: `ev/diesel/gpl`→`fuelCategory`, `essence`→`fuelCategory=gas` (04-03 expands to SP95/SP95_E10/SP98/E85), `e85`→`fuelType=E85`, `all`→none. Brand-grouping `useMemo` folds over accumulated `items`; header shows loaded count + `sur {total}` + brand count; **'Charger plus'** `CTAButton` (`page<totalPages`, `isLoadingMore`-guarded, mirrors trips/history) replaces unbounded client memory (T-04-05-03). All Phase 3 editorial-dark visuals + the add-vehicle `POST /vehicles/me` flow preserved verbatim (MD-2, data-layer-only). Deviation (Rule 3): dropped unused `FuelType` import + `FUEL_FILTERS.types` after removing the client filter (`no-unused-vars` build error). Verify: web `tsc --noEmit` + `next build` clean, **18/18 routes**, `/app/garage/add` 6.78 kB. Consumes the frozen `{ items, total, page, limit, totalPages }` contract Phase 5 mobile reuses. Not yet pushed — orchestrator pushes after phase verification.
- **Phase 4 prior:** plan 04-04 complete — **multi-source merge orchestrator + idempotent ON CONFLICT upsert** (Wave 3; CAT-03/CAT-05). 3 atomic commits. `126d97d` (test/RED) — `catalog-merge.spec.ts` (7 DB-agnostic cases: ADEME-precedence first-writer-wins, EPA gap-fill, case-insensitive key collision, precedence-independent-of-array-order, idempotency twice=identical, 3rd-adapter precedence-20 extensibility, null-skip) + `mergeCanonical` stub returning `[]` (6 failing). `03968a4` (feat/GREEN) — `mergeCanonical(adapters)` precedence-sort + first-writer-wins `Map` (uppercased `brand|model|fuelType`); `doSync` rewired to `[AdemeAdapter, EpaAdapter]` → merge (all inlined ADEME fetch/normalize removed, now owned by AdemeAdapter); `OnApplicationBootstrap`/`STARTUP_THRESHOLD`/`isSyncing` preserved; 7/7 green. `7f5b511` (feat) — `doSync` splits merged by source → ADEME `.orUpdate([consumption,battery_capacity_kwh,tank_capacity_liters,source],[brand,model,fuel_type])` / EPA `.orIgnore()` batches of 100 on the canonical UNIQUE triple (**resolves DEF-04-01-01**); every row carries `source`; `this.logger` only; **auto-bootstrap sync skipped under `NODE_ENV=test`** [Rule 1] (orUpdate would overwrite seeded fixtures via live ADEME fetch). STARTUP_THRESHOLD=500 retained + documented (prod ~266<500 → one-time multi-source populate on next deploy, then boots skip; manual `POST /vehicles/sync` refreshes). Verify: tsc + nest build clean; merge spec **7/7**; **full e2e 150/150** green; vehicles e2e 31/31 unchanged. Not yet pushed — orchestrator pushes after phase verification.
- **Phase 4 prior:** plan 04-03 complete — **server-side catalog search API** (Wave 2; CAT-06). 2 atomic commits. `d184eaf` (feat) — `CatalogQueryDto` +`brand`/+`fuelCategory` (`@IsIn`, `import type FuelCategory`), `categoryToFuelTypes` reverse helper in `common/fuel-type-categories.ts`, `findCatalog` rewritten to `LOWER(vm.brand || ' ' || vm.model) LIKE :s` (matches 04-01 GIN index expr; bound param; NOT ILIKE for SQLite) + `brand`/`fuelCategory` filters (fuelType precedence) via shared private `applyCatalogFilters`, new `findCatalogBrands` → `[{ brand, count }]` grouped/ordered by brand ASC, `GET catalog/brands` route declared **before `catalog/:id`**. `d5379b9` (test) — +2 seed rows (Peugeot 308 DIESEL, Tesla Model Y ELECTRIC) + 7 e2e (search=clio shape, fuelCategory=ev, brand=Tesla, page=1&limit=1, 400 on fuelCategory=banana, /brands facet ordered+numeric counts Tesla=2 + fuelCategory filter); aligned e2e ValidationPipe to production (`enableImplicitConversion`) so int query params coerce [Rule 3]. The `{ items, total, page, limit, totalPages }` shape is **frozen for Phase 5 mobile reuse**. Verify: tsc + nest build clean; vehicles e2e **31/31**, full suite **150/150** green (+7). `doSync` UNIQUE log persists (caught; DEF-04-01-01 = 04-04). Not yet pushed — orchestrator pushes after phase verification.
- **Phase 4 prior:** plan 04-02 complete — multi-source **ingestion spine** (Wave 2; CAT-01/CAT-02/CAT-04/CAT-05). 4 atomic commits. `9f7e1d0` (test/RED) — `catalog-source-adapter.interface.ts` (`CanonicalVehicle` + `CatalogSourceAdapter`) + failing `conversions.spec.ts` (19 AAA cases) + stub `conversions.ts`. `2a893ab` (feat/GREEN) — `conversions.ts`: `MPG_TO_L_PER_100KM=235.214583`, `MILES_TO_KM=1.609344`, `mpgToLper100km`/`combEToKwhPer100km` (1dp, null on ≤0), `epaFuelToType` returning `{fuelType, metric}` (combE for EV, comb08 for ICE; PHEV→primary; FFV/E85→SP95; CNG/H2→null); 19/19 green. `36e3c5e` (feat) — `build-epa-snapshot.ts` (inline RFC-4180 quote-aware reader, no csv-parse) + committed `data/epa-vehicles.snapshot.csv` (26749 allow-listed rows, 1.86 MB, provenance header, 9 `;`-cols). `fc5631c` (feat) — `ademe.adapter.ts` (extracted, source=ademe/precedence=0, exports name helpers) + `epa.adapter.ts` (source=epa/precedence=10, snapshot stream-parse, `EPA_BRAND_ALLOWLIST` Mercedes-Benz→Mercedes, trim-strip, skip-on-bad) + `nest-cli.json` data asset copy. Verify: `tsc --noEmit` + `nest build` clean (snapshot in `dist/data`); hand-trace — gas 25mpg→9.4 SP95, EV combE30→18.6 ELECTRIC, diesel-PHEV→DIESEL, non-allow-list/CNG/zero-metric→null. `VehicleSyncService.doSync` NOT touched (merge + DEF-04-01-01 = 04-04). Not yet pushed — orchestrator pushes after phase verification.
- **Phase 4 prior:** plan 04-01 complete — multi-source catalog **schema foundation** (Wave 1 BLOCKING; CAT-02/CAT-03/CAT-06). 2 atomic commits. `1523a57` `vehicle-model.entity.ts` — added `source!: string` (`@Column varchar(16) default 'ademe'`, provenance CAT-02, plain varchar not a PG enum for CAT-05) + class-level `@Index('UQ_vehicle_models_canonical', ['brand','model','fuelType'], { unique: true })` (imported `Index`; gives SQLite e2e the canonical constraint for `ON CONFLICT`); consumption/battery/tank/year untouched. `d8b9f81` `1748100000000-AddSourceAndCatalogIndexes.ts` — hand-written reversible migration (mirrors `1748000000000` style), `up()` in strict order: ADD COLUMN IF NOT EXISTS source → **DEDUPE (DELETE keep MIN(id) per (brand,model,fuel_type)) BEFORE the unique index** (Pitfall 5 — crash-proofs the `migrationsRun:true` auto-deploy) → `CREATE EXTENSION IF NOT EXISTS pg_trgm` → `UNIQUE INDEX UQ_vehicle_models_canonical` → `btree(brand)` → `GIN ((lower(brand || ' ' || model)) gin_trgm_ops)` (expr matches 04-03 search); `down()` drops the 3 indexes + column reverse, leaves pg_trgm. Verify: backend `tsc --noEmit` + `nest build` clean, migration emitted to `dist/`; vehicles e2e **24/24 green** (new `source` rides along via SQLite synchronize). ⚠️ The background ADEME sync now logs a *caught* `UNIQUE constraint failed` because `doSync` still uses `repo.save()` (no ON CONFLICT) — **deferred to 04-02/04-04 (DEF-04-01-01)**, not failing tests. Not yet pushed — orchestrator pushes after phase verification.
- **Phase 3 prior:** plan 03-06 complete — Cluster E fuel-prices + settings editorial-dark restyle (Wave 2; WEB-01/WEB-04). 2 atomic commits. `0427ae9` `fuel-prices/page.tsx` — `h1` `text-2xl`→`text-display` 700; api/custom source toggle standardized (inactive `text-carbon-muted`, hover `text-carbon-ink2 bg-carbon-faint`, active `bg-carbon-accent text-white`, `font-normal` weight-constant + `aria-pressed`, canonical accent focus ring); emoji `📡`/`✏️` → lucide `RadioTower`/`Pencil` via `SOURCE_META`; section labels (Source/Carburants/Électricité) → `Eyebrow` atom (700); read-only price tiles value + `DeltaBadge` `font-semibold`→`font-bold` mono `tabular-nums`; mount-guard + inner `loadingDefaults` `animate-pulse` → `Skeleton` (header line + 3 card skeletons / 6 tile skeletons); `range` keeps `accent-carbon-accent`; localStorage source/price read+write + auto-save + toast + `PriceRow` math all preserved (MD-2). `8fc63b7` `settings/page.tsx` — `h1`→`text-display` 700; theme picker `font-medium`→`font-normal` (weight-constant active/inactive, CWV) + canonical accent focus + `aria-pressed`, active keeps sanctioned `bg-blue-500/10 text-carbon-accent`; profile name `font-semibold`→`font-bold`; version `text-[11px]`→`text-caption`; `mounted` hydration-guard skeleton rebuilt from `Skeleton` atom (no inline `bg-carbon-surface2` divs) — guard itself preserved; next-themes wiring + logout flow unchanged (MD-2). Web `tsc` clean + `build` 18/18 green; grep gate across both files: 0 `font-medium`/`font-semibold`/`font-serif`/`ring-blue-500`/`focus:ring-offset-0`/ad-hoc `animate-pulse`; fuel-prices 0 `📡`/`✏️`; fuel-prices contains `Skeleton`, settings contains `CTAButton`. Completes WEB-01 across the Cluster A/C/D/E screens (dashboard WEB-03 in 03-03 remains). Not yet pushed — orchestrator pushes after phase verification.
- **Phase 3 prior:** plan 03-05 complete — Cluster D trips history + trip detail + favorites editorial-dark restyle (Wave 2; WEB-01/WEB-04). 3 atomic commits. `3e25486` `trips/page.tsx` — `h1` `text-2xl`→`text-display` 700; stats/month labels `font-semibold`→`text-caption font-bold`; filter chips standardized (inactive `text-carbon-muted`, hover `text-carbon-ink2 bg-carbon-faint`, active `bg-carbon-accent`, `font-normal` weight-constant, canonical focus); row buttons get canonical focus, route line `font-medium`→`font-normal`, chevron `aria-hidden`; ad-hoc `animate-pulse h-16 ×6` → layout-mirroring `Skeleton` rows; filter/pagination/navigation preserved. `cf86a9b` `trips/[id]/page.tsx` — MIRRORS result page: route title `text-base font-semibold`→`font-display font-bold text-display` (no serif, D-11); total → mono `text-hero` figure on `!bg-carbon-surface3` plate (French comma); Variant A `DataBar` énergie/péage breakdown rendered when `tollsCost>0`, with the réel/≈ estimé `Pill`+`Tooltip` (Phase 1) relocated verbatim into the péage legend and the duplicate péage row removed (single source of truth); metric/404 labels → 400/700; back button canonical focus; note saving indicator de-pulsed; loading `animate-pulse` block → `Skeleton`; params resolver + fetch + note auto-save (useDebounce/savedNoteRef) + archive/refaire/delete all unchanged. `2bea436` `favorites/page.tsx` — `h1`→`text-display` 700; favorite name `font-semibold`→`font-bold`; "Chargement…" text → `Skeleton` rows; row hover `bg-carbon-surface2`; delete icon button gets canonical focus + clearer `aria-label`, `Trash2 aria-hidden`; "Utiliser" deep-link + danger delete `Modal` preserved. All visual-layer-only (MD-2). Web `tsc` clean + `build` 18/18 green; grep gate across the 3 files: 0 `font-medium`/`font-semibold`/`font-extrabold`/`font-serif`/`ring-blue-500`/`focus:ring-offset-0`/ad-hoc `animate-pulse`; detail contains `DataBar` + preserved `Pill`+`Tooltip`. Not yet pushed — orchestrator pushes after phase verification.
- **Phase 3 prior:** plan 03-02 complete — Cluster A landing + auth editorial-dark restyle (Wave 2; WEB-01/WEB-04). 2 atomic commits (`40360aa` landing `page.tsx` — `h1` `text-4xl/5xl font-extrabold`→`text-display` (Space Grotesk 700, NO serif D-11) + header "Créer un compte" and both hero CTAs → `CTAButton` (accent/surface, `<Link><CTAButton>` pattern from dashboard) + header "Se connecter" link + feature `<h3>` `font-semibold`→`font-bold` + body `text-xs`→`text-sm` + footer `text-caption`; primary hero CTA keeps `shadow-lg shadow-blue-500/20`; `40360aa` `c01a4c2` login + register — Google/Apple OAuth `<a>` `font-medium`→`font-normal` quiet options + canonical accent focus ring on OAuth buttons and footer cross-links; Input + accent CTAButton inherit 03-01 atoms unchanged; the two OAuth callback pages already compliant (transform-based `LoaderCircle animate-spin`, Suspense + token/redirect logic) — left untouched). All auth/OAuth flow, zod `.refine` confirmPassword, submit handlers preserved (MD-2). Web `tsc` clean + `build` 18/18 green; grep gate across the 5 files: 0 `font-medium`/`font-semibold`/`font-extrabold`/`font-serif`/`ring-blue-500`/`focus:ring-offset-0`; landing `h1` `text-display`, ≥3 `CTAButton`. Not yet pushed — orchestrator pushes after phase verification.
- **Phase 3 prior:** plan 03-01 complete — atom + AppLayout shell normalization (Wave 1 BLOCKING foundation; WEB-01/WEB-04). 2 atomic commits (`79b6d07` 13 `ui/` atoms → 2-weight 400/700 + CTAButton accent/surface/ghost focus ring `ring-blue-500/*`→`ring-carbon-accent/50` (accent-color fix #3b82f6→#4d8bff, danger keeps red) + Input/Select canonical `focus-visible` token replacing `focus:ring-offset-0`; `4cf06b4` AppLayout `+ Nouveau trajet` CTA `font-semibold`→`font-bold` (font-mono kept) + canonical focus ring on CTA/burger/collapse/drawer-close + `aria-label` on the icon-only-at-xs CTA, active SidebarItem `bg-blue-500/10 text-carbon-accent` tint kept). API-stable className-only (MD-2). Web `tsc` clean + `build` 18/18 green; grep gate: 0 `font-medium`/`font-semibold`/`font-extrabold`, 0 `ring-blue-500`, 0 `focus:ring-offset-0` across `ui/`+AppLayout. NO serif (D-11). IN-05 useReducedMotion provider hoist deferred (per-component hook works; out of MD-2 scope). Not yet pushed — orchestrator pushes after phase verification.
- **Phase 2 prior:** plan 02-04 complete — trip result page editorial-dark redesign (proof slice; DES-02/03/04 + WEB-02 now user-observable). 2 atomic commits (`9a0d217` hero region — serif `h1` title + `useCountUp` mono hero counter (aria-hidden + sr-only real value, tabular-nums fixed 2dp → no CLS) on `!bg-carbon-surface3` plate + Variant A `DataBar` Énergie/Péage breakdown with dotted legend (hide-when-toll-0, D-04) + metric tiles `reveal` stagger gated by `useReducedMotion`; new `reveal` keyframe in globals.css; `c28043c` comparison Variant B `DataBar` rows (energy/gpl CSS-var fills, current full-opacity + `← actuel` accent, others muted) replacing the deleted `categoryColor()` ad-hoc block + standardized `focus-visible:ring-carbon-accent/50` + stepper aria-labels (FLAG 1) + layout-mirroring `Skeleton`). All existing logic preserved (sessionStorage guard, `handleSave`/`/trips/save` payload, stepper math, multiResult, derivations) — MD-2. Web `tsc` clean + `build` 18/18 green (`/app/trips/result` 7.15 kB); grep gate passes (0 categoryColor/bg-emerald/sky/violet/amber/font-medium/font-semibold; DataBar/Skeleton/useCountUp/useReducedMotion/font-serif/aria-label/var(--c-toll) present). D-10: disclaimer washes neutralized to surface tokens. Not yet pushed — orchestrator pushes after phase verification.
- **Phase 2 prior:** plan 02-03 complete — DES-01/02/03 data-viz primitives. 3 atomic commits (1cc5c56 DataBar initial, e3faccb Skeleton, ac1e281 DataBar Variant A refactor). `web/src/components/ui/DataBar.tsx` — token-driven bar: Variant A segmented Énergie/Péage (energy segment passed `energyFillVar`, toll segment internal `var(--c-toll)`, hidden when `tollValue===0` → full-width energy bar, D-04), Variant B single comparison (width value/max, muted non-current at inline opacity 0.45); fills via inline CSS-var `style` only (no Tailwind palette classes); scaleX(0→1) 600ms cubic-bezier reveal via mounted-flag effect, gated by `useReducedMotion` (reduced → scaleX(1)/transition:none); `pct()` clamps 0–100 + guards zero/NaN totals; heights sm=h-1.5/md=h-2.5. `web/src/components/ui/Skeleton.tsx` — bg-carbon-surface2 block, opacity-based animate-pulse dropped under reduced motion, width/height number→px or string, rounded override, aria-hidden. Web `tsc` clean + `build` 18/18 green; plan grep gate passes (scaleX/var(--c-toll)/useReducedMotion present, no bg-emerald/sky/violet/amber). Not yet pushed — orchestrator pushes after phase verification.
- **Phase 2 prior:** plan 02-02 complete — DES-04 motion hooks. 2 atomic commits (92ed137 useReducedMotion, 538c3a9 useCountUp). `useReducedMotion.ts` SSR-safe matchMedia; `useCountUp.ts` rAF easeOutCubic 0→target 700ms with reduced-motion instant gate.
- **Phase 2 prior:** plan 02-01 complete — editorial-dark token foundation (DES-01). 3 atomic commits (794fe0e globals.css tokens, ab54acf tailwind utilities, 46978a6 layout font wiring). New tokens `--c-surface3`/`--c-fuel-gpl`/`--c-toll`, refined surface+neutral ramp (muted AA fix #8a8173), serif h1/h2; Instrument Serif via next/font (2 weights 400/700).
- **Phase 01.1 prior:** complete (plan 01.1-01) — route-aware free toll estimator behind unchanged `TollService` (D-09); 3 commits (d699031/f523620/70be240); full e2e 143/143 green.
- **Phase 01 prior:** complete + pushed to `master` (cfdb828); estimate-primary (D-07); precise TollGuru path built + dormant.
- **Watchpoint (Phase 3):** 72 `font-semibold`/`font-medium` usages across 30 web files still reference the now-unloaded 500/600 weights (deliberate per PD-2 2-weight system) — browser rounds to nearest loaded weight; Phase 3 migrates them to weight 700 emphasis or size hierarchy.
- **Next action:** Phases 6 + 7 COMPLETE & pushed (web auto-deploys). **Phase 6** (POL-01 toll badge removed / POL-02 trip share / POL-03 pseudo edit via `PATCH /users/me`). **Phase 7** (showroom v2: designed cards + simpler search + photos via free CarImages with graceful brand-placeholder fallback — `CARIMAGES_API_KEY` server-side, ships without it; checkpoint approved). **D-32 (2026-06-03) — 30j dashboard fix:** the web "Dépenses 30j" card was blank (getStats dailyExpenses was month-scoped/sparse → 1 point → Sparkline null); now a real rolling 30-day zero-filled series (dedicated query, both stat paths) + frontend `>=2` guard; e2e 160/160 (commit 229582d). Begin **Phase 8 — Onboarding** (ONB-01: post-signup tutorial explaining the app + each screen, replayable from Settings, web + mobile). Then **Phase 9 — iOS App Store release** (REL-01, Apple Developer account-gated). Optional: set `CARIMAGES_API_KEY` in Render to enable real vehicle photos.
- 🔒 Deferred (pre-existing, security): `web/src/components/MapboxMap.tsx` builds station popups via `setHTML` from IRVE name/address (trusted gov open-data) — low-risk latent XSS sink; sanitize / DOM-build the popup in a future cleanup. Plus IN: hero sr-only decimal nuance, stale globals.css serif comment.
- **Watchpoint (Phase 3 screens):** the normalized atoms now carry weight 700 / canonical focus; remaining per-screen `font-semibold`/`font-medium` usages in the page files (~30 files) still need migration in their cluster plans — atoms are no longer the source of the stale weights.
- **Phase 6 current:** plan 06-01 complete — **backend POL-03: JWT-guarded `PATCH /users/me`** (Wave 1 BLOCKING for web 06-02 + mobile 06-03). 2 atomic commits. `086ed94` (test/RED) — `backend/test/users.e2e-spec.ts` (auth SQLite in-memory harness clone; 7 cases: update→200+profile, trim `'  Trim Me  '`→`'Trim Me'`, `''`→400, `'   '`→400, 41-char→400, no-token→401, `{displayName,email}`→400 + follow-up `/auth/me` email unchanged) — red because the route didn't exist. `191b252` (feat/GREEN) — `UpdateProfileDto` (`@Transform` trim + `@IsString`/`@IsNotEmpty`/`@MaxLength(40)`, single field) + `UsersService.updateProfile(userId, displayName)` (update + `findOneByOrFail`, mirrors `linkOAuthProvider`) + `UsersController` `@Patch('me') @UseGuards(JwtAuthGuard)` `updateMe(@CurrentUser, @Body dto)` returning the `/auth/me` projection (no `passwordHash`). IDOR-safe (id from `@CurrentUser` only); no migration. Verify: `tsc --noEmit` + `nest build` clean; **full e2e 157/157** (the 7 new users cases green, no regressions). No deviations. Not yet pushed — orchestrator pushes after phase verification.
- **Updated:** 2026-06-03 (plan 08-01 complete)
