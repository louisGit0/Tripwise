---
phase: 07-showroom-v2
plan: 04
type: execute
wave: 3
depends_on: ["07-01", "07-02", "07-03"]
files_modified: []
autonomous: false
requirements: [SHOW-01, SHOW-02, SHOW-03]
must_haves:
  truths:
    - "On web AND mobile, the showroom renders designed editorial-dark cards (not text rows)"
    - "Each card shows a photo when available, else a stylized brand placeholder — with no CARIMAGES_API_KEY the showroom still works (placeholders everywhere)"
    - "Finding a vehicle feels simple and intuitive (clear search, filtering/jump); server-side search/pagination still works (no client load-all)"
    - "The add-vehicle flow still completes (POST /vehicles/me) on web and mobile"
  artifacts: []
  key_links: []
---

<objective>
Human-verify checkpoint for Phase 7 — Showroom v2. A human confirms the redesigned showroom on web AND mobile: designed editorial-dark cards, photos-or-placeholders (including the no-key graceful path), simpler navigation/search, with the Phase-4 server-side search and the add-vehicle flow intact. Closes SHOW-01/02/03.

Purpose: Final cross-platform sign-off before the phase is marked complete.
Output: human confirmation (or a punch-list of gaps for a gap-closure plan).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-showroom-v2/07-CONTEXT.md
@.planning/phases/07-showroom-v2/07-01-PLAN.md
@.planning/phases/07-showroom-v2/07-02-PLAN.md
@.planning/phases/07-showroom-v2/07-03-PLAN.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Cross-platform showroom v2 sign-off (web + mobile)</name>
  <what-built>
    - Backend (07-01): GET /vehicles/catalog/image?make=&model= → { imageUrl: string | null }, key server-side only, cached, never-throw, graceful-without-key.
    - Web (07-02): garage/add reworked into designed photo cards (photo-or-stylized-brand-placeholder) + simpler search/nav, Phase-4 server search preserved.
    - Mobile (07-03): vehicles add-flow catalog reworked into designed photo cards + simpler search, Phase-4 server search preserved.
  </what-built>
  <how-to-verify>
    WEB (run `cd web && npm run dev`, sign in, go to /app/garage/add):
    1. Each model appears as a DESIGNED card (image area + brand/model/fuel/consumption), NOT a flat text row.
    2. With NO CARIMAGES_API_KEY set on the backend: every card shows a STYLIZED BRAND PLACEHOLDER (per-brand tint + initials), no broken-image icons, no errors in the console.
    3. (If you set CARIMAGES_API_KEY on the backend and restart) at least some popular models (e.g. Tesla Model 3, Renault Clio, Peugeot 208) show a real PHOTO; models without a match still show the placeholder.
    4. Search is the clear primary way to find a car; typing filters server-side (network tab shows GET /vehicles/catalog with the search param — NOT one big load-all); fuel chips + brand jump work; 'Charger plus' loads the next page.
    5. Pick a model → Ajouter → the vehicle is added (POST /vehicles/me) and you land on /app/garage/:id (flow intact).

    MOBILE (run `cd mobile && npx expo start`; use an EAS dev build for images over the network, or Expo Go to at least confirm cards/placeholders + flow):
    6. Open the vehicles tab → Add → each catalog entry is a DESIGNED card with photo-or-placeholder + model/fuel/consumption.
    7. With no key: placeholders everywhere, no broken images. (With a key: photos light up for matched models.)
    8. Search filters server-side (brand-grouped sections, 'Charger plus' paginates — no client load-all); selecting a model → Save adds it (POST /vehicles/me).

    Confirm the whole experience feels SIMPLER/more intuitive than before and visually consistent with the editorial-dark language on both platforms.
  </how-to-verify>
  <resume-signal>Type "approved" if web + mobile match the above; otherwise list the gaps (per platform) and I will route them to a gap-closure plan.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
No new code in this checkpoint plan. Trust boundaries and STRIDE mitigations are owned by 07-01 (key server-side / SSRF / quota), 07-02 (web render-as-image-only), and 07-03 (mobile render-as-image-only). This checkpoint verifies the graceful no-key degradation (T-07-01-05) holds end-to-end on both platforms.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04-01 | Information disclosure | CARIMAGES_API_KEY | verify | Confirm the key never appears in the web bundle / mobile bundle nor in any /vehicles/catalog/image response (only `{ imageUrl }`). |
</threat_model>

<verification>
Human verification only (no automated commands). Automated gates were enforced in 07-01 / 07-02 / 07-03.
</verification>

<success_criteria>
- Web + mobile showrooms render designed editorial-dark cards with photo-or-placeholder (SHOW-01, SHOW-02).
- No-key graceful degradation confirmed (placeholders everywhere; ships without the key) (SHOW-02).
- Navigation/search feels simple + intuitive; server-side search/pagination intact (SHOW-03).
- Add-vehicle flow completes on both platforms (MD-2 preserved).
- Human approval recorded (or gaps captured for gap-closure).
</success_criteria>

<output>
Create `.planning/phases/07-showroom-v2/07-04-SUMMARY.md` when done.
</output>
