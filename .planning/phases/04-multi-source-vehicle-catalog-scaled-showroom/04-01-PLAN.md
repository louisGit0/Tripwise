---
phase: 04-multi-source-vehicle-catalog-scaled-showroom
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/vehicles/entities/vehicle-model.entity.ts
  - backend/src/database/migrations/1748100000000-AddSourceAndCatalogIndexes.ts
autonomous: true
requirements: [CAT-02, CAT-03, CAT-06]
must_haves:
  truths:
    - "Every vehicle_models row carries a source/provenance value (existing rows default to 'ademe')"
    - "The (brand, model, fuel_type) triple is unique — no two catalog rows share it (dedup safety net + enables ON CONFLICT upsert)"
    - "Catalog substring search and brand ordering are index-backed at thousands-of-rows scale"
    - "The migration removes pre-existing (brand,model,fuel_type) duplicates BEFORE creating the UNIQUE index, so migrationsRun:true cannot crash the prod deploy"
    - "The existing vehicles e2e suite stays green with the new source column riding along under SQLite synchronize"
  artifacts:
    - path: "backend/src/vehicles/entities/vehicle-model.entity.ts"
      provides: "source provenance column + unique canonical index decorator (so SQLite synchronize creates it for e2e too)"
      contains: "source"
    - path: "backend/src/database/migrations/1748100000000-AddSourceAndCatalogIndexes.ts"
      provides: "Hand-written reversible migration: dedupe → source column → pg_trgm → UNIQUE canonical → btree(brand) → trigram GIN"
      contains: "UQ_vehicle_models_canonical"
  key_links:
    - from: "backend/src/database/migrations/1748100000000-AddSourceAndCatalogIndexes.ts"
      to: "vehicle_models(brand, model, fuel_type)"
      via: "DELETE-keep-MIN(id) dedupe then CREATE UNIQUE INDEX IF NOT EXISTS"
      pattern: "CREATE UNIQUE INDEX IF NOT EXISTS \"UQ_vehicle_models_canonical\""
---

<objective>
Lay the schema foundation for the multi-source catalog: add a `source` provenance column to `VehicleModel`, enforce the canonical `(brand, model, fuel_type)` uniqueness that powers cross-source dedup + idempotent `ON CONFLICT` upserts, and add the search/ordering indexes that keep the showroom fast at thousands of rows. The migration MUST dedupe existing duplicates before the UNIQUE index so the auto-run on the next Render deploy (`migrationsRun:true`) cannot crash.

Purpose: CAT-02 (provenance field), CAT-03 (dedup-enforcing UNIQUE constraint), CAT-06 (search indexes). Foundation for plans 04-02 (adapters), 04-03 (search API), 04-04 (merge).
Output: extended entity + one hand-written reversible migration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-multi-source-vehicle-catalog-scaled-showroom/04-CONTEXT.md
@.planning/phases/04-multi-source-vehicle-catalog-scaled-showroom/04-RESEARCH.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add source provenance column + canonical unique index to VehicleModel entity</name>
  <files>backend/src/vehicles/entities/vehicle-model.entity.ts</files>
  <read_first>
    - backend/src/vehicles/entities/vehicle-model.entity.ts (the entity to extend; note `fuelType` maps to db column `fuel_type`, decimal columns use `decimalTransformer`)
    - backend/src/database/migrations/1748000000000-AddTollIsEstimateToTrips.ts (migration style reference for the NOT NULL DEFAULT pattern)
    - .planning/phases/04-multi-source-vehicle-catalog-scaled-showroom/04-RESEARCH.md section "Schema + Scale" (the `source` varchar(16) default 'ademe' column; NOT a PG enum, for CAT-05 extensibility)
  </read_first>
  <action>
    Add a `source` column to `VehicleModel`: `@Column({ type: 'varchar', length: 16, default: 'ademe' })` typed `source!: string;` with a comment that it is provenance (CAT-02) and a plain varchar — NOT a PG enum — so a 3rd source drops in without `ALTER TYPE` (CAT-05). Keep the existing `year` column unchanged (metadata, not part of the key). Add a class-level unique index decorator `@Index('UQ_vehicle_models_canonical', ['brand', 'model', 'fuelType'], { unique: true })` (import `Index` from typeorm) so that under SQLite `synchronize:true` (the e2e harness) the canonical unique constraint also exists — this is what lets `ON CONFLICT (brand, model, fuel_type)` work in tests, while the Postgres migration (Task 2) creates the same index with `IF NOT EXISTS` for prod. Do NOT change consumption/battery/tank columns.
  </action>
  <verify>
    <automated>cd backend && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `vehicle-model.entity.ts` declares `source!: string` (varchar(16), default 'ademe') and a class-level `@Index('UQ_vehicle_models_canonical', ['brand','model','fuelType'], { unique: true })`
    - `Index` is imported from 'typeorm'; `npx tsc --noEmit` is clean
    - No change to `consumption`, `batteryCapacityKwh`, `tankCapacityLiters`, or `year`
  </acceptance_criteria>
  <done>The entity carries provenance + a canonical unique constraint that exists in both the SQLite test schema and (via Task 2) the Postgres schema.</done>
</task>

<task type="auto">
  <name>Task 2: Hand-written reversible migration — dedupe, source column, pg_trgm + canonical UNIQUE + search indexes</name>
  <files>backend/src/database/migrations/1748100000000-AddSourceAndCatalogIndexes.ts</files>
  <read_first>
    - backend/src/database/migrations/1748000000000-AddTollIsEstimateToTrips.ts (exact migration class shape: `name` field, `up`/`down`, raw `queryRunner.query`, `IF [NOT] EXISTS` guards, epoch-ms filename + `NamePascalCase<epochms>` class)
    - .planning/phases/04-multi-source-vehicle-catalog-scaled-showroom/04-RESEARCH.md sections "Migration (hand-written, reversible...)" + "Pitfall 5: UNIQUE index creation fails on existing duplicates" (the dedupe-before-UNIQUE requirement; migrationsRun:true auto-runs on deploy)
    - CLAUDE.md "TypeORM migrations" note (synchronize:false prod, migrationsRun auto-applies dist migrations on Render boot)
  </read_first>
  <action>
    Create `backend/src/database/migrations/1748100000000-AddSourceAndCatalogIndexes.ts`, class `AddSourceAndCatalogIndexes1748100000000` with `name = 'AddSourceAndCatalogIndexes1748100000000'`. In `up(queryRunner)` run, IN THIS ORDER:
    (1) Add the provenance column idempotently: `ALTER TABLE "vehicle_models" ADD COLUMN IF NOT EXISTS "source" VARCHAR(16) NOT NULL DEFAULT 'ademe'`.
    (2) DEDUPE BEFORE the unique index (Pitfall 5 — must run before step 4 or the auto-run deploy crashes): delete rows that collide on the canonical triple keeping the lowest id per group, e.g. `DELETE FROM "vehicle_models" a USING "vehicle_models" b WHERE a.id > b.id AND a.brand = b.brand AND a.model = b.model AND a.fuel_type = b.fuel_type`.
    (3) `CREATE EXTENSION IF NOT EXISTS pg_trgm` (note in a comment that the target DB role must permit it — Supabase/Neon/Render do; if it ever fails, index #6 can be dropped and ILIKE/LIKE seq-scans the few-thousand rows).
    (4) `CREATE UNIQUE INDEX IF NOT EXISTS "UQ_vehicle_models_canonical" ON "vehicle_models" ("brand", "model", "fuel_type")`.
    (5) `CREATE INDEX IF NOT EXISTS "IDX_vehicle_models_brand" ON "vehicle_models" ("brand")`.
    (6) `CREATE INDEX IF NOT EXISTS "IDX_vehicle_models_search_trgm" ON "vehicle_models" USING gin ((lower("brand" || ' ' || "model")) gin_trgm_ops)` — the index expression MUST match the search query expression used in plan 04-03 (`lower(brand || ' ' || model)`).
    In `down(queryRunner)` drop in reverse: `IDX_vehicle_models_search_trgm`, `IDX_vehicle_models_brand`, `UQ_vehicle_models_canonical`, then `ALTER TABLE "vehicle_models" DROP COLUMN IF EXISTS "source"`. Leave the `pg_trgm` extension installed (other features may use it) with a comment. Do NOT run the migration here (no live DB in CI); verification is build + structural assertions.
  </action>
  <verify>
    <automated>cd backend && npx tsc --noEmit && npm run build && node -e "const m=require('./dist/database/migrations/1748100000000-AddSourceAndCatalogIndexes.js');console.log(Object.keys(m).join(','))"</automated>
  </verify>
  <acceptance_criteria>
    - File compiles and `npm run build` emits it to `dist/database/migrations/` (so migrationsRun picks it up on deploy)
    - The dedupe `DELETE` statement appears in `up` BEFORE `CREATE UNIQUE INDEX ... UQ_vehicle_models_canonical` (assert ordering by reading the file; the dedupe keeps MIN(id) per (brand,model,fuel_type) group)
    - `up` contains `ADD COLUMN IF NOT EXISTS "source"`, `CREATE EXTENSION IF NOT EXISTS pg_trgm`, the UNIQUE canonical index, `IDX_vehicle_models_brand`, and the `gin_trgm_ops` GIN index on `lower("brand" || ' ' || "model")`
    - `down` is the reversible inverse (drops the 3 indexes + the column, leaves pg_trgm); class name + `name` field match the epoch-ms filename
    - `cd backend && npx jest --config test/jest-e2e.json --testPathPatterns="vehicles" --forceExit` stays green (the new `source` column rides along via SQLite synchronize from the entity decorator)
  </acceptance_criteria>
  <done>A reversible migration that safely adds provenance + the canonical uniqueness + search indexes, deduping first so the auto-run deploy cannot fail.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| deploy pipeline → prod Postgres | `migrationsRun:true` auto-executes this migration on the next Render boot, mutating the live schema (DDL + a dedupe DELETE) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | Denial of Service (availability) | `CREATE UNIQUE INDEX` on existing duplicate rows | mitigate | Dedupe DELETE (keep MIN(id) per canonical group) runs in `up` BEFORE the unique index (Pitfall 5); `IF NOT EXISTS` guards make re-runs safe |
| T-04-01-02 | Tampering / data loss | dedupe DELETE removing wrong rows | mitigate | Deletes only strict canonical-triple collisions keeping the lowest id; ADEME sync already dedups in-memory so prod collisions should be zero; `down` is reversible |
| T-04-01-03 | Availability | `CREATE EXTENSION pg_trgm` lacking role permission | accept | Supabase/Neon/Render permit it (RESEARCH Environment Availability); fallback documented (drop GIN → LIKE seq-scan at thousands of rows is <5ms) |

No npm/pip/cargo install in this plan — no package-legitimacy (T-04-SC) gate required.
</threat_model>

<verification>
- `cd backend && npx tsc --noEmit && npm run build` — type + build clean, migration emitted to dist/
- `cd backend && npx jest --config test/jest-e2e.json --testPathPatterns="vehicles" --forceExit` — vehicles e2e green with the source column
- Manual read assertion: dedupe DELETE precedes the UNIQUE index in `up`
</verification>

<success_criteria>
- `VehicleModel` has a `source` provenance column (default 'ademe') and a canonical unique constraint visible to both SQLite (tests) and Postgres (migration)
- The migration is reversible, idempotent (`IF [NOT] EXISTS`), and dedupes before the UNIQUE index so the auto-run deploy cannot crash
- Search + brand-ordering indexes exist; full vehicles e2e stays green
</success_criteria>

<output>
Create `.planning/phases/04-multi-source-vehicle-catalog-scaled-showroom/04-01-SUMMARY.md` when done.
</output>
