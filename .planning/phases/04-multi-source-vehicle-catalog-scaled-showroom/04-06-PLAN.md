---
phase: 04-multi-source-vehicle-catalog-scaled-showroom
plan: 06
type: execute
wave: 4
depends_on: [04-04, 04-05]
files_modified: []
autonomous: false
requirements: [CAT-01, CAT-04, CAT-06]
must_haves:
  truths:
    - "After a real multi-source sync on the deployed backend, the catalog contains far more than the ~266 ADEME baseline (thousands)"
    - "Common international models present from EPA (e.g. a Tesla, a Toyota, a VW that wasn't in ADEME) AND FR-only ADEME models both appear, each with a real source-attributed consumption"
    - "The deployed showroom searches server-side (network shows debounced paginated requests, not a load-all) and stays fast + brand-grouped at scale"
    - "No (brand, model, fuel_type) duplicates exist after sync; re-running the sync does not grow the row count (idempotent)"
  artifacts: []
  key_links: []
---

<objective>
Human-verify the phase end-to-end on the deployed app — the parts that are only observable after a real ADEME-live + EPA-snapshot sync against the production Postgres: that the catalog actually grew to thousands of real-consumption models, that cross-source dedup holds, that re-sync is idempotent, and that the deployed showroom searches server-side and feels fast and brand-grouped. This closes CAT-01/CAT-04/CAT-06 against reality (the rest is unit/e2e-proven in 04-01..04-05).

Purpose: confirm the user-observable outcome (a far broader catalog, searched fast) that automated tests under SQLite cannot prove (live ADEME fetch, EPA snapshot volume, pg_trgm scale, real deploy).
Output: human sign-off (or a gap list feeding a `--gaps` plan).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-multi-source-vehicle-catalog-scaled-showroom/04-CONTEXT.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-checkpoint automation — full backend e2e + builds green before asking the human</name>
  <files></files>
  <read_first>
    - .planning/phases/04-multi-source-vehicle-catalog-scaled-showroom/04-01-SUMMARY.md … 04-05-SUMMARY.md (what shipped + any deferred notes)
  </read_first>
  <action>
    Run the full automated gate to confirm nothing regressed before the human verifies: backend `npx tsc --noEmit`, backend `npm run build`, backend full e2e (`npx jest --config test/jest-e2e.json --forceExit`), the adapter/merge unit specs (`npx jest src/vehicles/adapters`), and web `npx tsc --noEmit` + `npm run build`. If any fail, STOP and report — do not advance to the human checkpoint. Then ensure the phase work is committed + pushed to `master` so Render/Vercel deploy and `migrationsRun` applies the new migration (per the deploy preference). Note that on the next backend boot the catalog count is < STARTUP_THRESHOLD (~266) so the multi-source sync auto-runs; if needed, trigger it via the authenticated `POST /vehicles/sync` endpoint.
  </action>
  <verify>
    <automated>cd backend && npx tsc --noEmit && npm run build && npx jest --config test/jest-e2e.json --forceExit</automated>
  </verify>
  <acceptance_criteria>
    - Backend tsc + build + full e2e green; adapter/merge unit specs green; web tsc + build green
    - Phase commits pushed to master (deploy triggered); the multi-source sync has run (auto on boot or via POST /vehicles/sync)
  </acceptance_criteria>
  <done>All automated checks pass and the deployed backend has run the multi-source sync — ready for human verification.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    A multi-source (ADEME + EPA) vehicle catalog merged/deduped into thousands of canonical models with real source-attributed consumption, plus a server-side searched + paginated showroom (no client load-all) on the deployed web app.
  </what-built>
  <how-to-verify>
    1. Catalog grew: query the deployed catalog total — e.g. open `https://<your-app>/app/garage/add` (the showroom) OR call the API `GET /api/v1/vehicles/catalog?limit=1` and read `total`. Confirm `total` is in the thousands (≫ the ~266 ADEME baseline). (CAT-04)
    2. Multi-source presence: search a common international model that EPA contributes (e.g. "Tesla", "Toyota", "Volkswagen") — confirm results appear with a plausible real consumption (kWh/100km for EV, L/100km for ICE), not a fabricated/round default. Also confirm FR models (e.g. "Clio", "Peugeot") still appear (ADEME). (CAT-01)
    3. No duplicates: search a model that exists in both worlds (e.g. "Golf") and confirm you don't see obvious cross-source duplicate rows differing only by source. (CAT-03 — visual spot-check)
    4. Server-side + fast: open the browser Network tab on the showroom; type in the search box and confirm it fires ONE debounced `/vehicles/catalog` request (not many parallel page fetches up front), results return quickly, and "Charger plus" loads the next page. Brand grouping + the Phase 3 editorial-dark look are intact. (CAT-06)
    5. (Optional) Idempotency: re-trigger the sync (POST /vehicles/sync with a valid JWT) and confirm the catalog `total` does not increase. (CAT-05)
  </how-to-verify>
  <resume-signal>Type "approved" to mark Phase 4 verified, or describe what's off (e.g. "catalog only ~300 rows", "showroom still loads all", "duplicate Golf rows") to spawn a gap-closure plan.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → POST /vehicles/sync | Authenticated manual sync trigger (JWT-guarded); no untrusted input |
| deploy → prod Postgres | migrationsRun applies the new migration on boot (covered by 04-01's threat model) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06-01 | Availability | deploy boot crash if the migration mis-sequenced | mitigate | Verified in 04-01 (dedupe before UNIQUE index); this checkpoint confirms a clean deploy + sync on real data |
| T-04-06-02 | Integrity | catalog populated with fabricated consumption | mitigate | Human spot-checks real consumption values; 04-02 null-skip + unit tests enforce no fabrication upstream |

No npm/pip/cargo install in this plan — no package-legitimacy (T-04-SC) gate required.
</threat_model>

<verification>
- Automated gate (Task 1): backend tsc + build + full e2e + adapter/merge units + web tsc + build all green
- Human checkpoint (Task 2): catalog in the thousands, multi-source + real consumption, no cross-source dupes, server-side fast showroom, idempotent re-sync
</verification>

<success_criteria>
- The deployed catalog contains thousands of real-consumption, source-attributed, deduped models (CAT-01/CAT-03/CAT-04)
- The deployed showroom searches server-side, paginates, stays fast + brand-grouped (CAT-06); re-sync is idempotent (CAT-05)
- Human sign-off recorded, or gaps captured for a follow-up plan
</success_criteria>

<output>
Create `.planning/phases/04-multi-source-vehicle-catalog-scaled-showroom/04-06-SUMMARY.md` when done.
</output>
