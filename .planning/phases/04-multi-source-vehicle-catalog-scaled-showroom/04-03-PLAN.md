---
phase: 04-multi-source-vehicle-catalog-scaled-showroom
plan: 03
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - backend/src/vehicles/dto/catalog-query.dto.ts
  - backend/src/common/fuel-type-categories.ts
  - backend/src/vehicles/vehicles.service.ts
  - backend/src/vehicles/vehicles.controller.ts
  - backend/test/vehicles.e2e-spec.ts
autonomous: true
requirements: [CAT-06]
must_haves:
  truths:
    - "GET /vehicles/catalog supports server-side search + fuelType + fuelCategory + brand + page/limit and returns { items, total, page, limit, totalPages } — the contract Phase 5 mobile reuses"
    - "Substring search is case-insensitive over (brand || ' ' || model) using the same lowered expression the pg_trgm GIN index covers, so it is index-backed in Postgres and works under SQLite"
    - "fuelCategory (gas|diesel|ev|gpl) expands to the right FuelType set in ONE query (no per-chip round-trips), reusing the shared fuel-category logic"
    - "GET /vehicles/catalog/brands returns [{ brand, count }] so the showroom renders the brand directory without loading all rows"
    - "limit is bounded (@Max 100) and all user input is bound as QueryBuilder parameters (no SQL string interpolation)"
  artifacts:
    - path: "backend/src/vehicles/dto/catalog-query.dto.ts"
      provides: "Extended DTO: search, fuelType, fuelCategory, brand, page, limit"
      contains: "fuelCategory"
    - path: "backend/src/common/fuel-type-categories.ts"
      provides: "categoryToFuelTypes(category): FuelType[] (reverse of toCategory)"
      contains: "categoryToFuelTypes"
    - path: "backend/src/vehicles/vehicles.service.ts"
      provides: "findCatalog (ILIKE-equivalent + brand + fuelCategory) + findCatalogBrands facet"
      contains: "findCatalogBrands"
  key_links:
    - from: "backend/src/vehicles/vehicles.service.ts"
      to: "vehicle_models (lower(brand || ' ' || model))"
      via: "parameterized LOWER(...) LIKE :s matching the trigram index expression"
      pattern: "LOWER\\(vm\\.brand"
    - from: "backend/src/vehicles/vehicles.controller.ts"
      to: "VehiclesService.findCatalogBrands"
      via: "GET catalog/brands route declared BEFORE catalog/:id"
      pattern: "catalog/brands"
---

<objective>
Make the catalog searchable server-side at scale: extend `GET /vehicles/catalog` with `brand` + `fuelCategory` filters and case-insensitive substring search aligned to the trigram index, and add a `GET /vehicles/catalog/brands` facet so the showroom can render a brand directory with counts without ever loading all rows. Freeze the `{ items, total, page, limit, totalPages }` response shape as the contract Phase 5 mobile reuses.

Purpose: CAT-06 (server-side search + pagination + brand grouping, fast on web+mobile). Consumed by plan 04-05 (showroom rework) and Phase 5.
Output: extended DTO, a shared `categoryToFuelTypes` helper, `findCatalog`/`findCatalogBrands` service methods, the `/brands` controller route, and e2e coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-multi-source-vehicle-catalog-scaled-showroom/04-CONTEXT.md
@.planning/phases/04-multi-source-vehicle-catalog-scaled-showroom/04-RESEARCH.md
@CLAUDE.md

<interfaces>
<!-- Frozen response contract — Phase 5 mobile depends on this exact shape. -->

// GET /vehicles/catalog?search=&fuelType=&fuelCategory=&brand=&page=&limit=
{ items: VehicleModel[]; total: number; page: number; limit: number; totalPages: number }

// GET /vehicles/catalog/brands?fuelType=&fuelCategory=&search=
[ { brand: string; count: number } ]   // ordered by brand ASC
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend DTO + categoryToFuelTypes + findCatalog/findCatalogBrands + /brands route</name>
  <files>backend/src/vehicles/dto/catalog-query.dto.ts, backend/src/common/fuel-type-categories.ts, backend/src/vehicles/vehicles.service.ts, backend/src/vehicles/vehicles.controller.ts</files>
  <read_first>
    - backend/src/vehicles/dto/catalog-query.dto.ts (existing search/fuelType/page/limit + @IsOptional/@IsEnum/@Min/@Max pattern to extend)
    - backend/src/common/fuel-type-categories.ts (existing `toCategory` + `FuelCategory` type — add the reverse mapping here, NOT a new file)
    - backend/src/vehicles/vehicles.service.ts lines 37-57 (findCatalog QueryBuilder to extend; getManyAndCount + totalPages shape to KEEP)
    - backend/src/vehicles/vehicles.controller.ts lines 33-41 (catalog routes — the new `catalog/brands` route MUST be declared before `catalog/:id` or Nest matches 'brands' as an id)
    - .planning/phases/04-multi-source-vehicle-catalog-scaled-showroom/04-RESEARCH.md sections "Server-side Search/Pagination API" + "Security Domain" (parameterized QueryBuilder, @Max 100, ILIKE/SQLite caveat)
  </read_first>
  <action>
    DTO: add to `CatalogQueryDto` — `brand?: string` (`@IsOptional()` + `@IsString()`) and `fuelCategory?: FuelCategory` (`@IsOptional()` + `@IsIn(['gas','diesel','ev','gpl'])` importing `FuelCategory`). Keep `search`, `fuelType`, `page`, `limit` (keep the existing `@Max(100)` on limit — bounds pagination, T-04-03 DoS mitigation).
    Shared helper: add `categoryToFuelTypes(category: FuelCategory): FuelType[]` to `backend/src/common/fuel-type-categories.ts` — the reverse of `toCategory` (gas → [SP95, SP95_E10, SP98, E85], diesel → [DIESEL], ev → [ELECTRIC], gpl → [GPL]). Do NOT touch trips.service.ts.
    Service `findCatalog`: replace the existing `LIKE` clause with a case-insensitive substring match over the concatenation that the trigram index covers: `qb.andWhere("LOWER(vm.brand || ' ' || vm.model) LIKE :s", { s: '%' + search.toLowerCase() + '%' })` — use `LOWER(...) LIKE` (NOT `ILIKE`) so it is index-eligible via gin_trgm_ops in Postgres AND runs under the SQLite e2e harness; the lowered expression MUST equal the migration's index expression `lower(brand || ' ' || model)`. Add `if (brand) qb.andWhere('vm.brand = :brand', { brand })`. Add `if (fuelCategory) qb.andWhere('vm.fuelType IN (:...fts)', { fts: categoryToFuelTypes(fuelCategory) })`; keep the single-value `fuelType` filter too (fuelType takes precedence if both sent, or apply both — apply fuelType when present, else fuelCategory). Keep `orderBy('vm.brand','ASC').addOrderBy('vm.model','ASC').skip((page-1)*limit).take(limit)` and the `{ items, total, page, limit, totalPages }` return. ALL user values stay bound `:params` — never interpolated.
    Service `findCatalogBrands(query)`: a QueryBuilder selecting `vm.brand AS brand, COUNT(*) AS count` with the same search/fuelType/fuelCategory filters, `GROUP BY vm.brand ORDER BY vm.brand ASC`; return `[{ brand, count: Number(count) }]`.
    Controller: add `@Get('catalog/brands') getCatalogBrands(@Query() query: CatalogQueryDto)` returning `this.vehiclesService.findCatalogBrands(query)`, declared BEFORE the `@Get('catalog/:id')` route.
  </action>
  <verify>
    <automated>cd backend && npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - DTO has `brand` + `fuelCategory` (validated) alongside the existing fields; `@Max(100)` on limit retained
    - `categoryToFuelTypes` exported from `common/fuel-type-categories.ts`; trips.service.ts untouched
    - `findCatalog` searches `LOWER(vm.brand || ' ' || vm.model) LIKE :s` (matches the index expression; bound parameter); brand + fuelCategory filters added; response shape unchanged
    - `findCatalogBrands` returns `[{ brand, count }]` grouped/ordered by brand; `catalog/brands` route is declared before `catalog/:id`
    - No string interpolation of user input into SQL; `npx tsc --noEmit` + `npm run build` clean
  </acceptance_criteria>
  <done>The catalog API searches, filters, paginates, and faceting brands — all server-side, parameterized, index-aligned, with the frozen response shape.</done>
</task>

<task type="auto">
  <name>Task 2: Extend vehicles e2e — search/brand/fuelCategory/pagination + /brands facet</name>
  <files>backend/test/vehicles.e2e-spec.ts</files>
  <read_first>
    - backend/test/vehicles.e2e-spec.ts (existing 'GET /api/v1/vehicles/catalog' describe block + seeded thermal Renault Clio V / electric Tesla Model 3; SQLite synchronize harness; ValidationPipe config)
    - backend/src/vehicles/vehicles.controller.ts + vehicles.service.ts (Task 1 — the endpoints to assert)
    - .planning/phases/04-multi-source-vehicle-catalog-scaled-showroom/04-RESEARCH.md "Validation Architecture → Test Map" (CAT-06 e2e rows) + the SQLite e2e caveat (assert API shape, not pg_trgm internals)
  </read_first>
  <action>
    Extend the catalog describe block (seed a couple more rows if useful — e.g. a Diesel and another brand — via `modelRepo.save`). Add assertions: (a) `GET /vehicles/catalog?search=clio` returns paginated `items` containing the Clio and the `{ items, total, page, limit, totalPages }` shape; (b) `GET /vehicles/catalog?fuelCategory=ev` returns only ELECTRIC items; (c) `GET /vehicles/catalog?brand=Tesla` returns only Tesla items; (d) `GET /vehicles/catalog?page=1&limit=1` returns exactly 1 item with `totalPages` ≥ total/limit; (e) `GET /vehicles/catalog?fuelCategory=banana` returns 400 (DTO @IsIn); (f) `GET /vehicles/catalog/brands` returns an array of `{ brand, count }` ordered by brand, counts numeric and > 0. Do NOT assert pg_trgm-specific behavior (SQLite has none) — assert shape + filtering correctness only.
  </action>
  <verify>
    <automated>cd backend && npx jest --config test/jest-e2e.json --testPathPatterns="vehicles" --forceExit</automated>
  </verify>
  <acceptance_criteria>
    - e2e asserts search, fuelCategory, brand, pagination shape, the 400 on invalid fuelCategory, and the `/brands` facet shape — all green
    - The full e2e suite (`npx jest --config test/jest-e2e.json --forceExit`) remains green (no regression)
  </acceptance_criteria>
  <done>The CAT-06 server-side search contract is locked by e2e — the shape Phase 5 mobile reuses is proven.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → GET /vehicles/catalog(/brands) | Public (unauthenticated) endpoint; untrusted `search`/`brand`/`fuelCategory`/`page`/`limit` cross into SQL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-01 | Tampering / Injection | `search` / `brand` in the WHERE clause | mitigate | All values bound as QueryBuilder `:params` (`:s`, `:brand`, `:...fts`); never string-concatenated into SQL; e2e exercises the path |
| T-04-03-02 | Denial of Service | unbounded pagination / huge limit | mitigate | `@Max(100)` on `limit` retained; `skip/take` enforce a bounded page; the whole point of CAT-06 is no load-all |
| T-04-03-03 | Tampering | invalid `fuelCategory` reaching SQL | mitigate | `@IsIn(['gas','diesel','ev','gpl'])` + global ValidationPipe whitelist → 400 before the query |
| T-04-03-04 | Information Disclosure | catalog data sensitivity | accept | Vehicle catalog is non-sensitive public reference data (RESEARCH V2/V3/V4: no auth needed) |

No npm/pip/cargo install in this plan — no package-legitimacy (T-04-SC) gate required.
</threat_model>

<verification>
- `cd backend && npx tsc --noEmit && npm run build` — type + build clean
- `cd backend && npx jest --config test/jest-e2e.json --testPathPatterns="vehicles" --forceExit` — catalog search/brand/category/pagination + /brands facet green
- `cd backend && npx jest --config test/jest-e2e.json --forceExit` — full e2e no regression
</verification>

<success_criteria>
- `GET /vehicles/catalog` does server-side search + brand + fuelCategory + bounded pagination with the frozen `{ items, total, page, limit, totalPages }` shape
- `GET /vehicles/catalog/brands` powers brand grouping without client load-all
- Search is index-aligned + parameterized; SQLite e2e proves the shape Phase 5 reuses
</success_criteria>

<output>
Create `.planning/phases/04-multi-source-vehicle-catalog-scaled-showroom/04-03-SUMMARY.md` when done.
</output>
