---
phase: 01-precise-tolls-end-to-end-web
plan: 03
type: execute
wave: 3
depends_on: [01-02]
files_modified:
  - web/src/components/ui/Tooltip.tsx
  - web/src/types/api.ts
  - web/src/app/app/trips/result/page.tsx
  - web/src/app/app/trips/[id]/page.tsx
autonomous: true
requirements: [TOLL-04]
must_haves:
  truths:
    - "The trip result shows the toll as its own line, included in the displayed grand total"
    - "A 'réel' badge appears when the toll came from TollGuru, '≈ estimé' when it came from the heuristic (D-01)"
    - "The badge carries a tooltip explaining the source (hover on desktop, tap on touch) (D-02)"
    - "A toll-free route (cost 0 or null) hides the toll line entirely — no '0 €' (D-04)"
    - "The saved-trip detail page shows the same badge driven by the persisted tollIsEstimate"
  artifacts:
    - path: "web/src/components/ui/Tooltip.tsx"
      provides: "Lightweight Carbon-token tooltip atom (hover + tap)"
      contains: "function Tooltip"
    - path: "web/src/app/app/trips/result/page.tsx"
      provides: "Filtered metrics array (hide-when-0) + Pill badge + Tooltip"
      contains: "réel"
    - path: "web/src/app/app/trips/[id]/page.tsx"
      provides: "Persisted-toll badge on saved detail"
      contains: "tollIsEstimate"
  key_links:
    - from: "result/page.tsx"
      to: "result.tollIsEstimate"
      via: "Pill color success(réel)/warning(≈ estimé) wrapped in Tooltip"
      pattern: "tollIsEstimate"
    - from: "result/page.tsx save payload"
      to: "POST /trips/save"
      via: "save object includes tollIsEstimate: result.tollIsEstimate"
      pattern: "tollIsEstimate"
    - from: "trips/[id]/page.tsx"
      to: "trip.tollIsEstimate (persisted)"
      via: "same Pill+Tooltip badge beside the péages amount"
      pattern: "trip\\.tollIsEstimate"
---

<objective>
Present the toll trustworthily on the web: break it out as its own line, roll it into the displayed total, badge it "réel" vs "≈ estimé" with an explanatory tooltip, and hide it entirely when there is no toll. The detail page shows the same badge from the now-persisted flag. This is the user-facing end of the vertical slice.

Purpose: Deliver TOLL-04 (broken out + real-vs-estimate indicator in total) per D-01, D-02, D-04.
Output: new `Tooltip` UI atom, `SavedTrip.tollIsEstimate` type, result + detail page badges, hide-when-0 metrics.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-precise-tolls-end-to-end-web/01-CONTEXT.md
@.planning/phases/01-precise-tolls-end-to-end-web/01-RESEARCH.md
@.planning/phases/01-precise-tolls-end-to-end-web/01-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- Existing in-repo conventions to mirror. -->
Pill.tsx                : named export `function Pill(...)`, color map success=emerald / warning=amber, Carbon tokens
FuelBadge.tsx           : minimal one-element atom precedent
result/page.tsx L135-137: const tollCost = result.tollCost ?? 0; const tollIsEstimate = result.tollIsEstimate ?? false; totalCost already = (cost?.totalCost ?? 0) + tollCost
result/page.tsx L180-221: grid grid-cols-2 metrics array .map() — current PÉAGES cell at L188-194
result/page.tsx L108    : save payload object (add tollIsEstimate here)
trips/[id]/page.tsx L264-273: existing `{trip.tollsCost > 0 && (...)}` péages block (already satisfies hide-when-0 on detail)
types/api.ts L122-124   : TripResult.tollIsEstimate already exists; SavedTrip (L182) has only tollsCost — add tollIsEstimate
Badge copy (FR): "réel" / "≈ estimé"
Tooltip copy: estimate → "Estimation indicative (calcul français moyen)"; real → "Prix réel calculé par TollGuru le long de l'itinéraire"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tooltip UI atom + SavedTrip type</name>
  <files>web/src/components/ui/Tooltip.tsx, web/src/types/api.ts</files>
  <read_first>
    - web/src/components/ui/Pill.tsx (props/className/Carbon-token conventions, named export)
    - web/src/components/ui/FuelBadge.tsx (minimal atom precedent)
    - web/src/types/api.ts lines 122-124 (TripResult.tollIsEstimate) and ~182 (SavedTrip.tollsCost)
  </read_first>
  <action>
    Create `web/src/components/ui/Tooltip.tsx` as a lightweight client atom (`'use client'`) named-exported `function Tooltip({ children, content }: TooltipProps)`. It must work on hover (desktop) AND tap (touch) per D-02: a focusable wrapper (`tabIndex={0}`) with the tooltip surface shown via group-hover + group-focus-within (and a tap toggle via local state for touch). Style the surface with Carbon tokens `bg-carbon-surface2`, `text-carbon-ink2`, `border-carbon-hairline`, small rounded, absolute-positioned above the trigger. Keep it ~30-50 lines, single responsibility.
    In `web/src/types/api.ts`, add `tollIsEstimate: boolean;` to the `SavedTrip` interface immediately after `tollsCost: number;`, mirroring the comment style of `TripResult.tollIsEstimate`.
  </action>
  <verify>
    <automated>cd web && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `Tooltip` is a named export, client component, hover + tap reveal, Carbon-token styled, < 60 lines
    - `SavedTrip` now has `tollIsEstimate: boolean`
    - `npx tsc --noEmit` clean
  </acceptance_criteria>
  <done>The reusable tooltip and the persisted-toll type exist.</done>
</task>

<task type="auto">
  <name>Task 2: Result page — hide-when-0 + badge + tooltip + save payload</name>
  <files>web/src/app/app/trips/result/page.tsx</files>
  <read_first>
    - web/src/app/app/trips/result/page.tsx lines 135-137, 180-221 (metrics array, PÉAGES cell), 108 (save payload)
    - web/src/components/ui/Pill.tsx (color success/warning)
    - .planning/phases/01-precise-tolls-end-to-end-web/01-RESEARCH.md Pitfall 6 + "Web result page — badge + hide-when-0"
  </read_first>
  <action>
    Convert the inline metrics literal into `const metrics = [ ... ].filter(Boolean)` so the PÉAGES entry is `false` (dropped) when `result.tollCost == null || result.tollCost === 0` — the `grid grid-cols-2` reflows cleanly (D-04, Pitfall 6). For the PÉAGES entry (only when toll > 0): replace the `~` prefix and amber `note` with a `<Pill>` — color `success` + text "réel" when `!tollIsEstimate`, color `warning` + text "≈ estimé" when `tollIsEstimate` — wrapped in the new `<Tooltip>` whose content is the source copy (real vs estimate, see interfaces). Extend the existing cell render scaffold (which already supports an optional `note` slot) to render the badge node. Leave `totalCost` summation as-is (already includes the toll). Add `tollIsEstimate: result.tollIsEstimate` to the save payload object near line 108 so the flag persists on save (feeds plan 02 column).
  </action>
  <verify>
    <automated>cd web && npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Toll line is absent for `tollCost` null or 0; present (own line) when > 0, included in displayed total
    - Badge reads "réel" (success) for real, "≈ estimé" (warning) for estimate, with the source tooltip
    - Save payload includes `tollIsEstimate`; `tsc --noEmit` + `next build` clean
  </acceptance_criteria>
  <done>The live result page shows a trustworthy, clearly-labelled, total-inclusive toll line.</done>
</task>

<task type="auto">
  <name>Task 3: Saved-trip detail page badge</name>
  <files>web/src/app/app/trips/[id]/page.tsx</files>
  <read_first>
    - web/src/app/app/trips/[id]/page.tsx lines 264-273 (existing `{trip.tollsCost > 0 && (...)}` péages block)
    - web/src/components/ui/Pill.tsx + the new Tooltip.tsx
  </read_first>
  <action>
    Inside the existing `{trip.tollsCost > 0 && (...)}` block (which already satisfies hide-when-0 on the detail page), add the same `<Pill>` + `<Tooltip>` badge beside the amount, driven by the newly-persisted `trip.tollIsEstimate` — identical wording, colors, and tooltip copy as the result page for consistency (D-01/D-02, Pitfall 7, TOLL-05).
  </action>
  <verify>
    <automated>cd web && npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Detail page renders the réel/≈ estimé badge + tooltip from `trip.tollIsEstimate` when `tollsCost > 0`
    - Badge presentation matches the result page exactly
    - `tsc --noEmit` + `next build` clean
  </acceptance_criteria>
  <done>The saved-trip detail shows the same trustworthy toll badge as the live result.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server → browser | Only `tollCost`/`tollIsEstimate` (presentation data) cross to the client; no secret is rendered |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | Web bundle / rendered DOM | mitigate | Badge/tooltip render only `tollCost`/`tollIsEstimate`; the `TOLLGURU_API_KEY` is never imported or referenced in any web file (TOLL-06 — verified by the absence of the key in `web/`) |
| T-03-02 | Tampering (XSS) | Tooltip content | mitigate | Tooltip content is static FR copy + a formatted number via React text nodes (no `dangerouslySetInnerHTML`) |

No package installs in this plan — no `T-03-SC` gate required. (No web automated test infra exists per CONCERNS — visual behaviors verified in plan 04 human-verify.)
</threat_model>

<verification>
- `cd web && npx tsc --noEmit && npm run build` — type + build gate clean
- Visual behaviors (badge text/colors, tooltip hover+tap, hide-when-0) are verified manually in plan 04 (no web test infra)
</verification>

<success_criteria>
- Result + detail pages show the toll as its own line with the correct real/estimate badge + tooltip, included in the total, hidden when 0
- `tollIsEstimate` persists from the result save payload
- Web type/build gate green
</success_criteria>

<output>
Create `.planning/phases/01-precise-tolls-end-to-end-web/01-03-SUMMARY.md` when done.
</output>

## Decision Coverage

This plan implements the following locked CONTEXT.md decisions:
- D-01: "réel" / "≈ estimé" badge next to the toll amount, driven by `tollIsEstimate`.
- D-02: tooltip (hover/tap) on the badge explaining the source (TollGuru precise vs French estimation).
- D-04: hide the toll line entirely when the toll cost is 0 (no "0 €" clutter).
