---
phase: 01-precise-tolls-end-to-end-web
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - backend/src/trips/entities/trip.entity.ts
  - backend/src/database/migrations/1748000000000-AddTollIsEstimateToTrips.ts
  - backend/src/trips/dto/save-trip.dto.ts
  - backend/src/trips/trips.service.ts
  - backend/test/trips-crud.e2e-spec.ts
autonomous: true
requirements: [TOLL-05]
must_haves:
  truths:
    - "Saving a trip persists both the toll cost and whether it was an estimate"
    - "The saved-trip detail can read tollIsEstimate from the persisted row"
    - "Toll cost is reflected in history and monthly stats totals (already via totalCost)"
    - "The trips table has a toll_is_estimate boolean column after the migration runs"
  artifacts:
    - path: "backend/src/trips/entities/trip.entity.ts"
      provides: "tollIsEstimate persisted column (boolean, default false)"
      contains: "toll_is_estimate"
    - path: "backend/src/database/migrations/1748000000000-AddTollIsEstimateToTrips.ts"
      provides: "Hand-written migration adding toll_is_estimate"
      contains: "AddTollIsEstimateToTrips"
    - path: "backend/src/trips/dto/save-trip.dto.ts"
      provides: "Optional tollIsEstimate on the save payload"
      contains: "tollIsEstimate"
  key_links:
    - from: "POST /trips/save"
      to: "Trip.tollIsEstimate column"
      via: "saveTrip writes dto.tollIsEstimate ?? false into tripRepo.create"
      pattern: "tollIsEstimate"
    - from: "Trip.tollsCost"
      to: "monthly stats totals"
      via: "stats sum trip.totalCost (already includes toll); no double count"
      pattern: "totalCost"
---

<objective>
Close the persistence gap so a saved trip remembers both the toll amount AND whether it was real or an estimate — completing TOLL-05's "reflected in detail / history / stats" intent. The amount already persists; this plan adds the `toll_is_estimate` flag (entity + hand-written migration + DTO + save flow) so the detail page (plan 03) can render the same badge as the live result.

Purpose: Deliver TOLL-05 — toll persisted on saved trips and reflected in history/stats.
Output: New `toll_is_estimate` column (entity + migration, run in dev), `SaveTripDto.tollIsEstimate`, save-flow write, crud e2e asserting persistence + stats.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/01-precise-tolls-end-to-end-web/01-RESEARCH.md
@.planning/phases/01-precise-tolls-end-to-end-web/01-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- Existing patterns this plan mirrors exactly. -->

Trip.isArchived  (entity analog):  @Column({ name: 'is_archived', type: 'boolean', default: false })
Trip.tollsCost   (already persisted, decimalTransformer)  — add tollIsEstimate beside it
SaveTripDto.tollsCost  (optional, @IsOptional @IsNumber @Min(0)) — add a @IsBoolean sibling
saveTrip(): tripRepo.create({ ..., tollsCost: dto.tollsCost ?? 0 })  — add tollIsEstimate: dto.tollIsEstimate ?? false
Latest existing migration timestamp: 1747701000000 — new one MUST be greater.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add tollIsEstimate column + hand-written migration</name>
  <files>backend/src/trips/entities/trip.entity.ts, backend/src/database/migrations/1748000000000-AddTollIsEstimateToTrips.ts</files>
  <read_first>
    - backend/src/trips/entities/trip.entity.ts lines 112-114 (isArchived boolean column — exact template) and 124-129 (tollsCost + stale "V1 = 0" comment)
    - backend/src/database/migrations/1747701000000-AddTripMetaFields.ts (full file — exact hand-written ALTER TABLE precedent with up/down)
    - .planning/codebase/CONCERNS.md (why migration:generate is unreliable here — hand-write)
  </read_first>
  <action>
    In `trip.entity.ts`, add a `tollIsEstimate!: boolean` property mapped to `@Column({ name: 'toll_is_estimate', type: 'boolean', default: false })` directly beside `tollsCost`, mirroring the `isArchived` column. Update the stale `tollsCost` comment ("V1 = 0 (non calculé)") to reflect that it is now a real/estimated toll amount (RESEARCH Pitfall 8).
    Create `backend/src/database/migrations/1748000000000-AddTollIsEstimateToTrips.ts` (timestamp greater than the latest existing `1747701000000`), class `AddTollIsEstimateToTrips1748000000000`, hand-written like `AddTripMetaFields`: `up` runs `ALTER TABLE "trips" ADD COLUMN IF NOT EXISTS "toll_is_estimate" BOOLEAN NOT NULL DEFAULT false`; `down` runs `ALTER TABLE "trips" DROP COLUMN "toll_is_estimate"`. No data migration needed — default false ("unknown → estimate") is acceptable for existing rows.
  </action>
  <verify>
    <automated>cd backend && npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `Trip` entity exposes `tollIsEstimate` mapped to `toll_is_estimate boolean default false`
    - Migration file exists with class `AddTollIsEstimateToTrips1748000000000`, `IF NOT EXISTS` up and matching down, timestamp > 1747701000000
    - `npx tsc --noEmit` and `nest build` succeed (types come from the entity, not the live DB)
  </acceptance_criteria>
  <done>The persisted toll-estimate flag exists in code (entity + migration), ready to run.</done>
</task>

<task type="auto" gate="blocking">
  <name>Task 2: [BLOCKING] Run the migration (dev)</name>
  <files>(none — runs `npm run migration:run`; no file authored)</files>
  <read_first>
    - CLAUDE.md "Migrations TypeORM" section (npm run migration:run, requires DB up via docker compose)
  </read_first>
  <action>
    Ensure the local Postgres is up (`docker compose up -d` from repo root if needed), then run `cd backend && npm run migration:run` to apply `AddTollIsEstimateToTrips1748000000000`. This is BLOCKING: it must run AFTER Task 1 writes the entity/migration and BEFORE any verification that touches a real Postgres, because build/type checks pass WITHOUT the column (types come from the entity) and would otherwise be a false positive. Note for the operator: production applies migrations on deploy (Render) — no manual prod step here. SQLite e2e uses `synchronize` so the column appears automatically in tests without this run.
  </action>
  <verify>
    <automated>cd backend && npm run migration:show</automated>
  </verify>
  <acceptance_criteria>
    - `npm run migration:run` completes without error against the local DB
    - `npm run migration:show` lists `AddTollIsEstimateToTrips1748000000000` as applied [X]
  </acceptance_criteria>
  <done>The trips table has the toll_is_estimate column in the dev database.</done>
</task>

<task type="auto">
  <name>Task 3: Persist tollIsEstimate on save + crud e2e</name>
  <files>backend/src/trips/dto/save-trip.dto.ts, backend/src/trips/trips.service.ts, backend/test/trips-crud.e2e-spec.ts</files>
  <read_first>
    - backend/src/trips/dto/save-trip.dto.ts lines 61-65 (optional tollsCost field + stale "toujours 0 en V1" comment)
    - backend/src/trips/trips.service.ts saveTrip lines 247-271 (tripRepo.create object, line 264 tollsCost default)
    - backend/test/trips-crud.e2e-spec.ts (existing save/history/stats e2e to extend)
  </read_first>
  <action>
    In `save-trip.dto.ts`, add an optional `tollIsEstimate?: boolean` field guarded by `@IsOptional()` + `@IsBoolean()` (import `IsBoolean` from class-validator), mirroring the existing optional `tollsCost`; update the stale "toujours 0 en V1" comment.
    In `trips.service.ts` `saveTrip`, add `tollIsEstimate: dto.tollIsEstimate ?? false` to the `tripRepo.create({...})` object, next to `tollsCost: dto.tollsCost ?? 0` (same `?? false` defaulting style). Do not touch the call site or methods from plan 01.
    Extend `backend/test/trips-crud.e2e-spec.ts`: when saving a trip with `tollsCost` and `tollIsEstimate`, assert both persist and are returned by `GET /trips/:id`; assert the monthly `GET /trips/stats` totals reflect the toll via `totalCost` (no double count — `tollsCost` is a separate breakout column).
  </action>
  <verify>
    <automated>cd backend && npx jest --config test/jest-e2e.json --testPathPatterns="trips-crud" --forceExit</automated>
  </verify>
  <acceptance_criteria>
    - `SaveTripDto` accepts `tollIsEstimate` (validated boolean, optional)
    - `saveTrip` writes `tollIsEstimate` into the created row; `GET /trips/:id` returns it
    - crud e2e asserts persisted `tollsCost` + `tollIsEstimate` and stats include the toll once
  </acceptance_criteria>
  <done>Saved trips remember the toll amount and its source; history/stats reflect it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → POST /trips/save | JWT-guarded (existing); client supplies tollsCost/tollIsEstimate for its own trip |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | Client-supplied `tollsCost`/`tollIsEstimate` on save | accept | Low risk — user's own trip; server-side recompute on save deferred to V2 (RESEARCH Open Q3). Input still validated by class-validator (`@IsNumber/@IsBoolean/@Min(0)`) |
| T-02-02 | Information Disclosure | New boolean column | accept | `toll_is_estimate` is non-sensitive presentation metadata, returned only to the owning user (existing per-user access control unchanged) |
| T-02-03 | Tampering | Migration drift between dev/prod | mitigate | Hand-written `IF NOT EXISTS` migration; prod runs migrations on deploy; BLOCKING dev run (Task 2) prevents false-positive verification |

No package installs in this plan — no `T-02-SC` gate required.
</threat_model>

<verification>
- `cd backend && npm run migration:show` — new migration applied
- `cd backend && npx jest --config test/jest-e2e.json --testPathPatterns="trips" --forceExit` — full trips + trips-crud e2e green
- `cd backend && npx tsc --noEmit && npm run build` — clean
</verification>

<success_criteria>
- `trips.toll_is_estimate` column exists (entity + applied migration)
- Save flow persists the estimate flag; detail + stats reflect the toll
- All trips e2e green
</success_criteria>

<output>
Create `.planning/phases/01-precise-tolls-end-to-end-web/01-02-SUMMARY.md` when done.
</output>
