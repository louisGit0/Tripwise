---
phase: 01-precise-tolls-end-to-end-web
plan: 04
type: execute
wave: 4
depends_on: [01-01, 01-02, 01-03]
files_modified: []
autonomous: false
requirements: [TOLL-01, TOLL-02, TOLL-04]
must_haves:
  truths:
    - "With a real TollGuru key, Paris→Lyon returns a real toll (~€35-40 class 1) flagged precise, not an estimate — confirming TollGuru endpoint, polyline precision, cost path, and vehicle class (A1-A6)"
    - "Without the key, the same route degrades to a clearly-labelled '≈ estimé' value with no error shown"
    - "A genuinely toll-free route shows no toll line at all"
    - "The badge + tooltip render correctly on both the result and saved-detail pages, real and estimate states"
  artifacts: []
  key_links:
    - from: "live TOLLGURU_API_KEY"
      to: "tollIsEstimate=false on a known-toll route"
      via: "POST /trips/calculate Paris→Lyon"
      pattern: "tollIsEstimate"
---

<objective>
Confirm the only genuinely unverifiable area of this phase: the live TollGuru request/response (Assumptions A1–A6 — endpoint path, polyline-6 precision pairing, cost path `route.costs.*`, vehicle class string, currency, quota) against a real key, plus the web badge/tooltip/hide-when-0 visuals that have no automated test infra. Nothing in this phase's precise branch can be trusted until this passes.

Purpose: Gate TOLL-01 (precise) and TOLL-04 (presentation) on real-world confirmation before the phase is considered done.
Output: Human sign-off (or recorded issues to feed a gap-closure plan).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-precise-tolls-end-to-end-web/01-RESEARCH.md
@.planning/phases/01-precise-tolls-end-to-end-web/01-VALIDATION.md
@CLAUDE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify live TollGuru precise path (Paris→Lyon)</name>
  <files>(none — verification only; backend/.env holds the live key locally, never committed)</files>
  <read_first>
    - .planning/phases/01-precise-tolls-end-to-end-web/01-RESEARCH.md Assumptions Log A1-A6 + Pitfalls 1-3
  </read_first>
  <what-built>
    The extracted `TollService` now calls TollGuru's `complete-polyline-from-mapping-service` endpoint with the route polyline (precision-6, source "mapbox"), parses cost defensively from `route.costs.{tag,cash,minimumTollCost}`, requests a class-1 car, caches 30 days, and falls back silently to the heuristic on any failure. None of the live TollGuru specifics (A1–A6) could be auto-verified — no key in the build session.
  </what-built>
  <action>
    Human-verification checkpoint (no code change). The operator sets a real key and exercises the precise path on a known-toll route to confirm Assumptions A1–A6, the cache, and the silent fallback.
  </action>
  <how-to-verify>
    1. Set a real `TOLLGURU_API_KEY` in `backend/.env` and restart the backend (`npm run start:dev`).
    2. Calculate Paris → Lyon (A6) for a class-1 vehicle via the web dashboard (or POST /trips/calculate).
    3. Confirm `tollIsEstimate` is `false` and `tollCost` is a real value ≈ €35–40 (class 1) — validates the endpoint (A1), polyline precision/source pairing (A2), cost path (A3), vehicle class (A4), and EUR currency (A5).
    4. Repeat the identical route — confirm no second TollGuru call is issued (cache hit; watch logs / dashboard usage), validating TOLL-06 and A6 quota safety.
    5. Calculate a known toll-free route (e.g. two nearby city points) — confirm `tollCost` is 0/null and the result hides the toll line.
    6. Unset the key, restart, recalculate Paris→Lyon — confirm it degrades to a "≈ estimé" value with NO error surfaced (D-03).
    If the precise toll is wrong/zero with the key set: the polyline precision pairing (A2) or cost path (A3) is off — capture the raw TollGuru response and report it for a gap-closure plan.
  </how-to-verify>
  <verify>Operator confirms tollIsEstimate=false + tollCost ≈ €35-40 on Paris→Lyon; cache hit on repeat; no-toll route hides line; no-key degrades silently.</verify>
  <acceptance_criteria>
    - Paris→Lyon with key → tollIsEstimate=false, tollCost ≈ €35–40
    - Repeat → no second TollGuru call (cache)
    - Toll-free route → no toll line
    - No key → "≈ estimé", no user-facing error
  </acceptance_criteria>
  <done>The precise TollGuru path is confirmed real on a known route, with cache + silent fallback verified.</done>
  <resume-signal>Type "approved" or paste the raw TollGuru response + observed values if anything is off.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify web badge + tooltip + hide-when-0 (result + detail)</name>
  <files>(none — verification only)</files>
  <read_first>
    - .planning/phases/01-precise-tolls-end-to-end-web/01-CONTEXT.md D-01, D-02, D-04
  </read_first>
  <what-built>
    The result page filters out the PÉAGES cell when toll is 0/null, shows a "réel" (success) / "≈ estimé" (warning) Pill with a source tooltip (hover + tap), and persists `tollIsEstimate`. The saved-detail page shows the same badge from the persisted flag.
  </what-built>
  <action>
    Human-verification checkpoint (no code change). The operator inspects the result and saved-detail pages in real and estimate states and on a toll-free route.
  </action>
  <how-to-verify>
    1. With key set, calculate a tolled route → result shows the toll line with a green "réel" badge; hover (desktop) and tap (touch/devtools mobile) the badge → tooltip reads "Prix réel calculé par TollGuru le long de l'itinéraire".
    2. Without key, calculate the same route → amber "≈ estimé" badge; tooltip reads "Estimation indicative (calcul français moyen)".
    3. Calculate a toll-free route → confirm NO péage cell and the metrics grid reflows cleanly (no empty slot).
    4. Save a tolled trip, open it under /app/trips/[id] → the same badge appears beside the persisted amount with matching wording/colors.
  </how-to-verify>
  <verify>Operator confirms real/estimate badge text + colors + tooltip on result and detail, and hide-when-0 reflow.</verify>
  <acceptance_criteria>
    - Real → green "réel" + real tooltip; estimate → amber "≈ estimé" + estimate tooltip
    - Toll-free route → no toll line, grid intact
    - Detail page badge matches the result page and reflects the persisted flag
  </acceptance_criteria>
  <done>The web toll presentation is confirmed across real, estimate, and toll-free states on result and detail.</done>
  <resume-signal>Type "approved" or describe rendering/interaction issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → backend/.env | Real `TOLLGURU_API_KEY` introduced locally for verification; must not be committed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | Real key used in verification | mitigate | Key lives only in local `.env` (gitignored) and Render env; verification confirms it never appears in any response/DOM/bundle (TOLL-06) |
| T-04-02 | Denial of Service | Verification burns free-tier quota | accept | A handful of manual calls; cache prevents repeats; quota is the documented free-tier limit |

No package installs in this plan.
</threat_model>

<verification>
- Both checkpoints approved by the human operator (manual — gated on a live key + visual web inspection, no CI assertion possible per VALIDATION Manual-Only table)
</verification>

<success_criteria>
- Precise TollGuru path confirmed real on a known-toll route; silent estimate fallback confirmed
- Web badge/tooltip/hide-when-0 confirmed on result and detail, real and estimate states
- Any deviation captured for gap-closure rather than silently accepted
</success_criteria>

<output>
Create `.planning/phases/01-precise-tolls-end-to-end-web/01-04-SUMMARY.md` when done.
</output>
