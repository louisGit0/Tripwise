---
phase: 05-mobile-tolls-editorial-redesign
plan: 06
type: execute
wave: 3
depends_on: [05-02, 05-03]
files_modified:
  - mobile/app/(tabs)/vehicles.tsx
autonomous: true
requirements: [MOB-01]
user_setup: []

must_haves:
  truths:
    - "The vehicles/garage screen is restyled editorial-dark (MOB-01)"
    - "The add-vehicle catalog browses the scaled catalog via server-side search + pagination (no client load-all) — the frozen { items, total, page, limit, totalPages } contract"
    - "Catalog results are brand-grouped and load incrementally ('Charger plus'), mirroring the web showroom"
    - "Garage CRUD (add/edit/delete) flows are preserved (MD-2)"
  artifacts:
    - path: "mobile/app/(tabs)/vehicles.tsx"
      provides: "Editorial-dark garage + server-side catalog browse (debounced/paginated/brand-grouped)"
      contains: "items"
  key_links:
    - from: "mobile/app/(tabs)/vehicles.tsx"
      to: "GET /vehicles/catalog"
      via: "debounced server-side search + pagination"
      pattern: "/vehicles/catalog"
---

<objective>
Restyle the vehicles/garage screen to editorial-dark AND fix + scale the add-vehicle catalog browse: switch from the currently-broken client read (`r.data.data` against the old `{ data, total }` shape) to the frozen Phase-4 server-side contract `GET /vehicles/catalog → { items, total, page, limit, totalPages }`, with debounced search (already debounced), incremental pagination ("Charger plus"), and brand grouping — mirroring the web showroom (D-17). Preserve the garage list + add/edit/delete CRUD flows verbatim (MD-2).

Purpose: The mobile slice of CAT-06 — the showroom scales to thousands without client load-all (ROADMAP Phase 5 success criterion 3).
Output: a restyled, server-side, brand-grouped catalog browse on the vehicles screen.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/05-mobile-tolls-editorial-redesign/05-CONTEXT.md
@.planning/phases/05-mobile-tolls-editorial-redesign/05-02-PLAN.md
@.planning/phases/04-multi-source-vehicle-catalog-scaled-showroom/04-03-SUMMARY.md
@./CLAUDE.md
@mobile/AGENTS.md

<interfaces>
<!-- Frozen catalog contract (Phase 4, D-15/D-17): -->
<!-- GET /vehicles/catalog?search=&page=&limit=&fuelCategory=&fuelType=&brand= -->
<!--   → { items: VehicleModel[]; total; page; limit; totalPages } -->
<!-- The CURRENT mobile code reads r.data.data (old {data,total}) — BROKEN; switch to r.data.items. -->
<!-- Web showroom reference: web/src/app/app/garage/add/page.tsx (debounced reset + cancellable -->
<!--   paginated loader: page 1 REPLACES / page>1 APPENDS; brand-group over accumulated items; 'Charger plus'). -->
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Editorial-dark garage list + add/edit/delete modals</name>
  <read_first>
    - mobile/app/(tabs)/vehicles.tsx (full: garage list, AddVehicleModal, EditVehicleModal, delete Alert, styles, emoji ✏️/🗑 icon buttons)
    - mobile/constants/theme.ts (ThemeColors) + normalized atoms (Button/Card/Input) + Eyebrow/SectionCard/FuelBadge-equivalent (use Pill or a small inline badge for fuel type)
  </read_first>
  <files>mobile/app/(tabs)/vehicles.tsx</files>
  <action>
    Restyle the garage list + both modals to editorial-dark (MD-2 — keep `loadVehicles`, `handleDelete`, the add `POST /vehicles/me`, edit `PATCH /vehicles/me/:id`, delete `DELETE /vehicles/me/:id`, and all state/handlers verbatim). Page bg `bg`; title via Eyebrow + display heading; vehicle rows in editorial Cards (surface/hairline) with the vehicle name in `ink` (display 700) and the sub line (fuelType · consumption) in `ink2`, mono for the numeric. Replace emoji icon buttons (✏️/🗑) with neutral editorial controls — either `@expo/vector-icons` (Ionicons pencil/trash, editorial colors, Expo-Go safe) or text labels styled with tokens; the delete control uses `fuelGas` (destructive). Modal containers use `bg`/`surface` + `hairline` headers; the catalog item rows + selected-model preview use editorial tokens. Add a small fuel-type badge (Pill or inline) on catalog rows. Replace all weights 500/600 with 400/700; no blue literals.
  </action>
  <verify>
    <automated>cd mobile && npx tsc --noEmit 2>&1 | grep -E "vehicles.tsx" || echo "vehicles clean"</automated>
  </verify>
  <acceptance_criteria>
    - Garage list + add/edit modals render editorial-dark; CRUD flows behave identically (MD-2).
    - No emoji icon buttons / no blue literals / no 500-600 weights remain.
    - mobile tsc GREEN.
  </acceptance_criteria>
  <done>Garage screen is editorial-dark, CRUD intact.</done>
</task>

<task type="auto">
  <name>Task 2: Server-side, paginated, brand-grouped catalog browse</name>
  <read_first>
    - mobile/app/(tabs)/vehicles.tsx AddVehicleModal (current: `client.get('/vehicles/catalog', { params: { search, limit: 20 } })` then `r.data.data` — BROKEN shape)
    - web/src/app/app/garage/add/page.tsx (the canonical debounced reset + cancellable paginated loader: page 1 REPLACES / page>1 APPENDS; brand grouping over accumulated items; 'Charger plus')
    - .planning/phases/04-multi-source-vehicle-catalog-scaled-showroom/04-03-SUMMARY.md (the frozen { items, total, page, limit, totalPages } contract)
    - mobile/src/types/api.ts (CatalogPage type + VehicleModel.source added in 05-02) + mobile/src/hooks/useDebounce.ts + i18n vehicles.loadMore/noResults/resultsCount
  </read_first>
  <files>mobile/app/(tabs)/vehicles.tsx</files>
  <action>
    Rework `AddVehicleModal`'s catalog fetch to the server-side contract, mirroring the web showroom (D-17):
    - Add `page` state (reset to 1 on `debouncedSearch` change) and `total`/`totalPages` state.
    - Two effects: (a) reset effect on `[debouncedSearch]` → `setPage(1)`; (b) a CANCELLABLE loader effect on `[debouncedSearch, page]` → `client.get<CatalogPage>('/vehicles/catalog', { params: { search: debouncedSearch, page, limit: 30 } })`; read `r.data.items` (NOT `.data.data`); page 1 REPLACES the accumulated list, page>1 APPENDS; a cleanup `cancelled` flag discards an in-flight page>1 response when the query resets (so a search change while paged-in never wrongly appends). Catch → leave list unchanged.
    - Brand grouping: a `useMemo` folding the accumulated `items` into brand sections (mirror the web grouping); render brand headers (Eyebrow) + model rows.
    - "Charger plus": a Button shown when `page < totalPages`, guarded by an `isLoadingMore` flag, incrementing `page` (mirrors web/trips 'Charger plus').
    - Header shows the loaded count + `vehicles.resultsCount {{loaded}}/{{total}}`; empty state uses `vehicles.noResults`.
    - Keep `selected`/`handleSave` (the `POST /vehicles/me` add flow) verbatim; the FlatList → selection sets `selected` as before. Do NOT load all models client-side (no parallel multi-page prefetch).
  </action>
  <verify>
    <automated>cd mobile && npx tsc --noEmit 2>&1 | grep -E "vehicles.tsx" ; echo "---" ; grep -c "data.items" "mobile/app/(tabs)/vehicles.tsx" ; grep -c "data.data" "mobile/app/(tabs)/vehicles.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - The catalog fetch reads `r.data.items` (the frozen contract); `grep "data.data"` returns 0.
    - Pagination: page 1 replaces / page>1 appends, with a cancellable loader; a "Charger plus" control appears when `page < totalPages`; no client load-all.
    - Results are brand-grouped; the add-vehicle `POST /vehicles/me` flow is unchanged.
    - mobile tsc GREEN.
  </acceptance_criteria>
  <done>Mobile catalog browses the scaled catalog server-side, paginated + brand-grouped (CAT-06 mobile slice).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | catalog query params + vehicle create/update/delete |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06-01 | Denial of Service | catalog pagination | mitigate | Server-side `limit: 30` + incremental "Charger plus" replaces unbounded client load-all (memory bound); backend already `@Max(100)` caps limit |
| T-05-06-02 | Tampering | catalog query input | accept | Search string sent as a bound query param to the existing authed endpoint; backend binds `:params` (no SQL interpolation, D-15) |
</threat_model>

<verification>
- mobile `npx tsc --noEmit` GREEN.
- vehicles.tsx reads `data.items`, has 0 `data.data`, has a 'Charger plus' control + brand grouping.
- garage CRUD + add `POST /vehicles/me` flow unchanged.
</verification>

<success_criteria>
The mobile garage is editorial-dark and its catalog browse uses the Phase-4 server-side search + pagination API (no client load-all), brand-grouped — CRUD preserved (MOB-01 + CAT-06 mobile slice).
</success_criteria>

<output>
Create `.planning/phases/05-mobile-tolls-editorial-redesign/05-06-SUMMARY.md` when done.
</output>
