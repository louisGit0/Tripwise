---
phase: 05-mobile-tolls-editorial-redesign
plan: 08
type: execute
wave: 4
depends_on: [05-04, 05-05, 05-06, 05-07]
files_modified: []
autonomous: false
requirements: [MOB-01, MOB-02, MOB-03]
user_setup: []

must_haves:
  truths:
    - "All mobile screens are visually editorial-dark on a real device/Expo Go (MOB-01)"
    - "The dashboard result shows tolls with the real-vs-estimate badge consistent with web (MOB-02)"
    - "The catalog browse loads + paginates the scaled catalog without lag (CAT-06 mobile slice)"
    - "Design tokens are consistent web↔mobile from the shared source (MOB-03)"
  artifacts: []
  key_links: []
---

<objective>
Final human verification of the mobile redesign + tolls + catalog browse on the user's device (Expo Go, with the `@rnmapbox/maps` placeholder acceptable; dev build only if the map itself must be seen). This is the phase + milestone sign-off — no code changes; it pauses for the user to confirm the editorial-dark look, the toll real-vs-estimate display, the server-side catalog browse, fonts (no serif), and reduced-motion behavior.

Purpose: Confirm MOB-01/02/03 are user-observable before closing the final phase.
Output: user sign-off (or a defect list to route into a gap-closure plan).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/05-mobile-tolls-editorial-redesign/05-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-checkpoint automated gate</name>
  <read_first>
    - .planning/phases/05-mobile-tolls-editorial-redesign/05-01..05-07 SUMMARYs
  </read_first>
  <files>(none — verification only)</files>
  <action>
    Run the full mobile type baseline and a token-consistency grep before handing to the user. Confirm: mobile `npx tsc --noEmit` is GREEN (0 errors — all 7 pre-existing fixed); no `#3b82f6` and no `Primary` blue ramp remain anywhere under `mobile/`; `mobile/constants/theme.ts` imports the shared token module; the dashboard references `tollIsEstimate` + `DataBar` + `AnimatedCounter`; the vehicles catalog reads `data.items` (0 `data.data`). Summarize results for the user.
  </action>
  <verify>
    <automated>cd mobile && npx tsc --noEmit 2>&1 | grep -c "error TS" || echo 0 ; echo "--- blue check (expect 0) ---" ; grep -rn "#3b82f6" constants app src 2>/dev/null | wc -l ; echo "--- shared import (expect >=1) ---" ; grep -c "tokens" constants/theme.ts</automated>
  </verify>
  <acceptance_criteria>
    - mobile `npx tsc --noEmit` reports 0 errors.
    - No `#3b82f6` / `Primary[` blue-ramp references remain under `mobile/`.
    - theme.ts imports the shared token module; dashboard + vehicles assertions hold.
  </acceptance_criteria>
  <done>Automated gate green; ready for human visual verification.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    The Expo app restyled to editorial-dark from shared tokens (Space Grotesk + JetBrains Mono, no serif), the dashboard result showing tolls with a réel/≈ estimé badge + Énergie/Péage breakdown + animated total, and a server-side paginated brand-grouped catalog browse in the add-vehicle flow.
  </what-built>
  <how-to-verify>
    1. Start the app: `cd mobile && npx expo start`, open in Expo Go (or a dev build if you want the real Mapbox map; the placeholder is expected in Expo Go).
    2. AUTH: open login + register — confirm the editorial-dark look (warm-charcoal bg, Space Grotesk headings, accent buttons, NO serif). Log in (email or Google).
    3. DASHBOARD: pick a vehicle, enter a French origin→destination that uses an autoroute (e.g. Paris → Lyon), Calculate. Confirm: the total animates up smoothly; a "Péage · …" line shows with an "≈ estimé" badge; an Énergie/Péage bar renders. Then try a short toll-free trip and confirm the toll line is HIDDEN.
    4. REDUCED MOTION: enable the OS "Reduce Motion" accessibility setting and recalculate — the total should appear instantly (no count-up).
    5. GARAGE: open the vehicles tab → Add → type a brand (e.g. "Renault"); confirm results appear grouped by brand, scroll and tap "Charger plus" to load more (no freeze). Add a vehicle; edit + delete one — confirm CRUD still works.
    6. FAVORITES + SETTINGS: confirm both are editorial-dark; use a favorite (deep-links to dashboard); toggle theme + language; confirm logout works.
    7. CONSISTENCY: confirm the mobile palette/typography visually matches the web app (same warm-charcoal + accent + fonts).
  </how-to-verify>
  <resume-signal>Type "approved" to close Phase 5 (and the milestone), or list any visual/behavior defects to route into a gap-closure plan.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Verification only — no code change, no new dependency, no secret |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-08-01 | (n/a) | verification checkpoint | accept | No code/dependency change; this plan only inspects + confirms. Phase-level note: no new secret introduced (Mapbox public token pre-existing); new deps are first-party `@expo-google-fonts` (expo-managed) |
</threat_model>

<verification>
- Automated: mobile tsc 0 errors; no blue ramp; shared import present.
- Human: editorial-dark across all screens; tolls real-vs-estimate + hide-when-0; animated + reduced-motion total; server-side brand-grouped catalog browse; flows intact; web↔mobile token consistency.
</verification>

<success_criteria>
User confirms MOB-01 (editorial-dark screens), MOB-02 (mobile tolls real-vs-estimate), and MOB-03 (shared tokens consistent) on a real device — Phase 5 and the milestone are signed off.
</success_criteria>

<output>
Create `.planning/phases/05-mobile-tolls-editorial-redesign/05-08-SUMMARY.md` when done.
</output>
