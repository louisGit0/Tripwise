---
phase: 05-mobile-tolls-editorial-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - shared/src/tokens.ts
  - shared/src/index.ts
  - mobile/metro.config.js
  - mobile/tsconfig.json
  - mobile/constants/theme.ts
autonomous: true
requirements: [MOB-01, MOB-03]
user_setup: []

must_haves:
  truths:
    - "Mobile theme tokens resolve from one shared source of truth (MOB-03)"
    - "Mobile no longer ships the legacy blue (#3b82f6) palette"
    - "The dashboard/Button color-union tsc errors are gone (theme is one consistent shape)"
  artifacts:
    - path: "shared/src/tokens.ts"
      provides: "Framework-free canonical editorial-dark tokens (dark+light hex, spacing, radius, fontFamily, fontSize)"
      contains: "0e0c0a"
    - path: "mobile/constants/theme.ts"
      provides: "RN theme consuming the shared module via a real import; single ThemeColors interface"
      contains: "tokens"
    - path: "mobile/metro.config.js"
      provides: "Metro watchFolders so the out-of-root shared module bundles at runtime"
      contains: "watchFolders"
  key_links:
    - from: "mobile/constants/theme.ts"
      to: "shared/src/tokens.ts"
      via: "relative import"
      pattern: "shared/src/tokens"
    - from: "shared/src/index.ts"
      to: "shared/src/tokens.ts"
      via: "barrel re-export"
      pattern: "tokens"
---

<objective>
Create the shared editorial-dark token module (PD5-1, MOB-03) and rewrite the mobile theme to consume it as one consistent light/dark shape — the BLOCKING foundation every later plan builds on. Rewriting `theme.ts` to a single `ThemeColors` interface resolves 5 of the 7 pre-existing tsc errors (the `Colors[scheme]` union mismatches in `dashboard.tsx` ×3 and `Button.tsx` ×2). The remaining 2 (Button `false` ViewStyle, MapboxMap overload) are fixed in 05-02.

Purpose: Establish "shared source of truth where practical" for design tokens and clean the type baseline so build verification becomes meaningful (PD5-2).
Output: `shared/src/tokens.ts`, barrel export, Metro + tsconfig wiring, rewritten `mobile/constants/theme.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-mobile-tolls-editorial-redesign/05-CONTEXT.md
@./CLAUDE.md
@mobile/AGENTS.md

<interfaces>
<!-- Canonical token VALUES (from web/src/app/globals.css, the locked design contract). -->
<!-- DARK (default): bg #0e0c0a · surface #17150f · surface2 #211d16 · surface3 #2a251c ·
     ink #f2efe8 · ink2 #c9c2b4 · muted #8a8173 · faint #2e2820 · hairline #3a3328 ·
     accent #4d8bff · ev #4d8bff · fuelGas #ff7849 · fuelDie #ffc247 · fuelGpl #a78bfa · toll #b8a98c -->
<!-- LIGHT: bg #fafaf7 · surface #ffffff · surface2 #f3f1ec · surface3 #ece9e2 ·
     ink #1c1a16 · ink2 #4a463e · muted #8a8275 · faint #e8e5dd · hairline #d8d4ca ·
     accent #4d8bff · ev #4d8bff · fuelGas #ff7849 · fuelDie #ffc247 · fuelGpl #7c3aed · toll #9b8a6b -->
<!-- Type discipline (D-11, NO serif): Space Grotesk display/UI + JetBrains Mono numerics, 2 weights 400/700. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the framework-free shared token module + barrel export</name>
  <read_first>
    - web/src/app/globals.css (the canonical token hex values — :root dark + [data-theme="light"])
    - shared/src/index.ts (barrel to extend)
    - shared/src/types/fuel-category.types.ts (existing shared style for a pure-TS module)
  </read_first>
  <files>shared/src/tokens.ts, shared/src/index.ts</files>
  <action>
    Create `shared/src/tokens.ts` as a PURE TypeScript module with ZERO imports (no `react-native`, no framework) so the backend/web barrel stays clean when it pulls this in. Export a single `const tokens` (and a `Tokens`/`ThemeColorTokens` type) shaped:
    - `colors: { dark: {...}, light: {...} }` where each scheme object has EXACTLY these keys with the canonical hex from globals.css: `bg, surface, surface2, surface3, ink, ink2, muted, faint, hairline, accent, ev, fuelGas, fuelDie, fuelGpl, toll`. Dark values: bg `#0e0c0a`, surface `#17150f`, surface2 `#211d16`, surface3 `#2a251c`, ink `#f2efe8`, ink2 `#c9c2b4`, muted `#8a8173`, faint `#2e2820`, hairline `#3a3328`, accent `#4d8bff`, ev `#4d8bff`, fuelGas `#ff7849`, fuelDie `#ffc247`, fuelGpl `#a78bfa`, toll `#b8a98c`. Light values: bg `#fafaf7`, surface `#ffffff`, surface2 `#f3f1ec`, surface3 `#ece9e2`, ink `#1c1a16`, ink2 `#4a463e`, muted `#8a8275`, faint `#e8e5dd`, hairline `#d8d4ca`, accent `#4d8bff`, ev `#4d8bff`, fuelGas `#ff7849`, fuelDie `#ffc247`, fuelGpl `#7c3aed`, toll `#9b8a6b`.
    - `fontFamily: { display: 'SpaceGrotesk_700Bold', displayRegular: 'SpaceGrotesk_400Regular', mono: 'JetBrainsMono_700Bold', monoRegular: 'JetBrainsMono_400Regular' }` — the exact loaded-font keys @expo-google-fonts exposes (05-02 loads them). NO serif (D-11).
    - `fontWeight: { regular: '400', bold: '700' }` (2 weights only, per D-11/PD-2).
    - `fontSize: { hero: 56, display: 26, body: 14, caption: 11 }` — RN numeric mirror of the web 4-size scale (web hero uses a clamp; mobile is narrow → 56). Keep to 4 sizes.
    - `spacing: { 1:4, 2:8, 3:12, 4:16, 5:20, 6:24, 8:32 }` and `radius: { sm:6, md:10, lg:14, xl:18, card:12, chip:6, full:9999 }` (mirrors the web 8-pt rhythm + card radius 12).
    Add a top-of-file doc comment: "Canonical editorial-dark tokens. Web mirrors these values in web/src/app/globals.css (NOT consumed from here — locked, do not refactor web). Mobile consumes this module as its source (PD5-1)." Then re-export it from `shared/src/index.ts` (append `export * from './tokens';`).
  </action>
  <verify>
    <automated>cd shared && npx tsc --noEmit 2>&1 | head -20 ; echo "---grep---" ; grep -c "0e0c0a" src/tokens.ts</automated>
  </verify>
  <acceptance_criteria>
    - `shared/src/tokens.ts` exists, has ZERO imports, exports `tokens` with `colors.dark` + `colors.light` (15 keys each), `fontFamily`, `fontWeight` (400/700 only), `fontSize` (4 keys), `spacing`, `radius`.
    - Dark `bg` is `#0e0c0a`; light `bg` is `#fafaf7`; `toll` present in both schemes.
    - `shared/src/index.ts` re-exports `./tokens`.
    - `shared` tsc --noEmit is clean (no new errors).
  </acceptance_criteria>
  <done>The canonical token module exists and is barrel-exported; values match globals.css exactly.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Metro + tsconfig so mobile resolves the out-of-root shared module</name>
  <read_first>
    - mobile/tsconfig.json (current baseUrl/paths; `@/` alias)
    - mobile/package.json (Expo SDK 54; main = expo-router/entry)
    - https://docs.expo.dev/versions/v54.0.0/  AND  https://docs.expo.dev/guides/monorepos/ (the official Expo monorepo Metro pattern — REQUIRED reading per mobile/AGENTS.md)
  </read_first>
  <files>mobile/metro.config.js, mobile/tsconfig.json</files>
  <action>
    The shared module lives OUTSIDE `mobile/` (at repo-root `shared/`). Metro by default only watches the project root, so a relative import into `../shared` will fail to bundle at runtime unless Metro is told to watch it. Add `mobile/metro.config.js` per the Expo monorepo guide: start from `getDefaultConfig(__dirname)`, set `config.watchFolders = [<monorepo root>]` (resolve repo root as `path.resolve(__dirname, '..')`), and set `config.resolver.nodeModulesPaths = [<mobile node_modules>, <root node_modules>]` (+ `disableHierarchicalLookup = true` only if the guide for SDK 54 recommends it — follow the versioned doc). Export the config. Do NOT change any other Metro behavior. For tsc resolution, in `mobile/tsconfig.json` keep the existing `@/` alias untouched and ensure `compilerOptions` does not exclude `../shared` (the relative import in theme.ts must type-resolve). If a path alias aids clarity you MAY add `"@verygoodtrip/shared": ["../shared/src/index.ts"]` and `"@verygoodtrip/shared/*": ["../shared/src/*"]`, but the theme import in Task 3 uses a plain RELATIVE path to stay unambiguous regardless of alias.
  </action>
  <verify>
    <automated>cd mobile && node -e "require('./metro.config.js'); console.log('metro config loads')"</automated>
  </verify>
  <acceptance_criteria>
    - `mobile/metro.config.js` exists, requires successfully, sets `watchFolders` including the repo root and configures `resolver.nodeModulesPaths`.
    - `mobile/tsconfig.json` still compiles existing files (no regression) and does not exclude `../shared`.
  </acceptance_criteria>
  <done>Metro watches the shared folder; mobile can bundle + type-resolve the shared token module.</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite mobile/constants/theme.ts to editorial-dark consuming the shared tokens (single ThemeColors)</name>
  <read_first>
    - mobile/constants/theme.ts (current OLD blue palette — the light/dark SHAPE MISMATCH is the root of 5 of the 7 tsc errors)
    - shared/src/tokens.ts (the source created in Task 1)
    - mobile/src/components/ui/Button.tsx + mobile/app/(tabs)/dashboard.tsx (consumers that read `c.text/primary/muted/border/card/textSecondary/mutedFg/inputBg/inputBorder/placeholder/destructive/tabIconDefault/...` — the new theme must keep ALL these keys so nothing breaks before screens are restyled)
  </read_first>
  <files>mobile/constants/theme.ts</files>
  <action>
    Rewrite `theme.ts` to import the shared module via a REAL relative import (`import { tokens } from '../shared/src/tokens'` — adjust depth so it resolves from `mobile/constants/`; expected `../../shared/src/tokens`). Define ONE exported `interface ThemeColors` listing every key used across the app, then build `export const Colors: Record<ColorScheme, ThemeColors>` for `light` and `dark`, each derived from `tokens.colors[scheme]`. CRITICAL — to avoid breaking un-restyled consumers mid-flight, the new `ThemeColors` MUST be a SUPERSET that maps BOTH:
    (a) NEW editorial keys: `bg, surface, surface2, surface3, ink, ink2, muted, faint, hairline, accent, ev, fuelGas, fuelDie, fuelGpl, toll`; AND
    (b) the LEGACY keys current screens/atoms still reference, remapped to editorial tokens: `text`→ink, `textSecondary`→ink2, `background`→bg, `card`→surface, `border`→hairline, `tint`→accent, `icon`→muted, `tabIconDefault`→muted, `tabIconSelected`→accent, `primary`→accent, `primaryLight`→surface2, `destructive`→fuelGas, `success`→(keep a green, e.g. `#4ade80`/`#16a34a`), `amber`→fuelDie, `amberBg`→surface2, `inputBg`→surface, `inputBorder`→hairline, `placeholder`→muted, `muted` legacy fill→surface2, `mutedFg`→ink2.
    NOTE the name collision: editorial `muted` is a TEXT color (#8a8173) but legacy `muted` was a FILL (surface). Resolve by keeping legacy `muted` = surface2 (fill) and exposing the editorial muted-text via a distinct key (e.g. `mutedText` or reuse `ink2` consumers). Document the mapping in a comment so 05-04..07 restyles migrate cleanly. Replace the `Primary` blue ramp entirely (DELETE `#3b82f6` and the whole `Primary` object, or repoint every value to editorial tokens — no blue ramp may remain). Keep `ColorScheme`, `Fonts`, `FontSizes`, `Spacing`, `Radius` exports but source `Spacing`/`Radius`/`FontSizes` from `tokens` where they map; set `Fonts` to the editorial font family keys from `tokens.fontFamily`. Use `as const`/typed objects so `Colors.light` and `Colors.dark` share the SAME `ThemeColors` type (this is what kills the union errors).
  </action>
  <verify>
    <automated>cd mobile && npx tsc --noEmit 2>&1 | tee /tmp/tsc05.txt | grep -E "dashboard.tsx\(22[0-9]|Button.tsx\(4[129]" ; echo "REMAINING ERRORS:" ; grep -c "error TS" /tmp/tsc05.txt</automated>
  </verify>
  <acceptance_criteria>
    - `mobile/constants/theme.ts` imports `tokens` from the shared module via a real relative path (NOT a hardcoded copy of hex values).
    - No `#3b82f6` and no `Primary` blue ramp remains in theme.ts.
    - `Colors.light` and `Colors.dark` share one `ThemeColors` type; `Colors[scheme]` is assignable to `ThemeColors`.
    - mobile `npx tsc --noEmit` shows the 3 `dashboard.tsx` union errors AND the 2 `Button.tsx` color-arg union errors are GONE (only the Button `false`-ViewStyle + MapboxMap overload errors may remain — fixed in 05-02).
    - All legacy keys consumed by existing screens still exist on `ThemeColors`.
  </acceptance_criteria>
  <done>theme.ts is editorial-dark, sourced from shared tokens, single-typed; 5 of 7 pre-existing errors resolved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time module graph | shared/ pulled into the mobile bundle via Metro watchFolders |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-01 | Tampering | shared/src/tokens.ts barrel re-export | accept | Pure-TS constant object, zero imports, no runtime behavior; backend/web pull only static hex/number values — no new attack surface |
| T-05-01-02 | Information Disclosure | Metro watchFolders widening | accept | Only the repo-local `shared/` is added to the watch graph; no secrets in tokens.ts; no new network/IO |
</threat_model>

<verification>
- mobile `npx tsc --noEmit`: the 5 union errors (dashboard ×3, Button ×2 color args) are resolved.
- `shared` `npx tsc --noEmit` clean.
- `mobile/metro.config.js` loads via node require.
- No `#3b82f6` / `Primary` ramp left in theme.ts.
</verification>

<success_criteria>
Shared editorial-dark token module exists and is the real source mobile theme consumes; Metro/tsconfig resolve it; mobile theme is single-typed editorial-dark with all legacy keys preserved; 5 of 7 pre-existing tsc errors cleared.
</success_criteria>

<output>
Create `.planning/phases/05-mobile-tolls-editorial-redesign/05-01-SUMMARY.md` when done.
</output>
