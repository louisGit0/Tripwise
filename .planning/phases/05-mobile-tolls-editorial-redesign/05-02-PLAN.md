---
phase: 05-mobile-tolls-editorial-redesign
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - mobile/app/_layout.tsx
  - mobile/src/components/ui/Button.tsx
  - mobile/src/components/ui/Card.tsx
  - mobile/src/components/ui/Input.tsx
  - mobile/src/components/ui/Wordmark.tsx
  - mobile/src/components/AutocompleteInput.tsx
  - mobile/src/components/MapboxMap.tsx
  - mobile/src/types/api.ts
  - mobile/src/i18n/translations/fr.ts
  - mobile/src/i18n/translations/en.ts
  - mobile/package.json
autonomous: true
requirements: [MOB-01, MOB-03]
user_setup: []

must_haves:
  truths:
    - "Mobile UI renders in Space Grotesk (700/400) + JetBrains Mono — NO serif (D-11)"
    - "Shared atoms (Button/Card/Input/Wordmark/Autocomplete) wear the editorial-dark tokens"
    - "mobile npx tsc --noEmit is fully GREEN — all 7 pre-existing errors resolved"
  artifacts:
    - path: "mobile/app/_layout.tsx"
      provides: "useFonts loading Space Grotesk + JetBrains Mono with splash gate"
      contains: "useFonts"
    - path: "mobile/src/types/api.ts"
      provides: "TripResult.tollCost/tollIsEstimate + VehicleModel.source + catalog response shape"
      contains: "tollIsEstimate"
  key_links:
    - from: "mobile/src/components/ui/Button.tsx"
      to: "mobile/constants/theme.ts"
      via: "editorial token consumption"
      pattern: "Colors\\[scheme\\]"
    - from: "mobile/app/_layout.tsx"
      to: "@expo-google-fonts/space-grotesk"
      via: "useFonts"
      pattern: "SpaceGrotesk_700Bold"
---

<objective>
Load the editorial fonts (Space Grotesk + JetBrains Mono, weights 400/700, NO serif), normalize the shared mobile components (Button/Card/Input/Wordmark/AutocompleteInput/MapboxMap) to the editorial-dark tokens, and extend the mobile API types + i18n strings consumed by every Wave-3 screen. Completing this plan makes mobile `tsc` fully GREEN (fixes the last 2 pre-existing errors: Button `false` ViewStyle + MapboxMap overload).

Purpose: Provide the typed, fonted, restyled shared layer + the toll/catalog types + i18n keys so Wave-3 screen plans only consume them (no file overlap).
Output: font wiring, normalized atoms + shared components, extended types, pre-seeded i18n strings.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/05-mobile-tolls-editorial-redesign/05-CONTEXT.md
@.planning/phases/05-mobile-tolls-editorial-redesign/05-01-PLAN.md
@./CLAUDE.md
@mobile/AGENTS.md

<interfaces>
<!-- @expo-google-fonts exports: SpaceGrotesk_400Regular, SpaceGrotesk_700Bold,
     JetBrainsMono_400Regular, JetBrainsMono_700Bold + useFonts (from expo-font). -->
<!-- Backend POST /trips/calculate returns top-level tollCost: number|null and
     tollIsEstimate: boolean (backend/src/trips/trips.service.ts:221-222). -->
<!-- Frozen catalog contract (Phase 4): GET /vehicles/catalog → { items, total, page, limit, totalPages }. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install + load Space Grotesk and JetBrains Mono with a splash gate</name>
  <read_first>
    - mobile/app/_layout.tsx (root layout to wire fonts into; AuthProvider + Stack + Toast)
    - mobile/package.json (Expo SDK 54; expo-font + expo-splash-screen already present)
    - shared/src/tokens.ts (fontFamily keys the StyleSheets reference: SpaceGrotesk_700Bold etc.)
    - https://docs.expo.dev/versions/v54.0.0/sdk/font/ (useFonts API for SDK 54 — REQUIRED per mobile/AGENTS.md)
  </read_first>
  <files>mobile/package.json, mobile/app/_layout.tsx</files>
  <action>
    Install the two first-party Expo font packages with `npx expo install @expo-google-fonts/space-grotesk @expo-google-fonts/jetbrains-mono` (expo install pins SDK-54-compatible versions). These are official `@expo-google-fonts` org packages — verify the org on npmjs.com before install (supply-chain, T-05-02-SC). In `mobile/app/_layout.tsx` import `useFonts` (from `@expo-google-fonts/space-grotesk` or `expo-font` per the versioned doc) plus the four faces `SpaceGrotesk_400Regular, SpaceGrotesk_700Bold, JetBrainsMono_400Regular, JetBrainsMono_700Bold`. Call `SplashScreen.preventAutoHideAsync()` at module scope; in the component call `useFonts({...four faces...})`; while `!loaded` return `null` (splash stays up); when loaded call `SplashScreen.hideAsync()` (in an effect) and render the existing tree. Preserve the existing `'../src/i18n'` import, `AuthProvider`, `StatusBar`, `Stack screenOptions`, and `Toast` exactly (MD-2 — do not change navigation). The loaded face keys must match `tokens.fontFamily` so atoms/screens referencing `fontFamily: tokens.fontFamily.display` resolve.
  </action>
  <verify>
    <automated>cd mobile && npx tsc --noEmit 2>&1 | grep -E "_layout.tsx" ; echo "exit grep" ; node -e "const p=require('./package.json'); console.log(Object.keys(p.dependencies).filter(k=>k.includes('expo-google-fonts')))"</automated>
  </verify>
  <acceptance_criteria>
    - `@expo-google-fonts/space-grotesk` and `@expo-google-fonts/jetbrains-mono` are in `mobile/package.json` dependencies.
    - `_layout.tsx` loads all 4 faces via `useFonts`, holds splash until loaded, then `hideAsync`, and preserves AuthProvider/StatusBar/Stack/Toast/i18n import.
    - No new tsc error in `_layout.tsx`.
  </acceptance_criteria>
  <done>Editorial fonts load before first paint; no serif; navigation untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Normalize shared components to editorial-dark + fix the last 2 tsc errors</name>
  <read_first>
    - mobile/src/components/ui/Button.tsx (lines 38-50 `containerStyle: ViewStyle[]` with `false`; variantContainer/variantLabel `c: typeof Colors.light`)
    - mobile/src/components/ui/Card.tsx, mobile/src/components/ui/Input.tsx, mobile/src/components/ui/Wordmark.tsx
    - mobile/src/components/AutocompleteInput.tsx (reads inputBg/inputBorder/card/border/text/mutedFg/primary/placeholder)
    - mobile/src/components/MapboxMap.tsx (line ~99 `MV` cast lacks `children` → the overload error; route line color hardcoded `#2563eb`)
    - mobile/constants/theme.ts (the new ThemeColors from 05-01)
    - shared/src/tokens.ts (fontFamily for mono numerics)
  </read_first>
  <files>mobile/src/components/ui/Button.tsx, mobile/src/components/ui/Card.tsx, mobile/src/components/ui/Input.tsx, mobile/src/components/ui/Wordmark.tsx, mobile/src/components/AutocompleteInput.tsx, mobile/src/components/MapboxMap.tsx</files>
  <action>
    Editorial-dark restyle (MD-2 — keep every prop API, just change visuals/tokens):
    - Button: retype `c: ThemeColors` (import the new type) in `variantContainer`/`variantLabel`; fix the `false` ViewStyle error by typing the style array as `StyleProp<ViewStyle>` (or `(ViewStyle | false | undefined)[]` filtered with `.filter(Boolean)`). Map variants to editorial tokens: primary = `accent` bg with `#0e0c0a` ink label (matches web accent CTA contrast), secondary = `surface2` bg + `hairline` border + `ink` label, ghost = transparent + `accent` label, destructive = `fuelGas` bg + white label. Label uses `fontFamily: tokens.fontFamily.display`, weight 700 (no `font-semibold` 600 — D-11 2-weight). Add a pressed feel via `activeOpacity` already present.
    - Card: bg `surface`, border `hairline`, radius `card` (12). Keep padding map.
    - Input: bg `surface`, border `hairline`/focus `accent`/error `fuelGas`, text `ink`, placeholder `muted`, label `fontFamily display` 700. Keep TextInputProps passthrough.
    - Wordmark: replace hardcoded `#F0ECE4`/`#4D8BFF` defaults so "good"+period use `accent` and the rest uses the passed ink color; `fontFamily: tokens.fontFamily.display` weight 700.
    - AutocompleteInput: repoint to editorial tokens (inputBg→surface, border→hairline, text→ink, placeholder→muted, dropdown bg→surface2, spinner→accent). Keep the geocode debounce/logic verbatim.
    - MapboxMap: fix the overload by adding `children: React.ReactNode` to the `MV` cast type so `<MV>...</MV>` type-checks; change the route `lineColor` from `#2563eb` to the editorial accent `#4d8bff`; keep the Expo-Go placeholder branch and all map logic intact (do NOT break the placeholder).
  </action>
  <verify>
    <automated>cd mobile && npx tsc --noEmit 2>&1 | tee /tmp/tsc0502.txt ; echo "TOTAL ERRORS:" ; grep -c "error TS" /tmp/tsc0502.txt || echo 0</automated>
  </verify>
  <acceptance_criteria>
    - mobile `npx tsc --noEmit` is FULLY GREEN (0 errors) — all 7 pre-existing errors resolved (Button `false` ViewStyle + MapboxMap overload were the last 2).
    - No `#3b82f6` / `#2563eb` / `#F0ECE4` literals remain in these files; route line is `#4d8bff`.
    - Atoms label/heading text uses the editorial display font; only weights 400/700 used.
    - Every component keeps its existing prop API + behavior (Expo-Go map placeholder still works).
  </acceptance_criteria>
  <done>Shared components are editorial-dark, fonted, and the mobile type baseline is clean.</done>
</task>

<task type="auto">
  <name>Task 3: Extend mobile API types (tolls + catalog) and pre-seed i18n strings</name>
  <read_first>
    - mobile/src/types/api.ts (TripResult, VehicleModel, FuelCost/ElectricCost)
    - web/src/app/app/trips/result/page.tsx (reads result.tollCost / result.tollIsEstimate top-level; the réel/≈ estimé copy)
    - backend/src/trips/trips.service.ts (lines 70-80 + 221-222 — top-level tollCost/tollIsEstimate)
    - mobile/src/i18n/translations/fr.ts + en.ts (existing key namespaces: dashboard.*, vehicles.*, common.*)
  </read_first>
  <files>mobile/src/types/api.ts, mobile/src/i18n/translations/fr.ts, mobile/src/i18n/translations/en.ts</files>
  <action>
    Types (mobile/src/types/api.ts): add to `TripResult` two OPTIONAL top-level fields `tollCost?: number | null;` and `tollIsEstimate?: boolean;` (matching the backend calculate response). Add `source?: string;` (optional) to `VehicleModel`. Add a catalog page response type `CatalogPage { items: VehicleModel[]; total: number; page: number; limit: number; totalPages: number; }` (the frozen Phase 4 contract) — the current `vehicles.tsx` wrongly reads `{ data, total }`; the new type makes 05-06 fix it. Do NOT remove existing fields.
    i18n (fr.ts + en.ts, SYMMETRIC): add ALL new strings Wave-3 screens will consume so those screens never touch i18n files (avoids cross-plan overlap). Under `dashboard.*` add: `tollLabel` ("Péages"/"Tolls"), `tollReal` ("réel"/"actual"), `tollEstimate` ("≈ estimé"/"≈ estimate"), `tollEstimateTooltip` ("Estimation indicative (calcul français moyen)"/EN), `energyLabel` ("Énergie"/"Energy"), `costEstimated` ("Coût estimé"/"Estimated cost"), `breakdown` ("Détail"/"Breakdown"). Under `vehicles.*` add: `loadMore` ("Charger plus"/"Load more"), `noResults` ("Aucun modèle"/"No models"), `resultsCount` ("{{loaded}} sur {{total}}"/"{{loaded}} of {{total}}"), `searchHint` (catalog search prompt). Keep existing keys verbatim; mirror every key in both fr and en.
  </action>
  <verify>
    <automated>cd mobile && npx tsc --noEmit 2>&1 | grep -E "types/api.ts|translations" ; echo "---" ; grep -c "tollIsEstimate" src/types/api.ts ; grep -c "tollEstimate" src/i18n/translations/fr.ts src/i18n/translations/en.ts</automated>
  </verify>
  <acceptance_criteria>
    - `TripResult` has optional `tollCost` + `tollIsEstimate`; `VehicleModel` has optional `source`; `CatalogPage` type exists with the frozen shape.
    - fr.ts and en.ts both contain the new toll + catalog keys (symmetric); no existing key removed.
    - mobile tsc remains GREEN.
  </acceptance_criteria>
  <done>Types and i18n strings are ready for Wave-3 screens to consume with no file overlap.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm install (expo) | new font packages enter the build |
| API → mobile | calculate/catalog JSON typed into the app |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-SC | Tampering | `@expo-google-fonts/*` installs | mitigate | First-party Expo-org packages installed via `npx expo install` (SDK-compatible pinning); verify the `@expo-google-fonts` org on npmjs.com before install; no postinstall scripts expected |
| T-05-02-01 | Tampering | toll/catalog fields typed from API | accept | Display-only; backend already validates + returns these; mobile reuses the existing authed apiClient; optional typing tolerates nulls |
</threat_model>

<verification>
- mobile `npx tsc --noEmit` fully GREEN (0 errors — all 7 prior resolved).
- font packages present; `_layout.tsx` gates splash on `useFonts`.
- no legacy blue literals in shared components; route line `#4d8bff`.
- fr/en i18n symmetric with new toll + catalog keys.
</verification>

<success_criteria>
Editorial fonts load; shared components are editorial-dark with intact APIs; mobile tsc is clean; toll/catalog types + i18n strings are in place for Wave-3 consumption.
</success_criteria>

<output>
Create `.planning/phases/05-mobile-tolls-editorial-redesign/05-02-SUMMARY.md` when done.
</output>
