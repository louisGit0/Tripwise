---
phase: 05-mobile-tolls-editorial-redesign
plan: 04
type: execute
wave: 3
depends_on: [05-02, 05-03]
files_modified:
  - mobile/app/(auth)/login.tsx
  - mobile/app/(auth)/register.tsx
  - mobile/app/(auth)/_layout.tsx
  - mobile/app/(tabs)/_layout.tsx
autonomous: true
requirements: [MOB-01]
user_setup: []

must_haves:
  truths:
    - "Login + register screens wear the editorial-dark direction (MOB-01)"
    - "The tab bar uses editorial-dark surfaces + accent active tint"
    - "Auth flows (email login/register, Google OAuth, navigation) are preserved unchanged (MD-2)"
  artifacts:
    - path: "mobile/app/(auth)/login.tsx"
      provides: "Editorial-dark login (Wordmark, Input, accent Button, OAuth)"
      contains: "Wordmark"
  key_links:
    - from: "mobile/app/(tabs)/_layout.tsx"
      to: "mobile/constants/theme.ts"
      via: "tab bar editorial tokens"
      pattern: "tabBarActiveTintColor"
---

<objective>
Restyle the auth screens (login + register) and the auth/tab layouts to the editorial-dark direction using the new tokens, fonts, and atoms — preserving every flow (email login/register with zod, Google OAuth WebBrowser, register confirm-password refine, navigation links).

Purpose: Bring the entry experience to the editorial language (MOB-01).
Output: 2 restyled screens + 2 restyled layouts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/05-mobile-tolls-editorial-redesign/05-CONTEXT.md
@.planning/phases/05-mobile-tolls-editorial-redesign/05-02-PLAN.md
@./CLAUDE.md
@mobile/AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Editorial-dark login + register</name>
  <read_first>
    - mobile/app/(auth)/login.tsx (email/password zod, Google OAuth via WebBrowser, Wordmark header, divider, switch link)
    - mobile/app/(auth)/register.tsx (zod refine confirmPassword, password error mapping, switch link)
    - mobile/constants/theme.ts (new ThemeColors) + mobile/src/components/ui/{Button,Input,Wordmark}.tsx (normalized atoms) + Eyebrow
  </read_first>
  <files>mobile/app/(auth)/login.tsx, mobile/app/(auth)/register.tsx</files>
  <action>
    Restyle both screens to editorial-dark (MD-2 — change ONLY visuals; keep all form logic, handlers, zod schemas, OAuth, and navigation verbatim). Background `bg`; container keeps centered layout. Header: Wordmark in editorial ink + an `Eyebrow`/subtitle (`auth.login`/`auth.register`) in `muted`. Inputs use the normalized Input atom (already editorial). Submit uses the normalized accent Button. The OAuth Google option on login uses the `secondary` Button variant (quiet) — keep `handleGoogleLogin` + `WebBrowser.openAuthSessionAsync` exactly. Divider line → `hairline`; "ou" text → `muted`. Switch links ("noAccount"/"alreadyAccount") use `accent` text. Replace any `fontWeight: '800'`/'500'/'600' with the 2-weight system (400/700) and the display font. Remove the unused `title` style if present.
  </action>
  <verify>
    <automated>cd mobile && npx tsc --noEmit 2>&1 | grep -E "\(auth\)/login.tsx|\(auth\)/register.tsx" || echo "auth screens clean"</automated>
  </verify>
  <acceptance_criteria>
    - Both screens render editorial-dark (bg/surface/ink/accent tokens), Space Grotesk display, 2 weights only.
    - Email login/register, Google OAuth, register confirm-password refine, and the login↔register switch links are unchanged in behavior.
    - mobile tsc GREEN for both files.
  </acceptance_criteria>
  <done>Auth screens match the editorial direction; flows preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Editorial-dark auth + tab layouts</name>
  <read_first>
    - mobile/app/(auth)/_layout.tsx (Stack headerShown:false)
    - mobile/app/(tabs)/_layout.tsx (Tabs screenOptions: active/inactive tint, tabBarStyle bg/border; TabIcon stub currently returns null)
    - mobile/constants/theme.ts
  </read_first>
  <files>mobile/app/(auth)/_layout.tsx, mobile/app/(tabs)/_layout.tsx</files>
  <action>
    Tab layout (MD-2 — keep the 4 Tabs.Screen entries + titles + nav structure): set `tabBarActiveTintColor` to `accent`, `tabBarInactiveTintColor` to `muted`, `tabBarStyle.backgroundColor` to `surface`, `borderTopColor` to `hairline`. Keep `headerShown:false`. Leave the `TabIcon` stub behavior as-is (icons via label only) UNLESS swapping to `@expo/vector-icons` Ionicons is trivial and SDK-54-safe — if you add icons, use neutral editorial colors and keep the label fallback; do not block on native build (Expo-Go safe). Auth layout: keep `Stack headerShown:false`; optionally set a `contentStyle.backgroundColor` to `bg` so the auth stack background is editorial even before a screen paints. No flow/route changes.
  </action>
  <verify>
    <automated>cd mobile && npx tsc --noEmit 2>&1 | grep -E "\(auth\)/_layout.tsx|\(tabs\)/_layout.tsx" || echo "layouts clean"</automated>
  </verify>
  <acceptance_criteria>
    - Tab bar uses editorial surface bg + hairline border + accent active tint + muted inactive.
    - All 4 tabs + titles + navigation structure unchanged; tsc GREEN.
  </acceptance_criteria>
  <done>Layouts wear editorial-dark; navigation intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API (auth) | login/register credentials + OAuth redirect |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04-01 | Spoofing | OAuth WebBrowser redirect | accept | Restyle-only; the existing `openAuthSessionAsync` flow + token handling are unchanged (MD-2); no auth logic touched |
| T-05-04-02 | Information Disclosure | password inputs | accept | `secureTextEntry` preserved on password/confirm fields; no logging added |
</threat_model>

<verification>
- mobile `npx tsc --noEmit` GREEN.
- No legacy blue literals or weights 500/600/800 in the 4 files.
- Auth/OAuth/navigation behavior unchanged (visual diff only).
</verification>

<success_criteria>
Auth screens and layouts are editorial-dark with all flows preserved (MOB-01).
</success_criteria>

<output>
Create `.planning/phases/05-mobile-tolls-editorial-redesign/05-04-SUMMARY.md` when done.
</output>
