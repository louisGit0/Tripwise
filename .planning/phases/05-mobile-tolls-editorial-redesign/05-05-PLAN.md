---
phase: 05-mobile-tolls-editorial-redesign
plan: 05
type: execute
wave: 3
depends_on: [05-02, 05-03]
files_modified:
  - mobile/app/(tabs)/dashboard.tsx
autonomous: true
requirements: [MOB-01, MOB-02]
user_setup: []

must_haves:
  truths:
    - "The mobile dashboard result shows the toll cost broken out with a réel / ≈ estimé badge (MOB-02)"
    - "The total cost animates with the RN AnimatedCounter and respects reduced motion (PD5-3)"
    - "The toll line is hidden when there is no toll (tollCost 0/null), mirroring web (D-04)"
    - "Calc/share/save-favorite flows are preserved (MD-2)"
  artifacts:
    - path: "mobile/app/(tabs)/dashboard.tsx"
      provides: "Editorial-dark dashboard with hero AnimatedCounter + Énergie/Péage DataBar + toll badge"
      contains: "tollIsEstimate"
  key_links:
    - from: "mobile/app/(tabs)/dashboard.tsx"
      to: "POST /trips/calculate"
      via: "result.tollCost + result.tollIsEstimate"
      pattern: "tollCost"
    - from: "mobile/app/(tabs)/dashboard.tsx"
      to: "mobile/src/components/ui/DataBar.tsx"
      via: "Variant A breakdown"
      pattern: "DataBar"
---

<objective>
Restyle the dashboard to editorial-dark AND surface tolls on the inline result (the mobile equivalent of the web trip-result): a hero animated total (energy + toll), an Énergie/Péage segmented DataBar, and the réel / ≈ estimé toll badge driven by `result.tollIsEstimate` — hidden when there is no toll (MOB-01 + MOB-02). Preserve the calc, share, charging-mode, vehicle-picker, and save-favorite flows verbatim (MD-2).

Purpose: Deliver the milestone toll outcome on mobile, consistent with web.
Output: a fully restyled, toll-aware dashboard.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/05-mobile-tolls-editorial-redesign/05-CONTEXT.md
@.planning/phases/05-mobile-tolls-editorial-redesign/05-02-PLAN.md
@.planning/phases/05-mobile-tolls-editorial-redesign/05-03-PLAN.md
@./CLAUDE.md
@mobile/AGENTS.md

<interfaces>
<!-- POST /trips/calculate returns top-level tollCost: number|null + tollIsEstimate: boolean. -->
<!-- Total to display = (cost.totalCost ?? 0) + (tollCost ?? 0). hasToll = tollCost!=null && tollCost>0. -->
<!-- Web reference for the breakdown/badge copy + structure: web/src/app/app/trips/result/page.tsx. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Editorial-dark dashboard shell + calc form restyle</name>
  <read_first>
    - mobile/app/(tabs)/dashboard.tsx (full: calc form, vehicle chips, charging mode, result card, share, favorite modal, StatItem helper, styles)
    - mobile/constants/theme.ts (ThemeColors) + normalized atoms (Button/Card/Input/AutocompleteInput) + Eyebrow/SectionCard/Pill
  </read_first>
  <files>mobile/app/(tabs)/dashboard.tsx</files>
  <action>
    Restyle the dashboard shell + form to editorial-dark (MD-2 — keep `handleCalculate`, `handleShare`, `handleSaveFavorite`, vehicle fetch, charging-mode state, `canCalculate`, the favorite Modal, and all navigation/params verbatim). Page background `bg`; title uses `Eyebrow` ("Résultat"/section labels) + display heading. Wrap the calc inputs in the editorial Card/SectionCard. Vehicle chips + charging-mode buttons: active = `accent` bg + `#0e0c0a` ink label, inactive = `surface2` + `hairline` border + `ink` label (replace the old `c.primary`/`c.muted` blue look). Replace the `StatItem` helper's `c: typeof Colors.light` with `c: ThemeColors`; numerics use the mono font (`tokens.fontFamily.mono`). Disclaimer note → neutral `surface2`/`ink2` (NOT amber — mirrors web D-10). Replace all weights 500/600 with 400/700.
  </action>
  <verify>
    <automated>cd mobile && npx tsc --noEmit 2>&1 | grep -E "dashboard.tsx" || echo "dashboard clean"</automated>
  </verify>
  <acceptance_criteria>
    - Dashboard shell + calc form render editorial-dark; StatItem retyped to `ThemeColors`; no blue literals/500-600 weights remain.
    - calc/share/charging-mode/vehicle-picker/favorite-modal flows behave identically.
    - mobile tsc GREEN.
  </acceptance_criteria>
  <done>Dashboard form is editorial-dark, flows intact.</done>
</task>

<task type="auto">
  <name>Task 2: Toll-aware result — hero AnimatedCounter + Énergie/Péage DataBar + réel/≈ estimé badge</name>
  <read_first>
    - web/src/app/app/trips/result/page.tsx (hero total = energy+toll; Variant A DataBar; hasToll guard; Pill warning='≈ estimé'/success='réel'; legend with toll badge; hide-when-0)
    - mobile/src/components/ui/{AnimatedCounter,DataBar,Pill,SectionCard,Eyebrow}.tsx (the primitives from 05-03)
    - mobile/src/types/api.ts (TripResult.tollCost/tollIsEstimate added in 05-02) + i18n toll keys (dashboard.tollLabel/tollReal/tollEstimate/tollEstimateTooltip/energyLabel/costEstimated)
  </read_first>
  <files>mobile/app/(tabs)/dashboard.tsx</files>
  <action>
    Rebuild the inline result card (rendered when `result` exists) to mirror the web trip-result:
    - Compute `energyCost = result.cost?.totalCost ?? 0`, `tollCost = result.tollCost ?? 0`, `total = energyCost + tollCost`, `hasToll = result.tollCost != null && result.tollCost > 0`, `tollIsEstimate = result.tollIsEstimate ?? false`.
    - Hero: a SectionCard on the `surface3` elevated plate with an Eyebrow `dashboard.costEstimated`, then `<AnimatedCounter value={total} />` (mono, large `tokens.fontSize.hero`) + a "€" suffix; the counter respects reduced motion.
    - Breakdown: render `<DataBar variant A energyValue={energyCost} tollValue={tollCost} total={total} energyFill={energyFillForFuelType} />` (energy fill chosen by `result.vehicle.fuelType`: ELECTRIC→ev, DIESEL→fuelDie, GPL→fuelGpl, else fuelGas). Below it a legend: an "Énergie · {amount}" row (dot in energyFill) and — ONLY when `hasToll` — a "Péage · {amount}" row (dot in `toll` color) followed by the toll badge. HIDE the entire toll line/segment when `!hasToll` (D-04).
    - Toll badge: `<Pill color={tollIsEstimate ? 'warning' : 'success'} size="sm">{tollIsEstimate ? t('dashboard.tollEstimate') : t('dashboard.tollReal')}</Pill>` — render it in the péage legend (and optionally a metric row), mirroring web. (A tappable info → Alert/toast with `tollEstimateTooltip` is acceptable since RN has no hover tooltip.)
    - Keep the distance/duration metrics + the electric disclaimer (neutral surface) + the share/add-favorite actions. Format euros FR (comma, 2dp). Keep `handleShare`/favorite flow exactly.
  </action>
  <verify>
    <automated>cd mobile && npx tsc --noEmit 2>&1 | grep -E "dashboard.tsx" ; echo "---" ; grep -c "tollIsEstimate" "mobile/app/(tabs)/dashboard.tsx" ; grep -c "DataBar" "mobile/app/(tabs)/dashboard.tsx" ; grep -c "AnimatedCounter" "mobile/app/(tabs)/dashboard.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - The result card renders the toll cost broken out with a réel/≈ estimé Pill from `result.tollIsEstimate`, AND hides the toll line entirely when `tollCost` is 0/null (D-04).
    - Hero total = energy + toll, animated via `AnimatedCounter` (instant under reduced motion).
    - Énergie/Péage `DataBar` (Variant A) renders with the correct energy fill by fuel type.
    - share + add-favorite flows unchanged; mobile tsc GREEN.
  </acceptance_criteria>
  <done>Mobile dashboard shows tolls consistent with web (real-vs-estimate + breakdown), animated, reduced-motion-safe.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API → mobile | calculate response (cost + tollCost/tollIsEstimate) rendered |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05-01 | Tampering | toll fields from calculate | accept | Display-only; backend computes/validates tolls; mobile guards null (`?? 0`) and hides the line when absent |
| T-05-05-02 | Information Disclosure | toll precision label | accept | The réel/≈ estimé badge is driven by the backend `tollIsEstimate` flag (always estimate in practice, D-07/D-08) — honest labeling, no secret/key surfaced |
</threat_model>

<verification>
- mobile `npx tsc --noEmit` GREEN.
- dashboard.tsx references `tollIsEstimate`, `DataBar`, `AnimatedCounter`.
- toll line hidden when tollCost 0/null; calc/share/favorite flows unchanged.
</verification>

<success_criteria>
The mobile dashboard result shows precise-vs-estimate tolls + breakdown consistent with web, with an animated reduced-motion-safe total, all existing flows preserved (MOB-01 + MOB-02).
</success_criteria>

<output>
Create `.planning/phases/05-mobile-tolls-editorial-redesign/05-05-SUMMARY.md` when done.
</output>
