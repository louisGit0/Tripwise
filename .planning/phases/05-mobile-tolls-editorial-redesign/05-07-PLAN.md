---
phase: 05-mobile-tolls-editorial-redesign
plan: 07
type: execute
wave: 3
depends_on: [05-02, 05-03]
files_modified:
  - mobile/app/(tabs)/favorites.tsx
  - mobile/app/(tabs)/settings.tsx
autonomous: true
requirements: [MOB-01]
user_setup: []

must_haves:
  truths:
    - "Favorites + settings are restyled editorial-dark (MOB-01)"
    - "Favorite use-trip deep-link + delete flows are preserved (MD-2)"
    - "Theme picker, language switch, and logout flows are preserved (MD-2)"
  artifacts:
    - path: "mobile/app/(tabs)/favorites.tsx"
      provides: "Editorial-dark favorites list with use-trip + delete"
      contains: "Card"
    - path: "mobile/app/(tabs)/settings.tsx"
      provides: "Editorial-dark settings (theme/language/account/version)"
      contains: "logout"
  key_links:
    - from: "mobile/app/(tabs)/favorites.tsx"
      to: "/(tabs)/dashboard"
      via: "use-trip deep link"
      pattern: "dashboard"
---

<objective>
Restyle the favorites and settings screens to editorial-dark using the new tokens/atoms, preserving every flow: favorite use-trip deep-link to the dashboard, favorite delete; settings theme picker (Appearance), language switch (i18next), and logout (MD-2).

Purpose: Complete the editorial-dark rollout across the remaining mobile screens (MOB-01).
Output: 2 restyled screens.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/05-mobile-tolls-editorial-redesign/05-CONTEXT.md
@.planning/phases/05-mobile-tolls-editorial-redesign/05-02-PLAN.md
@./CLAUDE.md
@mobile/AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Editorial-dark favorites</name>
  <read_first>
    - mobile/app/(tabs)/favorites.tsx (full: list, handleUseTrip deep-link, handleDelete Alert, emoji 🗑, styles)
    - mobile/constants/theme.ts (ThemeColors) + normalized Card + Button + Eyebrow
  </read_first>
  <files>mobile/app/(tabs)/favorites.tsx</files>
  <action>
    Restyle to editorial-dark (MD-2 — keep `load`, `handleUseTrip` deep-link to `/(tabs)/dashboard?...`, and `handleDelete` verbatim). Page bg `bg`; title via Eyebrow + display heading. Favorite rows in editorial Cards: name `ink` display 700; origin/destination lines `ink2`; the `↓` arrow `muted`. "Utiliser" (use-trip) button uses the accent Button/control; the delete control replaces the 🗑 emoji with a neutral editorial icon (Ionicons trash or token-styled label) colored `fuelGas`. Empty state text `muted`. Replace weights 500/600 with 400/700; no blue literals.
  </action>
  <verify>
    <automated>cd mobile && npx tsc --noEmit 2>&1 | grep -E "favorites.tsx" || echo "favorites clean"</automated>
  </verify>
  <acceptance_criteria>
    - Favorites render editorial-dark; use-trip deep-link + delete behave identically.
    - No emoji icon / blue literals / 500-600 weights; mobile tsc GREEN.
  </acceptance_criteria>
  <done>Favorites is editorial-dark, flows intact.</done>
</task>

<task type="auto">
  <name>Task 2: Editorial-dark settings</name>
  <read_first>
    - mobile/app/(tabs)/settings.tsx (full: theme picker via Appearance, language via i18next, logout Alert, version, styles)
    - mobile/constants/theme.ts (ThemeColors) + normalized Card + Button + Eyebrow/SectionCard
  </read_first>
  <files>mobile/app/(tabs)/settings.tsx</files>
  <action>
    Restyle to editorial-dark (MD-2 — keep `applyTheme`/`Appearance.setColorScheme`, `changeLanguage`/`i18n.changeLanguage`, `handleLogout`/`signOut`, and the `version` read verbatim). Page bg `bg`; title via Eyebrow + display heading. Section labels (theme/language/account) → Eyebrow (caption, muted, 700, weight-constant). Option toggles (light/dark/system, fr/en): active = `accent` bg + `#0e0c0a` ink label, inactive = `surface2` + `hairline` border + `ink` — keep the toggle a constant weight (no weight jump on active). Account logout uses the destructive Button (`fuelGas`). Version line `muted` mono caption. Replace weights 500/600 with 400/700; no blue literals.
  </action>
  <verify>
    <automated>cd mobile && npx tsc --noEmit 2>&1 | grep -E "settings.tsx" || echo "settings clean"</automated>
  </verify>
  <acceptance_criteria>
    - Settings render editorial-dark; theme/language/logout flows behave identically.
    - Active toggle is weight-constant; no blue literals / 500-600 weights; mobile tsc GREEN.
  </acceptance_criteria>
  <done>Settings is editorial-dark, flows intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | favorite delete; client session | logout clears token |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07-01 | Elevation of Privilege | logout / session | accept | Restyle-only; `signOut` (token clear) unchanged (MD-2); no auth logic touched |
| T-05-07-02 | Tampering | favorite delete | accept | Existing authed `DELETE /favorites/:id` with backend ownership check unchanged |
</threat_model>

<verification>
- mobile `npx tsc --noEmit` GREEN.
- no emoji icons / blue literals / 500-600 weights in either file.
- use-trip deep-link, delete, theme/language/logout flows unchanged.
</verification>

<success_criteria>
Favorites + settings are editorial-dark with all flows preserved — completing the editorial-dark rollout across mobile screens (MOB-01).
</success_criteria>

<output>
Create `.planning/phases/05-mobile-tolls-editorial-redesign/05-07-SUMMARY.md` when done.
</output>
