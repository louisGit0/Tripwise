---
phase: 05-mobile-tolls-editorial-redesign
plan: 03
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - mobile/src/hooks/useReducedMotion.ts
  - mobile/src/components/ui/AnimatedCounter.tsx
  - mobile/src/components/ui/DataBar.tsx
  - mobile/src/components/ui/Pill.tsx
  - mobile/src/components/ui/SectionCard.tsx
  - mobile/src/components/ui/Eyebrow.tsx
autonomous: true
requirements: [MOB-01, MOB-02]
user_setup: []

must_haves:
  truths:
    - "An RN reduced-motion hook reads AccessibilityInfo and updates on change"
    - "An animated cost counter tweens 0→target and goes INSTANT under reduced motion (PD5-3)"
    - "A segmented Énergie/Péage DataBar mirrors the web data-viz contract (hide toll when 0)"
  artifacts:
    - path: "mobile/src/hooks/useReducedMotion.ts"
      provides: "AccessibilityInfo.isReduceMotionEnabled + change subscription → boolean"
      contains: "isReduceMotionEnabled"
    - path: "mobile/src/components/ui/AnimatedCounter.tsx"
      provides: "Animated.timing 0→target counter, reduced-motion-aware, FR-formatted"
      contains: "Animated"
    - path: "mobile/src/components/ui/DataBar.tsx"
      provides: "Variant A segmented (energy+toll) + Variant B single comparison bar"
      contains: "tollValue"
  key_links:
    - from: "mobile/src/components/ui/AnimatedCounter.tsx"
      to: "mobile/src/hooks/useReducedMotion.ts"
      via: "instant-when-reduced gate"
      pattern: "useReducedMotion"
    - from: "mobile/src/components/ui/DataBar.tsx"
      to: "mobile/constants/theme.ts"
      via: "toll/energy token fills"
      pattern: "toll"
---

<objective>
Re-implement the web data-viz/motion primitives in React Native (PD5-3): a `useReducedMotion` equivalent (AccessibilityInfo), an `AnimatedCounter` (RN Animated API, transform/opacity-friendly, reduced-motion → instant), a `DataBar` (Variant A segmented Énergie/Péage + Variant B single comparison), plus the small editorial atoms `Pill` / `SectionCard` / `Eyebrow` used across screens. All NEW files (no overlap with 05-02).

Purpose: Give Wave-3 screens the toll breakdown bar, the animated total, and the badge/section primitives that mirror the locked web contract.
Output: 1 hook + 5 components, all token-driven, fully typed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/05-mobile-tolls-editorial-redesign/05-CONTEXT.md
@.planning/phases/05-mobile-tolls-editorial-redesign/05-01-PLAN.md
@./CLAUDE.md
@mobile/AGENTS.md

<interfaces>
<!-- Web references to mirror (behavior, not code): -->
<!-- web/src/hooks/useReducedMotion.ts — subscribe + boolean -->
<!-- web/src/hooks/useCountUp.ts — easeOutCubic 0→target 700ms, instant under reduced motion -->
<!-- web/src/components/ui/DataBar.tsx — Variant A segmented (energyValue/tollValue/total/energyFillVar; toll hidden when tollValue===0); Variant B (value/max/fillVar/muted at 0.45) -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RN useReducedMotion + AnimatedCounter</name>
  <read_first>
    - web/src/hooks/useReducedMotion.ts (subscribe pattern to mirror)
    - web/src/hooks/useCountUp.ts (easeOutCubic 0→target 700ms; instant under reduced motion)
    - mobile/constants/theme.ts (fonts for the mono numeric)
    - https://docs.expo.dev/versions/v54.0.0/ → React Native AccessibilityInfo + Animated API (REQUIRED per mobile/AGENTS.md)
  </read_first>
  <behavior>
    - useReducedMotion: returns false initially, then the live `AccessibilityInfo.isReduceMotionEnabled()` value; re-renders on the `reduceMotionChanged` subscription; cleans up the subscription on unmount.
    - AnimatedCounter: given `value` (number) renders the FR-formatted number (comma decimal, fixed 2dp) tweened 0→value over ~700ms via `Animated.timing` (useNativeDriver where the listener pattern allows; otherwise a JS-driven `Animated.Value` + `addListener` updating local state). Under reduced motion (or value change) it shows the final value immediately with NO animation. Re-runs from 0 when `value` changes (mirrors web useCountUp).
  </behavior>
  <files>mobile/src/hooks/useReducedMotion.ts, mobile/src/components/ui/AnimatedCounter.tsx</files>
  <action>
    Create `mobile/src/hooks/useReducedMotion.ts`: on mount call `AccessibilityInfo.isReduceMotionEnabled().then(setReduced)` and subscribe via `AccessibilityInfo.addEventListener('reduceMotionChanged', setReduced)`; return the boolean; remove the listener on cleanup (use the subscription object's `.remove()` per RN 0.81). Create `mobile/src/components/ui/AnimatedCounter.tsx`: prop `{ value: number; style?: TextStyle; durationMs?: number }`. Hold an `Animated.Value(0)`; in an effect keyed on `value` + `reduced`, if `reduced` set displayed state to `value` instantly (no timing), else reset to 0 and run `Animated.timing(av, { toValue: value, duration: durationMs ?? 700, easing: Easing.out(Easing.cubic), useNativeDriver: false }).start()` with an `av.addListener(({value:v}) => setDisplay(v))` updating a `display` state; format `display` as `display.toFixed(2).replace('.', ',')`. Use `fontFamily: tokens.fontFamily.mono` weight 700 by default (mono numerics). Clean up listener + stop animation on unmount/value change (no leaked frames). NOTE: this is opacity/value-only — compositor-friendly, no layout-bound props animate.
  </action>
  <verify>
    <automated>cd mobile && npx tsc --noEmit 2>&1 | grep -E "useReducedMotion.ts|AnimatedCounter.tsx" || echo "no errors in new files"</automated>
  </verify>
  <acceptance_criteria>
    - `useReducedMotion` calls `AccessibilityInfo.isReduceMotionEnabled` and adds/removes the `reduceMotionChanged` listener.
    - `AnimatedCounter` tweens 0→value with `Animated.timing`, formats FR (comma, 2dp), and resolves INSTANTLY under reduced motion (gated by `useReducedMotion`).
    - mobile tsc GREEN for both files.
  </acceptance_criteria>
  <done>Motion primitives mirror the web behavior in RN, reduced-motion-safe.</done>
</task>

<task type="auto">
  <name>Task 2: RN DataBar (segmented Énergie/Péage + single comparison)</name>
  <read_first>
    - web/src/components/ui/DataBar.tsx (the two variants + pct clamp + hide-toll-when-0 + reveal)
    - mobile/src/hooks/useReducedMotion.ts (from Task 1 — gate the reveal)
    - mobile/constants/theme.ts (toll = `toll`, energy fills = ev/fuelGas/fuelDie/fuelGpl)
  </read_first>
  <files>mobile/src/components/ui/DataBar.tsx</files>
  <action>
    Create `mobile/src/components/ui/DataBar.tsx` mirroring the web contract with a discriminated-union prop type:
    - Variant A (segmented): `{ energyValue, tollValue, total, energyFill: string, height?: 'sm'|'md' }` — a rounded track (`surface2` bg) containing two flush `View`s: energy segment width `pct(energyValue,total)%` colored `energyFill`, then toll segment width `pct(tollValue,total)%` colored theme `toll`. HIDE the toll segment entirely when `tollValue <= 0` (D-04). Reuse a `pct()` that clamps 0–100 and guards zero/NaN totals.
    - Variant B (single): `{ value, max, fill: string, muted?: boolean, height?: 'sm'|'md' }` — one absolute fill bar width `pct(value,max)%`, opacity `muted ? 0.45 : 1`.
    Heights: sm = 6, md = 10 (px). Optional entrance: a width/scaleX `Animated` reveal on mount gated OFF under `useReducedMotion` (instant final width when reduced). Pass colors as RN color strings (e.g. `c.toll`, `c.ev`) — NOT CSS vars. Fully typed; export `DataBarProps`.
  </action>
  <verify>
    <automated>cd mobile && npx tsc --noEmit 2>&1 | grep -E "DataBar.tsx" ; echo "---" ; grep -c "tollValue" src/components/ui/DataBar.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Variant A renders energy+toll segments sized against `total` and hides the toll segment when `tollValue <= 0`.
    - Variant B renders a single fill with muted 0.45 opacity option.
    - Reveal animation (if any) is skipped under reduced motion; mobile tsc GREEN.
  </acceptance_criteria>
  <done>DataBar mirrors the web data-viz contract in RN, token-driven.</done>
</task>

<task type="auto">
  <name>Task 3: Editorial atoms — Pill, SectionCard, Eyebrow</name>
  <read_first>
    - web/src/components/ui/Pill.tsx + Eyebrow.tsx + SectionCard.tsx (naming/visual parity — fetch via web equivalents) — at minimum read the result page usage in web/src/app/app/trips/result/page.tsx for Pill color='warning'|'success' size='sm' and Eyebrow/SectionCard usage
    - mobile/src/components/ui/Card.tsx (SectionCard builds on the same surface/hairline language)
    - mobile/constants/theme.ts (tokens)
  </read_first>
  <files>mobile/src/components/ui/Pill.tsx, mobile/src/components/ui/SectionCard.tsx, mobile/src/components/ui/Eyebrow.tsx</files>
  <action>
    Create three small token-driven atoms matching web naming/semantics:
    - `Pill`: props `{ children: ReactNode; color?: 'success'|'warning'|'neutral'|'accent'; size?: 'sm'|'md' }`. A rounded chip; success = green tint, warning = amber/`fuelDie` tint, neutral = `surface2`/`ink2`, accent = `accent` tint. Use subtle tinted bg + matching text (mirror web Pill semantics). Used for the réel/≈ estimé toll badge.
    - `SectionCard`: props `{ title?: ReactNode; children: ReactNode; padding?: 'none'|'sm'|'md'|'lg'; style? }`. Wraps content on `surface` bg + `hairline` border + `card` radius; renders an optional title row (Eyebrow-styled) above children. Editorial equivalent of the web SectionCard.
    - `Eyebrow`: props `{ children: ReactNode; style? }`. Uppercase caption label — `fontSize: tokens.fontSize.caption`, letterSpacing ~1.6 (eye tracking), weight 700, color `muted`, fontFamily display.
    Named exports only (project convention). No serif.
  </action>
  <verify>
    <automated>cd mobile && npx tsc --noEmit 2>&1 | grep -E "Pill.tsx|SectionCard.tsx|Eyebrow.tsx" || echo "no errors in new atoms"</automated>
  </verify>
  <acceptance_criteria>
    - Pill supports success/warning variants (for the toll badge); SectionCard renders title + surface card; Eyebrow is an uppercase caption in the display font weight 700.
    - All three are token-driven (no hardcoded blue), named exports, tsc GREEN.
  </acceptance_criteria>
  <done>Editorial atoms exist with web-parity naming for Wave-3 screens.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Pure presentational primitives; no input, no network, no secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03-01 | Denial of Service | Animated counter rAF/listener leak | mitigate | Stop animation + remove `Animated.Value` listener on unmount/value change (no leaked frames), mirroring the web useCountUp cleanup |
</threat_model>

<verification>
- mobile `npx tsc --noEmit` GREEN across the 6 new files.
- useReducedMotion subscribes to AccessibilityInfo; AnimatedCounter goes instant when reduced.
- DataBar Variant A hides toll segment at 0; Variant B muted option works.
</verification>

<success_criteria>
RN data-viz + motion primitives + editorial atoms exist, token-driven, reduced-motion-aware, web-parity — ready for the dashboard tolls slice and screen restyles.
</success_criteria>

<output>
Create `.planning/phases/05-mobile-tolls-editorial-redesign/05-03-SUMMARY.md` when done.
</output>
