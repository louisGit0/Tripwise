---
phase: 06-polish-account
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - web/src/app/app/trips/result/page.tsx
  - web/src/app/app/trips/[id]/page.tsx
  - web/src/app/app/settings/page.tsx
autonomous: true
requirements: [POL-01, POL-02, POL-03]
must_haves:
  truths:
    - "No réel/≈ estimé toll badge appears on the web result page or the web trip detail; the toll amount stays in the breakdown"
    - "A user can share a calculated trip from the result page via navigator.share, with a clipboard fallback, using a clean toll-inclusive FR summary"
    - "A user can edit their pseudo in Settings; it persists via PATCH /users/me and updates immediately with a success toast"
  artifacts:
    - path: "web/src/app/app/trips/result/page.tsx"
      provides: "toll badge removed + share action"
      contains: "navigator.share"
    - path: "web/src/app/app/trips/[id]/page.tsx"
      provides: "toll badge removed on detail"
    - path: "web/src/app/app/settings/page.tsx"
      provides: "editable displayName field"
      contains: "/users/me"
  key_links:
    - from: "web/src/app/app/settings/page.tsx"
      to: "PATCH /users/me"
      via: "apiClient.patch"
      pattern: "apiClient\\.patch\\(['\"]/users/me"
    - from: "web/src/app/app/trips/result/page.tsx"
      to: "Web Share / Clipboard"
      via: "navigator.share with navigator.clipboard fallback"
      pattern: "navigator\\.(share|clipboard)"
---

<objective>
Ship the three web refinements: (POL-01) remove the redundant toll réel/≈ estimé badge from the
trip result page and the saved trip detail (keep the toll amount + keep `tollIsEstimate` in the data),
(POL-02) add a "Partager" action on the result page using `navigator.share` with a clipboard fallback
and a clean toll-inclusive FR summary, and (POL-03) turn the read-only pseudo in Settings into an
editable field that persists via `PATCH /users/me` (built in 06-01) and reflects immediately.

Purpose: Cleaner result UI + trip sharing + account editing on web.
Output: edited result, detail, and settings pages. Editorial-dark atoms/tokens reused; existing flows preserved (MD-2).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-polish-account/06-CONTEXT.md
@CLAUDE.md

<interfaces>
<!-- Exact identifiers the executor needs. -->

Result page (web/src/app/app/trips/result/page.tsx):
- Imports `Pill` (line ~12) and `Tooltip` (line ~13) used ONLY by the toll badge.
- `tollBadge` is built at ~lines 232–244 (Tooltip>Pill, driven by local `tollIsEstimate` at ~line 214).
- It is consumed in: the PÉAGES metrics tile (`badge: tollBadge`, ~line 257) and the Péage legend (`{tollBadge}`, ~line 339).
- `hasToll`, `tollCost`, `energyCost`, `totalCost`, the `DataBar`, the Péage legend line and the PÉAGES tile VALUE must all stay.
- `handleSave` (~lines 113–160) still sends `tollIsEstimate: session.result.tollIsEstimate` — DO NOT touch the save payload (keep the field flowing).
- CTA buttons block at ~lines 484–507 (Save + "Nouveau trajet" CTAButton). `useToast()` → `showToast`. `fmtEur` is an `Intl.NumberFormat('fr-FR', { style:'currency', currency:'EUR' })`. Route labels: `session.origin?.label`, `session.destination?.label`. `result.distance.km`, `result.duration.seconds` + local `formatDuration()`.

Detail page (web/src/app/app/trips/[id]/page.tsx):
- Imports `Pill` (~line 10) and `Tooltip` (~line 11) used ONLY by the toll badge.
- The Tooltip>Pill block is at ~lines 274–284 inside the Péage legend; the Péage amount line (`Péage · {fmtEur.format(trip.tollsCost)}`, ~line 273) and the énergie/péage `DataBar` must stay. `trip.tollIsEstimate` remains a field on the trip object (no local-var cleanup needed beyond the removed JSX).

Settings page (web/src/app/app/settings/page.tsx):
- Already fetches `apiClient.get<UserProfile>('/auth/me')` into `userProfile` state (~line 31).
- The Account SectionCard (~lines 93–112) currently renders `userProfile.displayName` read-only.
- `apiClient` from '@/lib/api' (axios; baseURL already includes /api/v1; JWT injected from cookie).
- `CTAButton` from '@/components/ui/CTAButton', `Input` from '@/components/ui/Input', `useToast` from '@/providers/ToastProvider'. `UserProfile` type has `displayName: string | null`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: POL-01 — remove the toll réel/≈ estimé badge on result + detail</name>
  <read_first>
    - web/src/app/app/trips/result/page.tsx (the tollBadge block + its two usages + the two imports)
    - web/src/app/app/trips/[id]/page.tsx (the Tooltip>Pill block in the Péage legend + the two imports)
  </read_first>
  <action>
    Result page: delete the `tollBadge` constant (the Tooltip>Pill block) and remove its two
    consumers — drop `badge: tollBadge` from the PÉAGES metrics tile entry (keep `label: 'PÉAGES'` and
    its `value`) and remove the `{tollBadge}` reference in the Péage legend (keep the dot + "Péage ·
    {amount}"). Remove the now-unused local `tollIsEstimate` declaration and the now-unused `Pill` and
    `Tooltip` imports. Keep `hasToll`, the DataBar, the metrics tile value, and the entire `handleSave`
    payload (including `tollIsEstimate`) untouched. The `badge?` field in the metrics type may stay or
    be dropped — if any `badge` rendering remains for other tiles keep the field; otherwise remove the
    unused `badge` plumbing so `next build` ESLint stays clean.
    Detail page: delete the Tooltip>Pill block inside the Péage legend (keep the dot + "Péage ·
    {fmtEur.format(trip.tollsCost)}" line and the DataBar). Remove the now-unused `Pill` and `Tooltip`
    imports. Do NOT remove `trip.tollIsEstimate` from any data path — it simply stops being rendered.
    Pill/Tooltip atoms remain in the codebase (other screens). No package changes.
  </action>
  <verification>
    <automated>cd web && npx tsc --noEmit && npm run build</automated>
  </verification>
  <acceptance_criteria>
    - `npx tsc --noEmit` clean and `npm run build` succeeds (no unused-import/var ESLint failures).
    - No "réel" / "≈ estimé" string and no `Pill`/`Tooltip` import remain in either file (grep both files → 0).
    - The PÉAGES amount + the Péage legend amount still render when a toll is present; the énergie/péage DataBar is unchanged.
  </acceptance_criteria>
  <done>The toll badge is gone from result + detail; the toll amount and breakdown remain.</done>
</task>

<task type="auto">
  <name>Task 2: POL-02 — add "Partager" on the result page (navigator.share + clipboard fallback)</name>
  <read_first>
    - web/src/app/app/trips/result/page.tsx (CTA buttons block, fmtEur, totalCost/energyCost/tollCost/hasToll, session.origin/destination labels, formatDuration, useToast)
  </read_first>
  <action>
    Add a `handleShare` async function and a "Partager" `CTAButton` (variant="ghost", size="lg",
    full width, lucide `Share2` icon) into the CTA buttons block (alongside "Nouveau trajet").
    Build a clean FR summary string from the already-computed values: a route line
    `${origin label} → ${destination label}` (use the part before the first comma, like the header),
    the toll-INCLUSIVE total via `fmtEur.format(totalCost)`, an energy + (conditional) péage breakdown
    line (`Énergie ${fmtEur.format(energyCost)}` + when `hasToll` ` · Péage ${fmtEur.format(tollCost)}`),
    a `${km} km · ${formatDuration(seconds)}` line, and a trailing "Calculé avec verygoodtrip" line.
    All currency via the existing `fmtEur` (FR comma). In handleShare: if `navigator.share` exists,
    call `await navigator.share({ title, text })` inside a try/catch that silently ignores an
    AbortError (user cancel); otherwise (or if share throws non-abort) fall back to
    `await navigator.clipboard.writeText(text)` and `showToast('success', 'Résumé copié dans le presse-papiers')`.
    Guard with a feature check so SSR/older browsers never crash. No i18n (FR hardcoded, per project).
    No new npm packages (Share2 is from the existing lucide-react; navigator APIs are browser-native).
    Do not alter Save/Nouveau-trajet behavior.
  </action>
  <verification>
    <automated>cd web && npx tsc --noEmit && npm run build</automated>
  </verification>
  <acceptance_criteria>
    - `npx tsc --noEmit` clean and `npm run build` succeeds.
    - The file contains `navigator.share` and a `navigator.clipboard` fallback; a "Partager" CTAButton renders in the CTA block.
    - The shared text includes the route, the toll-inclusive total (FR comma), the energy/péage breakdown, and distance · duration.
  </acceptance_criteria>
  <done>A user can share the result; the summary total includes the toll, FR-formatted.</done>
</task>

<task type="auto">
  <name>Task 3: POL-03 — editable pseudo in web Settings (PATCH /users/me)</name>
  <read_first>
    - web/src/app/app/settings/page.tsx (the Account SectionCard + userProfile state + apiClient + imports)
    - web/src/components/ui/Input.tsx (the Input atom API: label/error/etc.)
  </read_first>
  <action>
    In the Account SectionCard, replace the read-only displayName paragraph with an editable form:
    an `Input` (label "Pseudo") bound to a `displayName` state seeded from `userProfile.displayName ?? ''`
    once the profile loads (set it in the existing `/auth/me` effect), plus a "Enregistrer" `CTAButton`
    (variant="accent", size="md"). Keep the email line read-only below. On save: trim the value, guard
    against empty, set a saving flag, call `apiClient.patch<UserProfile>('/users/me', { displayName })`,
    then `setUserProfile(data)` and reseed the input from the response, and `showToast('success',
    'Pseudo mis à jour')`; on error `showToast('error', 'Une erreur est survenue')`. Import `Input`,
    add `useToast` (from '@/providers/ToastProvider'), keep the existing mount/hydration skeleton and
    the logout flow unchanged (MD-2). No new packages.
  </action>
  <verification>
    <automated>cd web && npx tsc --noEmit && npm run build</automated>
  </verification>
  <acceptance_criteria>
    - `npx tsc --noEmit` clean and `npm run build` succeeds.
    - The Settings Account section shows an editable pseudo Input + a save button; the file contains `apiClient.patch('/users/me'` (or patch<...>('/users/me')).
    - On save the local `userProfile` updates from the PATCH response and a success toast fires; logout + theme picker unchanged.
  </acceptance_criteria>
  <done>The pseudo is editable in web Settings and persists via PATCH /users/me, reflected immediately.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → backend | Settings PATCH carries the new pseudo to PATCH /users/me (validated server-side, 06-01) |
| app → OS share/clipboard | Result page hands a plain-text summary to the Web Share / Clipboard APIs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02-01 | Tampering | Settings pseudo edit | mitigate | Authorization + validation/length-bound enforced server-side by PATCH /users/me (06-01); the client only sends `{ displayName }`. The value is rendered as escaped React text — no HTML sink. |
| T-06-02-02 | Information disclosure | share summary | accept | The shared text contains only the user's own route/cost figures they explicitly chose to share; no token/PII beyond what is on screen. |
| T-06-02-03 | DoS/UX | navigator.share absent | mitigate | Feature-check + clipboard fallback + AbortError swallow so a missing API or user-cancel never throws an unhandled rejection. |
| T-06-02-SC | Tampering | npm installs | accept | No package installs (lucide-react + navigator APIs already available). |
</threat_model>

<verification>
- `cd web && npx tsc --noEmit` → 0 errors
- `cd web && npm run build` → all routes compile, 0 ESLint errors
- grep result + detail pages: 0 `Pill`/`Tooltip` import, 0 `réel`/`≈ estimé`
- grep result page: `navigator.share` present; settings page: `/users/me` present
</verification>

<success_criteria>
- No toll badge on web result/detail; toll amount + breakdown preserved.
- Result page shares a toll-inclusive FR summary via navigator.share with a clipboard fallback.
- Settings pseudo is editable and persists via PATCH /users/me, updating the UI immediately.
- Editorial-dark atoms/tokens reused; existing flows (save, logout, theme) preserved.
</success_criteria>

<output>
Create `.planning/phases/06-polish-account/06-02-SUMMARY.md` when done.
</output>
