---
phase: 06-polish-account
plan: 03
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - mobile/app/(tabs)/dashboard.tsx
  - mobile/app/(tabs)/settings.tsx
  - mobile/src/i18n/translations/fr.ts
  - mobile/src/i18n/translations/en.ts
autonomous: true
requirements: [POL-01, POL-02, POL-03]
must_haves:
  truths:
    - "No réel/≈ estimé toll badge appears on the mobile dashboard trip result; the toll amount stays in the Péage legend"
    - "Sharing a calculated trip on mobile produces a clean FR summary with the toll-inclusive total (FR comma), route, energy + toll breakdown, distance/duration"
    - "A user can edit their pseudo in mobile Settings; it persists via PATCH /users/me and shows a success toast"
  artifacts:
    - path: "mobile/app/(tabs)/dashboard.tsx"
      provides: "toll badge removed + enriched share summary"
    - path: "mobile/app/(tabs)/settings.tsx"
      provides: "editable displayName field"
      contains: "/users/me"
    - path: "mobile/src/i18n/translations/fr.ts"
      provides: "enriched shareText + pseudo strings (FR)"
    - path: "mobile/src/i18n/translations/en.ts"
      provides: "enriched shareText + pseudo strings (EN)"
  key_links:
    - from: "mobile/app/(tabs)/settings.tsx"
      to: "PATCH /users/me"
      via: "client.patch"
      pattern: "client\\.patch\\(['\"]/users/me"
    - from: "mobile/app/(tabs)/dashboard.tsx"
      to: "Share.share"
      via: "toll-inclusive FR message"
      pattern: "Share\\.share"
---

<objective>
Ship the three mobile refinements to match web: (POL-01) remove the toll réel/≈ estimé badge from the
dashboard `ResultCard` (keep the Péage amount + keep `tollIsEstimate` in the data), (POL-02) enrich the
existing `handleShare` so the shared FR summary includes the route + energy/toll breakdown alongside the
already toll-inclusive total (FR comma), and (POL-03) add an editable pseudo field in Settings that
persists via `PATCH /users/me` (built in 06-01). Editorial-dark tokens/atoms reused; all existing flows
preserved (MD-2). Files are disjoint from the web plan — safe to run in parallel.

Purpose: Cross-platform parity for the badge removal, share, and pseudo editing.
Output: edited dashboard + settings + fr/en i18n.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-polish-account/06-CONTEXT.md
@CLAUDE.md
@mobile/AGENTS.md

<interfaces>
<!-- Exact identifiers the executor needs. -->

dashboard.tsx:
- `Pill` import (~line 22) used ONLY by the toll badge.
- The `ResultCard` builds `tollBadge` (~lines 318–326): a `TouchableOpacity` (onPress → `Alert.alert(...)`)
  wrapping `<Pill color={tollIsEstimate?'warning':'success'} size="sm">{...}</Pill>`. Used once in the
  Péage legend (~line 362, `{tollBadge}`). Local `tollIsEstimate` is at ~line 315 (drives only the badge).
- KEEP: `hasToll`, `tollCost`, the `DataBar`, and the Péage legend amount line
  `{t('dashboard.tollLabel')} · {formatEur(tollCost)}` (~lines 359–360). KEEP `result.tollIsEstimate`
  flowing in the data (types unchanged).
- `handleShare` (~lines 97–111) already computes `energyCost + tollCost` and `formatEur(...)` and calls
  `Share.share({ message: t('dashboard.shareText', { distance, duration, cost }) })`. The screen has
  `origin`/`destination` state (used in handleSaveFavorite ~lines 119–124) and `result`. `formatEur`
  helper is already in the file.
- Existing i18n keys available: `dashboard.energyLabel`, `dashboard.tollLabel`, `dashboard.share`,
  `dashboard.shareText`, `dashboard.distance`, `dashboard.duration`.

settings.tsx:
- Imports `Colors, Fonts, FontSize, FontSizes, Spacing` from '@/constants/theme', `SectionCard`,
  `Eyebrow`, `Button` (ui), `useAuth`. Sections use `SectionCard title={...}`; toggles use the
  accent/surface2 pattern; logout uses the destructive `Button`.
- The api client is `mobile/src/api/client.ts` default export (`client`) — axios, JWT auto-injected,
  baseURL includes /api/v1. The Input atom is at `mobile/src/components/ui/Input.tsx`.
- AuthContext exposes only { token, isLoading, signIn, signOut } — it does NOT hold the profile, so the
  current displayName must be fetched via `GET /auth/me` on mount.

GET /auth/me / PATCH /users/me return shape: { id, email, displayName, locale, provider, createdAt }.
Toast: `import Toast from 'react-native-toast-message'` → `Toast.show({ type, text1 })` (pattern already in dashboard.tsx).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: POL-01 — remove the toll badge from the dashboard ResultCard</name>
  <read_first>
    - mobile/app/(tabs)/dashboard.tsx (the ResultCard tollBadge block, its legend usage, the Pill import, Alert usage)
  </read_first>
  <action>
    In `ResultCard`, delete the `tollBadge` constant (the TouchableOpacity>Pill block) and remove its
    single usage `{tollBadge}` from the Péage legend (keep the dot + `{t('dashboard.tollLabel')} ·
    {formatEur(tollCost)}` line, the `DataBar`, and the `hasToll` guard). Remove the now-unused local
    `tollIsEstimate` declaration. Remove the `Pill` import. If `Alert` and `TouchableOpacity` are no
    longer referenced anywhere else in the file (grep to confirm), remove those imports too; if they
    are still used elsewhere, leave them. Do NOT change the data layer: `result.tollIsEstimate` stays in
    types and continues to flow. The `Pill` atom remains in the codebase (other screens use it).
  </action>
  <verification>
    <automated>cd mobile && npx tsc --noEmit</automated>
  </verification>
  <acceptance_criteria>
    - `npx tsc --noEmit` is GREEN (0 errors).
    - grep dashboard.tsx → 0 `Pill` import, 0 `tollEstimate`/`tollReal` badge usage; the Péage legend amount line remains.
    - The énergie/péage DataBar and the hide-when-toll-0 guard are unchanged.
  </acceptance_criteria>
  <done>The toll badge is gone from the mobile result; the Péage amount + breakdown remain.</done>
</task>

<task type="auto">
  <name>Task 2: POL-02 — enrich the share summary (route + energy/toll breakdown, toll-inclusive FR total)</name>
  <read_first>
    - mobile/app/(tabs)/dashboard.tsx (handleShare, origin/destination state, formatEur, result)
    - mobile/src/i18n/translations/fr.ts (dashboard.shareText + energyLabel/tollLabel)
    - mobile/src/i18n/translations/en.ts (same keys, EN)
  </read_first>
  <action>
    Update `dashboard.shareText` in BOTH fr.ts and en.ts to a clean multi-line template that
    interpolates `{{route}}`, `{{cost}}`, `{{breakdown}}`, `{{distance}}`, `{{duration}}` and ends with
    a "Calculé avec verygoodtrip" / "Calculated with verygoodtrip" line (keep fr/en symmetric). In
    `handleShare`, compute: `route = `${origin?.label} → ${destination?.label}`` (fall back to '—' when
    missing); `total = (result.cost?.totalCost ?? 0) + (result.tollCost ?? 0)` and `cost =
    formatEur(total)` (toll-inclusive, FR comma — preserved behavior); a `breakdown` string built from
    `t('dashboard.energyLabel')` + `formatEur(energyCost)`, and — only when `result.tollCost != null &&
    result.tollCost > 0` — append ` · ` + `t('dashboard.tollLabel')` + `formatEur(tollCost)`;
    `distance = `${result.distance.km} km`` and `duration = result.duration.formatted`. Pass all five
    into `Share.share({ message: t('dashboard.shareText', { route, cost, breakdown, distance, duration }) })`.
    Do not add a separate share button (the ghost "Partager" Button already exists in ResultCard).
    No new npm packages (Share is from react-native, already imported).
  </action>
  <verification>
    <automated>cd mobile && npx tsc --noEmit</automated>
  </verification>
  <acceptance_criteria>
    - `npx tsc --noEmit` GREEN.
    - `dashboard.shareText` in fr.ts AND en.ts contains the new interpolation tokens (route/cost/breakdown/distance/duration), symmetric across both files.
    - handleShare composes a toll-inclusive `cost` (FR comma) and a route + energy/toll breakdown; `Share.share` is called with the enriched message.
  </acceptance_criteria>
  <done>The mobile share summary includes route + breakdown + the toll-inclusive FR total.</done>
</task>

<task type="auto">
  <name>Task 3: POL-03 — editable pseudo in mobile Settings (PATCH /users/me)</name>
  <read_first>
    - mobile/app/(tabs)/settings.tsx (SectionCard usage, imports, Button, styles)
    - mobile/src/components/ui/Input.tsx (Input atom API)
    - mobile/src/api/client.ts (default export `client`, JWT auto-injected)
    - mobile/src/i18n/translations/fr.ts + en.ts (where to add the pseudo strings)
  </read_first>
  <action>
    Add a new "Account/Pseudo" `SectionCard` (place it near the existing account section) containing an
    `Input` bound to a `displayName` state + a "Enregistrer" `Button` (variant default/accent). On mount,
    `client.get('/auth/me')` and seed `displayName` from `data.displayName ?? ''` (use a useEffect; guard
    against unmounted set). On save: trim, guard empty, set a saving flag, `client.patch('/users/me',
    { displayName })`, then reseed from the response and `Toast.show({ type: 'success', text1:
    t('settings.pseudoSaved') })`; on error `Toast.show({ type: 'error', text1: t('common.error') })`.
    Add i18n keys to BOTH fr.ts and en.ts under `settings`: `pseudo` (label, FR "Pseudo"/EN "Display name"),
    `pseudoSave` (FR "Enregistrer"/EN "Save"), `pseudoSaved` (FR "Pseudo mis à jour"/EN "Display name updated").
    Import `Input`, `client` (default import), and `Toast` (react-native-toast-message). Keep the theme,
    language, logout sections and `version` line unchanged (MD-2). Reuse editorial tokens (no blue
    literals, weights 400/700, no serif). No new npm packages.
  </action>
  <verification>
    <automated>cd mobile && npx tsc --noEmit</automated>
  </verification>
  <acceptance_criteria>
    - `npx tsc --noEmit` GREEN.
    - settings.tsx contains an editable pseudo Input + save Button and calls `client.patch('/users/me'`; it fetches `/auth/me` on mount to seed the value.
    - `settings.pseudo`/`pseudoSave`/`pseudoSaved` exist in both fr.ts and en.ts (symmetric); a success toast fires on save.
    - Theme/language/logout/version sections unchanged.
  </acceptance_criteria>
  <done>The pseudo is editable in mobile Settings and persists via PATCH /users/me with a success toast.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app → backend | Settings PATCH carries the new pseudo to PATCH /users/me (validated server-side, 06-01) |
| app → OS share sheet | Dashboard hands a plain-text summary to the native Share sheet |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03-01 | Tampering | Settings pseudo edit | mitigate | Authorization + trim/length-bound enforced server-side by PATCH /users/me (06-01); client sends only `{ displayName }`. The value renders as RN `<Text>` (no HTML/markup sink). |
| T-06-03-02 | Information disclosure | share message | accept | The shared text contains only the user's own route/cost they chose to share; no token/PII beyond what is on screen. |
| T-06-03-03 | Spoofing | /auth/me + /users/me calls | mitigate | The api client auto-injects the JWT; a 401 triggers token deletion + re-route (existing interceptor). |
| T-06-03-SC | Tampering | npm installs | accept | No package installs (Share, axios client, Input, Toast already present). |
</threat_model>

<verification>
- `cd mobile && npx tsc --noEmit` → 0 errors
- grep dashboard.tsx: 0 `Pill` import; `Share.share` present; Péage legend amount line intact
- grep fr.ts + en.ts: enriched `shareText` tokens + `settings.pseudo`/`pseudoSave`/`pseudoSaved` in both, symmetric
- grep settings.tsx: `client.patch('/users/me'` present; `/auth/me` fetched on mount
</verification>

<success_criteria>
- No toll badge on the mobile dashboard result; the Péage amount + breakdown remain.
- The mobile share summary includes route + energy/toll breakdown + the toll-inclusive FR total.
- The pseudo is editable in mobile Settings and persists via PATCH /users/me, with a success toast.
- Editorial-dark tokens/atoms reused; theme/language/logout/calc/share/favorite flows preserved (MD-2).
</success_criteria>

<output>
Create `.planning/phases/06-polish-account/06-03-SUMMARY.md` when done.
</output>
