---
phase: 03-web-redesign-rollout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/components/ui/CTAButton.tsx
  - web/src/components/ui/SectionCard.tsx
  - web/src/components/ui/Modal.tsx
  - web/src/components/ui/Input.tsx
  - web/src/components/ui/Select.tsx
  - web/src/components/ui/Eyebrow.tsx
  - web/src/components/ui/KPICell.tsx
  - web/src/components/ui/FuelBadge.tsx
  - web/src/components/ui/Pill.tsx
  - web/src/components/ui/NumberDisplay.tsx
  - web/src/components/ui/SegmentedControl.tsx
  - web/src/components/ui/Wordmark.tsx
  - web/src/components/ui/BrandAvatar.tsx
  - web/src/components/layouts/AppLayout.tsx
  - web/src/hooks/useReducedMotion.ts
  - web/src/providers/Providers.tsx
autonomous: true
requirements: [WEB-01, WEB-04]
must_haves:
  truths:
    - "Every shared UI atom uses exactly the 2-weight system (400/700) — no font-medium/font-semibold/font-extrabold remains in web/src/components/ui/ or AppLayout.tsx"
    - "Every interactive atom (CTAButton, Input, Select) uses the single canonical focus-visible accent-ring token — no ring-blue-500/* and no focus:ring-offset-0 remain in the atoms"
    - "The AppLayout shell '+ Nouveau trajet' CTA is weight 700 and its burger/collapse/CTA controls use the canonical focus ring"
    - "Screens importing these atoms inherit the corrected weights, focus, and colors with no per-screen change required"
  artifacts:
    - path: "web/src/components/ui/CTAButton.tsx"
      provides: "Primary button atom: font-bold (700) label, canonical accent focus ring on all variants"
      contains: "ring-carbon-accent"
    - path: "web/src/components/layouts/AppLayout.tsx"
      provides: "Restyled shell: 700-weight inline CTA + canonical focus on burger/collapse/CTA"
      contains: "font-bold"
  key_links:
    - from: "web/src/components/ui/CTAButton.tsx"
      to: "the 2-weight typography contract"
      via: "font-medium → font-bold; ring-blue-500/* → ring-carbon-accent/50"
      pattern: "font-bold"
    - from: "web/src/components/ui/Input.tsx"
      to: "the canonical focus token (result/page.tsx:33)"
      via: "focus:ring-carbon-accent focus:ring-offset-0 → focus-visible:ring-carbon-accent/50 ring-offset-2 ring-offset-carbon-bg"
      pattern: "focus-visible:ring-carbon-accent"
---

<objective>
Normalize the shared UI atoms and the AppLayout shell to the locked editorial-dark contract BEFORE
any screen is restyled (PD3-1, atoms-first). This is the BLOCKING foundation: every Phase 3 screen
imports these atoms, so correcting weights (→ 400/700), focus rings (→ the single canonical accent
ring), and the one color-correctness bug here once guarantees consistency (Success Criterion 3) and
clears the Phase 2 review debt IN-04/IN-05. Visual layer only (MD-2): no API surface change, no prop
renames, no behavior change.

Purpose: One DRY normalization so screens inherit a correct, consistent component vocabulary.
Output: Normalized `web/src/components/ui/*` atoms + restyled `AppLayout.tsx` shell (+ an optional,
discretionary `useReducedMotion` provider hoist).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-web-redesign-rollout/03-UI-SPEC.md
@.planning/phases/03-web-redesign-rollout/03-CONTEXT.md

<interfaces>
<!-- The canonical focus-visible token (already shipped at result/page.tsx:33) — apply VERBATIM. -->
focus:outline-none focus-visible:ring-2 focus-visible:ring-carbon-accent/50 focus-visible:ring-offset-2 focus-visible:ring-offset-carbon-bg

<!-- Accent value: --c-accent = #4d8bff. ring-blue-500 = #3b82f6 (WRONG accent — the bug to fix). -->
<!-- Weight rule (PD3-1, D-11 system): font-medium(500)/font-semibold(600)/font-extrabold(800) are FORBIDDEN.
     Map: body/labels/segments/units/deltas → font-normal (400); titles/eyebrows/badges/CTAs/emphasis → font-bold (700). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Normalize all ui/ atoms — weights to 400/700, focus to canonical accent ring, accent-color fix</name>
  <files>web/src/components/ui/CTAButton.tsx, web/src/components/ui/SectionCard.tsx, web/src/components/ui/Modal.tsx, web/src/components/ui/Input.tsx, web/src/components/ui/Select.tsx, web/src/components/ui/Eyebrow.tsx, web/src/components/ui/KPICell.tsx, web/src/components/ui/FuelBadge.tsx, web/src/components/ui/Pill.tsx, web/src/components/ui/NumberDisplay.tsx, web/src/components/ui/SegmentedControl.tsx, web/src/components/ui/Wordmark.tsx, web/src/components/ui/BrandAvatar.tsx</files>
  <read_first>
    - .planning/phases/03-web-redesign-rollout/03-UI-SPEC.md (section "Atom Normalization" — the per-atom diff table is the authoritative contract for this task; also "Interaction States" for the focus token and "Color" for the accent-ring fix)
    - web/src/components/ui/CTAButton.tsx (font-medium l.54; ring-blue-500/50 l.20; ring-blue-500/30 l.22,24)
    - web/src/components/ui/SectionCard.tsx (h2 font-semibold l.42)
    - web/src/components/ui/Input.tsx (label font-semibold l.17; focus:ring-2 focus:ring-carbon-accent focus:ring-offset-0 l.24)
    - web/src/components/ui/Select.tsx, web/src/components/ui/Modal.tsx, web/src/components/ui/Eyebrow.tsx, web/src/components/ui/KPICell.tsx, web/src/components/ui/FuelBadge.tsx, web/src/components/ui/Pill.tsx, web/src/components/ui/NumberDisplay.tsx, web/src/components/ui/SegmentedControl.tsx, web/src/components/ui/Wordmark.tsx, web/src/components/ui/BrandAvatar.tsx (apply each row of the UI-SPEC Atom Normalization table)
  </read_first>
  <action>
    Apply the UI-SPEC "Atom Normalization" table exactly, file by file. Do NOT change any prop, type,
    export name, or behavior — visual className tokens only (MD-2).
    CTAButton: change the shared `font-medium` (l.54) to `font-bold` (700). Replace every focus ring in
    `variantClasses` — `focus-visible:ring-blue-500/50` (accent), `focus-visible:ring-blue-500/30`
    (surface, ghost) — with the canonical accent ring `focus-visible:ring-carbon-accent/50`; the
    `danger` variant keeps its red ring `focus-visible:ring-red-500/40`. (The shared className already
    carries `focus:outline-none focus-visible:ring-2 focus-visible:ring-offset-2
    focus-visible:ring-offset-carbon-bg` — keep that; only the per-variant ring color changes.) Update the
    stale `// accent colour ≈ blue-500` comment to reflect `--c-accent #4d8bff`.
    SectionCard: the `<h2>` title (l.42) `font-semibold` → `font-bold`; keep its `text-sm` size (it is a
    card-label tier, not a page title).
    Modal: the `<h2>` (l.36) `font-semibold` → `font-bold`.
    Input: label (l.17) `font-semibold` → `font-bold`; replace `focus:ring-2 focus:ring-carbon-accent
    focus:ring-offset-0` (l.24) with the canonical token `focus-visible:ring-2
    focus-visible:ring-carbon-accent/50 focus-visible:ring-offset-2 focus-visible:ring-offset-carbon-bg`
    plus `focus:outline-none`; keep the error-state `focus:ring-red-400` branch and the
    `focus:border-carbon-accent` border behavior.
    Select: identical changes to Input — label `font-semibold` → `font-bold`; same focus-token swap.
    Eyebrow: `font-semibold` → `font-bold`.
    KPICell: label `font-semibold` (l.25) → `font-bold`; unit `font-medium` (l.31) → `font-normal`; delta
    `font-medium` (l.36) → `font-normal`. The value sizes already use `font-bold` — keep them.
    FuelBadge: `font-semibold` → `font-bold`. Keep the existing semantic energy color classes.
    Pill: `font-medium` → `font-bold` (badge emphasis). Keep the existing status color classes.
    NumberDisplay: `font-medium` (l.49) → `font-normal` (the 400 default; hero/large callers add
    `font-bold` themselves at the call site).
    SegmentedControl: `font-medium` (l.50) → `font-normal`; verify the active state uses color/background
    only (no weight change between active/inactive) so there is no sub-pixel reflow on toggle (CWV).
    Wordmark: the sm/md `font-semibold` → `font-bold`; the lg size already `font-bold` — keep.
    BrandAvatar: `font-semibold` → `font-bold`.
    After editing, grep web/src/components/ui/ to confirm zero `font-medium`, `font-semibold`,
    `font-extrabold`, zero `ring-blue-500`, and zero `focus:ring-offset-0` remain. The atoms listed as
    "already compliant" in the UI-SPEC (DataBar, Skeleton, Sparkline, Tooltip, StatusDot, Hairline,
    Card, TWAppIcon, CommandBar) must NOT be edited — only verify the grep stays clean for them.
  </action>
  <verify>
    <automated>cd web; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `cd web; npx tsc --noEmit` exits 0.
    - Grep over web/src/components/ui/ (excluding comment lines, e.g. `grep -rn 'font-medium\|font-semibold\|font-extrabold' web/src/components/ui/ | grep -v '^\s*//'`) returns ZERO matches.
    - Grep `ring-blue-500` over web/src/components/ui/ returns ZERO matches; grep `focus:ring-offset-0` returns ZERO matches.
    - CTAButton's accent/surface/ghost variants use `ring-carbon-accent/50`; the danger variant keeps `ring-red-500/40`.
    - Input and Select labels are `font-bold` and both use the canonical `focus-visible:ring-carbon-accent/50 ... ring-offset-carbon-bg` token (no `focus:ring-offset-0`).
    - No prop/type/export name changed in any atom (API-stable — diff is className strings + one comment only).
  </acceptance_criteria>
  <done>All shared atoms conform to the 2-weight + canonical-focus + correct-accent contract, ready for screens to inherit.</done>
</task>

<task type="auto">
  <name>Task 2: Restyle the AppLayout shell + (discretionary) useReducedMotion provider hoist</name>
  <files>web/src/components/layouts/AppLayout.tsx, web/src/hooks/useReducedMotion.ts, web/src/providers/Providers.tsx</files>
  <read_first>
    - .planning/phases/03-web-redesign-rollout/03-UI-SPEC.md (section "3. AppLayout shell"; "Atom Normalization" AppLayout row; "IN-05 (useReducedMotion hoist) — DISCRETIONARY")
    - web/src/components/layouts/AppLayout.tsx (the "+ Nouveau trajet" CTA font-semibold l.213; burger l.200-207; collapse toggle l.153-168; SidebarItem l.312-345)
    - web/src/hooks/useReducedMotion.ts (current per-component matchMedia hook — only if implementing the optional provider)
    - web/src/providers/Providers.tsx (existing ThemeProvider + ToastProvider wrapper — the wrap point if implementing the optional provider)
  </read_first>
  <action>
    AppLayout (required): on the inline "+ Nouveau trajet" CTA (l.210-217), change `font-semibold` → `font-bold`.
    Standardize focus on the three shell controls — the "+ Nouveau trajet" button (l.210), the mobile
    burger (l.200), and the collapse toggle (l.153) — by adding the canonical ring
    `focus:outline-none focus-visible:ring-2 focus-visible:ring-carbon-accent/50 focus-visible:ring-offset-2 focus-visible:ring-offset-carbon-bg`
    to each (the drawer close "X" button at l.241 too, for consistency). Do NOT change the sidebar layout,
    widths, breadcrumb mono styling, StatusDot, or the active SidebarItem's `bg-blue-500/10
    text-carbon-accent` tint (sanctioned accent tint — keep it). Verify no `font-semibold`/`font-medium`
    remains anywhere in AppLayout.tsx after the edit. Keep all existing `aria-label`s on the burger,
    collapse, and close buttons (already present) intact.
    useReducedMotion provider (DISCRETIONARY — implement ONLY as a clean, zero-behavior-change refactor;
    if it adds any risk or scope, SKIP and record it as deferred in the SUMMARY): create a single
    `ReducedMotionProvider` + React context backing the existing `useReducedMotion()` hook so the hook's
    public signature (`() => boolean`) stays IDENTICAL for all current callers (result page, DataBar,
    Skeleton) — no caller edits required — and wrap it inside `web/src/providers/Providers.tsx` alongside
    ThemeProvider/ToastProvider. The hook must keep working SSR-safe (return false on the server). If you
    skip this, leave useReducedMotion.ts and Providers.tsx untouched and note "IN-05 provider hoist
    deferred (per-component hook retained, works correctly)" in the SUMMARY.
  </action>
  <verify>
    <automated>cd web; npx tsc --noEmit; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - The AppLayout "+ Nouveau trajet" CTA is `font-bold`; grep `font-semibold\|font-medium` over AppLayout.tsx returns ZERO matches.
    - The "+ Nouveau trajet", burger, collapse, and drawer-close controls each carry the canonical `focus-visible:ring-carbon-accent/50 ... ring-offset-carbon-bg` token.
    - The active SidebarItem still uses `bg-blue-500/10 text-carbon-accent` (unchanged); sidebar widths/layout unchanged.
    - If the provider was implemented: `useReducedMotion()` keeps its `() => boolean` signature, no existing caller was edited, and it is wrapped in Providers.tsx; otherwise the SUMMARY records IN-05 as deferred.
    - `cd web; npm run build` compiles all routes with 0 errors and 0 ESLint errors.
  </acceptance_criteria>
  <done>The shared shell is normalized to the contract and motion access is (optionally) consolidated, with no layout or behavior change.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Pure presentational normalization of existing components; no new input, endpoint, secret, or dependency crosses any boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | ui/ atoms + AppLayout | accept | className-only edits; no prop/behavior/markup-injection change; no `dangerouslySetInnerHTML` introduced. |
| T-03-02 | Information Disclosure | shell render | accept | No new data surfaced; the shell renders the same nav/status it does today. |
| T-03-SC | Tampering | npm/pip/cargo installs | n/a | No package installs — every atom, hook, and icon (lucide-react) already exists; restyle only. |
</threat_model>

<verification>
- `cd web; npx tsc --noEmit` → 0 errors.
- `cd web; npm run build` → all routes compile, 0 ESLint errors.
- Grep web/src/components/ui/ + AppLayout.tsx (excluding comment lines): NO `font-medium`/`font-semibold`/`font-extrabold`; NO `ring-blue-500`; NO `focus:ring-offset-0`.
- CTAButton accent/surface/ghost rings = `ring-carbon-accent/50`; danger = `ring-red-500/40`.
- API stability: no atom prop/type/export renamed (downstream screens compile unchanged).
</verification>

<success_criteria>
All shared atoms and the AppLayout shell conform to the locked 2-weight (400/700) typography, the
single canonical accent focus ring, and the corrected accent color — API-stable, green tsc/build —
so every Phase 3 screen inherits one consistent component vocabulary.
</success_criteria>

<output>
Create `.planning/phases/03-web-redesign-rollout/03-01-SUMMARY.md` when done.
</output>
