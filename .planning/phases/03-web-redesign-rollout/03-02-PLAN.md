---
phase: 03-web-redesign-rollout
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - web/src/app/page.tsx
  - web/src/app/login/page.tsx
  - web/src/app/register/page.tsx
  - web/src/app/auth/callback/google/page.tsx
  - web/src/app/auth/callback/apple/page.tsx
autonomous: true
requirements: [WEB-01, WEB-04]
must_haves:
  truths:
    - "The landing page headline is Space Grotesk text-display 700 (NO serif, no font-extrabold) and its hero/header CTAs are CTAButton instances with designed states + canonical focus"
    - "Login and register cards use normalized Input atoms (700 labels, canonical focus) and an accent full-width CTAButton; OAuth buttons read as quiet 400-weight options with canonical focus"
    - "The Google and Apple OAuth callback screens render a spinner + muted status copy on the editorial-dark background with no font-medium/font-semibold"
    - "All existing flows are preserved: register/login submit, zod refine on confirmPassword, OAuth redirect handoff (MD-2)"
  artifacts:
    - path: "web/src/app/page.tsx"
      provides: "Restyled editorial-dark landing — text-display 700 headline, CTAButton hero + header CTAs, SectionCard feature grid"
      contains: "text-display"
    - path: "web/src/app/login/page.tsx"
      provides: "Restyled login card with normalized Input + accent CTAButton + canonical-focus OAuth options"
      contains: "CTAButton"
  key_links:
    - from: "web/src/app/page.tsx"
      to: "web/src/components/ui/CTAButton.tsx"
      via: "hand-rolled hero/header <Link> buttons replaced with CTAButton"
      pattern: "CTAButton"
    - from: "web/src/app/login/page.tsx"
      to: "the normalized Input + canonical focus from 03-01"
      via: "form fields inherit corrected label weight + focus ring"
      pattern: "Input"
---

<objective>
Restyle the public surfaces — landing `/` and the auth screens (`/login`, `/register`,
`/auth/callback/{google,apple}`) — into the editorial-dark language, inheriting the atoms normalized
in 03-01. Visual layer only (MD-2): every existing flow (registration, login, zod validation, OAuth
redirect handoff) is preserved byte-for-byte. NO serif (D-11): titles are Space Grotesk bold 700.

Purpose: First impression + auth must feel like the same premium product as the app shell.
Output: Restyled `page.tsx` (landing), `login/page.tsx`, `register/page.tsx`, and the two callback pages.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-web-redesign-rollout/03-UI-SPEC.md
@.planning/phases/03-web-redesign-rollout/03-CONTEXT.md

<interfaces>
<!-- Normalized atoms from 03-01 — import directly, no exploration. -->
import { CTAButton } from '@/components/ui/CTAButton';   // variant accent|surface|ghost|danger, size sm|md|lg, loading, icon
import { Input } from '@/components/ui/Input';           // label (now 700) + canonical focus
import { SectionCard } from '@/components/ui/SectionCard';
import { Eyebrow } from '@/components/ui/Eyebrow';
import { Pill } from '@/components/ui/Pill';
import { Hairline } from '@/components/ui/Hairline';
import { TWAppIcon } from '@/components/ui/TWAppIcon';
import { Wordmark } from '@/components/ui/Wordmark';
<!-- Canonical focus token (for any remaining hand-rolled <a>/<button>): -->
focus:outline-none focus-visible:ring-2 focus-visible:ring-carbon-accent/50 focus-visible:ring-offset-2 focus-visible:ring-offset-carbon-bg
<!-- D-11: titles = Space Grotesk 700 (text-display). NEVER font-serif. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Landing page restyle</name>
  <files>web/src/app/page.tsx</files>
  <read_first>
    - web/src/app/page.tsx (the current landing — header, hero h1/lede/CTAs, feature grid, footer; the FEATURES constant and all copy to PRESERVE)
    - .planning/phases/03-web-redesign-rollout/03-UI-SPEC.md (section "Per-Screen Application → 1. Landing"; "Typography"; "Color")
    - web/src/components/ui/CTAButton.tsx + web/src/components/ui/SectionCard.tsx + web/src/components/ui/Pill.tsx + web/src/components/ui/Eyebrow.tsx (normalized in 03-01)
  </read_first>
  <action>
    Restyle visual layer only — keep all copy and the FEATURES data. Header: TWAppIcon + Wordmark on the
    left; replace the "Connexion" link and the "Créer un compte" link-button with `CTAButton` (login →
    `variant="ghost"` or a plain accent link styled with canonical focus; "Créer un compte" →
    `variant="accent" size="md"`), so both gain designed hover/focus/press states. Hero: change the `h1`
    from raw `text-4xl/5xl font-extrabold` to `text-display` weight 700 — drop `font-extrabold` entirely
    (it inherits 700 from the globals h1 rule; do NOT add any serif class, D-11). Keep the badge `Pill`
    row, the lede paragraph, the feature `Pill`s, and the two hero CTAs — convert the two hand-rolled
    hero `<Link>` "buttons" to `CTAButton` (`accent` for "Commencer gratuitement"/primary, `surface` for
    the secondary). Keep the existing `shadow-lg shadow-blue-500/20` accent glow on the primary CTA
    wrapper (sanctioned accent tint). Feature grid: render each feature in a `SectionCard`; change the
    feature `<h3>` from `font-semibold` to `font-bold` (700) and body copy to `text-sm` (400). Footer:
    `Hairline` + the version/meta line in `text-caption` muted. Replace every `font-medium`/`font-semibold`/
    `font-extrabold` in the file with 400 or 700 per the typography rule. Apply the canonical focus token
    to any link that still renders as a hand-rolled button.
  </action>
  <verify>
    <automated>cd web; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - The landing `h1` uses `text-display` and weight 700 with NO `font-extrabold` and NO `font-serif` (grep both → 0 in page.tsx).
    - The header "Créer un compte" and both hero CTAs are `CTAButton` instances (grep `CTAButton` ≥ 3 in page.tsx).
    - Feature cards use `SectionCard`; feature `<h3>` is `font-bold`.
    - Grep `font-medium\|font-semibold\|font-extrabold` over page.tsx (excluding comments) → 0.
    - All original landing copy/strings preserved (no text removed/changed — MD-2).
    - `cd web; npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>The landing page wears the editorial-dark language with consistent CTAs and a Space Grotesk 700 headline.</done>
</task>

<task type="auto">
  <name>Task 2: Auth screens restyle — login, register, OAuth callbacks</name>
  <files>web/src/app/login/page.tsx, web/src/app/register/page.tsx, web/src/app/auth/callback/google/page.tsx, web/src/app/auth/callback/apple/page.tsx</files>
  <read_first>
    - web/src/app/login/page.tsx + web/src/app/register/page.tsx (the form logic, react-hook-form + zod schema incl. the confirmPassword `.refine`, submit handlers, OAuth `<a>` buttons, cross-links — ALL flow logic to PRESERVE)
    - web/src/app/auth/callback/google/page.tsx + web/src/app/auth/callback/apple/page.tsx (the Suspense-wrapped *Content subcomponent + spinner/status copy)
    - .planning/phases/03-web-redesign-rollout/03-UI-SPEC.md (section "Per-Screen Application → 2. Auth")
    - web/src/components/ui/Input.tsx + web/src/components/ui/CTAButton.tsx + web/src/components/ui/Hairline.tsx (normalized in 03-01)
  </read_first>
  <action>
    Restyle visual layer only — do NOT touch the zod schemas, react-hook-form wiring, submit handlers,
    OAuth redirect URLs, the confirmPassword `.refine`, or the Suspense subcomponent structure (MD-2).
    Login + Register: centered column → logo block (TWAppIcon + Wordmark + muted subtitle) → a
    `SectionCard` (or bordered card) holding the `Input` fields (they now inherit the 700 label + canonical
    focus from 03-01 — no per-field focus class needed) → a full-width accent `CTAButton`
    (`variant="accent" size="lg" className="w-full"`, wire its `loading` prop to the existing submit-pending
    state so the spinner shows during submit) → a `Hairline` "ou" divider → the Google/Apple OAuth `<a>`
    buttons styled as quiet options: change their `font-medium` to `font-normal` (400), keep their Google/
    Apple icon SVGs, and add the canonical focus ring → footer cross-link with `font-medium` → `font-normal`.
    Register keeps its `confirmPassword` `Input` and the zod `.refine` mismatch error rendered via the
    Input `error` prop. Preserve the login error copy "Email ou mot de passe incorrect" surfaced on a
    failed submit (user stays to retry).
    Callbacks (google + apple): the spinner + status copy already render on `--c-bg` with `text-carbon-muted`/
    `text-carbon-accent` — verify weights are 400/700 only (change any `font-medium`/`font-semibold` to
    400/700) and that the loader uses a transform/opacity spin (lucide `LoaderCircle animate-spin` is fine).
    Do not alter the token read / cookie POST / redirect logic.
    Across all four files, ensure zero `font-medium`/`font-semibold`/`font-extrabold` and zero
    `ring-blue-500`/`focus:ring-offset-0` remain.
  </action>
  <verify>
    <automated>cd web; npx tsc --noEmit; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Login + register render their fields via the `Input` atom and submit via a full-width accent `CTAButton` whose `loading` reflects submit-pending state.
    - The register confirmPassword field + zod `.refine` mismatch behavior is unchanged (flow preserved).
    - OAuth `<a>` buttons are `font-normal` (400) with the canonical focus ring; cross-links are `font-normal`.
    - Both callback pages keep their Suspense subcomponent + token/redirect logic; spinner uses opacity/transform animation only.
    - Grep `font-medium\|font-semibold\|font-extrabold` and `ring-blue-500\|focus:ring-offset-0` across the four files (excluding comments) → 0.
    - `cd web; npm run build` compiles all routes with 0 errors and 0 ESLint errors.
  </acceptance_criteria>
  <done>Auth screens are restyled to the editorial-dark language with preserved auth flows and consistent interaction states.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| auth form → backend | Existing login/register/OAuth flow — UNCHANGED here (presentation-only restyle, MD-2) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Tampering | login/register forms | accept | No change to zod schema, RHF wiring, submit handler, or OAuth redirect — visual className/atom swaps only; validation integrity unchanged. |
| T-03-04 | Information Disclosure | OAuth callbacks | accept | Token read / cookie POST / redirect logic untouched; no token value rendered or logged; copy stays generic. |
| T-03-05 | Spoofing | auth UI | accept | Auth logic (credential check, OAuth handoff) is server-side and unchanged; this phase cannot weaken it (no logic touched). |
| T-03-SC | Tampering | npm/pip/cargo installs | n/a | No package installs — all atoms/icons already exist. |
</threat_model>

<verification>
- `cd web; npx tsc --noEmit` → 0 errors; `cd web; npm run build` → all routes compile, 0 ESLint errors.
- Grep across the 5 files (excluding comments): NO `font-medium`/`font-semibold`/`font-extrabold`, NO `font-serif`, NO `ring-blue-500`, NO `focus:ring-offset-0`.
- Landing `h1` = `text-display` 700; ≥3 `CTAButton` on landing; login/register submit via full-width accent `CTAButton`.
- Manual observable (folded into 03-07 checkpoint): landing + auth read as the same product; OAuth/login/register flows still work end-to-end.
</verification>

<success_criteria>
Landing and all auth screens are restyled in the editorial-dark language with NO serif and the
2-weight system, CTAs/inputs/focus consistent with the rest of the app, and every existing flow
(registration, login, validation, OAuth) preserved — green tsc/build.
</success_criteria>

<output>
Create `.planning/phases/03-web-redesign-rollout/03-02-SUMMARY.md` when done.
</output>
