---
phase: 03-web-redesign-rollout
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - web/src/app/app/dashboard/page.tsx
autonomous: true
requirements: [WEB-01, WEB-03, WEB-04]
must_haves:
  truths:
    - "The dashboard has an editorial header (Eyebrow + text-display 700 title) and a hero calc-entry plate on bg-carbon-surface3 with a SegmentedControl replacing the two hand-rolled tabs"
    - "The quick-calc result renders as a designed JetBrains Mono figure animated with useCountUp (instant under reduced-motion, fixed decimals → no CLS), unit demoted to muted"
    - "KPIs render as designed data-viz: mono figures (NumberDisplay) + a Variant-B DataBar micro-viz + the retained Sparkline, with KPICell delta for the % saving"
    - "The exact calculation/entry behavior is unchanged (handleQuickCalc, readUserPrices, fuelPrice, distance/budget math, vehicle selection, suggestion + recent-trip navigation) — MD-2"
    - "Loading uses the Skeleton atom mirroring the layout (no ad-hoc placeholders); KPI tiles + recent rows stagger via the existing reveal keyframe gated by useReducedMotion; all motion is transform/opacity only"
  artifacts:
    - path: "web/src/app/app/dashboard/page.tsx"
      provides: "Editorial-dark data-viz dashboard: hero calc plate (SegmentedControl + animated mono result), NumberDisplay/DataBar/Sparkline KPI band, skeletons, staggered reveal"
      contains: "useCountUp"
  key_links:
    - from: "web/src/app/app/dashboard/page.tsx"
      to: "web/src/components/ui/SegmentedControl.tsx"
      via: "replaces the hand-rolled distance/budget tab buttons (l.322-342)"
      pattern: "SegmentedControl"
    - from: "web/src/app/app/dashboard/page.tsx"
      to: "web/src/hooks/useCountUp.ts"
      via: "animated quick-calc result figure"
      pattern: "useCountUp"
    - from: "web/src/app/app/dashboard/page.tsx"
      to: "web/src/components/ui/DataBar.tsx + NumberDisplay.tsx + Sparkline.tsx"
      via: "KPI band data-viz"
      pattern: "DataBar"
---

<objective>
Redesign the dashboard around designed data-viz (WEB-03, PD3-3) in the editorial-dark language — a
hero calc entry + KPIs as data-viz with measured micro-interactions, consistent with the trip-result
page. Same calculation and entry behavior (MD-2): only the presentation changes. NO serif (D-11):
title is Space Grotesk bold 700.

Purpose: The dashboard is the daily landing surface — it must feel like a premium analytics view, not
a token-swapped form.
Output: A fully restyled `web/src/app/app/dashboard/page.tsx` (hero plate + animated result +
NumberDisplay/DataBar/Sparkline KPI band + skeletons + staggered reveal), within CWV.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-web-redesign-rollout/03-UI-SPEC.md
@.planning/phases/03-web-redesign-rollout/03-CONTEXT.md

<interfaces>
<!-- Reference implementation to mirror (CURRENT, post-D-11 — Space Grotesk title, NO serif): -->
@web/src/app/app/trips/result/page.tsx
<!-- Atoms/hooks to consume (normalized in 03-01, or already compliant): -->
import { SegmentedControl } from '@/components/ui/SegmentedControl';  // active = color/bg only (no weight change)
import { NumberDisplay } from '@/components/ui/NumberDisplay';        // mono figure, 400 default (add font-bold for emphasis)
import { DataBar } from '@/components/ui/DataBar';                    // Variant B single bar (value/max), token-var fill, scaleX reveal
import { Sparkline } from '@/components/ui/Sparkline';                // var(--c-accent), full-width
import { KPICell } from '@/components/ui/KPICell';                    // delta % badge
import { Skeleton } from '@/components/ui/Skeleton';
import { useCountUp } from '@/hooks/useCountUp';                      // (target, durationMs=700) => number; reduced-motion = instant
import { useReducedMotion } from '@/hooks/useReducedMotion';         // () => boolean
import { SectionCard, CTAButton, Eyebrow, Hairline, Select, FuelBadge, BrandAvatar } from '@/components/ui/*';
<!-- The `reveal` keyframe (opacity + translateY(8px)) ALREADY EXISTS in web/src/app/globals.css (added Phase 2). Reuse it; do NOT edit globals.css. -->
<!-- D-11: title = Space Grotesk text-display 700. NEVER font-serif. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dashboard header + hero calc-entry plate (SegmentedControl + animated mono result)</name>
  <files>web/src/app/app/dashboard/page.tsx</files>
  <read_first>
    - web/src/app/app/dashboard/page.tsx (FULL — preserve every piece of logic: readUserPrices/fuelPrice/FALLBACK_PRICES, loadData, handleQuickCalc distance/budget math, applySuggestion, vehicleOptions, searchParams vehicleId selection, fmtEur/fmtNum/fmtDate, the Suspense wrapper)
    - .planning/phases/03-web-redesign-rollout/03-UI-SPEC.md (section "Dashboard Data-Viz Spec" items 1-2; "Interaction States"; "CWV Guard")
    - web/src/app/app/trips/result/page.tsx (the hero plate + useCountUp pattern to mirror — Space Grotesk title, mono animated figure, aria-hidden + sr-only)
    - web/src/components/ui/SegmentedControl.tsx + web/src/hooks/useCountUp.ts + web/src/hooks/useReducedMotion.ts
  </read_first>
  <action>
    Keep ALL calculation/entry logic untouched (MD-2): readUserPrices, fuelPrice, handleQuickCalc's
    distance→cost and budget→km math, the vehicle selection, suggestions, and recent-trip navigation.
    Add a header at the top of the page column (currently absent): `Eyebrow` "Tableau de bord" + an `h1`
    in `text-display` weight 700 (e.g. "Aperçu") — Space Grotesk, NO serif.
    Convert the "Calcul rapide" `SectionCard` into the hero calc plate: render it on the elevated surface
    via `className` carrying `bg-carbon-surface3` (mirroring the result-page hero plate). Replace the two
    hand-rolled tab buttons (l.322-342) with a `SegmentedControl` for `Distance → Coût` / `Budget →
    Distance` wired to the existing `quickCalcTab` state and its onChange (which resets quickResult +
    quickInput) — the active segment changes color/bg only, never weight (CWV). Keep the numeric `<input>`
    (mono) and the distance quick-chips (50/100/200/500) — restyle the chips to the segmented/chip
    interaction states + canonical focus ring (active/hover/rest per the states table); keep the vehicle
    `Select`.
    Animate the result figure: introduce additive presentation state `quickResultValue: number | null`
    and `quickResultUnit: '€' | 'km' | null` set alongside the existing `setQuickResult` call inside
    handleQuickCalc (do NOT change the computed numbers — just also store the numeric value + its unit).
    Call `const animatedResult = useCountUp(quickResultValue ?? 0)` unconditionally at the top of the
    component. Render the result block only when `quickResultValue !== null`: the figure in JetBrains Mono
    `tabular-nums`, `text-display` scaling up toward `text-hero` for the dominant number, weight 700,
    `text-carbon-ink`, formatted with fixed decimals (2 for €, 0 for km) so width is constant (no CLS);
    demote the unit (€ / km) to `text-carbon-muted`. Wrap the animating number span with `aria-hidden`
    and add an adjacent `sr-only` span holding the final formatted value (the existing `quickResult`
    string) so SR users get the real number. Keep the result chip's sanctioned `bg-blue-500/10
    border-blue-500/20` wash if retained, or move the figure onto the plate — either is acceptable; do not
    introduce energy-palette colors here. "Calculer" stays a full-width accent `CTAButton size="lg"` with
    its existing disabled conditions.
    Replace every `font-medium`/`font-semibold` in the touched regions with 400/700; map raw title sizes
    to `text-display`.
  </action>
  <verify>
    <automated>cd web; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - The page opens with `Eyebrow` + an `h1` in `text-display` 700 (grep `text-display` present; NO `font-serif`).
    - The distance/budget tabs are a `SegmentedControl` (grep `SegmentedControl` present; the hand-rolled tab `<button>` block at l.322-342 is gone).
    - The quick-calc result figure is driven by `useCountUp` (grep `useCountUp` present), rendered mono `tabular-nums` with fixed decimals, the animating span `aria-hidden`, and an `sr-only` sibling holds the real value.
    - handleQuickCalc's distance/budget math and all existing logic are byte-for-byte preserved (only additive presentation state added).
    - No `font-medium`/`font-semibold` remains in the header/hero region; no energy-palette `bg-emerald/sky/violet` added.
    - `cd web; npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>The dashboard hero is a designed, animated calc plate with a consistent SegmentedControl and a CLS-safe mono result figure.</done>
</task>

<task type="auto">
  <name>Task 2: KPI band data-viz + active vehicle + lists + skeletons + staggered reveal</name>
  <files>web/src/app/app/dashboard/page.tsx</files>
  <read_first>
    - web/src/app/app/dashboard/page.tsx (the KPI grid l.214-249, sparkline + active-vehicle l.251-299, onboarding banner l.301-316, favorite suggestions l.424-461, recent trips l.463-502 — all being restyled; the `statsLoading`/`vehiclesLoading` flags + stats shape to PRESERVE)
    - .planning/phases/03-web-redesign-rollout/03-UI-SPEC.md (section "Dashboard Data-Viz Spec" items 3-6; "Loading Skeletons"; "CWV Guard"; "Consistency Checklist" lists pattern)
    - web/src/components/ui/NumberDisplay.tsx + DataBar.tsx + Sparkline.tsx + KPICell.tsx + Skeleton.tsx
    - web/src/app/app/trips/result/page.tsx (the metric-tile reveal stagger pattern + Skeleton usage to mirror)
  </read_first>
  <action>
    KPI band: keep the "Ce mois" `SectionCard` and the four KPIs (Dépenses du mois · Trajets · Distance
    totale · Économies vs essence) and their existing data bindings (stats.totalCost, tripCount,
    totalDistance, savedVsGas.amount/.percent). Render each KPI figure as a designed mono figure via
    `NumberDisplay` (JetBrains Mono `tabular-nums`, normalized 400/700 — 700 for the dominant figure)
    instead of plain `KPICell` text, keeping the `KPICell` delta treatment for the savedVsGas `%` (and the
    "Uniquement pour VE" fallback when savedVsGas is null). Add ONE `DataBar` Variant-B micro-viz under
    "Dépenses du mois" sized against the trailing-period max derived from the existing
    `stats.dailyExpenses` (max of the cost series; if empty/zero, render nothing or a 0-width guarded bar)
    with fill `var(--c-accent)` — token-driven, `scaleX` reveal (compositor-friendly, gated by
    useReducedMotion inside DataBar). Keep the `Sparkline` "Dépenses 30j" (`var(--c-accent)`, full-width)
    with its "Aucune donnée" empty state.
    Active vehicle card: keep BrandAvatar + name (name `font-semibold` → `font-bold`) + `FuelBadge`; the
    empty/"Ajouter un véhicule" link gets the canonical focus + `font-normal`.
    Onboarding banner (no vehicles): keep it; normalize `font-semibold` → `font-bold` on the title and
    keep the accent `CTAButton`.
    Favoris rapides + Trajets récents: keep both `SectionCard(padding=none)` row lists and their
    navigation (applySuggestion / router.push). Normalize the shared row pattern to match trips/result
    rows: mono date · route (truncated, `→` muted) · `FuelBadge` · mono cost (700, `tabular-nums`) · hover
    `bg-carbon-surface2`; each row button gets the canonical focus ring. Change the row `font-medium`/
    `font-semibold` to 400/700.
    Loading: replace the `'—'` KPI placeholders and the "Chargement..." text with `Skeleton` sets that
    MIRROR the final layout (4 KPI-tile skeletons + a sparkline block + the active-vehicle row + a few
    recent-row skeletons) to avoid CLS on swap.
    Micro-interactions: apply the existing `reveal` keyframe (already in globals.css) to the KPI tiles and
    recent rows via inline `animation-delay: ${Math.min(i*60,300)}ms` + `animation-fill-mode: both`, and
    gate the reveal class off when `useReducedMotion()` is true (render the final state). Transform/opacity
    only — no layout-bound animation, no CLS.
    Ensure zero `font-medium`/`font-semibold` and zero ad-hoc `animate-pulse` placeholder divs remain in
    the file; energy colors only via DataBar/FuelBadge.
  </action>
  <verify>
    <automated>cd web; npx tsc --noEmit; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - KPI figures render via `NumberDisplay` (grep `NumberDisplay` present); the savedVsGas `%` still uses the `KPICell` delta treatment and the "Uniquement pour VE" fallback.
    - A `DataBar` Variant-B micro-viz renders under "Dépenses du mois" with `var(--c-accent)` fill, guarded against empty/zero series.
    - The `Sparkline` "Dépenses 30j" is retained with its empty state; favoris + recent-trip lists keep their navigation and use the shared row pattern with canonical focus.
    - Loading uses `Skeleton` blocks mirroring the layout — grep `Skeleton` present; ZERO ad-hoc `animate-pulse` divs and ZERO `'—'` KPI placeholders remain.
    - KPI tiles + recent rows use the `reveal` stagger gated by `useReducedMotion`; grep `font-medium\|font-semibold` over the file (excluding comments) → 0; no `bg-emerald/sky/violet` ad-hoc energy fills.
    - `cd web; npm run build` compiles all routes with 0 errors and 0 ESLint errors.
  </acceptance_criteria>
  <done>The dashboard KPI band is designed data-viz with mono figures, a token DataBar, the Sparkline, CLS-safe skeletons, and reduced-motion-aware staggered reveal — same data and calc behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage / API → page | Prices (localStorage) + stats/trips/favorites (existing authenticated API) are read exactly as today (MD-2); rendered as text/widths only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06 | Tampering | quick-calc render | accept | handleQuickCalc math + localStorage parse are unchanged; new state is additive presentation only; values render as text/CSS widths, never `dangerouslySetInnerHTML`. |
| T-03-07 | Information Disclosure | KPI/recent figures | accept | Only the user's own already-fetched stats/trips are shown; no new endpoint or data surfaced; no secret involved. |
| T-03-SC | Tampering | npm/pip/cargo installs | n/a | No package installs — all atoms/hooks/icons already exist. |
</threat_model>

<verification>
- `cd web; npx tsc --noEmit` → 0 errors; `cd web; npm run build` → all routes compile, 0 ESLint errors.
- Grep dashboard/page.tsx: contains `SegmentedControl`, `useCountUp`, `NumberDisplay`, `DataBar`, `Sparkline`, `Skeleton`; contains NO `font-medium`/`font-semibold`, NO `font-serif`, NO ad-hoc `animate-pulse`, NO `'—'` KPI placeholder.
- Calculation parity: handleQuickCalc distance/budget formulas + readUserPrices/fuelPrice unchanged (diff shows only additive presentation state + className/JSX).
- Manual observable (folded into 03-07 checkpoint): result counts up smoothly + instant under reduced-motion; no layout shift on skeleton→content; KPI tiles reveal-stagger; dashboard feels consistent with the result page.
</verification>

<success_criteria>
The dashboard is redesigned in the editorial-dark language as designed data-viz (hero calc plate with
animated mono result, NumberDisplay/DataBar/Sparkline KPI band, CLS-safe skeletons, reduced-motion
staggered reveal) with the exact same calculation and entry behavior — green tsc/build.
</success_criteria>

<output>
Create `.planning/phases/03-web-redesign-rollout/03-03-SUMMARY.md` when done.
</output>
