---
phase: 03-web-redesign-rollout
plan: 07
type: execute
wave: 3
depends_on: ["03-02", "03-03", "03-04", "03-05", "03-06"]
files_modified: []
autonomous: false
requirements: [WEB-01, WEB-03, WEB-04]
must_haves:
  truths:
    - "Across all restyled web screens there are zero residual 3rd-weight (font-medium/font-semibold/font-extrabold) classes, zero serif (font-serif), zero non-canonical focus rings (ring-blue-500 / focus:ring-offset-0), and zero ad-hoc animate-pulse placeholders outside the Skeleton atom"
    - "Navigating between any two restyled screens feels visually consistent — shared shell, surfaces, typography, and interaction states (Criterion 3)"
    - "Restyled pages meet CWV: no new layout shift on skeleton→content swap or count-up, animations run on transform/opacity only, and motion is off under prefers-reduced-motion (Criterion 4)"
    - "The dashboard reads as designed data-viz with the exact same calculation behavior, and the trip detail mirrors the result page (no serif)"
  artifacts:
    - path: ".planning/phases/03-web-redesign-rollout/03-07-SUMMARY.md"
      provides: "Cross-screen consistency + CWV verification record + human sign-off"
      contains: "Checkpoint"
  key_links:
    - from: "all restyled screens"
      to: "the consistency checklist + CWV guard (03-UI-SPEC.md)"
      via: "repo-wide automated grep gate + human visual/motion verification"
      pattern: "font-medium"
---

<objective>
Verify the rollout as a whole: (1) an automated, repo-wide consistency + CWV gate enforcing the
UI-SPEC "Consistency Checklist" and "CWV Guard", then (2) a human visual + motion checkpoint across a
representative set of screens. This is the phase's proof that the whole web app now feels like one
premium product (Success Criteria 1-4). No production code changes here — verification + sign-off only.

Purpose: Catch any residual 3rd-weight / serif / non-canonical-focus / ad-hoc-skeleton drift and
confirm consistency, motion, and CWV on the running app before the phase closes.
Output: `03-07-SUMMARY.md` recording the automated gate results + the human checkpoint outcome.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-web-redesign-rollout/03-UI-SPEC.md
@.planning/phases/03-web-redesign-rollout/03-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated repo-wide consistency + CWV gate</name>
  <files>(verification only — no production files modified)</files>
  <read_first>
    - .planning/phases/03-web-redesign-rollout/03-UI-SPEC.md (sections "Consistency Checklist" and "CWV Guard" — the exact items this gate enforces)
  </read_first>
  <action>
    Run the build and a set of repo-wide greps that encode the consistency checklist. Treat ANY violation
    as a failure to fix (or, if a violation is a genuine sanctioned exception, document it explicitly in
    the SUMMARY with its UI-SPEC justification — but the default expectation is zero).
    Build: `cd web; npx tsc --noEmit` then `cd web; npm run build` — both must be green (all routes).
    Grep gates (exclude comment lines to avoid self-invalidating prose, e.g. pipe through
    `grep -v '^\s*//' | grep -v '^\s*\*'`):
      1. Zero 3rd-weight classes in web/src: `font-medium`, `font-semibold`, `font-extrabold` → expect 0
         matches across web/src (atoms + screens + shell).
      2. Zero serif: `font-serif` → expect 0 matches across web/src (D-11).
      3. Zero non-canonical focus rings: `ring-blue-500` and `focus:ring-offset-0` → expect 0 matches
         across web/src.
      4. Zero ad-hoc skeleton placeholders: `animate-pulse` → expect matches ONLY inside
         web/src/components/ui/Skeleton.tsx (the single skeleton primitive); 0 matches in web/src/app/**.
      5. Zero ad-hoc energy fills outside DataBar/FuelBadge: `bg-emerald-`, `bg-sky-`, `bg-violet-` used as
         energy data-viz fills → spot-check that any remaining occurrences are sanctioned Pill/FuelBadge/
         DeltaBadge status tints (per the Color exception), not energy bars (energy bars must be `DataBar`
         + `var(--c-*)`).
    Record each grep's result (count + file list) in the SUMMARY. If items 1-4 are non-zero, fix the
    offending file(s) (apply the 2-weight / canonical-focus / Skeleton normalization) and re-run until
    green before handing to the human checkpoint.
  </action>
  <verify>
    <automated>cd web; npx tsc --noEmit; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `cd web; npx tsc --noEmit` exits 0 and `cd web; npm run build` compiles all routes with 0 ESLint errors.
    - Grep over web/src (excluding comment lines): `font-medium` + `font-semibold` + `font-extrabold` → 0; `font-serif` → 0; `ring-blue-500` → 0; `focus:ring-offset-0` → 0.
    - `animate-pulse` appears ONLY in web/src/components/ui/Skeleton.tsx; 0 occurrences under web/src/app/**.
    - Any remaining `bg-emerald-`/`bg-sky-`/`bg-violet-` is a sanctioned status tint (Pill/FuelBadge/DeltaBadge), not an energy data-viz bar — documented in the SUMMARY.
  </acceptance_criteria>
  <done>The repo-wide consistency + CWV gate is green and recorded — the codebase encodes the locked contract everywhere.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human visual + motion + CWV checkpoint (cross-screen consistency)</name>
  <what-built>
    The full web redesign rollout: normalized atoms + shell (03-01), landing + auth (03-02), the
    data-viz dashboard (03-03), garage list/detail/showroom (03-04), trips history/detail + favorites
    (03-05), fuel-prices + settings (03-06) — all in the editorial-dark language with the 2-weight system,
    canonical focus, Skeleton loading, and reduced-motion-aware micro-interactions. The automated
    consistency + CWV gate (Task 1) is green.
  </what-built>
  <how-to-verify>
    Run the app (`cd web; npm run dev`, or verify the deployed Vercel build after push) and check:
    1. CONSISTENCY (Criterion 3): navigate landing → login → dashboard → garage → garage detail →
       showroom → trips → trip detail → favorites → fuel-prices → settings. Every page shares the shell,
       `flex flex-col gap-6` rhythm, `Eyebrow` + Space Grotesk 700 titles, `SectionCard` surfaces, mono
       `tabular-nums` figures, and the same hover/focus/active states. It should feel like ONE product.
    2. NO SERIF (D-11): confirm NO heading anywhere renders in a serif face — all titles are Space Grotesk.
    3. DASHBOARD (WEB-03): the hero result figure counts up smoothly; the SegmentedControl + KPI
       NumberDisplay/DataBar/Sparkline read as designed data-viz; the calculation result matches the old
       behavior for a known input (e.g. pick a vehicle, enter 100 km, verify the cost is the same number
       as before the redesign).
    4. TRIP DETAIL: mirrors the result page (mono hero figure + énergie/péage DataBar when toll present +
       réel/≈ estimé badge); péage segment hidden when toll = 0.
    5. FOCUS: tab through buttons/inputs/chips on 3-4 screens — every focused element shows the same
       accent ring (no missing/blue-mismatched rings); icon-only buttons (garage star/edit/delete, trip
       archive) are reachable and announce a label (screen-reader or inspect `aria-label`).
    6. CWV / MOTION (Criterion 4): reload dashboard + a list screen and confirm NO layout shift on
       skeleton→content swap (figures don't jump width). Enable OS "Reduce motion" and confirm the
       count-up is instant, reveal/stagger is off, and DataBars render filled — no animation.
    7. THEME parity: toggle dark/light in settings — both themes look intentional, no hydration flash,
       and the theme picker doesn't reflow when toggled.
    Report any inconsistency, missing focus ring, layout shift, serif heading, or calculation mismatch.
  </how-to-verify>
  <resume-signal>Type "approved" if the rollout is consistent + CWV-clean, or describe the issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Verification + sign-off only; no production code, endpoint, secret, or dependency changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-16 | (n/a) | verification checkpoint | accept | No code shipped here; the gate only reads/greps the codebase and runs the build. If Task 1 must fix drift, those fixes inherit the same MD-2 className-only constraints as the cluster plans. |
| T-03-SC | Tampering | npm/pip/cargo installs | n/a | No package installs. |
</threat_model>

<verification>
- Task 1 automated gate green (tsc + build + all consistency greps at expected counts).
- Task 2 human checkpoint returns "approved" (consistency, no-serif, dashboard data-viz + calc parity, trip-detail mirror, focus rings + icon aria-labels, no CLS, reduced-motion, theme parity).
</verification>

<success_criteria>
The whole web app is verified — automatically (consistency + CWV greps + green build) and by a human
(cross-screen consistency, no serif, dashboard data-viz with calc parity, trip-detail mirror,
canonical focus + accessible icon actions, no layout shift, reduced-motion honored, theme parity) —
confirming Success Criteria 1-4 and closing Phase 3.
</success_criteria>

<output>
Create `.planning/phases/03-web-redesign-rollout/03-07-SUMMARY.md` when done (record the grep results + the human checkpoint outcome).
</output>
