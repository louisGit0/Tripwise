---
phase: 03-web-redesign-rollout
plan: 04
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - web/src/app/app/garage/page.tsx
  - web/src/app/app/garage/[id]/page.tsx
  - web/src/app/app/garage/add/page.tsx
autonomous: true
requirements: [WEB-01, WEB-04]
must_haves:
  truths:
    - "The garage list, vehicle detail, and showroom (add) screens are restyled in the editorial-dark language with header (Eyebrow + text-display 700), SectionCard surfaces, normalized atoms, and canonical focus"
    - "Every icon-only action (star / edit / delete on list; default-star on detail) carries an explicit aria-label and the canonical focus ring"
    - "Loading states use the Skeleton atom mirroring each layout (no ad-hoc animate-pulse); destructive flows use Modal + danger CTAButton"
    - "Existing flows are preserved: garage CRUD (PATCH/DELETE/set-default), detail edit/save, and the showroom's CLIENT-LOAD-ALL data loading is NOT touched (CAT-06/Phase 4) — restyle only (MD-2)"
  artifacts:
    - path: "web/src/app/app/garage/page.tsx"
      provides: "Restyled garage list — SectionCard vehicle rows, BrandAvatar/FuelBadge/Pill, aria-labelled icon buttons, Skeleton rows, danger Modal"
      contains: "aria-label"
    - path: "web/src/app/app/garage/[id]/page.tsx"
      provides: "Restyled vehicle detail — KPICell stats strip, normalized edit form, default toggle, danger zone"
      contains: "KPICell"
  key_links:
    - from: "web/src/app/app/garage/page.tsx"
      to: "the canonical focus + aria-label contract"
      via: "star/edit/delete icon buttons get aria-label + focus ring"
      pattern: "aria-label"
    - from: "web/src/app/app/garage/add/page.tsx"
      to: "the locked atoms (restyle only)"
      via: "Input/SectionCard/BrandAvatar/FuelBadge/Skeleton applied without changing data loading"
      pattern: "Skeleton"
---

<objective>
Restyle the garage cluster — list `/app/garage`, detail `/app/garage/[id]`, showroom `/app/garage/add`
— into the editorial-dark language, inheriting the 03-01 atoms. Visual layer only (MD-2): garage CRUD,
detail edit/save, set-default, and the showroom's data loading are all preserved. CRITICAL: the
showroom's CLIENT-LOAD-ALL behavior is OUT OF SCOPE (that is CAT-06 / Phase 4 server-side search) —
restyle its UI only, do NOT change its fetch/search/data flow. NO serif (D-11). Address the checker
FLAG: every icon-only button gets an explicit `aria-label`.

Purpose: The garage is a core daily surface (and the showroom is scaled in Phase 4 in its final design).
Output: Restyled `garage/page.tsx`, `garage/[id]/page.tsx`, `garage/add/page.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-web-redesign-rollout/03-UI-SPEC.md
@.planning/phases/03-web-redesign-rollout/03-CONTEXT.md

<interfaces>
import { SectionCard, CTAButton, Input, Modal, Pill, FuelBadge, BrandAvatar, Eyebrow, Hairline, KPICell, Skeleton } from '@/components/ui/*';
import { SegmentedControl } from '@/components/ui/SegmentedControl'; // optional step indicator on the showroom
<!-- Canonical focus token (apply to every interactive element incl. icon buttons): -->
focus:outline-none focus-visible:ring-2 focus-visible:ring-carbon-accent/50 focus-visible:ring-offset-2 focus-visible:ring-offset-carbon-bg
<!-- Sanctioned semantic hover tints (keep): star→amber, edit→accent, delete→red. Energy colors only via FuelBadge. -->
<!-- D-11: titles = Space Grotesk text-display 700. NEVER font-serif. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Garage list restyle — vehicle rows, aria-labelled icon buttons, skeletons, danger Modal</name>
  <files>web/src/app/app/garage/page.tsx</files>
  <read_first>
    - web/src/app/app/garage/page.tsx (FULL — the vehicle list, the type guards hasStats()/isDefaultVehicle(), the stats strip, the ad-hoc animate-pulse loading, the edit + delete Modals, the empty state; all CRUD logic + apiClient calls to PRESERVE)
    - .planning/phases/03-web-redesign-rollout/03-UI-SPEC.md (section "Per-Screen Application → 5. Garage"; "Interaction States" icon-button row; "Loading Skeletons" garage row; "Copywriting Contract")
    - web/src/components/ui/SectionCard.tsx + Pill.tsx + FuelBadge.tsx + BrandAvatar.tsx + Skeleton.tsx + Modal.tsx + CTAButton.tsx (normalized in 03-01)
  </read_first>
  <action>
    Restyle visual layer only — keep all CRUD logic, type guards, and apiClient calls (MD-2). Header:
    `Eyebrow` + an `h1` in `text-display` 700 + the "Ajouter au garage" accent `CTAButton` (its copy
    preserved). Vehicle list: render each vehicle as a `SectionCard(padding="none")` row containing
    `BrandAvatar` + name (`font-semibold` → `font-bold`) + the "Par défaut" `Pill` (when default) +
    `FuelBadge` + meta line + the star/edit/delete icon buttons. Each icon button: 32px (`w-8 h-8`) tap
    target, `text-carbon-muted` rest, canonical focus ring, and keep the sanctioned semantic hover tints
    (star→amber, edit→accent, delete→red). Address the checker FLAG — add an explicit `aria-label` to each
    icon-only button: e.g. `aria-label="Définir comme véhicule par défaut"` (star), `aria-label="Modifier
    le véhicule"` (edit), `aria-label="Supprimer le véhicule"` (delete). Stats strip (when present): keep
    the 4-col `divide` layout; figures stay JetBrains Mono `tabular-nums` 700. Empty state: keep the copy
    "Aucun véhicule dans le garage..." + the accent CTA. Loading: replace the ad-hoc `animate-pulse` h-16
    ×3 blocks with 3 `Skeleton` vehicle-row skeletons (avatar circle + 2 text lines + an actions block)
    mirroring the real row to avoid CLS. Edit `Modal`: normalize its field/label weights via the
    normalized `Input`. Delete `Modal`: danger `CTAButton` "Supprimer" + ghost "Annuler" with the
    confirmation copy. Replace every `font-medium`/`font-semibold` with 400/700 and any `ring-blue-500`/
    `focus:ring-offset-0` with the canonical token.
  </action>
  <verify>
    <automated>cd web; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Header is `Eyebrow` + `text-display` 700 title + "Ajouter au garage" accent `CTAButton`; vehicle rows use `SectionCard` + `BrandAvatar` + `FuelBadge` + the "Par défaut" `Pill`.
    - Every icon-only button (star, edit, delete) has a non-empty `aria-label` AND the canonical focus ring (grep `aria-label` ≥ 3 occurrences on the row buttons).
    - Loading uses `Skeleton` rows mirroring the layout — ZERO ad-hoc `animate-pulse` divs remain.
    - Delete uses `Modal` + danger `CTAButton` with confirmation copy; CRUD logic unchanged.
    - Grep `font-medium\|font-semibold` (excluding comments) and `ring-blue-500\|focus:ring-offset-0` over the file → 0.
    - `cd web; npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>The garage list is restyled with accessible icon actions, skeleton loading, and a danger delete flow — CRUD intact.</done>
</task>

<task type="auto">
  <name>Task 2: Vehicle detail restyle — KPICell stats strip, edit form, default toggle, danger zone</name>
  <files>web/src/app/app/garage/[id]/page.tsx</files>
  <read_first>
    - web/src/app/app/garage/[id]/page.tsx (FULL — the use(params) resolver, the GET /vehicles/me find, the stats strip, the edit form + PATCH save, the set-default toggle, the danger zone delete Modal; all flow logic to PRESERVE)
    - .planning/phases/03-web-redesign-rollout/03-UI-SPEC.md (section "Per-Screen Application → 6. Garage detail")
    - web/src/components/ui/KPICell.tsx + BrandAvatar.tsx + FuelBadge.tsx + Input.tsx + CTAButton.tsx + Modal.tsx + Skeleton.tsx (normalized in 03-01)
  </read_first>
  <action>
    Restyle visual layer only (MD-2). Header: `BrandAvatar` + name (700) + `FuelBadge`. Stats strip:
    migrate the trips / distance / spent / €/km figures to `KPICell` (`size="sm"`) for consistency with
    the dashboard, keeping their existing data bindings and the "only when tripsCount > 0" guard. Edit
    form: render nickname / license plate / read-only consumption / EV-price fields via the normalized
    `Input` (labels now 700, canonical focus inherited); keep the read-only consumption styling and the
    EV-only price fields. Default toggle: if already default show the `Star` state (give the toggle an
    `aria-label="Véhicule par défaut"` or "Définir comme véhicule par défaut" depending on state); else an
    accent `CTAButton` wired to the existing set-default PATCH. Danger zone: keep `border-red-500/20`, with
    a danger `CTAButton` opening the delete `Modal` (danger confirm + ghost cancel). Loading: `Skeleton`
    mirroring the header + stats + form. Replace every `font-medium`/`font-semibold` with 400/700 and any
    `ring-blue-500`/`focus:ring-offset-0` with the canonical token. Do NOT change the params resolver,
    fetch, PATCH save payload, set-default, or delete logic.
  </action>
  <verify>
    <automated>cd web; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Stats strip figures render via `KPICell` (grep `KPICell` present) with the tripsCount>0 guard preserved.
    - The edit form uses the normalized `Input`; the default toggle (icon or CTA) carries an `aria-label` and the canonical focus ring.
    - Danger zone keeps `border-red-500/20` and the delete `Modal` + danger `CTAButton`; loading uses `Skeleton`.
    - Grep `font-medium\|font-semibold` (excluding comments) and `ring-blue-500\|focus:ring-offset-0` over the file → 0.
    - Fetch / PATCH save / set-default / delete logic unchanged (MD-2).
    - `cd web; npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>The vehicle detail page is restyled with KPICell stats and a consistent edit/default/danger layout — flows intact.</done>
</task>

<task type="auto">
  <name>Task 3: Showroom (garage/add) restyle ONLY — atoms + canonical focus + Skeleton (no data-loading change)</name>
  <files>web/src/app/app/garage/add/page.tsx</files>
  <read_first>
    - web/src/app/app/garage/add/page.tsx (FULL — the 2-step flow: debounced search Input → catalog result list → selected-model preview + nickname/plate/EV-price Inputs + submit; the GET /vehicles/catalog client-load-all + the POST /vehicles/me submit — ALL data loading to PRESERVE unchanged)
    - .planning/phases/03-web-redesign-rollout/03-UI-SPEC.md (section "Per-Screen Application → 7. Showroom — RESTYLE ONLY"; "Scope Fence")
    - web/src/components/ui/Input.tsx + SectionCard.tsx + BrandAvatar.tsx + FuelBadge.tsx + CTAButton.tsx + Skeleton.tsx + SegmentedControl.tsx (normalized in 03-01)
  </read_first>
  <action>
    RESTYLE ONLY — under no circumstances change the data loading: keep the GET /vehicles/catalog call,
    the client-side search/filter, the debounce, the pagination/result handling, and the POST /vehicles/me
    submit exactly as they are. The client-load-all scaling problem is CAT-06 / Phase 4 (server-side
    search) and is explicitly out of scope here (MD-2 + Scope Fence).
    Step 1: the debounced search `Input` (canonical focus inherited from 03-01); render the catalog result
    list as brand-grouped `SectionCard` rows with `BrandAvatar` + model name (700) + `FuelBadge`; each
    result row is a button with the canonical focus ring + `bg-carbon-surface2` hover. Step 2:
    selected-model preview + nickname / plate / EV-price `Input`s + the accent `CTAButton` submit. Optional:
    a `SegmentedControl` or back-link as the step indicator (keep whatever navigation exists; do not change
    its behavior). Loading: replace any ad-hoc search-loading placeholder with `Skeleton` result-row
    skeletons mirroring the list. Replace every `font-medium`/`font-semibold` with 400/700 and any
    `ring-blue-500`/`focus:ring-offset-0` with the canonical token.
  </action>
  <verify>
    <automated>cd web; npx tsc --noEmit; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - The catalog data loading is UNCHANGED: GET /vehicles/catalog call, client-side search/debounce/pagination, and POST /vehicles/me submit are byte-for-byte preserved (diff shows only className/JSX + atom swaps).
    - Search uses the normalized `Input`; result rows use `SectionCard` + `BrandAvatar` + `FuelBadge` with canonical-focus button rows.
    - Search-loading uses `Skeleton` rows; grep `Skeleton` present; ZERO ad-hoc `animate-pulse` divs.
    - Grep `font-medium\|font-semibold` (excluding comments) and `ring-blue-500\|focus:ring-offset-0` over the file → 0.
    - `cd web; npm run build` compiles all routes with 0 errors and 0 ESLint errors.
  </acceptance_criteria>
  <done>The showroom UI is restyled to the editorial-dark language while its data loading is left intact for Phase 4.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| garage forms / catalog → API | Existing authenticated CRUD + catalog endpoints — UNCHANGED here (restyle only, MD-2) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-08 | Tampering | garage CRUD forms | accept | No change to PATCH/DELETE/POST payloads, validation, or ownership checks (server-side) — className/atom swaps only. |
| T-03-09 | Information Disclosure | vehicle figures / catalog | accept | Only the user's own vehicles + the existing public catalog are shown; no new data surfaced; no secret involved. |
| T-03-10 | Denial of Service | showroom client-load-all | accept | Pre-existing (CAT-06/Phase 4 fixes it server-side); this plan does NOT change the data loading, so it neither worsens nor is required to fix it here. |
| T-03-SC | Tampering | npm/pip/cargo installs | n/a | No package installs — all atoms/icons already exist. |
</threat_model>

<verification>
- `cd web; npx tsc --noEmit` → 0 errors; `cd web; npm run build` → all routes compile, 0 ESLint errors.
- Grep across the 3 files (excluding comments): NO `font-medium`/`font-semibold`/`font-extrabold`, NO `font-serif`, NO `ring-blue-500`, NO `focus:ring-offset-0`, NO ad-hoc `animate-pulse`.
- Garage list: ≥3 icon buttons with `aria-label`; detail uses `KPICell`; showroom data loading unchanged.
- Manual observable (folded into 03-07 checkpoint): garage rows + detail + showroom feel consistent with the rest of the app; CRUD + showroom search still work.
</verification>

<success_criteria>
The garage list, vehicle detail, and showroom are restyled in the editorial-dark language with
accessible icon actions (aria-label + canonical focus), Skeleton loading, danger Modals, and the
showroom's data loading untouched — all garage flows preserved, green tsc/build.
</success_criteria>

<output>
Create `.planning/phases/03-web-redesign-rollout/03-04-SUMMARY.md` when done.
</output>
