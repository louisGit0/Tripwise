---
phase: 03-web-redesign-rollout
plan: 05
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - web/src/app/app/trips/page.tsx
  - web/src/app/app/trips/[id]/page.tsx
  - web/src/app/app/favorites/page.tsx
autonomous: true
requirements: [WEB-01, WEB-04]
must_haves:
  truths:
    - "Trips history is restyled: Eyebrow + text-display 700 header, stats strip, standardized filter chips (canonical focus, weight-constant active), month-grouped row lists, Skeleton loading"
    - "Trip detail mirrors the CURRENT result page (Space Grotesk title, mono hero figure, DataBar Variant A énergie/péage when toll present, réel/≈ estimé Pill+Tooltip kept) — NO serif (D-11)"
    - "Favorites is restyled as SectionCard row lists with a 'Utiliser' accent action, canonical-focus row buttons, danger delete Modal, and Skeleton loading"
    - "All existing flows preserved: history filter/pagination, note auto-save, archive toggle, refaire/delete, favorite use/delete (MD-2)"
  artifacts:
    - path: "web/src/app/app/trips/[id]/page.tsx"
      provides: "Restyled trip detail mirroring the result-page data-viz patterns (no serif)"
      contains: "DataBar"
    - path: "web/src/app/app/trips/page.tsx"
      provides: "Restyled trips history — standardized filter chips, month-grouped rows, Skeleton"
      contains: "Skeleton"
  key_links:
    - from: "web/src/app/app/trips/[id]/page.tsx"
      to: "web/src/app/app/trips/result/page.tsx (the Phase-2 reference pattern)"
      via: "reuse hero figure + DataBar + SectionCard composition (Space Grotesk, no serif)"
      pattern: "DataBar"
    - from: "web/src/app/app/trips/page.tsx"
      to: "the canonical focus + segmented/chip states"
      via: "filter chips standardized; ad-hoc animate-pulse → Skeleton"
      pattern: "focus-visible:ring-carbon-accent"
---

<objective>
Restyle the trips cluster — history `/app/trips`, detail `/app/trips/[id]`, and favorites
`/app/favorites` — into the editorial-dark language, inheriting the 03-01 atoms. Trip detail MIRRORS
the CURRENT trip-result page (the Phase-2 reference) — do not invent a second style — using Space
Grotesk titles, NO serif (D-11). Visual layer only (MD-2): history filter/pagination, note auto-save,
archive, refaire/delete, and favorite use/delete are all preserved.

Purpose: History and detail are where users review cost over time — they must match the result page.
Output: Restyled `trips/page.tsx`, `trips/[id]/page.tsx`, `favorites/page.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-web-redesign-rollout/03-UI-SPEC.md
@.planning/phases/03-web-redesign-rollout/03-CONTEXT.md

<interfaces>
<!-- Reference implementation to mirror (CURRENT, post-D-11 — Space Grotesk title, NO serif): -->
@web/src/app/app/trips/result/page.tsx
import { SectionCard, CTAButton, Input, Modal, Pill, FuelBadge, Eyebrow, Hairline, Skeleton } from '@/components/ui/*';
import { DataBar } from '@/components/ui/DataBar';        // Variant A segmented énergie/péage (hide péage when toll=0)
import { Tooltip } from '@/components/ui/Tooltip';        // réel/≈ estimé badge tooltip (Phase 1 — keep)
import { SegmentedControl } from '@/components/ui/SegmentedControl'; // optional for the filter chips if it fits horizontally
<!-- Canonical focus token: -->
focus:outline-none focus-visible:ring-2 focus-visible:ring-carbon-accent/50 focus-visible:ring-offset-2 focus-visible:ring-offset-carbon-bg
<!-- D-11: titles = Space Grotesk text-display 700. NEVER font-serif (even though the OLD 02-04 plan mentioned font-serif, the CURRENT result page uses Space Grotesk — mirror the current file). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Trips history restyle — header, stats strip, filter chips, month-grouped rows, Skeleton</name>
  <files>web/src/app/app/trips/page.tsx</files>
  <read_first>
    - web/src/app/app/trips/page.tsx (FULL — the stats strip (GET /trips/stats), the filter chips (fuelCategory param), groupByMonth(), the row list + router.push, the "Charger plus" pagination, the ad-hoc animate-pulse loading, empty states; all data/filter/pagination logic to PRESERVE)
    - .planning/phases/03-web-redesign-rollout/03-UI-SPEC.md (section "Per-Screen Application → 8. Trips history"; "Interaction States" chip/segmented + list-row rows; "Loading Skeletons"; "Copywriting Contract")
    - web/src/components/ui/SectionCard.tsx + FuelBadge.tsx + CTAButton.tsx + Eyebrow.tsx + Skeleton.tsx (normalized in 03-01)
  </read_first>
  <action>
    Restyle visual layer only — keep groupByMonth, the fuelCategory filter, pagination, and navigation
    (MD-2). Header: `Eyebrow` + an `h1` `text-display` 700. Stats strip: a `SectionCard` with TOTAL / KM /
    €/KM in a 3-col `divide` layout, figures JetBrains Mono `tabular-nums`. Filter chips (all/ev/gas/
    diesel/gpl): standardize to the segmented/chip interaction states — inactive `text-carbon-muted`,
    hover `text-carbon-ink2 bg-carbon-faint`, active `bg-carbon-accent text-white`, the canonical focus
    ring, and keep the weight CONSTANT across active/inactive (color/bg only — no weight change, CWV).
    (Optional: lift the chips into `SegmentedControl` if they fit horizontally; otherwise keep them as
    chips with these states.) Month groups: render each month as a `SectionCard(padding="none")` with the
    month header (700) and the row list — each row a canonical-focus button: mono date · route (truncated,
    `→`/chevron muted, chevron `aria-hidden`) · `FuelBadge` · km · duration · cost (mono 700) · chevron,
    hover `bg-carbon-surface2`. "Charger plus ({n})" stays a ghost `CTAButton` (preserve the `page <
    totalPages` condition). Empty states: keep "Aucun trajet enregistré" / "Aucun trajet pour ce filtre"
    + the "Calculer un trajet" ghost CTA. Loading: replace the ad-hoc `animate-pulse` h-16 ×6 with a stats
    strip skeleton + 6 row `Skeleton`s mirroring the layout. Replace every `font-medium`/`font-semibold`
    with 400/700 and any `ring-blue-500`/`focus:ring-offset-0` with the canonical token.
  </action>
  <verify>
    <automated>cd web; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Header is `Eyebrow` + `text-display` 700; stats strip uses a `SectionCard` 3-col divide with mono figures.
    - Filter chips use the standardized states + canonical focus, active = `bg-carbon-accent`, weight constant across states (no `font-*` change between active/inactive).
    - Month-grouped rows use the shared row pattern with canonical-focus buttons; chevron icons are `aria-hidden`; "Charger plus" is a ghost `CTAButton` with its pagination condition intact.
    - Loading uses `Skeleton` (stats + 6 rows); ZERO ad-hoc `animate-pulse` remains.
    - Grep `font-medium\|font-semibold` (excluding comments) and `ring-blue-500\|focus:ring-offset-0` over the file → 0.
    - `cd web; npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Trips history is restyled with standardized chips, month-grouped rows, and Skeleton loading — filter/pagination intact.</done>
</task>

<task type="auto">
  <name>Task 2: Trip detail restyle — mirror the result-page data-viz (Space Grotesk, no serif)</name>
  <files>web/src/app/app/trips/[id]/page.tsx</files>
  <read_first>
    - web/src/app/app/trips/[id]/page.tsx (FULL — the use(params) resolver, GET /trips/:id, the note auto-save (useDebounce + savedNoteRef), the archive toggle PATCH, the "Refaire" URL build, the delete Modal; all flow logic to PRESERVE)
    - web/src/app/app/trips/result/page.tsx (THE reference — mirror its hero figure + DataBar + SectionCard composition; note it uses Space Grotesk, NOT serif — do the same)
    - .planning/phases/03-web-redesign-rollout/03-UI-SPEC.md (section "Per-Screen Application → 9. Trip detail")
    - web/src/components/ui/DataBar.tsx + Pill.tsx + Tooltip.tsx + SectionCard.tsx + FuelBadge.tsx + Skeleton.tsx + Input.tsx + CTAButton.tsx + Modal.tsx
  </read_first>
  <action>
    Restyle visual layer only (MD-2) and MIRROR the current result page — do not invent a second style.
    Route title: `Origin → Destination` with the `→` in `text-carbon-muted`, `text-display` 700, Space
    Grotesk, NO `font-serif` (D-11). Cost + breakdown: render the total as a mono hero figure
    (`tabular-nums`, 700) on a `SectionCard` (optionally the `bg-carbon-surface3` plate like the result
    page); when toll data is present render the Variant A `DataBar` énergie/péage breakdown sized against
    the total (hide the péage segment AND legend when toll = 0, D-04); keep the réel/≈ estimé `Pill` +
    `Tooltip` exactly as it exists today (Phase 1 — do not change its logic/copy). Meta row: `FuelBadge` +
    distance + duration in mono muted. Note: keep the auto-save textarea/`Input` + useDebounce +
    savedNoteRef pattern (do not change the debounce or PATCH). Archive toggle: if it is icon-only, add an
    `aria-label` (e.g. "Archiver le trajet" / "Désarchiver le trajet" by state) + canonical focus; if it
    is a CTA, use `CTAButton`. "Refaire" + delete: keep the "Refaire" URL build → router.push; delete via
    `Modal` + danger `CTAButton`. Loading: `Skeleton` mirroring the title + hero + breakdown + meta.
    Replace every `font-medium`/`font-semibold` with 400/700 and any `ring-blue-500`/`focus:ring-offset-0`
    with the canonical token. Do NOT change the params resolver, fetch, note PATCH, archive PATCH, refaire
    URL, or delete logic.
  </action>
  <verify>
    <automated>cd web; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - The route title uses `text-display` 700 Space Grotesk with NO `font-serif` (grep `font-serif` → 0).
    - The total renders as a mono `tabular-nums` hero figure; when toll data is present a Variant A `DataBar` breakdown renders, hiding the péage segment + legend when toll = 0.
    - The réel/≈ estimé `Pill` + `Tooltip` is preserved unchanged; the note auto-save (useDebounce + savedNoteRef) and archive/refaire/delete flows are unchanged.
    - Any icon-only control (archive toggle) carries an `aria-label` + canonical focus; loading uses `Skeleton`.
    - Grep `font-medium\|font-semibold` (excluding comments) and `ring-blue-500\|focus:ring-offset-0` over the file → 0.
    - `cd web; npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Trip detail mirrors the result-page data-viz in Space Grotesk (no serif) with the toll badge + all flows preserved.</done>
</task>

<task type="auto">
  <name>Task 3: Favorites restyle — SectionCard row lists, 'Utiliser' action, danger Modal, Skeleton</name>
  <files>web/src/app/app/favorites/page.tsx</files>
  <read_first>
    - web/src/app/app/favorites/page.tsx (FULL — the GET /favorites list, the "Utiliser" deep-link build, the delete confirm Modal, the empty state; all flow logic to PRESERVE)
    - .planning/phases/03-web-redesign-rollout/03-UI-SPEC.md (section "Per-Screen Application → 10. Favorites")
    - web/src/components/ui/SectionCard.tsx + CTAButton.tsx + Eyebrow.tsx + Modal.tsx + Hairline.tsx + Skeleton.tsx (normalized in 03-01)
  </read_first>
  <action>
    Restyle visual layer only (MD-2). Header: `Eyebrow` + `h1` `text-display` 700. List: render each
    favorite as a `SectionCard(padding="none")` row — index/`MapPin` + name (700) + `origin → destination`
    meta (`→` muted) + a "Utiliser" accent action (the existing deep-link build → router.push, preserved).
    Each row/action button gets the canonical focus ring + `bg-carbon-surface2` hover. Delete: `Modal` +
    danger `CTAButton` with confirmation copy. Empty state: keep its copy. Loading: `Skeleton` rows
    mirroring the list. Replace every `font-medium`/`font-semibold` with 400/700 and any `ring-blue-500`/
    `focus:ring-offset-0` with the canonical token.
  </action>
  <verify>
    <automated>cd web; npx tsc --noEmit; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Header is `Eyebrow` + `text-display` 700; rows use `SectionCard` with the "Utiliser" accent action and the deep-link navigation preserved.
    - Row/action buttons use canonical focus + `bg-carbon-surface2` hover; delete uses `Modal` + danger `CTAButton`.
    - Loading uses `Skeleton`; ZERO ad-hoc `animate-pulse` remains.
    - Grep `font-medium\|font-semibold` (excluding comments) and `ring-blue-500\|focus:ring-offset-0` over the file → 0.
    - `cd web; npm run build` compiles all routes with 0 errors and 0 ESLint errors.
  </acceptance_criteria>
  <done>Favorites is restyled as consistent row lists with a danger delete flow — use/delete flows intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| trips/favorites → API | Existing authenticated trip/favorite endpoints — UNCHANGED here (restyle only, MD-2) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-11 | Tampering | note auto-save / archive / delete | accept | No change to PATCH/DELETE payloads, debounce, or ownership checks (server-side) — className/atom swaps only. |
| T-03-12 | Information Disclosure | trip figures / favorites | accept | Only the user's own trips/favorites (already fetched) are shown; no new data surfaced; no secret involved. |
| T-03-SC | Tampering | npm/pip/cargo installs | n/a | No package installs — all atoms/icons already exist. |
</threat_model>

<verification>
- `cd web; npx tsc --noEmit` → 0 errors; `cd web; npm run build` → all routes compile, 0 ESLint errors.
- Grep across the 3 files (excluding comments): NO `font-medium`/`font-semibold`/`font-extrabold`, NO `font-serif`, NO `ring-blue-500`, NO `focus:ring-offset-0`, NO ad-hoc `animate-pulse`.
- Trip detail contains `DataBar` + the preserved réel/≈ estimé `Pill`+`Tooltip`; history filter chips weight-constant across states.
- Manual observable (folded into 03-07 checkpoint): history/detail/favorites feel consistent with the result page; filter/pagination/note/archive/use/delete still work.
</verification>

<success_criteria>
Trips history, trip detail (mirroring the result page in Space Grotesk, no serif), and favorites are
restyled in the editorial-dark language with standardized chips/rows, the toll breakdown + badge,
danger Modals, and Skeleton loading — all flows preserved, green tsc/build.
</success_criteria>

<output>
Create `.planning/phases/03-web-redesign-rollout/03-05-SUMMARY.md` when done.
</output>
