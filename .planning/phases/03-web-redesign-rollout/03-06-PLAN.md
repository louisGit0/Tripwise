---
phase: 03-web-redesign-rollout
plan: 06
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - web/src/app/app/fuel-prices/page.tsx
  - web/src/app/app/settings/page.tsx
autonomous: true
requirements: [WEB-01, WEB-04]
must_haves:
  truths:
    - "Fuel-prices is restyled: Eyebrow + text-display 700 header, a standardized api/custom source toggle (lucide icons replace emoji), Eyebrow section labels, read-only mono price tiles, custom-price inputs + range, accent save CTA, Skeleton loading"
    - "Settings is restyled: Apparence theme picker with weight-constant active/inactive states + canonical focus, Compte section with danger logout CTAButton, the mounted hydration guard preserved and its skeleton built from the Skeleton atom"
    - "All existing behavior preserved: localStorage price read/write + source toggle, theme switching via next-themes, logout (MD-2)"
  artifacts:
    - path: "web/src/app/app/fuel-prices/page.tsx"
      provides: "Restyled fuel-prices — standardized source toggle (lucide), mono read-only tiles, custom inputs, Skeleton"
      contains: "Skeleton"
    - path: "web/src/app/app/settings/page.tsx"
      provides: "Restyled settings — weight-constant theme picker, danger logout, Skeleton mounted-guard"
      contains: "CTAButton"
  key_links:
    - from: "web/src/app/app/fuel-prices/page.tsx"
      to: "the standardized segmented/toggle states + canonical focus"
      via: "api/custom source toggle; emoji → lucide RadioTower/Pencil; font-semibold labels → Eyebrow"
      pattern: "focus-visible:ring-carbon-accent"
    - from: "web/src/app/app/settings/page.tsx"
      to: "the danger CTAButton + weight-constant toggle contract"
      via: "logout = danger CTAButton; theme picker weight constant across states (CWV)"
      pattern: "CTAButton"
---

<objective>
Restyle the settings cluster — fuel-prices `/app/fuel-prices` and settings `/app/settings` — into the
editorial-dark language, inheriting the 03-01 atoms. Visual layer only (MD-2): the localStorage price
read/write + source toggle, the next-themes theme switching (with its mounted hydration guard), and
logout are all preserved. NO serif (D-11).

Purpose: Close out WEB-01 — every remaining web screen wears the language.
Output: Restyled `fuel-prices/page.tsx` and `settings/page.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-web-redesign-rollout/03-UI-SPEC.md
@.planning/phases/03-web-redesign-rollout/03-CONTEXT.md

<interfaces>
import { SectionCard, CTAButton, Eyebrow, Hairline, Skeleton } from '@/components/ui/*';
import { SegmentedControl } from '@/components/ui/SegmentedControl'; // optional for source toggle / theme picker
import { RadioTower, Pencil } from 'lucide-react';                   // replace 📡 / ✏️ emoji on fuel-prices
<!-- Canonical focus token: -->
focus:outline-none focus-visible:ring-2 focus-visible:ring-carbon-accent/50 focus-visible:ring-offset-2 focus-visible:ring-offset-carbon-bg
<!-- Sanctioned: active theme/source = bg-blue-500/10 text-carbon-accent (accent tint). range keeps accent-carbon-accent. -->
<!-- D-11: titles = Space Grotesk text-display 700. NEVER font-serif. Toggle weight CONSTANT across active/inactive (CWV). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fuel-prices restyle — source toggle (lucide), mono read-only tiles, custom inputs, Skeleton</name>
  <files>web/src/app/app/fuel-prices/page.tsx</files>
  <read_first>
    - web/src/app/app/fuel-prices/page.tsx (FULL — the GET /prices/defaults load, the api/custom source toggle + PRICE_SOURCE_KEY localStorage, the read-only "Prix en temps réel" tiles + DeltaBadge, the custom PriceRow inputs + fast-share range, the auto-save + toast, the animate-pulse mount guard; all read/write logic to PRESERVE)
    - .planning/phases/03-web-redesign-rollout/03-UI-SPEC.md (section "Per-Screen Application → 11. Fuel-prices"; "Interaction States" segmented/toggle; "Loading Skeletons")
    - web/src/components/ui/SectionCard.tsx + CTAButton.tsx + Eyebrow.tsx + Skeleton.tsx (normalized in 03-01)
  </read_first>
  <action>
    Restyle visual layer only — keep the prices load, the source toggle + localStorage persistence, the
    auto-save + toast, the PriceRow math, and the range behavior (MD-2). Header: `Eyebrow` + `h1`
    `text-display` 700 + the lede. Source toggle (api/custom): standardize to the segmented/toggle states
    — active `bg-blue-500/10 text-carbon-accent` (sanctioned) OR `bg-carbon-accent text-white`, inactive
    `text-carbon-muted`, hover `text-carbon-ink2 bg-carbon-faint`, canonical focus, and weight CONSTANT
    across states (CWV). Replace the emoji labels `📡` / `✏️` with the lucide icons `RadioTower` /
    `Pencil` for editorial consistency. (Optional: use `SegmentedControl` for the 2-option toggle.) "Prix
    en temps réel" read-only `SectionCard`: 2-col tiles with the price figures in JetBrains Mono
    `tabular-nums` + the existing `DeltaBadge`; normalize the tile label weights to 400/700. "Prix
    personnalisés" `SectionCard`: the `PriceRow` `Input`s + the fast-share `range` (keep
    `accent-carbon-accent` on the range). Convert the `font-semibold` section labels to the `Eyebrow` atom
    (700). Save: an accent `CTAButton` "Enregistrer mes prix" (copy preserved). Loading: replace the
    `animate-pulse` 3-block mount guard with a header-line `Skeleton` + 2 card skeletons mirroring the two
    `SectionCard`s. Replace every remaining `font-medium`/`font-semibold` with 400/700 and any
    `ring-blue-500`/`focus:ring-offset-0` with the canonical token.
  </action>
  <verify>
    <automated>cd web; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Header is `Eyebrow` + `text-display` 700 + lede; the api/custom source toggle uses standardized states + canonical focus, weight constant across active/inactive.
    - The `📡`/`✏️` emoji are replaced by lucide `RadioTower`/`Pencil`; section labels use the `Eyebrow` atom.
    - Read-only price tiles use mono `tabular-nums` + the `DeltaBadge`; the custom range keeps `accent-carbon-accent`; save is the accent `CTAButton`.
    - Loading uses `Skeleton` (header + 2 cards); ZERO ad-hoc `animate-pulse` remains.
    - localStorage source/price read+write + auto-save + toast logic unchanged (MD-2).
    - Grep `font-medium\|font-semibold` (excluding comments) and `ring-blue-500\|focus:ring-offset-0` over the file → 0; grep `📡\|✏️` → 0.
    - `cd web; npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Fuel-prices is restyled with a standardized lucide source toggle, mono tiles, and Skeleton loading — price logic intact.</done>
</task>

<task type="auto">
  <name>Task 2: Settings restyle — weight-constant theme picker, danger logout, Skeleton mounted-guard</name>
  <files>web/src/app/app/settings/page.tsx</files>
  <read_first>
    - web/src/app/app/settings/page.tsx (FULL — the next-themes useTheme + the `mounted` hydration guard, the theme picker buttons, the profile/account section, the logout flow; all logic to PRESERVE)
    - .planning/phases/03-web-redesign-rollout/03-UI-SPEC.md (section "Per-Screen Application → 12. Settings"; "Interaction States"; "Loading Skeletons")
    - web/src/components/ui/SectionCard.tsx + CTAButton.tsx + Eyebrow.tsx + Hairline.tsx + Skeleton.tsx + SegmentedControl.tsx (normalized in 03-01)
  </read_first>
  <action>
    Restyle visual layer only — keep the next-themes wiring, the `mounted` hydration guard, and the logout
    flow (MD-2). Header: `Eyebrow` + `h1` `text-display` 700. "Apparence" `SectionCard`: the dark/light
    theme picker buttons → `font-medium` to `font-normal` (400), weight CONSTANT across active/inactive
    (CWV — active changes color/bg only), add the canonical focus ring, active uses `bg-blue-500/10
    text-carbon-accent` (sanctioned). (Optional: use `SegmentedControl` for the 2-option theme picker.)
    "Compte" `SectionCard`: the profile block (name `font-semibold` → `font-bold`) + a danger `CTAButton`
    "Se déconnecter" wired to the existing logout. Version line in `text-caption` muted. Loading: keep the
    `mounted` guard but rebuild its skeleton shape from the `Skeleton` atom (mirroring the two SectionCards)
    instead of inline `bg-carbon-surface2` divs. Replace every remaining `font-medium`/`font-semibold` with
    400/700 and any `ring-blue-500`/`focus:ring-offset-0` with the canonical token. Do NOT remove the
    `mounted` guard (it prevents the next-themes hydration mismatch).
  </action>
  <verify>
    <automated>cd web; npx tsc --noEmit; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Header is `Eyebrow` + `text-display` 700; the theme picker is weight-constant across active/inactive with the canonical focus ring and the sanctioned `bg-blue-500/10 text-carbon-accent` active state.
    - Logout is a danger `CTAButton` "Se déconnecter" wired to the existing logout; the `mounted` hydration guard is preserved.
    - The mounted-guard loading shape is built from the `Skeleton` atom (no inline `bg-carbon-surface2` placeholder divs).
    - Grep `font-medium\|font-semibold` (excluding comments) and `ring-blue-500\|focus:ring-offset-0` over the file → 0.
    - `cd web; npm run build` compiles all routes with 0 errors and 0 ESLint errors.
  </acceptance_criteria>
  <done>Settings is restyled with a CLS-safe theme picker, danger logout, and Skeleton mounted-guard — theme/logout flows intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage / theme / logout | Existing price localStorage, next-themes preference, and logout — UNCHANGED here (restyle only, MD-2) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-13 | Tampering | fuel-prices localStorage | accept | No change to the price read/write, source key, or PriceRow math — className/atom/icon swaps only; values render as text, never `dangerouslySetInnerHTML`. |
| T-03-14 | Spoofing | logout | accept | Logout flow (cookie clear / redirect) is unchanged; this plan cannot weaken it (no logic touched). |
| T-03-15 | Tampering | next-themes hydration | accept | The `mounted` guard is explicitly preserved → no hydration-mismatch regression; theme switching logic unchanged. |
| T-03-SC | Tampering | npm/pip/cargo installs | n/a | No package installs — lucide-react (RadioTower/Pencil) already a dependency; all atoms exist. |
</threat_model>

<verification>
- `cd web; npx tsc --noEmit` → 0 errors; `cd web; npm run build` → all routes compile, 0 ESLint errors.
- Grep across both files (excluding comments): NO `font-medium`/`font-semibold`/`font-extrabold`, NO `font-serif`, NO `ring-blue-500`, NO `focus:ring-offset-0`, NO ad-hoc `animate-pulse`; fuel-prices has NO `📡`/`✏️`.
- Settings keeps the `mounted` guard; logout is a danger `CTAButton`; theme picker + source toggle are weight-constant across states.
- Manual observable (folded into 03-07 checkpoint): both screens feel consistent with the app; theme switch (no hydration flash), price save, and logout still work.
</verification>

<success_criteria>
Fuel-prices and settings are restyled in the editorial-dark language with standardized weight-constant
toggles, lucide iconography, danger logout, and Skeleton loading — completing WEB-01 across all web
screens — with price/theme/logout flows preserved, green tsc/build.
</success_criteria>

<output>
Create `.planning/phases/03-web-redesign-rollout/03-06-SUMMARY.md` when done.
</output>
