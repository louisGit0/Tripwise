---
phase: 09-ios-app-store-release
plan: 05
type: execute
wave: 2
depends_on: []
files_modified:
  - .planning/phases/09-ios-app-store-release/ASSETS.md
  - .planning/phases/09-ios-app-store-release/METADATA.md
  - .planning/phases/09-ios-app-store-release/RUNBOOK.md
autonomous: true
requirements: [REL-01]
tags: [ios-release, docs, runbook]
must_haves:
  truths:
    - "ASSETS.md tells the user exactly which screenshots to capture (5 screens × 6.7\" iPhone, FR copy)"
    - "METADATA.md is a paste-ready App Store Connect listing (FR) + reviewer demo account spec + privacy nutrition answers"
    - "RUNBOOK.md is a numbered checklist from 'enroll Apple Dev' through 'click Submit for Review'"
  artifacts:
    - path: .planning/phases/09-ios-app-store-release/ASSETS.md
      provides: "Icon check command + 5-shot screenshot brief"
    - path: .planning/phases/09-ios-app-store-release/METADATA.md
      provides: "Listing draft + reviewer credentials spec + privacy nutrition mapping"
    - path: .planning/phases/09-ios-app-store-release/RUNBOOK.md
      provides: "Step-by-step submission runbook"
  key_links: []
---

<objective>
Produce the three submission documents that drive the user-owned waves (09-06, 09-07). All
content already exists in 09-CONTEXT.md (D-44, D-45) and 09-RESEARCH.md — this plan consolidates
it into paste-ready docs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
**Source:** 09-CONTEXT.md (D-44 = listing draft; D-45 = wave order; "App Privacy" section = nutrition labels) and 09-RESEARCH.md (Pitfall 1 = icon alpha check; Pitfall 7 = reviewer demo account). No new content invented; this plan re-shapes existing decisions into operator docs.

**No code edits.** Pure documentation. All three files live in `.planning/phases/09-…/` (not committed to `mobile/assets/` — they are submission instructions, not app-bundled assets).
</context>

<tasks>

<task type="auto">
  <name>Task 1: ASSETS.md — icon check + screenshot brief</name>
  <files>.planning/phases/09-ios-app-store-release/ASSETS.md</files>
  <action>
    Write ASSETS.md with: (1) Icon verification — command `file mobile/assets/images/icon.png` must show 1024×1024 RGB (no alpha); if alpha, flatten on opaque background; explicit re-export instruction. (2) Screenshot brief — 5 FR screenshots, primary set 6.7" iPhone (1290×2796 iPhone 15/16 Pro Max), secondary set 6.5" iPhone (1242×2688 iPhone 11 Pro Max). Per D-44: (a) Dashboard avec calcul Paris→Lyon en cours, (b) Résultat trajet avec coût total + DataBar énergie/péage + AnimatedCounter, (c) Garage avec photo véhicule, (d) Showroom catalogue avec recherche + photos, (e) Paramètres. Seeded data — no Lorem. For each shot: device, scene, what data must be visible, what to avoid (no real PII, no draft watermark). (3) Capture method — TestFlight build on physical iPhone preferred; iOS Simulator at exact device size acceptable. (4) Export — PNG, no alpha, exact pixel dimensions; save to `.planning/phases/09-…/assets/screenshots/`.
  </action>
  <verify>
    <automated>test -f .planning/phases/09-ios-app-store-release/ASSETS.md &amp;&amp; grep -q "1290.2796" .planning/phases/09-ios-app-store-release/ASSETS.md &amp;&amp; grep -q "Paris.Lyon" .planning/phases/09-ios-app-store-release/ASSETS.md</automated>
  </verify>
  <done>ASSETS.md exists with icon check + 5-shot brief + device dimensions.</done>
</task>

<task type="auto">
  <name>Task 2: METADATA.md — listing + reviewer + nutrition labels</name>
  <files>.planning/phases/09-ios-app-store-release/METADATA.md</files>
  <action>
    Write METADATA.md with three sections.
    (A) App Store Connect listing (FR-only) — copy verbatim from D-44: Name "verygoodtrip"; Subtitle "Coût réel d'un trajet voiture" (≤30 chars); Primary category Travel, Secondary Navigation; Age 4+; Keywords "trajet,voiture,péage,carburant,coût,essence,diesel,électrique,EV,GPL" (≤100 chars); Promotional text "Calculez le coût total de votre trajet en voiture (carburant + péages) en quelques secondes. Trajets, favoris, statistiques." (≤170 chars); Description — draft a 4-paragraph FR description covering core value, tolls real-or-estimated, multi-energy (gas/diesel/EV/GPL), shared catalog of thousands of real-consumption models; Support URL = https://verygoodtrip.vercel.app/privacy; Privacy Policy URL = same. NO App Preview video v1.0.
    (B) Reviewer demo account spec — Email `apple-review@verygoodtrip.test` (the user creates via /register against prod backend), strong password the user picks; seeded server-side with: one EV vehicle (e.g., Tesla Model 3) marked default, one gas vehicle, two saved trips (Paris→Lyon, home→work pattern). Include FR + EN Reviewer Notes per D-44: tolls labelled as estimates, energy uses ADEME/EPA reference consumption + Mapbox routing, third-party data is public open data (data.gouv.fr, Opendatasoft) or the user's own configuration; onboarding tour fires once per account ("Press Passer to dismiss").
    (C) App Privacy nutrition labels — map from 09-CONTEXT "App Privacy" section: Contact info → Email Address (Linked, App Functionality, NO tracking); User Content → vehicles/trips/favorites/display_name (Linked, App Functionality, NO tracking); Identifiers → NONE for advertising; "Data Not Used to Track You"; "Data Linked to You" for the above. Third parties (functional, not advertising): Mapbox, CarImages, data.gouv/Opendatasoft, Render+Vercel.
  </action>
  <verify>
    <automated>test -f .planning/phases/09-ios-app-store-release/METADATA.md &amp;&amp; grep -q "verygoodtrip" .planning/phases/09-ios-app-store-release/METADATA.md &amp;&amp; grep -q "apple-review" .planning/phases/09-ios-app-store-release/METADATA.md &amp;&amp; grep -q "Mapbox" .planning/phases/09-ios-app-store-release/METADATA.md</automated>
  </verify>
  <done>METADATA.md exists with the 3 sections; all D-44 fields present verbatim.</done>
</task>

<task type="auto">
  <name>Task 3: RUNBOOK.md — numbered submission checklist</name>
  <files>.planning/phases/09-ios-app-store-release/RUNBOOK.md</files>
  <action>
    Write RUNBOOK.md as a single numbered checklist the user works through. Sections:
    (1) Apple Developer Program — enroll at developer.apple.com ($99/yr), verify account is "Active" not "Pending Identity Verification", record Apple Team ID from Membership page.
    (2) App Store Connect — search globally for "verygoodtrip" name availability (Pitfall 8); create App Record (Platform iOS, Bundle ID com.verygoodtrip.app, Primary lang FR, SKU = bundle id, User Access Full); record numeric ascAppId from the URL.
    (3) ASC API key — Users and Access → Integrations → Keys → "+", role "App Manager", download AuthKey_<KEY_ID>.p8 (one-time only — store securely outside git); record Key ID + Issuer ID.
    (4) EAS env vars — `cd mobile && eas login`; `eas env:create --environment production` for each of EXPO_PUBLIC_API_URL (prod Render URL, /api/v1), EXPO_PUBLIC_MAPBOX_TOKEN (public, domain-restricted to com.verygoodtrip.app in Mapbox dashboard), MAPBOX_DOWNLOAD_TOKEN (DOWNLOADS:READ scope, secret); verify with `eas env:list --environment production`.
    (5) Fill eas.json — replace all 5 `TO_FILL_IN_09_06` sentinels with the recorded values (this happens in plan 09-06); place AuthKey_*.p8 in `mobile/` (gitignored by 09-04).
    (6) Production build — `npx eas-cli build --profile production --platform ios`; expect ~25-40 min cloud build; record build URL.
    (7) TestFlight smoke — install build via TestFlight on a physical iPhone (Apple Sign In cert chain requires real device); golden path: signup → login → calc Paris→Lyon → save trip → favorites → vehicle photos → tolls breakdown → onboarding replay from Settings → pseudo edit → account deletion → re-login fails with 401. Document any blocker.
    (8) Submission — `npx eas-cli submit --profile production --platform ios --latest`; in ASC: upload 5 screenshots (paste from METADATA + ASSETS), fill listing fields (paste from METADATA), answer App Privacy nutrition (paste from METADATA), confirm export compliance pre-answered (Pitfall 4), enter reviewer demo credentials in App Review Information (paste from METADATA reviewer notes FR+EN); click "Submit for Review".
    (9) Wait — expect 24-48h review; respond to any Apple Review notes by reopening Phase 9 as a gap-closure if rejection lands.
    Add a one-line warning header: "Steps 1-5 + 7-9 are user-owned; the agent owns step 6 only via the user's `eas` CLI session and step 5's eas.json edit."
  </action>
  <verify>
    <automated>test -f .planning/phases/09-ios-app-store-release/RUNBOOK.md &amp;&amp; grep -q "AuthKey" .planning/phases/09-ios-app-store-release/RUNBOOK.md &amp;&amp; grep -q "TestFlight" .planning/phases/09-ios-app-store-release/RUNBOOK.md &amp;&amp; grep -q "Submit for Review" .planning/phases/09-ios-app-store-release/RUNBOOK.md</automated>
  </verify>
  <done>RUNBOOK.md exists with 9 numbered sections covering enrollment → submission → wait.</done>
</task>

</tasks>

<threat_model>

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-05-01 | Information Disclosure | reviewer demo credentials in docs | mitigate | RUNBOOK + METADATA reference the demo account but do NOT contain the password; user enters it directly into ASC. |
| T-09-05-02 | Repudiation | listing copy drift from app behavior | mitigate | Description copy matches the app's actual behavior (tolls "réel ou estimé", energy real source-attributed) — Apple Review cross-checks. |
| T-09-05-SC | Tampering | npm/pip/cargo installs | mitigate | No installs — N/A. |

</threat_model>

<verification>
- All 3 files exist under `.planning/phases/09-ios-app-store-release/`
- Each grep gate in each task passes
- No code changes; no commits to `mobile/` or `web/` from this plan
</verification>

<success_criteria>
The user can execute 09-06 + 09-07 by following RUNBOOK.md step-by-step, pasting from METADATA.md and ASSETS.md.
</success_criteria>

<output>
Create `.planning/phases/09-ios-app-store-release/09-05-SUMMARY.md` when done.
</output>
