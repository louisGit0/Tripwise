---
phase: 09-ios-app-store-release
plan: 04
type: execute
wave: 2
depends_on: []
files_modified:
  - mobile/app.config.ts
  - mobile/eas.json
  - mobile/.gitignore
autonomous: true
requirements: [REL-01]
tags: [ios-release, app-config, privacy-manifest, eas]
must_haves:
  truths:
    - "mobile tsc --noEmit GREEN after the edits"
    - "app.config.ts declares `ios.config.usesNonExemptEncryption = false` AND `ios.infoPlist.ITSAppUsesNonExemptEncryption = false`"
    - "app.config.ts declares `ios.privacyManifests` with NSPrivacyTracking=false and the 4 collected data types (email, name, userId, otherUserContent) — all linked, none used for tracking, all App Functionality"
    - "app.config.ts has `ios.supportsTablet = false` (phone-only v1.0)"
    - "eas.json submit.production.ios block exists with placeholder strings for appleId, ascAppId, appleTeamId, ascApiKeyPath, ascApiKeyId, ascApiIssuerId — clearly marked TO FILL"
    - "mobile/.gitignore excludes AuthKey_*.p8 (and any *.p8 in mobile/)"
    - "no secrets committed; no console.log added"
  artifacts:
    - path: mobile/app.config.ts
      provides: "Hardened iOS config (encryption, privacy manifest, supportsTablet=false, infoPlist)"
      contains: "privacyManifests"
    - path: mobile/eas.json
      provides: "Production submit.ios block with ASC API key placeholders"
      contains: "ascApiKeyPath"
    - path: mobile/.gitignore
      provides: "*.p8 / AuthKey_*.p8 ignore rules"
      contains: "AuthKey"
  key_links:
    - from: "app.config.ts ios.privacyManifests"
      to: "PrivacyInfo.xcprivacy in the built .ipa"
      via: "Expo CNG prebuild — writes the manifest from this declarative config"
      pattern: "privacyManifests:"
    - from: "eas.json submit.production.ios.ascApiKeyPath"
      to: "AuthKey_<KEY_ID>.p8 file (provided by user in 09-06)"
      via: "eas submit reads the file at build time"
      pattern: "ascApiKeyPath"
---

<objective>
Harden the mobile config for App Store submission. Two App Review traps closed:
- **Pitfall 4 (export compliance loop)** — `usesNonExemptEncryption = false` answers the question in code once and for all (D-38).
- **Pitfall 2 (missing privacy manifest for known SDKs)** — `ios.privacyManifests` declares the 4 collected data types and `NSPrivacyTracking: false`.
Plus D-42 (`supportsTablet: false`, phone-only v1.0) and D-43-adjacent prep (eas.json submit
block with ASC API key placeholders, `.p8` `.gitignore`).

Purpose: REL-01 — these are the only code/config changes that need to land BEFORE the production
build runs, and they are independent of the account-deletion + privacy-page surfaces (Wave 2
parallel).
Output: a buildable, App-Review-trap-resistant mobile config + a fillable submit block.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
**Locked decisions:** D-37 (production env vars via EAS, NOT committed), D-38 (encryption flag +
infoPlist), D-42 (no iPad), D-43 (ASC API key, not Apple-ID password).

**Working-tree state (uncommitted):** `mobile/app.config.ts` already has the START of the harden
(supportsTablet: false + ITSAppUsesNonExemptEncryption: false in infoPlist — verified by
`git diff`). `mobile/eas.json` already has the START (`production.environment: "production"`).
This plan **finishes** the harden — privacy manifests, the redundant `ios.config.usesNonExemptEncryption`,
and the submit block.

**App.config.ts — final ios block shape (per 09-RESEARCH Pattern 1, only iOS edits):**
- Keep all existing top-level keys (`name`, `slug`, `version`, `scheme`, `orientation`, `icon`,
  `userInterfaceStyle`, `newArchEnabled`, `android`, `web`, `plugins`, `experiments`, `extra`)
  EXACTLY as-is (MD-2 — no behavioral drift for android/web/plugins).
- `ios`:
  - `supportsTablet: false` (keep — already in tree)
  - `bundleIdentifier: 'com.verygoodtrip.app'` (keep)
  - `usesAppleSignIn: true` (keep)
  - `config: { usesNonExemptEncryption: false }` **NEW** — written to `ITSAppUsesNonExemptEncryption`
    in Info.plist by Expo CNG; declares export-compliance answer in code.
  - `infoPlist: { ITSAppUsesNonExemptEncryption: false }` (keep — already in tree; redundant with
    `ios.config.usesNonExemptEncryption` but Apple occasionally checks both, low cost.)
  - `privacyManifests: { NSPrivacyTracking: false, NSPrivacyTrackingDomains: [], NSPrivacyCollectedDataTypes: [ … 4 entries … ], NSPrivacyAccessedAPITypes: [] }` **NEW**
    — the 4 entries are EmailAddress, Name, UserID, OtherUserContent, each
    `NSPrivacyCollectedDataTypeLinked: true`, `NSPrivacyCollectedDataTypeTracking: false`,
    `NSPrivacyCollectedDataTypePurposes: ['NSPrivacyCollectedDataTypePurposeAppFunctionality']`.
    Verbatim shape from 09-RESEARCH Pattern 1. `NSPrivacyAccessedAPITypes: []` relies on Expo SDK
    54's bundled baseline for `expo-secure-store` / `react-native-reanimated` /
    `@rnmapbox/maps` (Assumptions A4/A5 — verify post-build by extracting `PrivacyInfo.xcprivacy`
    from the artifact; not a blocking gate for the plan).
- NO new permission strings (no Camera / Location / Mic / Photos / Contacts — none used).

**eas.json — final shape:**
- Keep `cli.version` and `build.development` / `build.preview` / `build.production` blocks
  exactly as-is (`production.environment: "production"` already added, keep).
- `submit.production.ios` (currently the JSON block exists per the file; verify by reading) —
  fill with TO-BE-FILLED placeholder strings:
  ```json
  "submit": {
    "production": {
      "ios": {
        "appleId": "louissoudy2@gmail.com",
        "ascAppId": "TO_FILL_IN_09_06",
        "appleTeamId": "TO_FILL_IN_09_06",
        "ascApiKeyPath": "./AuthKey_TO_FILL_IN_09_06.p8",
        "ascApiKeyId": "TO_FILL_IN_09_06",
        "ascApiIssuerId": "TO_FILL_IN_09_06"
      }
    }
  }
  ```
  The placeholders are intentional sentinels; 09-06 (USER CHECKPOINT) replaces them with the
  values the user provides. The grep gate in 09-06 verifies all `TO_FILL_IN_09_06` are gone before
  the production build.

**mobile/.gitignore** — append `AuthKey_*.p8` and `*.p8` if not already present. Verify the file
exists; if it doesn't, fall back to repo-root `.gitignore` with a `mobile/AuthKey_*.p8` entry.

**Verification approach:** mobile `tsc --noEmit` GREEN; static grep for the privacy manifest keys
and submit placeholders. Do NOT run `npx eas-cli build` — that's 09-07.
</context>

<tasks>

<task type="auto">
  <name>Task 1: app.config.ts — privacyManifests + ios.config.usesNonExemptEncryption</name>
  <files>mobile/app.config.ts</files>
  <action>
    Edit ONLY the `ios` block. Keep `supportsTablet: false`, `bundleIdentifier`, `usesAppleSignIn`,
    and `infoPlist.ITSAppUsesNonExemptEncryption: false` as already-present in the working tree.
    Add a sibling `config: { usesNonExemptEncryption: false }` to the `ios` block (the
    `ios.config` key — Expo's typed surface for App Store Connect attributes; mirrors the Info.plist
    answer). Add a sibling `privacyManifests` block per 09-RESEARCH Pattern 1 with EXACTLY the 4
    `NSPrivacyCollectedDataTypes` entries (EmailAddress, Name, UserID, OtherUserContent), each with
    `NSPrivacyCollectedDataTypeLinked: true`, `Tracking: false`, `Purposes: ['…AppFunctionality']`;
    `NSPrivacyTracking: false`; `NSPrivacyTrackingDomains: []`; `NSPrivacyAccessedAPITypes: []`.
    All other top-level keys (android, web, plugins, experiments, extra) UNCHANGED. The
    `ExpoConfig` import already typed everything — if a key isn't accepted by the type, prefer
    casting the `ios` literal as `ExpoConfig['ios']` over weakening the file's strictness.
  </action>
  <verify>
    <automated>cd mobile &amp;&amp; npx tsc --noEmit &amp;&amp; grep -q "usesNonExemptEncryption" app.config.ts &amp;&amp; grep -q "NSPrivacyCollectedDataTypeEmailAddress" app.config.ts &amp;&amp; grep -q "NSPrivacyCollectedDataTypeOtherUserContent" app.config.ts</automated>
  </verify>
  <done>tsc clean; iOS block carries ios.config.usesNonExemptEncryption, supportsTablet:false, the 4-entry privacyManifests; android/web/plugins untouched.</done>
</task>

<task type="auto">
  <name>Task 2: eas.json submit placeholders + .gitignore .p8</name>
  <files>mobile/eas.json, mobile/.gitignore</files>
  <action>
    eas.json: ensure `submit.production.ios` exists with the 6 fields per the context block — appleId="louissoudy2@gmail.com", the other 5 set to the literal `TO_FILL_IN_09_06` sentinel (or `./AuthKey_TO_FILL_IN_09_06.p8` for the path). Do NOT touch build profiles. mobile/.gitignore: append `AuthKey_*.p8` and `*.p8` (idempotent — skip if already present). Validate eas.json is valid JSON.
  </action>
  <verify>
    <automated>cd mobile &amp;&amp; node -e "JSON.parse(require('fs').readFileSync('eas.json'))" &amp;&amp; grep -q "ascApiKeyPath" eas.json &amp;&amp; grep -q "AuthKey" .gitignore</automated>
  </verify>
  <done>eas.json submit block has 6 ios fields with TO_FILL_IN_09_06 sentinels; .gitignore excludes .p8 files; JSON parses.</done>
</task>

</tasks>

<threat_model>

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04-01 | Information Disclosure | .p8 ASC API key in repo | mitigate | .gitignore excludes AuthKey_*.p8 and *.p8 before the file ever lands. |
| T-09-04-02 | Repudiation | privacy manifest accuracy | mitigate | 4 declared data types match the App Privacy nutrition labels (D-CONTEXT app privacy section) — single source of truth. |
| T-09-04-03 | Tampering | placeholder sentinels reach prod build | mitigate | 09-06 grep gate blocks production build if any `TO_FILL_IN_09_06` remains in eas.json. |
| T-09-04-SC | Tampering | npm/pip/cargo installs | mitigate | No installs — N/A. |

</threat_model>

<verification>
- mobile tsc --noEmit GREEN
- grep `privacyManifests` app.config.ts → 1; `NSPrivacyCollectedDataType` → 4 (one per data type)
- grep `ascApiKeyPath` eas.json → 1; `TO_FILL_IN_09_06` ≥ 5
- grep `AuthKey` .gitignore → ≥ 1
- node JSON.parse on eas.json succeeds
</verification>

<success_criteria>
Config is App-Review-trap-resistant (Pitfalls 2 + 4 + iPad screenshot trap closed) and ready for 09-06 to fill the submit credentials.
</success_criteria>

<output>
Create `.planning/phases/09-ios-app-store-release/09-04-SUMMARY.md` when done.
</output>