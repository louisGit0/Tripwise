---
phase: 09-ios-app-store-release
plan: 03
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - mobile/app/(tabs)/settings.tsx
  - mobile/src/i18n/translations/fr.ts
  - mobile/src/i18n/translations/en.ts
autonomous: true
requirements: [REL-01]
tags: [ios-release, account-deletion, apple-guideline-5-1-1, mobile]
must_haves:
  truths:
    - "A logged-in mobile user can delete their account from Settings via a confirm Alert.alert"
    - "After successful DELETE /users/me the local SecureStore token is cleared and the app routes to (auth)"
    - "A failed DELETE shows a Toast error and leaves the session intact"
    - "FR + EN strings exist for: button label, confirm title, confirm body, confirm CTA, cancel, success toast, error toast"
  artifacts:
    - path: mobile/app/(tabs)/settings.tsx
      provides: "Editorial-dark 'Zone de danger' SectionCard with destructive Button + Alert.alert confirm + DELETE /users/me + signOut()"
      contains: "handleDeleteAccount"
    - path: mobile/src/i18n/translations/fr.ts
      provides: "settings.deleteAccount.* keys (FR)"
      contains: "deleteAccount"
    - path: mobile/src/i18n/translations/en.ts
      provides: "settings.deleteAccount.* keys (EN)"
      contains: "deleteAccount"
  key_links:
    - from: "settings.tsx handleDeleteAccount"
      to: "backend DELETE /users/me"
      via: "client.delete('/users/me')"
      pattern: "client\\.delete\\('/users/me'\\)"
    - from: "settings.tsx post-delete"
      to: "AuthContext.signOut"
      via: "const { signOut } = useAuth()"
      pattern: "signOut\\(\\)"
---

<objective>
Land the **mobile** half of D-41 (account deletion). Apple Guideline 5.1.1 (v) requires Sign in
with Apple apps to provide in-app account deletion; mobile is the surface Apple actually reviews.

Purpose: REL-01 — without a functional in-app delete on the iPhone the reviewer holds, the
submission is rejected.
Output: a destructive "Supprimer mon compte" entry in `(tabs)/settings.tsx` mirroring the existing
logout pattern (Alert.alert confirm → `client.delete('/users/me')` → `signOut()` → routes to `(auth)`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
**Locked decisions:** D-41 (delete is hard blocker), D-11 (no serif), D-18→D-20 (editorial-dark
tokens). The pattern mirrors the existing mobile **logout** flow already in `settings.tsx`:
- A `SectionCard` block (editorial `surface`/`hairline`/`Radius.card`).
- A destructive `Button` from `@/components/ui/Button` with `variant="destructive"` (token
  `fuelGas` background, white label) — same atom the current logout button uses.
- `Alert.alert(title, body, [cancel, confirm])` with `confirm.style: 'destructive'`.
- A success-path `signOut()` from `useAuth()` that the AuthContext already wires to clear
  `expo-secure-store` and route to `(auth)`. Post-delete behavior is identical to logout — no
  extra navigation calls needed.

**No new dependency.** **No serif.** Editorial tokens only (`bg`/`ink`/`mutedText`/`hairline`/
`fuelGas`/`accent`). The `client` axios instance from `mobile/src/api/client.ts` already injects
the Authorization header from SecureStore (same instance used by the pseudo edit in 06-03).

**Working-tree state (uncommitted):** none for this file — start fresh from the current
`settings.tsx` checked-in version. Place the new block AFTER the Account section and AFTER the
logout button, so destructive actions read as a clearly-separated "Zone de danger".

**No Toast spam during signOut:** on success, do NOT fire a success Toast — the screen unmounts
immediately because `signOut()` triggers the AuthContext segments-guard to route the user to
`(auth)`. A success Toast on an unmounting screen flickers. The error case keeps a Toast since
the user remains on the Settings screen.

**i18n contract (symmetric FR + EN):**
- `settings.deleteAccount.button` — "Supprimer mon compte" / "Delete my account"
- `settings.deleteAccount.confirmTitle` — "Supprimer définitivement votre compte ?" / "Permanently delete your account?"
- `settings.deleteAccount.confirmBody` — "Vos véhicules, trajets et favoris seront supprimés. Cette action est irréversible." / "Your vehicles, trips and favorites will be deleted. This action cannot be undone."
- `settings.deleteAccount.confirmCta` — "Supprimer" / "Delete"
- `settings.deleteAccount.cancel` — reuse `common.cancel` if present, else add "Annuler" / "Cancel"
- `settings.deleteAccount.error` — "Suppression impossible. Réessayez plus tard." / "Could not delete your account. Please try again."
- `settings.deleteAccount.sectionTitle` — "Zone de danger" / "Danger zone" (Eyebrow surtitle)

Pre-existing logout/i18n keys (`settings.logout`, `settings.logoutConfirm`) stay untouched (MD-2).
</context>

<tasks>

<task type="auto">
  <name>Task 1: i18n strings (FR + EN, symmetric)</name>
  <files>mobile/src/i18n/translations/fr.ts, mobile/src/i18n/translations/en.ts</files>
  <action>
    Add a `settings.deleteAccount` namespace to BOTH translation files with the 7 keys above
    (button, confirmTitle, confirmBody, confirmCta, cancel, error, sectionTitle). Keep alphabetical
    nesting consistent with the existing `settings.*` block. Do NOT touch any other key (MD-2).
    Symmetric — every FR key MUST have an EN twin and vice-versa.
  </action>
  <verify>
    <automated>cd mobile &amp;&amp; npx tsc --noEmit &amp;&amp; node -e "const fr=require('./src/i18n/translations/fr').default; const en=require('./src/i18n/translations/en').default; const f=Object.keys(fr.settings.deleteAccount).sort().join(','); const e=Object.keys(en.settings.deleteAccount).sort().join(','); if(f!==e) {console.error('FR/EN drift:',f,'vs',e); process.exit(1)}"</automated>
  </verify>
  <done>tsc clean; both FR and EN expose an identical `settings.deleteAccount` key set.</done>
</task>

<task type="auto">
  <name>Task 2: Settings — destructive "Supprimer mon compte" block</name>
  <files>mobile/app/(tabs)/settings.tsx</files>
  <action>
    AFTER the existing Account/logout `SectionCard`, append a new `SectionCard` whose `title` prop
    is an `Eyebrow` reading `t('settings.deleteAccount.sectionTitle')`. Inside: a `Text` with the
    editorial `mutedText` color giving the explainer (reuse the i18n `confirmBody` copy or add a
    short `settings.deleteAccount.explainer` if cleaner — keep FR/EN symmetric if you do). Then a
    destructive `Button` (`variant="destructive"`) labeled `t('settings.deleteAccount.button')`
    with `loading={isDeleting}`. Its `onPress` calls a local `handleDeleteAccount` that:

    1. Opens `Alert.alert(t('settings.deleteAccount.confirmTitle'), t('settings.deleteAccount.confirmBody'), [{ text: t('settings.deleteAccount.cancel'), style: 'cancel' }, { text: t('settings.deleteAccount.confirmCta'), style: 'destructive', onPress: confirmDelete }])`.
    2. `confirmDelete` sets `setIsDeleting(true)`, then `try { await client.delete('/users/me'); await signOut(); } catch { Toast.show({ type: 'error', text1: t('settings.deleteAccount.error') }); setIsDeleting(false); }`.
    3. NO success Toast (the AuthContext routes the user to `(auth)` immediately on signOut).

    `client` is the existing axios instance from `@/api/client` (the same one `handleSaveName`
    uses for `PATCH /users/me`). `signOut` comes from the existing `useAuth()` destructure already
    on the screen (`const { signOut } = useAuth()`). Reuse the same destructive token (`fuelGas`)
    the logout button already uses — do not introduce a new color. Do NOT modify the existing
    theme/language/pseudo/logout/version sections (MD-2). NO serif. NO blue literals.
  </action>
  <verify>
    <automated>cd mobile &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <done>Settings screen shows a clearly-separated "Zone de danger" SectionCard with a working destructive Button that confirms via Alert and signs the user out on success; tsc clean; theme/pseudo/logout flows untouched.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| iOS app → /api/v1/users/me | authenticated DELETE; Authorization header injected by the axios `client` interceptor reading SecureStore |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03-01 | Tampering | accidental tap | mitigate | Alert.alert with `style:'destructive'` two-step confirm; Button `loading` prevents double-fire. |
| T-09-03-02 | Elevation of Privilege | id from request | mitigate | Backend (09-01) derives the id from `@CurrentUser()` — the client cannot delete another account by tampering the request. |
| T-09-03-03 | Information Disclosure | error Toast leakage | accept | Error copy is generic ("Suppression impossible…"); does not echo server error text. |
| T-09-03-04 | Spoofing | post-delete stale token | mitigate | `signOut()` clears SecureStore; AuthContext segments-guard re-routes to `(auth)` synchronously. |
| T-09-03-SC | Tampering | npm/pip/cargo installs | mitigate | No installs — N/A. |

</threat_model>

<verification>
- mobile `npx tsc --noEmit` GREEN
- grep `client.delete('/users/me')` in settings.tsx → exactly 1
- grep `signOut()` in settings.tsx → at least 2 (one for the existing logout, one for the new delete-success path)
- grep `font-serif` / `Instrument` / hardcoded blue (`#3b82f6` / `#2563eb`) in the 3 files → 0
- FR/EN symmetric `settings.deleteAccount.*` (programmatic check in Task 1 verify)
</verification>

<success_criteria>
- Apple Reviewer, logging in with the demo account, can delete that account via Settings → confirm → 204 → logged out → routes to login.
- No regression to logout, pseudo edit, theme picker, or language switcher.
</success_criteria>

<output>
Create `.planning/phases/09-ios-app-store-release/09-03-SUMMARY.md` when done.
</output>
