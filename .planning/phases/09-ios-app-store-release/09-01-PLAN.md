---
phase: 09-ios-app-store-release
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - backend/src/users/users.controller.ts
  - backend/src/users/users.service.ts
  - backend/test/users.e2e-spec.ts
autonomous: true
requirements: [REL-01]
tags: [ios-release, account-deletion, apple-guideline-5-1-1, backend]
must_haves:
  truths:
    - "JWT-authed user can DELETE /api/v1/users/me and receives 204"
    - "After deletion, the same JWT on GET /auth/me returns 401"
    - "All cascaded data (user_vehicles, favorites, trips) is gone for that user"
    - "Calling DELETE /users/me without a token returns 401 (never 204)"
  artifacts:
    - path: backend/src/users/users.controller.ts
      provides: "@Delete('me') @UseGuards(JwtAuthGuard) @HttpCode(204) handler"
      contains: "deleteMe"
    - path: backend/src/users/users.service.ts
      provides: "deleteAccount(userId): Promise<void>"
      contains: "deleteAccount"
    - path: backend/test/users.e2e-spec.ts
      provides: "DELETE /users/me e2e describe block — 204, 401-no-token, post-delete /auth/me → 401"
      contains: "DELETE /api/v1/users/me"
  key_links:
    - from: "UsersController.deleteMe"
      to: "UsersService.deleteAccount"
      via: "constructor injection"
      pattern: "this\\.usersService\\.deleteAccount"
    - from: "UsersService.deleteAccount"
      to: "TypeORM Repository<User>.delete"
      via: "userRepo.delete(userId)"
      pattern: "userRepo\\.delete\\("
    - from: "User entity"
      to: "user_vehicles / favorites / trips rows"
      via: "FK ON DELETE CASCADE (already wired at schema time)"
      pattern: "onDelete: 'CASCADE'"
---

<objective>
Ship the backend half of the Apple Guideline 5.1.1 (v) **in-app account deletion** requirement
(D-41). Without this, App Review rejects the submission on sight because Sign in with Apple is
offered.

Purpose: unblock the web (09-02) and mobile (09-03) UI surfaces; satisfy REL-01 on the server side.
Output: `DELETE /api/v1/users/me` (JWT-guarded, IDOR-safe, 204) covered by 3+ e2e cases.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
**Locked decisions in play:** D-41 (account deletion is a HARD App Review blocker).

**Working-tree state (uncommitted at plan-write time):** the implementation and e2e are already
drafted in the working tree (`git status`: `M backend/src/users/users.service.ts`,
controller untracked changes already present, e2e block exists at lines 179+). This plan's job is
to VERIFY the draft is correct, that e2e passes, and to land it as one clean commit on `master`.
Treat the in-tree draft as the RED→GREEN result of a previous TDD pass; if it already verifies,
the RED step is satisfied historically.

**Cascade safety:** `User → UserVehicle / Favorite / Trip` FKs are already `onDelete: 'CASCADE'`
(verified at entity creation time; do NOT add new migrations). A single `userRepo.delete(userId)`
collapses the user and every dependent row.

**IDOR safety:** the target id MUST come exclusively from `@CurrentUser().id` — never accept
`:id` / `userId` from the request. Mirror exactly the `PATCH /users/me` IDOR pattern (D-26).

**Auth e2e harness:** mirror the existing `users.e2e-spec.ts` SQLite in-memory pattern that
`PATCH /users/me` and `POST /auth/login` already use (no new harness).
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Verify (or re-establish) RED — DELETE /users/me e2e</name>
  <files>backend/test/users.e2e-spec.ts</files>
  <behavior>
    - 401 when called with no Authorization header
    - 204 (No Content) when called with a valid JWT for an existing user
    - Follow-up GET /auth/me with the same JWT → 401 (user record gone)
    - Re-attempting DELETE /users/me with the same JWT after deletion → 401
  </behavior>
  <action>
    Open the describe('DELETE /api/v1/users/me', ...) block already present at line ~181. Confirm
    it covers all 4 behaviors above; if any are missing, add them mirroring the PATCH /users/me
    e2e style (supertest, in-memory SQLite, register → login → use returned token). Do NOT change
    the harness setup. If the file is in the historical RED state (impl absent), run
    `npx jest --config test/jest-e2e.json --testPathPatterns=users --forceExit` to confirm RED;
    if impl already landed (working tree shows it), proceed to Task 2 verification.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; npx jest --config test/jest-e2e.json --testPathPatterns=users --forceExit</automated>
  </verify>
  <done>Test file declares the 4 behaviors and the suite either fails on missing endpoint (RED) or passes against the impl (GREEN).</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Verify GREEN — controller + service land cleanly + full e2e suite green</name>
  <files>backend/src/users/users.controller.ts, backend/src/users/users.service.ts</files>
  <behavior>
    - UsersController exposes `@Delete('me') @UseGuards(JwtAuthGuard) @HttpCode(HttpStatus.NO_CONTENT) deleteMe(@CurrentUser() user)` — IDOR-safe
    - UsersService.deleteAccount(userId) calls `userRepo.delete(userId)` and returns void
    - No new DTO needed (no body); no path/query param accepts an id (IDOR gate)
  </behavior>
  <action>
    Verify the working-tree controller + service draft matches the contract: signature, decorators,
    HttpCode 204, no id param accepted, single `userRepo.delete(userId)` call (FK cascades do the
    rest). If a deviation exists, conform. Run the full e2e suite (not just users) to confirm no
    regression in the 157+ existing tests. Run `npx tsc --noEmit` and `nest build` clean. Stage
    + commit the three files as one `feat(09)` commit referencing D-41 and REL-01. Do NOT push —
    the orchestrator pushes after wave verification.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; npx tsc --noEmit &amp;&amp; npm run build &amp;&amp; npx jest --config test/jest-e2e.json --forceExit</automated>
  </verify>
  <done>tsc + nest build clean, full e2e suite green (≥ existing baseline + the new DELETE cases), one atomic commit on master containing controller + service + e2e.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /api/v1/users/me | mobile/web client submits authenticated DELETE; userId derived server-side from JWT |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01-01 | Elevation of Privilege | UsersController.deleteMe | mitigate | Target id comes only from `@CurrentUser()`; no `:id`/body id param accepted — IDOR-safe (mirrors D-26). |
| T-09-01-02 | Spoofing | route auth | mitigate | `@UseGuards(JwtAuthGuard)`; 401 e2e case proves unauthed cannot delete. |
| T-09-01-03 | Repudiation | account removal audit | accept | No audit log written; v1.0 scope. The user explicitly initiates the action client-side; Render/Postgres operational backups remain. Revisit if SLA/compliance ever demands it. |
| T-09-01-04 | Denial of Service | rate-limit | accept | Global throttler covers DELETE; per-endpoint hardening unnecessary (auth required, action is irreversible only for that user). |
| T-09-01-SC | Tampering | npm/pip/cargo installs | mitigate | No installs in this plan — N/A. |

</threat_model>

<verification>
- backend tsc --noEmit GREEN
- backend npm run build GREEN
- full e2e suite GREEN (baseline 157+ → 157+ with new DELETE cases; no regressions)
- grep: `@Delete('me')` exactly 1 hit in users.controller.ts; `deleteAccount` exactly 1 hit in users.service.ts
- grep: no `:id` / `@Param('id')` in deleteMe; no path param accepted (IDOR check)
</verification>

<success_criteria>
- `DELETE /api/v1/users/me` ships with 204 semantics and full cascade behavior
- e2e proves 401-no-token, 204-happy-path, post-delete /auth/me → 401
- Mobile (09-03) + Web (09-02) can safely call the endpoint
</success_criteria>

<output>
Create `.planning/phases/09-ios-app-store-release/09-01-SUMMARY.md` when done.
</output>
