---
phase: 02-editorial-dark-design-system-trip-result-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/app/globals.css
  - web/tailwind.config.ts
  - web/src/app/layout.tsx
autonomous: true
requirements: [DES-01]
must_haves:
  truths:
    - "A developer can use bg-carbon-surface3, text-carbon-toll, text-carbon-fuelGpl, font-serif, and text-hero/display/body/caption utilities on any screen"
    - "Display headings (h1/h2) render in the editorial serif; body stays Space Grotesk; numbers stay JetBrains Mono"
    - "muted caption text passes WCAG AA on small text (contrast ≥ 4.5:1 on --c-bg)"
    - "Only 2 web text weights (400/700) are loaded for Space Grotesk and JetBrains Mono; Instrument Serif loads only weight 400"
  artifacts:
    - path: "web/src/app/globals.css"
      provides: "Refined surface/neutral ramp + new --c-surface3/--c-fuel-gpl/--c-toll vars + 4 --text-* size vars + serif h1/h2"
      contains: "--c-surface3"
    - path: "web/tailwind.config.ts"
      provides: "carbon.surface3/fuelGpl/toll utilities, fontFamily.serif, fontSize.{hero,display,body,caption}, documented 12px/20px spacing aliases"
      contains: "surface3"
    - path: "web/src/app/layout.tsx"
      provides: "Instrument_Serif next/font wiring + trimmed weight lists + --font-serif on body"
      contains: "Instrument_Serif"
  key_links:
    - from: "web/tailwind.config.ts"
      to: "web/src/app/globals.css"
      via: "carbon.* + fontSize.* utilities resolve to --c-* / --text-* CSS vars"
      pattern: "var\\(--c-surface3\\)"
    - from: "web/src/app/layout.tsx"
      to: "web/src/app/globals.css"
      via: "--font-serif variable consumed by h1/h2 font-family"
      pattern: "--font-serif"
---

<objective>
Establish the editorial-premium-dark token foundation (DES-01): evolve the existing Carbon
dark tokens in place, add the editorial display font (Instrument Serif), and expose the new
tokens as Tailwind utilities so every later plan and Phase 3 consume one source of truth.

Purpose: The token system IS the phase deliverable (MD-4). Everything downstream (primitives,
result-page redesign) depends on these vars/utilities existing first.
Output: Refined `globals.css` tokens + `tailwind.config.ts` utilities + `layout.tsx` font wiring.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-editorial-dark-design-system-trip-result-redesign/02-CONTEXT.md
@.planning/phases/02-editorial-dark-design-system-trip-result-redesign/02-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Evolve globals.css tokens (surface/neutral ramp, new vars, type-size vars, serif headings)</name>
  <files>web/src/app/globals.css</files>
  <read_first>
    - web/src/app/globals.css (the file being modified — current :root / [data-theme="dark"] / [data-theme="light"] blocks and the h1..h6 reset)
    - .planning/phases/02-editorial-dark-design-system-trip-result-redesign/02-UI-SPEC.md (sections "Color" and "Typography" — the authoritative exact values)
  </read_first>
  <action>
    In `:root` AND the `[data-theme="dark"]` block (keep them mirrored), update the surface ramp per PD-1:
    `--c-surface` `#181612`→`#17150f`; `--c-surface2` `#221e18`→`#211d16`; ADD `--c-surface3: #2a251c`.
    Keep `--c-faint #2e2820` and `--c-hairline #3a3328` unchanged. Update the neutral text ramp:
    `--c-ink` `#f0ece4`→`#f2efe8`; `--c-ink2` `#c4bdaf`→`#c9c2b4`; `--c-muted` `#7a7163`→`#8a8173`
    (the muted bump is the AA fix for small text — do not revert it). Keep `--c-accent`/`--c-ev`
    `#4d8bff`, `--c-fuel-gas #ff7849`, `--c-fuel-die #ffc247` unchanged. ADD `--c-fuel-gpl: #a78bfa`
    and `--c-toll: #b8a98c` (warm taupe — deliberately NOT an energy color). In `[data-theme="light"]`
    ADD `--c-surface3: #ece9e2`, `--c-fuel-gpl: #7c3aed`, `--c-toll: #9b8a6b` and set `--c-muted: #8a8275`;
    leave existing light accents/energy unchanged.
    Add the 4 typography size vars (define once on `:root`): `--text-hero: clamp(64px, 14vw, 112px)`,
    `--text-display: clamp(24px, 5vw, 40px)`, `--text-body: 14px`, `--text-caption: 11px`.
    Rewrite the `h1..h6` reset to the 2-weight editorial system: `h1, h2` → `font-family: var(--font-serif), Georgia, serif`,
    `font-weight: 400`, `letter-spacing: 0`, `line-height: 1.05` (h1) / `1.1` (h2); `h3, h4, h5, h6` →
    `font-family: var(--font-display)`, `font-weight: 700` (drop the 600 weight — it is no longer loaded).
    Leave `body { font-family: var(--font-display) }` (Space Grotesk stays the UI/body face) and the
    existing selection/scrollbar/transition rules untouched.
    Do NOT change any legacy semantic var mappings (`--background`, `--card`, etc.) except where they
    inherit from the updated `--c-*` vars automatically.
  </action>
  <verify>
    <automated>cd web; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `globals.css` defines `--c-surface3`, `--c-fuel-gpl`, `--c-toll` in both `:root`/`[data-theme="dark"]` and light overrides for all three.
    - `--c-surface` is `#17150f`, `--c-surface2` is `#211d16`, `--c-ink` is `#f2efe8`, `--c-muted` is `#8a8173`.
    - `--text-hero`, `--text-display`, `--text-body`, `--text-caption` exist as CSS vars.
    - `h1`/`h2` use `var(--font-serif)` at weight 400; no rule references `font-weight: 600`.
    - `cd web; npx tsc --noEmit` exits 0 (CSS change must not break type-check).
  </acceptance_criteria>
  <done>Editorial-dark token vars + serif headings live in globals.css with light-mode parity and AA-passing muted.</done>
</task>

<task type="auto">
  <name>Task 2: Map new tokens to Tailwind utilities + document the 12px/20px spacing aliases</name>
  <files>web/tailwind.config.ts</files>
  <read_first>
    - web/tailwind.config.ts (the file being modified — current colors.carbon map, fontFamily, letterSpacing, borderRadius)
    - .planning/phases/02-editorial-dark-design-system-trip-result-redesign/02-UI-SPEC.md (sections "Design System", "Spacing Scale", "Typography" → Tailwind subsection)
    - web/src/app/globals.css (so the executor maps utilities to the var names just defined in Task 1)
  </read_first>
  <action>
    In `theme.extend.colors.carbon` ADD `surface3: 'var(--c-surface3)'`, `fuelGpl: 'var(--c-fuel-gpl)'`,
    `toll: 'var(--c-toll)'` (keep all existing carbon keys). In `theme.extend.fontFamily` ADD
    `serif: ['var(--font-serif)', 'Georgia', 'serif']` (keep `sans`/`display`→`var(--font-display)` and
    `mono`→`var(--font-mono)` for back-compat). ADD `theme.extend.fontSize` mapping the 4 tokens to the CSS vars:
    `hero: 'var(--text-hero)'`, `display: 'var(--text-display)'`, `body: 'var(--text-body)'`,
    `caption: 'var(--text-caption)'` (these become `text-hero`/`text-display`/`text-body`/`text-caption`).
    Address checker FLAG 2 — the 12px and 20px values used ad-hoc on the result page: ADD a documented
    `theme.extend.spacing` block aliasing the 8-point scale from the UI-SPEC, explicitly including the
    12px metric/tile gap and the 20px card inset with an inline comment naming them (do not remove or
    shadow Tailwind's default numeric spacing — only add named aliases so the two recurring values are
    documented design tokens rather than magic numbers).
    Keep `darkMode`, `content`, `letterSpacing`, `borderRadius`, and `plugins` unchanged.
  </action>
  <verify>
    <automated>cd web; npx tsc --noEmit; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `carbon.surface3`, `carbon.fuelGpl`, `carbon.toll` exist in the Tailwind color map.
    - `fontFamily.serif` and `fontSize.{hero,display,body,caption}` exist and resolve to the matching `--*` vars.
    - A documented spacing alias block names the 12px and 20px values (comment present).
    - `cd web; npm run build` compiles all routes with 0 errors (utilities resolve, no broken config).
  </acceptance_criteria>
  <done>New token utilities (`carbon.surface3/fuelGpl/toll`, `font-serif`, `text-hero/display/body/caption`) and documented spacing aliases are consumable by later plans.</done>
</task>

<task type="auto">
  <name>Task 3: Wire Instrument Serif via next/font and trim weights to the 2-weight system</name>
  <files>web/src/app/layout.tsx</files>
  <read_first>
    - web/src/app/layout.tsx (the file being modified — current Space_Grotesk + JetBrains_Mono next/font config and the body className)
    - .planning/phases/02-editorial-dark-design-system-trip-result-redesign/02-UI-SPEC.md (Typography → "next/font wiring" subsection)
  </read_first>
  <action>
    Import `Instrument_Serif` from `next/font/google` alongside the existing `Space_Grotesk`/`JetBrains_Mono`.
    Configure Instrument Serif: `subsets: ['latin']`, `weight: ['400']`, `style: ['normal', 'italic']`,
    `variable: '--font-serif'`, `display: 'swap'`, `preload: true`, plus a serif fallback stack
    (`['Georgia', 'serif']`). (Instrument Serif ships only weight 400 — it IS the critical/preloaded weight.)
    Trim `Space_Grotesk` `weight` from `['400','500','600','700']` to `['400','700']` and `JetBrains_Mono`
    likewise to `['400','700']` (drop 500 and 600 — the 2-weight system; keep their `variable`/`display: 'swap'`/fallbacks).
    Add `${instrumentSerif.variable}` to the `<body>` className (keep the existing `${spaceGrotesk.variable}`,
    `${jetbrainsMono.variable}`, and `font-display`). Do NOT touch metadata, viewport, icons, or the Providers wrapper.
  </action>
  <verify>
    <automated>cd web; npx tsc --noEmit; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `layout.tsx` imports and configures `Instrument_Serif` with `weight: ['400']`, `variable: '--font-serif'`, `preload: true`.
    - Space Grotesk and JetBrains Mono `weight` arrays are exactly `['400','700']` (no 500/600).
    - The `<body>` className includes `instrumentSerif.variable`.
    - `cd web; npm run build` succeeds and emits the new font assets without error.
  </acceptance_criteria>
  <done>The serif display face is loaded (swap, preloaded), the system runs on exactly 2 weights, and `--font-serif` is available app-wide.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build pipeline → Google Fonts (next/font) | Instrument Serif is fetched at BUILD time by next/font and self-hosted in the bundle — no runtime third-party request |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | Instrument Serif via `next/font/google` | accept | No new npm package; uses the already-present `next/font/google` import. Font is build-time fetched + self-hosted (no runtime CSP/SRI surface). No user input, no secret, no endpoint touched. |
| T-02-02 | Information Disclosure | CSS token values | accept | Token hex values are public design data — no sensitivity. |
| T-02-SC | Tampering | npm/pip/cargo installs | n/a | No package-manager installs in this plan — `next` is already a dependency; nothing to audit. |
</threat_model>

<verification>
- `cd web; npx tsc --noEmit` → 0 errors.
- `cd web; npm run build` → all routes compile.
- Grep `globals.css` for `--c-surface3`, `--c-toll`, `--c-fuel-gpl`, `--text-hero` (all present).
- Grep `tailwind.config.ts` for `surface3`, `font-serif`-feeding `serif`, `hero` (fontSize) (all present).
- Grep `layout.tsx` for `Instrument_Serif` and `--font-serif`.
</verification>

<success_criteria>
A developer can apply `bg-carbon-surface3`, `text-carbon-toll`, `text-carbon-fuelGpl`, `font-serif`,
and `text-hero/display/body/caption` anywhere; headings render in Instrument Serif; the app loads
exactly 2 weights per non-serif family; muted captions pass AA — all with a green `tsc`/`build`.
</success_criteria>

<output>
Create `.planning/phases/02-editorial-dark-design-system-trip-result-redesign/02-01-SUMMARY.md` when done.
</output>
