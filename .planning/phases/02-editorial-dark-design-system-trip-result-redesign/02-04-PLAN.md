---
phase: 02-editorial-dark-design-system-trip-result-redesign
plan: 04
type: execute
wave: 3
depends_on: ["02-01", "02-02", "02-03"]
files_modified:
  - web/src/app/app/trips/result/page.tsx
autonomous: true
requirements: [DES-02, DES-03, DES-04, WEB-02]
must_haves:
  truths:
    - "The trip result page shows total cost as a designed hero figure (serif route title + animated mono counter), not a plain text row"
    - "Cost is broken down as a segmented Énergie/Péage bar (Péage hidden when toll is 0) plus token-driven multi-energy comparison bars"
    - "Interactive elements (CTAs, stepper) show designed hover/focus/press states; a layout-mirroring skeleton renders while session resolves"
    - "The hero total animates 0→target compositor-friendly with zero layout shift, and is static under prefers-reduced-motion"
  artifacts:
    - path: "web/src/app/app/trips/result/page.tsx"
      provides: "Redesigned editorial-dark trip result page consuming DataBar, Skeleton, useCountUp, useReducedMotion, serif title, token colors"
      contains: "useCountUp"
  key_links:
    - from: "web/src/app/app/trips/result/page.tsx"
      to: "web/src/components/ui/DataBar.tsx"
      via: "breakdown (Variant A) + comparison (Variant B) bars replace ad-hoc colored divs"
      pattern: "DataBar"
    - from: "web/src/app/app/trips/result/page.tsx"
      to: "web/src/hooks/useCountUp.ts"
      via: "animated hero total"
      pattern: "useCountUp"
    - from: "web/src/app/app/trips/result/page.tsx"
      to: "web/src/components/ui/Skeleton.tsx"
      via: "loading state mirrors the new layout"
      pattern: "Skeleton"
---

<objective>
Redesign the trip result page around the editorial-dark system — the end-to-end proof slice that
makes DES-02/DES-03/DES-04/WEB-02 user-observable. Same data, same flow, same calculation (MD-2):
only the visual layer changes. Consumes every artifact from 02-01/02-02/02-03.

Purpose: Prove the design language on the highest-value screen before Phase 3 rollout.
Output: A fully restyled `web/src/app/app/trips/result/page.tsx` (hero counter + breakdown bar +
comparison bars + skeleton + designed interaction states), within CWV (transform/opacity only).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-editorial-dark-design-system-trip-result-redesign/02-UI-SPEC.md

<interfaces>
<!-- New artifacts from this phase the page consumes (import directly — no exploration needed). -->
import { DataBar } from '@/components/ui/DataBar';          // 02-03 — Variant A segmented + Variant B single
import { Skeleton } from '@/components/ui/Skeleton';        // 02-03
import { useCountUp } from '@/hooks/useCountUp';            // 02-02 — (target, durationMs=700) => number
import { useReducedMotion } from '@/hooks/useReducedMotion';// 02-02 — () => boolean
// Tokens (02-01): font-serif, text-hero/display/body/caption, bg-carbon-surface3, var(--c-toll), carbon.fuelGpl.

<!-- Existing data shapes the page already uses (web/src/types/api.ts) -->
TripResult: { distance:{km}, duration:{seconds}, vehicle:{fuelType}, cost?: FuelCostResult|ElectricCostResult, tollCost: number|null, tollIsEstimate: boolean }
FuelCostResult: { type:'fuel', consumptionLitres, pricePerLitre, totalCost }
ElectricCostResult: { type:'electric', consumptionKwh, pricePerKwh, totalCost, disclaimer? }
EnergyComparison: { category:'gas'|'diesel'|'ev'|'gpl', label, isCurrent, consumption, consumptionUnit:'L'|'kWh', unitPrice, totalCost }
PendingTripSession: { origin, destination, result: TripResult, multiResult: { comparisons: EnergyComparison[] }|null, selectedVehicleId, mode:'address'|'distance' }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Header, hero plate + animated counter, segmented breakdown bar, metric tiles</name>
  <files>web/src/app/app/trips/result/page.tsx</files>
  <read_first>
    - web/src/app/app/trips/result/page.tsx (the file being redesigned — current header, hero, 2×2 metrics grid, session/save/cost logic to PRESERVE)
    - web/src/components/ui/DataBar.tsx (Variant A segmented API)
    - web/src/hooks/useCountUp.ts + web/src/hooks/useReducedMotion.ts
    - web/src/components/ui/Eyebrow.tsx, web/src/components/ui/FuelBadge.tsx, web/src/components/ui/SectionCard.tsx
    - .planning/phases/02-editorial-dark-design-system-trip-result-redesign/02-UI-SPEC.md (sections "Result-Page Layout & Hierarchy", "Motion", "Color")
  </read_first>
  <action>
    Keep ALL existing logic untouched (session load + `router.replace('/app/dashboard')` guard, `handleSave`,
    `handleNewTrip`, `isFuelCost`, `formatDuration`, `fmtEur`, the `canSave`/`hasToll`/`totalCost` derivations).
    Restyle the visual layer only (MD-2).
    Header: keep the `Eyebrow` "Résultat"; change the h1 route title to the editorial serif — apply `font-serif`
    and `text-display`, weight 400, with the `→` separator in `text-carbon-muted` (replaces the current
    `text-2xl font-bold font-display`).
    Hero plate: render the hero `SectionCard` on the elevated surface (`bg-carbon-surface3` via className).
    Keep the "Coût estimé" eyebrow. Replace the static `(totalCost/passengers).toFixed(2)` figure with the
    animated counter: call `const animated = useCountUp(totalCost / passengers)` and render it in JetBrains Mono
    at `text-hero` (`var(--text-hero)`), weight 700, `tabular-nums`, fixed 2 decimals (format the animated value
    with `.toFixed(2)`), `text-carbon-ink`; demote the `€` symbol to `text-carbon-muted`. Wrap the animating span
    with `aria-hidden` and add an adjacent `sr-only` span holding the final formatted total (`fmtEur.format(totalCost/passengers)`)
    so SR users get the real value (no `aria-live`). Keep the per-person sub-line when `passengers > 1` (mono, muted).
    Below the figure, add the Variant A `DataBar` breakdown: Énergie segment = `cost.totalCost` with the energy
    fill var by the trip fuel type (`ELECTRIC→var(--c-ev)`, gas variants→`var(--c-fuel-gas)`, `DIESEL→var(--c-fuel-die)`),
    Péage segment = `tollCost` with `var(--c-toll)`, sized against `totalCost`. A legend row beneath shows
    `Énergie · {fmtEur energyCost}` and `Péage · {fmtEur tollCost}` each prefixed by a 6px color dot matching its
    segment; the Péage legend carries the existing réel/≈ estimé `Pill`+`Tooltip` (reuse the current `tollBadge`
    construction at lines 157-169). When `tollCost === 0`, hide the Péage segment AND its legend entirely and render
    a single full-width energy bar (preserve D-04 hide-when-0).
    Metric tiles: keep the 2×2 grid (ÉNERGIE / [PÉAGES if >0] / €/KM / PAR PERS.) as inset `bg-carbon-surface2`
    tiles; apply the staggered reveal — add a CSS keyframe `reveal { from { opacity:0; transform: translateY(8px) } to { opacity:1; transform:none } }`
    (in globals.css or a styled block) and apply it per tile via inline `animation-delay: ${Math.min(i*60,300)}ms`,
    360ms ease-out, `animation-fill-mode: both`; gate the reveal class off when `useReducedMotion()` is true (render final state).
    Keep the metadata row (`FuelBadge` + distance + duration in mono muted) and the distance-mode + EV-disclaimer
    amber wash notes.
    Across this page, replace every `font-medium`/`font-semibold` weight with the 2-weight system (400 or 700) and
    map `text-2xl`/`text-4xl`/`text-[10px]`/`text-[104px]` etc. to the tokenized sizes (`text-hero`, `text-display`,
    `text-caption`, `text-body`). No fenced code — apply the UI-SPEC values directly.
  </action>
  <verify>
    <automated>cd web; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - The route-title `h1` uses `font-serif` (Instrument Serif) at the display size, weight 400.
    - The hero figure is rendered via `useCountUp` in JetBrains Mono `tabular-nums` at `text-hero`/700 with a fixed 2-decimal format; the animating span is `aria-hidden` and an `sr-only` sibling holds the final formatted total.
    - A Variant A `DataBar` renders the Énergie/Péage segmented breakdown using `var(--c-toll)` + the energy var; when `tollCost === 0` the Péage segment AND legend are absent (single full-width energy bar).
    - Metric tiles use the `reveal` staggered animation gated by `useReducedMotion`.
    - No `font-medium`/`font-semibold` and no ad-hoc `bg-emerald/sky/violet/amber` remain in the hero/breakdown region.
    - `cd web; npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>The hero presents total cost as a designed, animated, accessible figure with a token-driven segmented breakdown bar.</done>
</task>

<task type="auto">
  <name>Task 2: Comparison bars (Variant B), standardized interaction states + stepper a11y, layout-mirroring skeleton</name>
  <files>web/src/app/app/trips/result/page.tsx</files>
  <read_first>
    - web/src/app/app/trips/result/page.tsx (the comparison block at lines 272-321, the passenger stepper at 332-357, and the loading placeholder at 127-135 — all being replaced)
    - web/src/components/ui/DataBar.tsx (Variant B single-bar API + muted opacity convention)
    - web/src/components/ui/Skeleton.tsx
    - web/src/components/ui/CTAButton.tsx (the canonical focus-visible ring token to standardize across the page)
    - .planning/phases/02-editorial-dark-design-system-trip-result-redesign/02-UI-SPEC.md (sections "Data-Viz Bar Primitive → Variant B", "Interaction States", "Loading Skeletons")
  </read_first>
  <action>
    Comparison section: replace the ad-hoc comparison block AND delete the local `categoryColor()` helper
    (lines 37-48 — the `bg-emerald/sky/violet/amber` mapping). Render one Variant B `DataBar` row per
    `EnergyComparison`, kept sorted cheapest-first. Each row: top line = category `label` (left) + `totalCost`
    in JetBrains Mono `tabular-nums` (right); the bar fill = `totalCost / maxCost` with the energy var by category
    (`ev→var(--c-ev)`, `gas→var(--c-fuel-gas)`, `diesel→var(--c-fuel-die)`, `gpl→var(--c-fuel-gpl)`). The
    `isCurrent` row: fill at full opacity, label `text-carbon-ink`, `← actuel` marker in `text-carbon-accent`;
    non-current rows: fill muted (DataBar `muted`/opacity 0.45), label `text-carbon-ink2`. Keep the mono muted
    sub-caption `{consumption} {unit} · {unitPrice} €/{unit}`. Keep the whole section hidden when there are no comparisons.
    Interaction states: standardize the focus token across every interactive element on the page using the canonical
    ring from CTAButton (`focus:outline-none focus-visible:ring-2 focus-visible:ring-carbon-accent/50 focus-visible:ring-offset-2 focus-visible:ring-offset-carbon-bg`).
    Passenger stepper: keep the 1–9 logic and the 32px (`w-8 h-8`) tap targets; apply the standardized focus ring and
    the spec hover/press states (`hover:text-carbon-ink hover:bg-carbon-surface2`, `active:bg-carbon-hairline`,
    `disabled:opacity-30`). Address checker FLAG 1 — add `aria-label="Retirer un passager"` to the minus button and
    `aria-label="Ajouter un passager"` to the plus button. CTAs: keep `CTAButton` accent (save) + ghost (new trip)
    as-is (their states are already correct).
    Loading skeleton: replace the current 3-block `animate-pulse` placeholder (lines 127-135) with a `Skeleton`-based
    skeleton that MIRRORS the new layout to avoid CLS on swap — a ~40% header line + small eyebrow line; a tall
    hero-plate block (`rounded-card`) + a 2×2 grid of tile skeletons; one full-width `h-2.5 rounded-full` bar +
    2 short legend lines; 3–4 comparison rows (label line + `h-2.5` bar); 2 button skeletons (`h-11 rounded-xl`).
    No fenced code — apply the UI-SPEC structure directly. Do NOT change `handleSave`/`handleNewTrip`/save payload.
  </action>
  <verify>
    <automated>cd web; npx tsc --noEmit; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - The local `categoryColor()` helper is deleted; comparison rows render via Variant B `DataBar` with energy/gpl CSS-var fills (no `bg-emerald/sky/violet/amber` anywhere in the file); cheapest-first order preserved; section hidden when no comparisons.
    - Current-energy row uses full-opacity fill + `← actuel` accent marker; non-current rows are muted.
    - Both stepper buttons carry `aria-label` (FLAG 1); every interactive element uses the standardized `focus-visible:ring-carbon-accent/50` token.
    - The loading state uses `Skeleton` blocks mirroring the new layout (no remaining inline 3-block `animate-pulse`).
    - `cd web; npm run build` compiles all routes with 0 errors and 0 ESLint errors.
  </acceptance_criteria>
  <done>Comparison data-viz, designed interaction states, stepper a11y, and a CLS-safe layout-mirroring skeleton are all in place — the result page fully embodies the editorial-dark system.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sessionStorage → page | Trip session data is read from sessionStorage as it already is today (MD-2: unchanged); rendered as text/widths only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | Tampering | result/page.tsx render | accept | No new input parsing or markup injection; the existing `JSON.parse` + guard path is preserved unchanged. Values render as text/CSS widths, never `dangerouslySetInnerHTML`. |
| T-02-08 | Information Disclosure | hero / comparison figures | accept | Only the user's own trip figures (already on screen today) are displayed; no new data surfaced; no secret/key involved. |
| T-02-09 | Repudiation | save flow | accept | `handleSave` payload + endpoint are untouched (MD-2); no auth/integrity change. |
| T-02-SC | Tampering | npm/pip/cargo installs | n/a | No package-manager installs (lucide-react, all atoms/hooks already exist). |
</threat_model>

<verification>
- `cd web; npx tsc --noEmit` → 0 errors.
- `cd web; npm run build` → all routes compile, 0 ESLint errors.
- Grep `result/page.tsx`: contains `DataBar`, `Skeleton`, `useCountUp`, `useReducedMotion`, `font-serif`, `aria-label`, `var(--c-toll)`; contains NO `categoryColor`, NO `bg-emerald`/`bg-sky`/`bg-violet`/`bg-amber`, NO `font-medium`/`font-semibold`.
- Manual observable (folded into final checkpoint plan 02-05): hero counts up smoothly; breakdown bar hides Péage when toll=0; prefers-reduced-motion stops the counter; no layout shift on skeleton→content swap.
</verification>

<success_criteria>
The trip result page presents total cost as a designed animated hero figure with a token-driven
Énergie/Péage breakdown and multi-energy comparison bars, designed interaction states + a
layout-mirroring skeleton, all compositor-friendly and reduced-motion-aware — same data/flow,
green `tsc`/`build`.
</success_criteria>

<output>
Create `.planning/phases/02-editorial-dark-design-system-trip-result-redesign/02-04-SUMMARY.md` when done.
</output>
