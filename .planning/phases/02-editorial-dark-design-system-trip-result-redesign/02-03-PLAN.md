---
phase: 02-editorial-dark-design-system-trip-result-redesign
plan: 03
type: execute
wave: 2
depends_on: ["02-01", "02-02"]
files_modified:
  - web/src/components/ui/DataBar.tsx
  - web/src/components/ui/Skeleton.tsx
autonomous: true
requirements: [DES-01, DES-02, DES-03]
must_haves:
  truths:
    - "A developer can render a token-driven horizontal bar — either a single value bar (Variant B) or a two-segment Énergie/Péage bar (Variant A) — using only the energy/toll CSS-var colors"
    - "Bar fills animate width 0→target via transform: scaleX (no reflow), and render at final width instantly under reduced motion"
    - "A developer can render compositor-friendly loading skeleton blocks that drop their pulse under reduced motion"
  artifacts:
    - path: "web/src/components/ui/DataBar.tsx"
      provides: "Reusable token-driven data-viz bar primitive (Variant A segmented + Variant B single)"
      exports: ["DataBar"]
      min_lines: 40
    - path: "web/src/components/ui/Skeleton.tsx"
      provides: "Loading skeleton atom (bg-carbon-surface2, animate-pulse, reduced-motion-aware)"
      exports: ["Skeleton"]
  key_links:
    - from: "web/src/components/ui/DataBar.tsx"
      to: "web/src/app/globals.css"
      via: "fill colors via inline style var(--c-ev|--c-fuel-gas|--c-fuel-die|--c-fuel-gpl|--c-toll); track via bg-carbon-surface2"
      pattern: "var\\(--c-(ev|toll|fuel)"
    - from: "web/src/components/ui/DataBar.tsx"
      to: "web/src/hooks/useReducedMotion.ts"
      via: "import to gate the scaleX reveal"
      pattern: "useReducedMotion"
---

<objective>
Build the two net-new presentational primitives the result-page redesign consumes: the
token-driven `DataBar` (the reusable data-viz bar — Variant A segmented breakdown + Variant B
single comparison bar, DES-01/DES-02) and the `Skeleton` loading atom (DES-03). Both are
compositor-friendly and reduced-motion-aware.

Purpose: PD-3 mandates the bars be a reusable design-system primitive, not ad-hoc markup. These
unblock 02-04 and become Phase 3's shared data-viz vocabulary.
Output: `web/src/components/ui/DataBar.tsx` + `web/src/components/ui/Skeleton.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-editorial-dark-design-system-trip-result-redesign/02-UI-SPEC.md

<interfaces>
<!-- Token utilities created in 02-01 (consume directly; no Tailwind opacity modifier on CSS-var colors). -->
Tailwind: bg-carbon-surface2, bg-carbon-surface3, text-carbon-toll, text-carbon-fuelGpl, font-mono.
CSS vars for fills: var(--c-ev) #4d8bff, var(--c-fuel-gas) #ff7849, var(--c-fuel-die) #ffc247,
  var(--c-fuel-gpl) #a78bfa, var(--c-toll) #b8a98c.
Hook (02-02): import { useReducedMotion } from '@/hooks/useReducedMotion';  // returns boolean
Energy category type (web/src/types/api.ts): EnergyComparison['category'] = 'gas'|'diesel'|'ev'|'gpl'.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DataBar primitive — Variant A (segmented Énergie/Péage) + Variant B (single), scaleX reveal</name>
  <files>web/src/components/ui/DataBar.tsx</files>
  <read_first>
    - web/src/components/ui/SectionCard.tsx (existing atom style — named export, typed props interface, Tailwind class composition)
    - web/src/components/ui/FuelBadge.tsx (existing token/energy-color convention to align with)
    - web/src/hooks/useReducedMotion.ts (the gate this primitive imports)
    - .planning/phases/02-editorial-dark-design-system-trip-result-redesign/02-UI-SPEC.md (section "Data-Viz Bar Primitive" — Variant A, Variant B, reveal/reduced-motion behavior)
  </read_first>
  <behavior>
    - Variant B (single bar): fill width = (value / max) * 100%, fill color from an energy CSS var, muted (non-current) fills render at inline opacity 0.45 and current at opacity 1.
    - Variant A (segmented bar): one track with two flush adjacent fills — Énergie segment sized energyCost/total (energy color) and Péage segment sized tollCost/total (var(--c-toll)); outer corners rounded-full.
    - Heights: sm = 6px (h-1.5), md = 10px (h-2.5); default md. Track is bg-carbon-surface2 rounded-full overflow-hidden.
    - Reveal: fills animate from scaleX(0) to scaleX(1) (transform-origin: left) over 600ms cubic-bezier(0.16,1,0.3,1) on mount.
    - Reduced motion: fills render at scaleX(1) immediately with no transition.
  </behavior>
  <action>
    Create a client component `DataBar` with a typed props interface supporting both variants — design a
    clean API such as a discriminated prop (e.g. a `segments` array for Variant A vs. `value`/`max` for
    Variant B) plus `height?: 'sm' | 'md'` (default `md`), `fillVar` (the energy CSS-var string), and a
    `muted?: boolean` for the 0.45-opacity non-current state. Track: `bg-carbon-surface2 rounded-full
    overflow-hidden` at h-1.5 (sm) / h-2.5 (md). Fills: absolutely-sized children, `rounded-full`, color via
    inline `style={{ background: 'var(--c-ev)' }}` (NEVER a Tailwind opacity modifier on a CSS-var color —
    use inline `opacity` for the muted state). Width = computed percentage. For Variant A, render two flush
    segments (energy then toll) in one track; the energy segment uses the passed energy var and the toll
    segment uses `var(--c-toll)`. Animate each fill via `transform: scaleX()` with `transform-origin: left`,
    600ms `cubic-bezier(0.16,1,0.3,1)` — start at `scaleX(0)` and flip to `scaleX(1)` after mount (a mounted
    flag + effect). Import `useReducedMotion()`; when true, set `scaleX(1)` with no transition. Named export,
    typed props, no `console.log`. Do NOT hard-code Tailwind palette classes (no `bg-emerald-500` etc.) — colors
    come only from the CSS vars.
  </action>
  <verify>
    <automated>cd web; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `DataBar` is a named export with a typed props interface covering both Variant A (segmented) and Variant B (single).
    - Track uses `bg-carbon-surface2 rounded-full overflow-hidden`; heights sm=`h-1.5`, md=`h-2.5` (default md).
    - Fill colors come exclusively from CSS vars via inline `style` (no `bg-emerald/sky/violet/amber` classes anywhere in the file).
    - Reveal uses `transform: scaleX` with `transform-origin: left` and the 600ms `cubic-bezier(0.16,1,0.3,1)` curve; reduced motion renders final `scaleX(1)` with no transition.
    - `cd web; npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>A single reusable, token-driven, reduced-motion-aware bar primitive supports both result-page bar use cases.</done>
</task>

<task type="auto">
  <name>Task 2: Skeleton loading atom (compositor-friendly, reduced-motion-aware)</name>
  <files>web/src/components/ui/Skeleton.tsx</files>
  <read_first>
    - web/src/components/ui/SectionCard.tsx (atom style/props convention reference)
    - web/src/hooks/useReducedMotion.ts (used to drop the pulse)
    - .planning/phases/02-editorial-dark-design-system-trip-result-redesign/02-UI-SPEC.md (section "Loading Skeletons")
    - web/src/app/app/trips/result/page.tsx (current 3-block animate-pulse placeholder this atom will replace in 02-04)
  </read_first>
  <action>
    Create a client component `Skeleton` rendering a `bg-carbon-surface2 rounded` block with `animate-pulse`
    (opacity-based → compositor-friendly). Typed props: `width?`, `height?` (accept number→px or string),
    `rounded?` (Tailwind radius class override, default `rounded`), and `className?`. Import `useReducedMotion()`;
    when true, omit `animate-pulse` (static block). Sizing via inline `style` (width/height) when numeric.
    Named export, typed props, no `console.log`.
  </action>
  <verify>
    <automated>cd web; npx tsc --noEmit; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `Skeleton` is a named export accepting `width`/`height`/`rounded`/`className`.
    - Renders `bg-carbon-surface2` with `animate-pulse` by default, and drops `animate-pulse` when reduced motion is set.
    - `cd web; npm run build` compiles all routes with 0 errors.
  </acceptance_criteria>
  <done>A reusable skeleton atom exists, mirroring the new surfaces and respecting reduced motion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller props → primitive | Numeric values come from already-validated trip data (MD-2: no new input parsing); rendered as widths only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Tampering | DataBar width math | accept | Pure presentational % math on trusted internal numbers; no DOM injection, no `dangerouslySetInnerHTML`, no user-controlled strings rendered as markup. |
| T-02-06 | Information Disclosure | Skeleton/DataBar | accept | No data fetched or exposed; presentational only. |
| T-02-SC | Tampering | npm/pip/cargo installs | n/a | No package-manager installs in this plan (lucide-react already present; no new dependency). |
</threat_model>

<verification>
- `cd web; npx tsc --noEmit` → 0 errors.
- `cd web; npm run build` → all routes compile.
- Grep `DataBar.tsx`: contains `scaleX`, `var(--c-toll)`, `useReducedMotion`; contains NO `bg-emerald`/`bg-sky`/`bg-violet`/`bg-amber`.
- Grep `Skeleton.tsx`: contains `animate-pulse` and `bg-carbon-surface2`.
</verification>

<success_criteria>
DataBar renders both a single comparison bar and a segmented Énergie/Péage bar using only token
colors, animating via scaleX (instant under reduced motion); Skeleton renders compositor-friendly
placeholders — all consumable by 02-04 with a green `tsc`/`build`.
</success_criteria>

<output>
Create `.planning/phases/02-editorial-dark-design-system-trip-result-redesign/02-03-SUMMARY.md` when done.
</output>
