---
phase: 08-onboarding
plan: 03
type: execute
wave: 2
depends_on: ["08-01", "08-02"]
files_modified: []
autonomous: false
requirements: [ONB-01]
user_setup: []

must_haves:
  truths:
    - "On a fresh per-user state the tour auto-shows on first authenticated load — web AND mobile."
    - "After finishing/skipping, the tour does not auto-reshow on the next load — web AND mobile."
    - "Settings 'Revoir le tutoriel' re-opens the tour regardless of the seen flag — web AND mobile."
    - "All 7 steps render, editorial-dark, FR (web) and FR+EN (mobile), dismissible, no regression to existing flows."
  artifacts: []
  key_links: []
---

<objective>
Human-verify the onboarding tour end-to-end on both platforms: first-login auto-show, no-reshow
persistence, and replay-from-Settings — confirming ONB-01 is delivered with no regression.

Purpose: The seen-flag + auto-trigger + replay behavior is only fully provable by a human running
the apps. This checkpoint signs off Phase 8.
Output: Sign-off (or a punch list of issues feeding a gap-closure plan).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/08-onboarding/08-CONTEXT.md
@.planning/phases/08-onboarding/08-01-PLAN.md
@.planning/phases/08-onboarding/08-02-PLAN.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Cross-platform onboarding human-verify (web + mobile)</name>
  <action>Pause for human verification. The agent performs no edits in this task — it presents the verification steps below, waits for the human to exercise the web and mobile onboarding tour (first-show, no-reshow, replay, 7 steps, no regression), and records the sign-off or a punch list.</action>
  <what-built>
    A web + mobile onboarding tour (full-screen editorial-dark 7-step carousel) that auto-shows once
    per user on first authenticated load via a client-side per-user "seen" flag (web localStorage,
    mobile expo-secure-store), is dismissible, and can be replayed from Settings ("Revoir le tutoriel").
  </what-built>
  <how-to-verify>
    WEB (run `cd web && npm run dev`, open the app, log in):
    1. First-show: ensure a clean per-user state — in DevTools → Application → Local Storage, delete any
       key starting `verygoodtrip.onboarding.seen.`, then reload an /app/* page. The full-screen tour
       should appear automatically.
    2. Steps: page through all 7 steps (Bienvenue → Calculer → Garage → Péages → Trajets → Favoris →
       Paramètres). Confirm progress dots, Précédent/Suivant, Passer, and Terminer behave; the backdrop
       and X dismiss it. Confirm editorial-dark (no serif, Space Grotesk) and that step 4 (Péages) says
       tolls are estimated and included in the total — no real/estimate badge wording.
    3. No reshow: after Terminer (or Passer), reload /app/* — the tour must NOT auto-appear again.
       Confirm the localStorage key `verygoodtrip.onboarding.seen.<userId>` = '1'.
    4. Replay: go to Settings → "Revoir le tutoriel" → the tour re-opens even though the flag is set.
    5. No regression: navigation, sidebar/drawer, theme picker, pseudo save, and logout still work.
    6. Reduced motion (optional): enable OS "reduce motion" and confirm the tour shows with no slide animation.

    MOBILE (run `cd mobile && npx expo start`, open in Expo Go or a dev build, log in):
    7. First-show: on a device/simulator where this user hasn't seen it (or after uninstalling/clearing
       app data), log in — the tour Modal should auto-appear on the tabs.
    8. Steps + i18n: page through all 7 steps; switch language in Settings (FR/EN) and replay to confirm
       both languages render. Confirm editorial-dark (no serif) and the Péages step wording (estimated +
       included, no badge).
    9. No reshow: finish/skip, then fully close and reopen the app — the tour must NOT auto-appear.
    10. Replay: Settings → "Revoir le tutoriel" → the tour re-opens regardless of the flag.
    11. No regression: tab navigation, theme, language, pseudo save, and logout still work.
  </how-to-verify>
  <resume-signal>
    Type "approved" if all checks pass on both platforms, or list the failing items (platform + step)
    so a gap-closure plan can address them.
  </resume-signal>
  <verify>
    <human-check>Web + mobile: fresh-state auto-show, no-reshow after finish/skip, Settings replay, 7 editorial-dark steps (FR / FR+EN), no regression to existing flows.</human-check>
  </verify>
  <done>Human types "approved" after all web + mobile checks pass, or a punch list is recorded for gap closure.</done>
</task>

</tasks>

<verification>
This plan is a human verification gate; the automated gates were satisfied by 08-01 (web tsc + build)
and 08-02 (mobile tsc) in Wave 1.
</verification>

<success_criteria>
- Web + mobile: first authed load auto-shows the tour on a fresh per-user state; it does not auto-reshow
  after finish/skip; Settings replay re-opens it.
- 7 steps, editorial-dark, FR (web) / FR+EN (mobile), dismissible, no regression — human-approved.
</success_criteria>

<output>
Create `.planning/phases/08-onboarding/08-03-SUMMARY.md` when done (record the sign-off / any punch list).
</output>
